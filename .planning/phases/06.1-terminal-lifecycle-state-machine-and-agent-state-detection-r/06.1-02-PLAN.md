---
phase: 06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r
plan: 02
type: execute
wave: 1
depends_on: [06.1-01]
files_modified:
  - src/renderer/SessionView.tsx
  - tests/smoke/alt-screen-reset.smoke.test.ts
autonomous: true
requirements: [TERM-09, TERM-12]
must_haves:
  truths:
    - "SessionView ticks term.buffer.active (~100ms), FNV-1a hashes the viewport, and on a ≥SETTLE_MS settle calls classify() — an animated 'Thinking…' churns the frame and reads in-progress, never settling falsely"
    - "A backgrounded (hidden) running session still ticks and can go amber while another session is active (D-12) — reading buffer.active needs no WebGL context"
    - "On restart and abnormal exit, SessionView emits MOUSE_RESET so term.modes.mouseTrackingMode reads 'none' (D-13) — scroll-wheel scrolls the buffer, no [%30/] garble"
    - "Restart preserves scrollback across ≥3 restarts (D-07) — term.reset() is never called on the restart path; the alt-screen exit is gated on buffer.active.type==='alternate'"
  artifacts:
    - path: "src/renderer/SessionView.tsx"
      provides: "frame-stability tick detector (SEAM A) + mouse-mode-safe frame reset (SEAM B)"
      contains: "MOUSE_RESET"
    - path: "tests/smoke/alt-screen-reset.smoke.test.ts"
      provides: "≥3-restart scrollback + mouseTrackingMode==='none' + scroll-wheel smoke"
      contains: "mouseTrackingMode"
  key_links:
    - from: "src/renderer/SessionView.tsx"
      to: "src/shared/agent-state.ts classify()"
      via: "import classify, TICK_MS, SETTLE_MS; tick loop over term.buffer.active"
      pattern: "classify\\("
    - from: "src/renderer/SessionView.tsx"
      to: "term.modes.mouseTrackingMode"
      via: "MOUSE_RESET literal write on restart/abnormal-exit"
      pattern: "1000l.*1002l.*1006l"
    - from: "tests/smoke/alt-screen-reset.smoke.test.ts"
      to: "window.__sessionTerms[id].modes.mouseTrackingMode"
      via: "readBufferOf / a new modes-read driver helper"
      pattern: "mouseTrackingMode"
---

<objective>
Axis 2 (TERM-09) + Axis 4 (D-13/D-07 — Core Value fidelity) in the one file that owns both
seams: `SessionView.tsx`. SEAM A swaps the output-silence `IDLE_MS` detector for a
`setInterval` frame-stability tick over the live `term.buffer.active`, feeding the new
`classify()`. SEAM B re-derives the alt-screen/mouse writes so a killed/restarted TUI never
leaves mouse-tracking hot (the `[%30/]` scroll garble) and a restart never trims scrollback
(no `term.reset()` on the restart path).

Purpose: Phase 6's byte-silence model left `claude --rc` permanently blue (an agent TUI
repaints continuously so silence never happens); frame-stability triggers on the rendered
content settling. The fidelity regression is a Core-Value break — scroll-wheel must scroll
the xterm buffer after a TUI dies.
Output: A refactored `SessionView.tsx` (SEAM A + SEAM B) and an extended alt-screen smoke
suite covering ≥3-restart scrollback survival, `mouseTrackingMode==='none'`, and scroll-wheel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-CONTEXT.md
@.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-RESEARCH.md
@.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-PATTERNS.md
@.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-VALIDATION.md
@.planning/spikes/001-frame-stability-mechanism/record.cjs
@.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SEAM A — replace the output-silence detector with a frame-stability tick over term.buffer.active</name>
  <read_first>
    - src/renderer/SessionView.tsx (the file being refactored — the detector block l.222–269: `agentTail`/`idleTimer`/`emitAgent` l.231–239, the onPtyData classify path l.256–268; the `agentRunning` gate set at l.321 / cleared at l.338–344; the cleanup at l.367–390 clearing `idleTimer` l.369; the mount effect keyed on `[id]` l.391; term.write runs unconditionally for hidden panes l.248)
    - src/shared/agent-state.ts (after Plan 01 — import `classify`, `TICK_MS`, `SETTLE_MS`; the AgentState type)
    - .planning/spikes/001-frame-stability-mechanism/record.cjs (the REFERENCE: `viewportLines` l.46–55 using `b.viewportY`..`+term.rows`, `fnv1a` l.56–60, the tick loop l.116–140 with the change→WORKING / settle→classify transitions)
    - .planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-PATTERNS.md (§SessionView.tsx SEAM A — the exact CURRENT-to-REPLACE excerpt l.86–126 + Pattern 3 lifecycle / Pitfall 7)
  </read_first>
  <behavior>
    - While the viewport text keeps changing tick-over-tick (an agent streaming "Thinking…"), the detector emits 'in-progress' and never settles
    - When the viewport hash is unchanged for ≥ SETTLE_MS, the detector emits classify(viewportLines())
    - emitAgent is change-only: re-emitting the same state does NOT call onAgentStateRef (no parent render churn)
    - The detector is gated on agentRunning: a not_started/exited/stopped session is never classified
    - A hidden (inactive) running pane still ticks (term.write runs unconditionally) so a backgrounded claude --rc can settle to 'waiting'
    - On in-place restart the same Terminal persists; lastHash/changeAt reset so the restart's fresh frame is re-evaluated (no SessionView re-key)
  </behavior>
  <action>
    Refactor `SessionView.tsx` SEAM A per 06.1-PATTERNS.md. DELETE the byte-tail detector:
    remove `agentTail` (l.231), the `agentTail = (agentTail + data).slice(-4096)` +
    `idleTimer`/`classifyIdle(agentTail)` path inside `onPtyData` (l.256–268), and the
    `idleTimer` declaration. KEEP `emitAgent` change-only (l.235–239) and the `agentRunning`
    flag. Add a module-local `viewportLines = (): string[]` reading `term.buffer.active`
    (`const b = term.buffer.active; const top = b.viewportY;` loop `term.rows` rows pushing
    `b.getLine(top+i)?.translateToString(true) ?? ''`) and a `fnv1a(s: string): string`
    (the 8-line non-crypto hash from record.cjs l.56–60). Replace `idleTimer` with a
    `setInterval` armed in the mount effect (keyed on `[id]` ONLY — Pitfall 7): every
    `TICK_MS` (imported), `if (!agentRunning) return;` then compute
    `lines = viewportLines(); h = fnv1a(lines.join('\n'));` — on `h !== lastHash` set
    `lastHash = h; changeAt = performance.now(); emitAgent('in-progress')`; else if
    `performance.now() - changeAt >= SETTLE_MS` then `emitAgent(classify(lines))`. Clear the
    interval in the effect cleanup (replace the `idleTimer` clear at l.369) AND when
    `agentRunning` flips false (l.338 region — reset `lastHash = null` / `changeAt` there so
    a later restart starts fresh; do NOT clear the interval on stop since the same term
    persists across in-place restart — just gate it). Keep the change-only emit so a stopped
    overlay never lingers. Replace the import of `classifyIdle`/`IDLE_MS` with
    `classify`/`TICK_MS`/`SETTLE_MS`.
  </action>
  <verify>
    <automated>npm run test:unit && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'IDLE_MS|classifyIdle|agentTail|idleTimer' src/renderer/SessionView.tsx` returns nothing
    - `grep -q 'setInterval' src/renderer/SessionView.tsx` and `grep -q 'term.buffer.active' src/renderer/SessionView.tsx`
    - `grep -q 'classify(' src/renderer/SessionView.tsx`
    - `npm run test:unit` exits 0 (agent-state-replay oracle still GREEN — the classifier contract is unchanged)
    - `npx tsc --noEmit` exits 0
    - Maps to 06.1-VALIDATION.md row: "Backgrounded session goes amber within ~1s" (the tick mechanism; smoke-confirmed in Task 2 / human-verify)
  </acceptance_criteria>
  <done>The output-silence detector is gone; a 100ms tick over term.buffer.active hashes the viewport and classifies on a ≥SETTLE_MS settle, gated on agentRunning, change-only emit, armed once per id, ticking on hidden panes; tsc + unit GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: SEAM B — mouse-mode-safe frame reset (D-13) + no-reset restart (D-07), and extend the alt-screen smoke</name>
  <read_first>
    - src/renderer/SessionView.tsx (the SEAM B block l.306–333: the restart seam writes `\x1b[?1049l` only l.315 then the `— restarted HH:MM —` separator l.316–317; the abnormal-exit `term.reset()` l.331–332; `hasRunBeforeRef` l.125/l.307/l.319; the onPtyExit `[process exited]` notice l.279–281; `sanitizeNotice` l.92; `window.__sessionTerms[id]` registration l.203–208)
    - .planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-RESEARCH.md (§Axis-4 D-13 root cause + the exact MOUSE_RESET sequence; §Axis-1 D-07 restart trim root cause + the buffer.active.type gate; Open Q1 on the abnormal-exit frame)
    - .planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-PATTERNS.md (§SessionView.tsx SEAM B — the RE-DERIVE excerpt l.128–154; the verification handle `modes.mouseTrackingMode`)
    - tests/smoke/alt-screen-reset.smoke.test.ts (the file being extended — current 2-restart `KEEP_` scrollback spec l.83–127; the abnormal-exit spec l.49–81 asserting reset; `readBufferOf`/`ptyPidOf`/`killProcess` helpers)
    - tests/smoke/helpers/xterm-driver.ts (the smoke driver — add a `mouseTrackingModeOf(id)` helper reading `window.__sessionTerms[id].modes.mouseTrackingMode`, and a wheel-dispatch helper if not present)
  </read_first>
  <action>
    Refactor `SessionView.tsx` SEAM B per 06.1-PATTERNS.md / RESEARCH §Axis-4. Define module
    constants `const MOUSE_RESET = '\x1b[?1000l\x1b[?1001l\x1b[?1002l\x1b[?1003l\x1b[?1004l\x1b[?1005l\x1b[?1006l\x1b[?1015l';`
    and `const ALT_SCREEN_EXIT = '\x1b[?1049l';`. RESTART seam (the
    `hasRunBeforeRef.current` branch l.307–318): write
    `MOUSE_RESET + (term.buffer.active.type === 'alternate' ? ALT_SCREEN_EXIT : '')` (gate
    the alt-screen exit on actually being in the alternate buffer — D-07 avoids a needless
    primary-buffer toggle that trims scrollback) THEN the `— restarted HH:MM —` separator;
    NEVER call `term.reset()` on this path. ABNORMAL-EXIT seam (the `exited`/`error` branch
    l.331): REPLACE `term.reset()` with `term.write(MOUSE_RESET + ALT_SCREEN_EXIT)`
    (scrollback-preserving, mouse-safe — RESEARCH Open Q1 recommendation; flag the
    blank-vs-preserve choice for the end-of-phase human-verify). The MOUSE_RESET/separator
    writes are FIXED literals (no interpolation — ASVS V5; the only interpolated text, the
    SC2 notice, stays `sanitizeNotice`-stripped, unchanged). Confirm `hasRunBeforeRef`
    starts false so a fresh dormant Start shows NO separator (D-08 — Pitfall 8). Then EXTEND
    `tests/smoke/alt-screen-reset.smoke.test.ts`: (a) change the existing `KEEP_` restart
    spec to ≥3 restarts and assert the `KEEP_` marker survives ALL N (D-07); (b) add a spec
    that sends `\x1b[?1002h` (enable button-event mouse tracking) into a live session, fires
    `header-restart`, waits for a new ptyPid, and asserts `mouseTrackingModeOf(id) === 'none'`
    (D-13); (c) add a spec that enters alt-screen + `\x1b[?1002h`, kills the PTY, dispatches a
    wheel event over the terminal, and asserts the buffer scrolled (viewportY changed / no
    SGR mouse-report bytes echoed — the `[%30/]` garble is absent from `readBufferOf`).
    Update the abnormal-exit spec's expectation if its assertion conflicts with the
    scrollback-preserving choice (it currently expects the alt-frame marker GONE — still
    true via ALT_SCREEN_EXIT; keep that assertion). Use deterministic waits
    (`browser.waitUntil` on observable state) — NO `waitForTimeout`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx wdio run wdio.conf.ts --spec tests/smoke/alt-screen-reset.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'MOUSE_RESET' src/renderer/SessionView.tsx` and the literal contains `1000l`, `1002l`, `1006l`
    - `grep -c 'term.reset()' src/renderer/SessionView.tsx` is 0 (no RIS anywhere in SessionView per D-07)
    - `grep -q "buffer.active.type === 'alternate'" src/renderer/SessionView.tsx`
    - The smoke suite asserts `mouseTrackingMode === 'none'` after restart and that the `KEEP_` marker survives ≥3 restarts
    - `npx wdio run wdio.conf.ts --spec tests/smoke/alt-screen-reset.smoke.test.ts` passes
    - Maps to 06.1-VALIDATION.md rows: "Mouse mode reset on restart", "Scroll-wheel scrolls buffer after killed TUI", "Repeated Restart preserves scrollback", "Restart never calls term.reset()"
  </acceptance_criteria>
  <done>Every death/restart emits MOUSE_RESET (mouseTrackingMode reads 'none'); restart preserves scrollback across ≥3 restarts and never calls term.reset(); the alt-screen exit is gated on the alternate buffer; the smoke suite covers all four D-13/D-07 rows.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (NEW symbols introduced in this plan)
- `MOUSE_RESET` / `ALT_SCREEN_EXIT` — module-local frame-write literals in `src/renderer/SessionView.tsx`
- `viewportLines()` / `fnv1a()` — module-local detector helpers in `SessionView.tsx` (ported from record.cjs)
- The frame-stability `setInterval` tick replacing the `idleTimer` (no exported symbol)
- `mouseTrackingModeOf(id)` (+ a wheel-dispatch helper) — NEW smoke-driver helpers in `tests/smoke/helpers/xterm-driver.ts`
- REMOVED (excluded from drift checks): `agentTail`, `idleTimer`, the `classifyIdle`/`IDLE_MS` imports, `term.reset()` in SessionView
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PTY output → xterm buffer → tick detector | Untrusted terminal bytes are read as rendered-frame text on each tick |
| renderer frame writes → xterm | The app emits MOUSE_RESET/separator literals into the terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-04 | Tampering | frame-write strings (MOUSE_RESET, separator) | mitigate | All frame writes are FIXED string literals — no interpolated/attacker-controlled content; the only interpolated text (the SC2 cwd-bearing notice) remains `sanitizeNotice`-stripped of control chars (unchanged). No ANSI-injection surface added (ASVS V5). |
| T-06.1-05 | DoS | the 100ms tick reading buffer.active over a viewport | accept | A bounded `rows×cols` viewport read + FNV-1a hash every 100ms is O(viewport) and runs renderer-local; no growing buffer, no unbounded scan (RESEARCH: reads only viewportY..+rows, never scrollback). Low cost, no remote target. |
| T-06.1-06 | Info disclosure (privacy line) | detector input | mitigate | The detector hashes/classifies frame SHAPE only and never sends frame content over IPC or to disk (renderer-local, zero-IPC — D-09/D-12). |
| T-06.1-07 | EoP | new bridge key | mitigate | NONE added — detection is renderer-local; smoke-driver helpers read `window.__sessionTerms` (a test handle, not a bridge key). `security.guard.test.ts` (19 keys) stays green. |
</threat_model>

<verification>
- `npm run test:unit` GREEN (the classifier oracle from Plan 01 is unaffected by the SessionView wiring).
- `npx tsc --noEmit` clean.
- The extended `alt-screen-reset.smoke.test.ts` passes all D-13/D-07 specs.
- `src/main/window-config.ts` NOT in this plan's diff (EXPECTED_API_KEYS stays 19).
- `src/renderer/status-colors.ts` NOT in this plan's diff (presentation()/AGENT_STYLE untouched — D-14).
</verification>

<success_criteria>
- SEAM A: an agent TUI reads in-progress while streaming and settles to waiting/free on a stable frame; hidden panes still tick.
- SEAM B: mouseTrackingMode reads 'none' after restart/abnormal-exit; scroll-wheel scrolls the buffer; no `[%30/]` garble; scrollback survives ≥3 restarts; no term.reset() on restart.
</success_criteria>

<output>
Create `.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-02-SUMMARY.md` when done.
</output>
