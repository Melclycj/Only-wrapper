---
phase: 06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r
plan: 04
subsystem: renderer-two-bucket-ui
tags: [term-12, term-09, two-bucket, working-area, inactive-list, remove, delete, keyboard-focus, d-08, wdio-smoke]

# Dependency graph
requires:
  - phase: 06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r
    provides: "06.1-02 renderer SEAMs (frame-stability tick + presentation() overlay driving the row/header dot; the npm run package smoke note) + 06.1-03 main-side two-bucket model (listConfiguredSessions, configured auto-promotion, onExit self-exit routing, dormantRecords)"
provides:
  - "Two-section sidebar — labeled Working Area (live) + Inactive List (dormant not_started); each Inactive entry carries Start ▶ / Start-without-command / permanent Delete (D-01/D-06)"
  - "Live-only IdentityHeader — Clear + Restart + Remove; the contextual header Start (header-start) is GONE (D-06 supersedes Phase-6 D-11)"
  - "Remove (keep recipe → Inactive List for a configured session) vs Delete (permanent) split, both behind the existing ConfirmModal (D-03)"
  - "Keyboard-focus fix — Tab/Space/Enter reach the header buttons (xterm no longer swallows keys when focus is off its textarea)"
  - "app-restart-restore.smoke.test.ts — D-08 (configured persists + dormant first Start has no separator)"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Two-bucket render-time partition: workingArea = status !== 'not_started' (live; an error card stays here per D-05) vs inactiveList = status === 'not_started' (dormant), grouped into labeled <div data-testid> sections over the SAME order-sorted listSessions() snapshot — NOT a new state machine (main stays authoritative); a single SortableContext still spans all ids so cross-section drag-reorder is intact"
    - "Renderer-only Remove for a configured live session: window.api.ptyStop (kill, no new bridge key) + an optimistic flip of the renderer row to not_started → it moves to the Inactive List in-session; main keeps the configured record (persisted via listConfiguredSessions) so it restores dormant on the next boot. Ephemeral Remove + any Delete use window.api.ptyClose (permanent)."
    - "Keyboard-focus gate in attachCustomKeyEventHandler: when the keydown target is NOT xterm's .xterm-helper-textarea, return false so xterm does not consume the key and the browser performs native button focus-traversal/activation (Tab/Space/Enter) — keys are never injected into the PTY while a control is focused"

key-files:
  created:
    - "tests/smoke/app-restart-restore.smoke.test.ts"
  modified:
    - "src/renderer/Sidebar.tsx"
    - "src/renderer/IdentityHeader.tsx"
    - "src/renderer/SessionManager.tsx"
    - "src/renderer/SessionView.tsx"
    - "src/renderer/IdleCard.tsx"
    - "src/renderer/terminal.css"
    - "tests/smoke/persistence.smoke.test.ts"
    - "tests/smoke/reorder.smoke.test.ts"

key-decisions:
  - "Remove of a configured live session is renderer-only (ptyStop + optimistic not_started flip), NOT a new main primitive: main's stop() keeps a user-stopped session 'stopped' in the live map by design (Plan 03 test asserts user-stopped is NOT moved to dormant, so the restart path can respawn), so a user-driven Remove must do the dormant flip in the renderer. The configured record still persists, so on the next boot coerceOnLoad restores it not_started (the Inactive List). No new bridge key (security.guard 19-key invariant stays green)."
  - "The literal OS quit/relaunch for the D-08 smoke is replaced by the Remove→dormant path, which is byte-identical in shape to a boot-restore (coerced not_started, pid cleared). @wdio/electron-service hands each launch an EPHEMERAL Chromium temp userData dir and browser.reloadSession() drops the CDP bridge (ECONNREFUSED on relaunch) — the documented 'WDIO cannot drive a full quit/relaunch' limitation. The smoke proves BOTH D-08 halves: persistence (configured record on disk) + no-separator (dormant first Start). The literal reopen is the Task-3 human-verify."
  - "configured is mirrored onto the renderer row at the edit save sites (handleSaveLive/handleSaveProfile set configured:true; rehydrateProfiles carries main's configured truth) so the renderer knows whether a Remove keeps the recipe or is permanent."

patterns-established:
  - "Section labels (.sidebar-section-label) hide in the collapsed icon-only rail; the Inactive List section only renders when ≥1 dormant session exists"
  - "Two distinct destructive verbs share one ConfirmModal: removeMode ('remove' | 'delete') drives the title/body/confirm-label and the side effect"

requirements-completed: [TERM-12, TERM-09]

# Metrics
duration: ~38min (Tasks 1-2) + ~75min (gap-closure round 1)
completed: 2026-06-08
status: complete
gap-closure-round: 3
human_verified: 2026-06-09
---

# Phase 06.1 Plan 04: Renderer Two-Bucket UI (TERM-12) Summary

**The sidebar now surfaces the two-bucket lifecycle as a labeled Working Area (live) + Inactive List (dormant configured) split, the live header is reframed to Clear + Restart + Remove with the contextual header Start dropped (D-06), the destructive Close is split into Remove (keep recipe → Inactive List) vs Delete (permanent), the terminal no longer swallows Tab/Space so the header controls are genuinely keyboard-focusable, and a new app-restart-restore smoke proves a configured session persists + its dormant first Start shows no "— restarted —" separator (D-08).**

## Performance

- **Duration:** ~38 min
- **Completed:** 2026-06-08
- **Tasks:** 2 of 3 (Task 3 is the blocking end-of-phase human-verify — NOT yet run)
- **Files:** 1 created, 8 modified

## Accomplishments

- **Task 1 — two-bucket sidebar + live-only header + Remove/Delete + keyboard-focus fix:**
  - `Sidebar.tsx`: partitions the order-sorted rows into a labeled **Working Area** section (`status !== 'not_started'`; the error card stays here per D-05) and an **Inactive List** section (`status === 'not_started'`), both inside the existing single SortableContext (cross-section drag-reorder preserved). Each Inactive entry keeps Start ▶ (existing), gains **Start without command** (when a startupCommand is saved — `start-no-cmd-session`) and a permanent **Delete** (`delete-session`); the per-row `presentation()` dot is unchanged so the Plan-02 agent-state overlay still drives it.
  - `IdentityHeader.tsx`: now renders ONLY for a running session and shows **Clear + Restart + Remove** (`header-remove`). The contextual header Start (`header-start`) branch is **deleted** entirely (Start now lives on every Inactive entry — D-06 supersedes Phase-6 D-11); the `onStart` prop is gone, replaced by `onRemove`.
  - `SessionManager.tsx`: splits the single destructive Close into **Remove** (a live action) and **Delete** (an Inactive action), both behind the existing `ConfirmModal` (`removeMode` drives the copy + confirm label). Remove of a **configured** live session → `window.api.ptyStop` + an optimistic flip of the row to `not_started` (it moves to the Inactive List; main persists it as configured so it restores dormant next boot). Remove of an **ephemeral** session and any **Delete** → `window.api.ptyClose` (permanent). The row's `configured` flag is mirrored at the edit save sites. The context menu's Close item becomes Remove (live) / Delete (dormant).
  - `SessionView.tsx`: fixes the keyboard-focus bug in `attachCustomKeyEventHandler` — when the keydown target is NOT xterm's `.xterm-helper-textarea` (i.e. focus is on a header `<button>`), it returns `false` so xterm does not consume Tab/Space/Enter and the browser performs native focus traversal + button activation; no PTY injection.
  - `IdleCard.tsx`: header comment aligned to the Inactive-List framing (the dormant card is the Inactive entry's active view; Start spawns fresh — D-04). No behavior change.
  - `terminal.css`: `.sidebar-section` + `.sidebar-section-label` styling (labels hidden when collapsed).
- **Task 2 — D-08 smoke + persistence/reorder alignment:**
  - NEW `tests/smoke/app-restart-restore.smoke.test.ts`: configures a session (edit → `configured=true`), asserts the **configured record is written to the on-disk store** (persistence half — it WOULD restore), Removes it so it lands as a genuine dormant Inactive-List entry (restore-equivalent shape: `not_started`, pid cleared), Starts it, and asserts the first-Start buffer contains **NO `— restarted `** (D-08). Real Electron + real PTY, deterministic `browser.waitUntil` only.
  - `persistence.smoke.test.ts` + `reorder.smoke.test.ts`: configure (edit) the sessions before asserting on-disk persistence — see Deviations (a Plan-03 regression those smokes were not updated for).

## Task Commits

1. **Task 1: two-bucket sidebar + live-only header + keyboard-focus fix** — `2d3b8b3` (feat)
2. **Task 2: app-restart-restore D-08 smoke + persistence/reorder D-02 alignment** — `1261d52` (test)

## Decisions Made

- **Remove = renderer-only ptyStop + optimistic dormant flip** (no new main primitive / no new bridge key): see frontmatter key-decisions.
- **D-08 smoke uses the Remove→dormant path** instead of a literal OS relaunch (WDIO/electron-service cannot drive a reliable quit/relaunch): see frontmatter key-decisions; the literal reopen is the Task-3 human-verify.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - test correctness] persistence.smoke + reorder.smoke assumed ephemeral sessions persist (stale post-Plan-03)**
- **Found during:** Task 2 (full smoke suite run, the "after every plan wave" sampling)
- **Issue:** Plan 06.1-03 changed `syncStore()` to persist only `configured===true` records (`listConfiguredSessions`, D-02), so a bare `+ Add` (ephemeral) session is intentionally never written to disk. Two pre-existing smokes — `persistence.smoke` ("a created session profile is written to the store file") and `reorder.smoke` ("a persisted custom order round-trips") — Add ephemeral sessions and assert they land on disk, so they went RED. Plan 03's validation sampling was unit-only (its SUMMARY ran `npx vitest run`, not the smoke suite), so the regression was undetected until this wave's smoke run.
- **Fix:** Configure (edit the name → `configured=true`) the sessions before asserting persistence, aligning the tests with the now-current D-02 contract (the PRODUCT behavior — ephemeral never persists — is correct and intended). Both specs now pass.
- **Files modified:** tests/smoke/persistence.smoke.test.ts, tests/smoke/reorder.smoke.test.ts
- **Verification:** full smoke suite GREEN (14 specs).
- **Committed in:** 1261d52

**2. [Rule 3 - blocking test mechanism] literal OS relaunch is not driveable; D-08 smoke drives the restore-equivalent dormant path**
- **Found during:** Task 2 (initial smoke attempts)
- **Issue:** The plan's Task 2 calls for a relaunch/reload smoke. Empirically, `@wdio/electron-service` v10 hands each launch an EPHEMERAL Chromium temp userData dir (so the store from launch 1 is invisible to launch 2), and `browser.reloadSession()` drops the CDP bridge and fails to reconnect the relaunched session (ECONNREFUSED) — and pinning a stable `--user-data-dir` makes the relaunch fail outright (profile lock). This is the same "WDIO cannot reliably drive a full quit/relaunch" limitation the persistence/reorder smokes already document.
- **Fix:** The smoke proves BOTH D-08 guarantees without a literal relaunch: (a) PERSISTENCE — the configured record is asserted on the on-disk store (it WOULD restore; coerceOnLoad → not_started is unit-proven), and (b) NO-SEPARATOR — the configured session is Removed (the new Task-1 path) into a genuine dormant Inactive-List entry, byte-identical in shape to a boot-restore (not_started, pid cleared), and its first Start asserts no `— restarted ` separator. The literal quit/reopen is covered by the Task-3 human-verify.
- **Files modified:** tests/smoke/app-restart-restore.smoke.test.ts
- **Verification:** the new smoke passes (real Electron + real PTY).
- **Committed in:** 1261d52

---

**Total deviations:** 2 auto-fixed. No architectural changes. No new bridge key (`window-config.ts` not in this plan's diff — EXPECTED_API_KEYS stays 19; `status-colors.ts` untouched — D-14).

## Verification

- `npm run test:unit` → **206 unit tests GREEN** (27 files); `security.guard` 19-key bridge invariant GREEN.
- `npx tsc --noEmit -p tsconfig.json` → exit 0.
- `npx eslint` clean on all touched files + full `src/`.
- `npm run package` then `npm run test:smoke` → **14/14 smoke specs GREEN** (incl. the new app-restart-restore + the Plan-02 alt-screen-reset). Per the Plan-02 lesson, `npm run package` was run before every smoke run (the WDIO harness tests the packaged bundle in `out/`, not source).
- Acceptance greps: `Inactive` present in Sidebar; `header-start` GONE; `header-restart` + `clear-terminal` kept; `header-remove` present; smoke contains `restarted`; `presentation(` present in Sidebar.

## Known Stubs

None.

## Threat Flags

None — no new network/auth/file/schema surface. Remove reuses the existing `ptyStop`/`ptyClose` channels; Delete reuses `ptyClose`; the agent-state dot reuses the renderer-local overlay. No new bridge key (T-06.1-12 held); Delete stays behind the ConfirmModal (T-06.1-14); chrome labels render only session metadata, no PTY content (T-06.1-15).

## Next Phase Readiness

- **Task 3 (this plan, BLOCKING):** the end-of-phase human-verify is the ONLY remaining step. All automated suites (unit + smoke incl. alt-screen-reset + app-restart-restore) are GREEN. On the user's "approved" the executor flips `nyquist_compliant: true` in BOTH `06-VALIDATION.md` and `06.1-VALIDATION.md` and records the abnormal-exit frame choice (RESEARCH Open Q1). **The flags are NOT flipped yet** — that requires explicit human approval.

## Gap-Closure Round 1 (post-human-verify-FAIL — 2026-06-08)

The end-of-phase human-verify (Task 3) **FAILED**. Four defects were diagnosed and fixed; each is locked with a regression test. The `nyquist_compliant` flags remain `false` (awaiting a SECOND human-verify). 06.1-04 stays **in-progress**.

### FIX 1 — amber "waiting" never fired (stayed blue) — `080807a`

- **Root cause:** SEAM A only ran `classify()` AFTER the full-viewport hash was unchanged for `SETTLE_MS`. The real `claude` permission screen has a continuously-repainting footer (elapsed-time / token counter / "esc to interrupt · Ns…"), so the full-frame hash NEVER settles → `classify()` never ran → the prompt stayed `in-progress` (blue) forever. The spike capture had a fully-static menu that settled, masking the live failure. `classify()` itself was CORRECT (the oracle proves it) — untouched.
- **Fix:** Extracted the per-tick decision into a pure, DOM/xterm-free helper `src/renderer/agent-tick.ts` (`decideAgentTick`). It now runs `classify()` on EVERY tick and, once it reads `'waiting'` for `WAITING_TICKS` (~300ms) consecutive ticks, emits `'waiting'` IMMEDIATELY — even while the full-frame hash is still churning. The settle-then-classify path is kept for `in-progress`/`free`. Safe from false positives because `classify()` only returns `'waiting'` for genuine menus (oracle-proven). The `❯`/arrow caret was NOT reintroduced.
- **Lock:** `src/renderer/__tests__/agent-tick.test.ts` — a synthetic frame sequence with a CONSTANT menu body + a per-tick incrementing footer resolves to `'waiting'` within ~300ms despite a never-settling hash. Replay oracle stays GREEN.

### FIX 3 — remove the header Restart (↻) button (user decision) — `90e87e5`

- Removed the `↻` Restart button from `IdentityHeader.tsx` + its `onRestart` prop + the `SessionManager` pass-through. The live header is now **Clear + Remove** only.
- **Assessment (as required):** restart-in-place AND the `— restarted HH:MM —` divider are STILL reachable via the Sidebar row `↻` control and the context-menu **Restart** item — so that code path + divider are LEFT intact (NOT dead). Only the header trigger was removed.
- **Tests:** `header-controls.smoke` asserts `header-restart`/`header-start` ABSENT and drives the restart-respawn + separator test via the context-menu Restart path; `alt-screen-reset.smoke` switched its two restart-in-place drivers to the context-menu path. Restart capability also exists via Remove → Start-from-Inactive-List (fresh process, D-04).

### FIX 4b — a running recipe session was lost on restart (persist policy) — `3e64e84`

- **Root cause:** `configured=true` was set ONLY by `updateProfile` (a manual edit), and `listConfiguredSessions()` persisted only `configured` records — so a session carrying a recipe (e.g. a `startupCommand`) but never manually edited never reached disk.
- **Fix (user decision, supersedes edit-only D-02):** persist if **"configured OR has identity"**, where identity = non-empty `startupCommand` OR custom name (not auto `Session N`) OR custom icon OR non-default cwd OR non-default shell. A bare blank `+New` stays ephemeral. New pure predicate `src/main/session-identity.ts` (`hasIdentity`/`shouldPersist`) gates both `listConfiguredSessions()` and the onExit self-exit → Inactive-List routing. `06.1-CONTEXT.md` D-02 refined accordingly.
- **Lock:** `src/main/__tests__/session-identity.test.ts` (the predicate) + re-spec'd `pty-lifecycle.test.ts` two-bucket cases to the identity contract, a recipe self-exit→dormant case, and a persist+restore round-trip; `persistence.smoke` adds a recipe (startupCommand, no name change) persists case.

### FIX 4a — a self-exiting configured session never entered the Inactive List mid-session — `4455169`

- **Root cause:** when a configured session self-exits, main moves it to dormant and broadcasts `pty:status 'exited'`, but the renderer's status subscription only set the row to `'exited'` (which the Sidebar partition keeps in the Working Area) — so it never visibly entered the Inactive List until the next boot.
- **Fix:** new pure reducer `src/renderer/session-status.ts` (`resolveRowStatus`/`hasRendererIdentity`) presents an IDENTITY row's self-exit (`'exited'`/`'error'`) as `'not_started'` so the partition shows it immediately (and drops the dead pid + stale error/overlay). Notice events + non-exit transitions pass through unchanged.
- **Lock:** `src/renderer/__tests__/session-status.test.ts`.

### Follow-on FIX — stale onExit race exposed by FIX 4a — `0cdb149`

- **Root cause (Rule 1 bug):** the dormant Start path (`create({id})`) re-spawns under the SAME logicalId while the previous child is still draining its SIGTERM→exit, overwriting `this.sessions.get(id)` with the NEW PtySession. The OLD child's `onExit` then read the NEW (live) session and relabeled it off the dead child's exitCode — flipping a freshly re-Started session to `'exited'`/dormant (FIX 4a then unmounted its SessionView → no prompt). Surfaced as `app-restart-restore.smoke` timing out on the post-Start prompt.
- **Fix:** in `child.onExit`, no-op when the exiting child is no longer the session's current pty (`s.pty !== child`). `restart()` already had a respawned-once guard; this covers the `create({id})` Start path it does not orchestrate.
- **Lock:** stale-exit unit guard added to `pty-lifecycle.test.ts`.

### Gap-closure verification

- `npx tsc --noEmit` → exit 0 (clean).
- `npx eslint src/ tests/` → exit 0 (clean). NOTE: `npm run lint` (`eslint .`) reports 8 errors confined to pre-existing `.planning/spikes/*.cjs` scratch files (present at the prior phase commit `e8b7bd1`, untouched by this work) — logged to `deferred-items.md`, out of scope per the executor scope boundary.
- `npm run test:unit` → **234 unit tests GREEN** (30 files); `security.guard` 19-key bridge invariant GREEN; `status-colors.ts` untouched (D-14).
- `npm run package` then `npm run test:smoke` → **14/14 smoke spec files GREEN** (31 tests; was 13/14 before the stale-exit follow-on fix). `npm run package` was re-run before the smoke suite (the WDIO harness tests the packaged bundle in `out/`, not source).
- **No new bridge key** (EXPECTED_API_KEYS stays 19). **`nyquist_compliant` NOT flipped** in any VALIDATION file — awaiting the second human-verify.

### Gap-closure commits

1. `080807a` — fix(06.1-04): make agent 'waiting' verdict settle-independent (amber fires) — FIX 1
2. `90e87e5` — fix(06.1-04): remove header Restart (↻) button — live header is Clear + Remove — FIX 3
3. `3e64e84` — fix(06.1-04): persist policy = identity/recipe (running recipe survives restart) — FIX 4b
4. `4455169` — fix(06.1-04): configured/recipe self-exit flips renderer row to Inactive List — FIX 4a
5. `0cdb149` — fix(06.1-04): guard stale onExit from a child replaced by re-Start (same id) — follow-on

## Gap-closure ROUND 2 — readiness-timer revert (ITEM 3 + ITEM 4)

Second human-verify: items 1/2/5 (amber, header Clear+Remove, scroll fidelity) APPROVED. Items 3 (persistence) + 4 (Inactive List) still failed: "the list exists for a second, but quickly the inactive list ui disappears, and they are in the working area."

### Root cause (verified, RED-reproduced first) — `4c5d091` (tests) + `c2ffd91` (fix)

- **ITEM 4 (the ~1s revert):** main's TERM-05 readiness-probe timer can fire AFTER a recipe session has self-exited (child died before the probe settled). The old code only cleared the timer on a successful probe match, so a self-exited recipe left the timer armed; it then fired ~`READINESS_TIMEOUT_MS` later and broadcast a stale `pty:status {status:'running'}` (carried as a ready-fail `notice`). The renderer's onPtyStatus reducer adopted the notice's status field → resurrected a row that had correctly moved to the Inactive List back into the Working Area. Plural "they" = multiple recipes each with a pending probe.
- **ITEM 3 (persistence):** the audit test (`pty-recipe-persistence.test.ts`) drives the FULL production store wiring (`PtyManager` + real temp-file `SessionStore` + `syncStore=listConfiguredSessions`) and PASSES — an unedited recipe (startupCommand, `configured` unset) persists AND restores as a dormant `not_started` entry, both on self-exit and when still running at quit. So main-side persistence had **no reproducible defect**; ITEM 3's user-visible symptom was downstream of ITEM 4 (the recipe never visibly STAYED in the Inactive List, so it looked un-persisted).

### Fix — single authoritative rule: a ready-fail notice is INFORMATIONAL ONLY, never drives a lifecycle status transition

- **MAIN (`src/main/pty-manager.ts`):** the readiness-timeout branch now bails when the exiting child is no longer the session's current live pty (self-exited / Removed / replaced by a re-Start under the same id) → no stale flush, no stale status.
- **RENDERER (`src/renderer/apply-status-event.ts`, new pure reducer wired into `SessionManager.tsx`):** a notice's status field no longer overrides a lifecycle row state — extracted to a pure, unit-testable module.

### Round-2 locks (RED→GREEN)

- `src/main/__tests__/pty-readiness-revert.test.ts` — recipe self-exits before probe settles → no stale 'running'.
- `src/renderer/__tests__/apply-status-event.test.ts` — stale running notice must NOT resurrect a dormant row (+7 regression guards for FIX 4a / error capture / overlay clearing).
- `src/main/__tests__/pty-recipe-persistence.test.ts` — full-wiring recipe persist+restore round-trip (regression guard; passes).

### Round-2 verification (re-run by orchestrator after the executor's API drop)

- `npx tsc --noEmit` → exit 0. `npx eslint src/ tests/` → exit 0.
- `npm run test:unit` → **245 unit tests GREEN** (33 files; +11 from round 1's RED→GREEN repros).
- `npm run package` then `npm run test:smoke` → **14/14 smoke spec files GREEN** (one flaky spec failed on the first run, clean on a full re-run — WDIO/Electron timing flake, logged for awareness).
- `nyquist_compliant` STILL `false` in both VALIDATION files — awaiting the THIRD human-verify of items 3 + 4.

### Round-2 commits

6. `4c5d091` — test(06.1-04): RED repro for ITEM 4 revert + ITEM 3 persistence audit
7. `c2ffd91` — fix(06.1-04): readiness timer no longer reverts a self-exited recipe to Working Area

## Gap-closure ROUND 3 — Remove revert (A) + dev-close loss (B) + duplicate Start (C)

Third human-verify: amber, SEAM-B mouse reset, header Clear+Remove APPROVED (untouched).
Three defects remained, root-caused with file:line evidence and each RED-reproduced first.

### DEFECT A — Remove of a live configured session reverts Inactive→Working

- **Chain:** configured-live Remove (`SessionManager.confirmClose`) → `window.api.ptyStop` →
  main `stop()` set `userStopped=true` → onExit `deriveStatus` → `'stopped'`; the record
  stayed live as `'stopped'` (self-exit routing was skipped because `selfExit` excludes
  userStopped). Renderer `resolveRowStatus` only mapped `'exited'`/`'error'` → not_started, so
  `'stopped'` passed through and the Sidebar partition put the row back in the Working Area.
- **Fix (main, the chosen mechanism):** new `PtyManager.removeLive()` — the configured-live
  Remove primitive. It kills the PTY like `stop()` but marks the session `removing`, so onExit
  (a) routes the kept record to `dormantRecords` as `not_started` (order preserved) AND
  (b) BROADCASTS `'not_started'` (the record's real terminal state) instead of `'stopped'`.
  The IPC `pty:stop` handler now routes to `removeLive()` (the renderer's only stop-a-live
  gesture — the Stop verb was abolished, D-01). `stop()` is UNCHANGED (the restart precursor —
  still keeps the record live as `'stopped'` so `restart()` can respawn).
- **Why NOT generalize `'stopped'`→not_started renderer-side:** that was the first attempt and
  it broke a RESTART's transient stopped→running — it unmounted the kept SessionView mid-restart,
  dropping the `'— restarted —'` separator and the startup-command re-run (smoke SC3 went RED).
  Reverted; the Remove is handled main-side as a `'not_started'` broadcast instead. `'stopped'`
  now passes through the renderer reducer untouched (restart safety).
- **restart-in-place verified ALIVE:** `restart()` is still wired (context-menu "Restart" +
  sidebar ↻ + `handleRestart`). It calls `this.stop(id)` internally (NOT via IPC), so it keeps
  the live-`'stopped'`→respawn path. The IPC reroute to `removeLive()` does not touch it.
- **Locks (RED→GREEN):** `pty-lifecycle.test.ts` removeLive()-lands-dormant test; the old
  `stop()`-stays-`'stopped'` contract realigned (stop() = restart precursor only) + comment.
  `session-status.test.ts` / `apply-status-event.test.ts`: `'stopped'` passes through; the
  `'not_started'` Remove broadcast lands the row dormant + drops pid/overlay (`movedToInactive`
  now fires on ANY resolution to not_started from a previously-live row).

### DEFECT B — dev close+restart loses the session (reboot keeps it)

- **Root cause:** dev + packaged share ONE store file; on macOS `window-all-closed` only quits
  on non-darwin, so closing the dev window fires no `before-quit`; the old `win.on('closed')`
  ran `disposeAll()` WITHOUT flushing the store. A session created within the ~300ms debounce
  was lost. On-disk file was stuck at `version:1` (SCHEMA_VERSION is 2) — writes re-persisted
  the stale version because lowdb `read()` replaces `db.data` wholesale.
- **Fix:** new `src/main/lifecycle.ts` (electron-free, unit-testable): `handleWindowClosed`
  flushes the store BEFORE `disposeAll()`; `handleWindowAllClosed` flushes on ALL platforms
  before any quit. `index.ts` wires both. `session-store.ts` bumps `db.data.version` to
  SCHEMA_VERSION post-load so writes record the current version.
- **Lock (RED→GREEN):** `pty-recipe-persistence.test.ts` drives the EXTRACTED close handler
  (not `store.flush()` directly) — a just-Started recipe is on disk after a window close, the
  persisted `version` is 2, and a reopen restores it dormant.

### DEFECT C — two Start buttons (different size) on the active dormant entry

- **Root cause:** for the ACTIVE not_started session BOTH primary Starts rendered — the sidebar
  Inactive-entry ▶ (`start-session`) AND the IdleCard large "▶ Start session"
  (`idle-start-session`). Two copies of the same primary Start.
- **Fix:** new pure `src/renderer/start-affordances.ts` — exactly ONE primary Start per dormant
  entry: the active dormant row's IdleCard owns it; the sidebar ▶ is suppressed there. Every
  non-active dormant row keeps its sidebar ▶. `Sidebar.tsx` renders the ▶ via
  `startAffordances().sidebarStart`; the Restart ↻ still shows on a has-run non-running row.
- **Lock (RED→GREEN):** `start-affordances.test.ts` (primaryStartCount === 1 for active +
  non-active dormant; "Start without command" ⏵ is a separate, uncounted affordance).
  `app-restart-restore.smoke.test.ts` realigned to Start via the single IdleCard affordance.

### Round-3 verification (re-run after the fixes)

- `npx tsc --noEmit` → exit 0 (clean). `npx eslint src/ tests/` → exit 0 (clean; pre-existing
  `.planning/spikes/*.cjs` eslint errors remain out of scope per the scope boundary).
- `npm run test:unit` → **256 unit tests GREEN** (34 files; +11 from round-2's 245 — the new
  round-3 RED→GREEN repros across pty-lifecycle / pty-recipe-persistence / session-status /
  apply-status-event / start-affordances).
- `npm run package` then `npm run test:smoke` → **14/14 smoke spec files GREEN**. First run had
  2 failures (`pty-resize` known timing flake + `app-restart-restore` which was a real
  test-coupling to the old duplicate-Start that the FIX-C smoke realignment then resolved);
  full re-run after the realignment → 14/14 clean. Notably `startup-command` SC3 (Restart
  re-runs) + `header-controls` Restart both PASS — confirming restart-in-place is intact.
- `nyquist_compliant` STILL `false` in both VALIDATION files — awaiting the FOURTH human-verify.

### Round-3 commits

8. `01f7c3e` — test(06.1-04): RED repros + contract realignment for round-3 defects A/B/C
9. `c48d521` — fix(06.1-04): round-3 defects A (Remove revert), B (dev-close loss), C (dup Start)

### Round-3 re-verify checklist (for the human)

- **A:** Start a configured session → Remove it (header ✕ → confirm "Remove"). It must move to
  the Inactive List AND STAY there (no ~1s revert to the Working Area). Then Start it again from
  the Inactive entry — it spawns fresh, no `— restarted —`. Separately: a context-menu/↻ Restart
  of a LIVE session still works in-place (same id, new pid, `— restarted —` separator).
- **B:** Start a session, then immediately CLOSE the window (don't Cmd+Q). Reopen the app — the
  session must be restored as a dormant Inactive-List entry (was lost before on a dev close).
- **C:** Select a dormant Inactive entry — there must be exactly ONE primary Start visible (the
  in-card "▶ Start session"); the small sidebar ▶ for that active row is gone. A non-selected
  dormant row still shows its sidebar ▶.

## Self-Check: PASSED

All gap-closure files exist on disk across rounds 1-3 (round 3 adds `src/main/lifecycle.ts`, `src/renderer/start-affordances.ts`, `src/renderer/__tests__/start-affordances.test.ts`, plus the modified `pty-manager.ts` / `session-store.ts` / `index.ts` / `session-status.ts` / `apply-status-event.ts` / `Sidebar.tsx` and the realigned test/smoke files); the round-3 commits (`01f7c3e` test, `c48d521` fix) and all prior round-1/round-2/task commits are present in git history. The now-FOURTH blocking human-verify is intentionally NOT executed — the `nyquist_compliant` flags stay `false` until explicit user approval.

---

## Post-completion regression fix (2026-06-09)

After the FOURTH human-verify, a code-review remediation (CR-01..CR-04 + WR-02, commits
`50d6c41` + `4ae8e1a`) was **REVERTED** (`fda9fe3`, `d765fa9`) because its CR-03 "coalesce"
fix DROPPED mid-write mutations — a worse data-loss bug than the one it set out to fix.
Storage (`session-store.ts` `flush()`) is restored to the user-approved-good version. Two
user-reported issues remained, sharing one root cause; both are now fixed surgically.

### R3 (primary) — "two Start buttons of different size, especially after clicking a task"

LATENT bug (present since before approval; NOT from the reverted remediation). On the ACTIVE
dormant recipe row (a dormant session WITH a saved `startupCommand`, selected so its IdleCard
shows), THREE Start surfaces existed and TWO co-rendered: the IdleCard large "▶ Start session"
AND the small sidebar "Start without command" ⏵. Round-3 FIX C deduped only the PRIMARY
sidebar ▶; it explicitly (and wrongly) treated the ⏵ as a separate affordance exempt from the
active-card dedupe.

**Fix:** `start-affordances.ts` now suppresses `startNoCmd` when `idleCardStart` is true and
exposes `totalStartCount` (sidebar ▶ + ⏵ + IdleCard ▶). `Sidebar.tsx` gates the ⏵ on
`startCtl.startNoCmd` (was the ungated `dormant && hasStartupCmd`). On the active dormant row
exactly ONE Start-labeled control now renders TOTAL — the IdleCard ▶. NON-active dormant
recipe rows keep their ▶ + ⏵ pair (D-06, not over-suppressed). Commits `2596356` (RED test),
`1911d40` (fix).

### R1 — "command not run on start" — SAME root cause as R3

The startup-command inject path (`pty-manager.ts` `create()` readiness-probe → inject) is
INTACT and UNCHANGED. The command did not run because the user was clicking the SMALL ⏵ ("Start
WITHOUT command" → `handleStartNoCmd` → `skipStartupCommand: true` → command deliberately
skipped). Removing the duplicate ⏵ from the active row (the R3 fix) leaves the IdleCard ▶
(`handleStart` → runs the command) as the only visible Start → R1 resolves. No `create()` change.

### Coverage the suite was missing (added)

- **R3 render contract** (`start-affordances.test.ts`): active dormant recipe row → `totalStartCount === 1`
  (IdleCard ▶); sidebar ▶ AND ⏵ both suppressed; NON-active dormant row keeps both. The old
  pure-predicate test encoded the WRONG assumption (allowed ⏵ on the active card) and missed this.
- **R1 dormant-Start smoke** (`startup-command.smoke.test.ts`): drives the DORMANT Start ▶
  (IdleCard `idle-start-session`, handleStart) end-to-end and asserts the command output + typed
  command appear — plus a live-DOM R3 cross-check (active row shows exactly one Start). Prior
  smoke covered only the Restart path (SC3).
- **R2 durability guard** (`session-store.test.ts`): mutate A → hold the write in flight →
  mutate B mid-write → release → on-disk === B and `writeCount === 2`. PASSES against the
  restored flush(); locks the "no mid-write mutation dropped" contract so a future coalesce
  optimization that drops B is caught.

### Deferred criticals (tracked debt — NOT re-applied)

`.planning/todos/pending/2026-06-09-redo-phase-06.1-code-review-criticals.md` documents that
CR-01 (dock-relaunch quit-flag), CR-02 (concurrent-restart lock — note its latent failed-respawn
`restarting`-stuck edge), CR-03 (failed-write data loss — clear dirty only AFTER write resolves +
follow-up write if dirtied mid-write; never coalesce), CR-04 (setOrder clamp), WR-02 (handleRestart
pid>0 guard) must be redone carefully with tests that exercise the REAL path (not stubs) and a
MANDATORY user re-verify of start/restart/persistence afterward.

### Gates (2026-06-09)

- `npx tsc --noEmit` → clean (exit 0)
- `npx eslint src/ tests/` → clean (exit 0; pre-existing `.planning/spikes/*.cjs` eslint errors are out of scope)
- `npm run test:unit` → 34 files, **260 passed**
- `npm run package` → success; `npm run test:smoke` → 14 spec files passed, **100% completed, no flake** (the new R1 dormant-Start smoke passes)

### Commits (2026-06-09)

- `2596356` — test(06.1): R3 regression — active dormant recipe row must show exactly one Start
- `1911d40` — fix(06.1): suppress duplicate Start ⏵ on active dormant recipe row (R3→R1)
- `6257097` — test(06.1): R2 guard — mid-write mutation must not be dropped by flush()
- `178e34a` — test(06.1): R1 smoke — dormant Start ▶ (IdleCard) runs the saved startupCommand

### User re-verify (still required — green is NOT proof; the suite passed while these were broken)

- **R3:** Select a dormant Inactive-List entry that has a saved startup command → there must be
  EXACTLY ONE Start control visible (the in-card "▶ Start session"). The small sidebar ⏵ "Start
  without command" must be GONE for that active row. A NON-selected dormant recipe row still shows
  its sidebar ▶ + ⏵.
- **R1:** Click that single in-card "▶ Start session" → the saved startup command must actually RUN
  (its output appears + the command is typed at the prompt).
- **R2:** Make rapid changes (rename / reorder) then quit and relaunch → the LAST change must
  survive (persistence is not lost under back-to-back mutations).

`nyquist_compliant` flags are intentionally UNTOUCHED — the orchestrator handles phase status
after the USER re-verifies in the running app.

## Post-completion resolution — USER RE-VERIFIED 2026-06-09

All three post-completion regressions are fixed and CONFIRMED by the user in the running app:
- **R3 (duplicate Start button)** ✓ — latent FIX-C gap (the sidebar "Start without command" ⏵ co-rendered with the IdleCard ▶ on the active dormant recipe row); now suppressed on the active row. User: approved.
- **R1 (startup command not run on Start)** ✓ — same root cause as R3 (the user was clicking the smaller "Start without command" ⏵). Resolved by R3; the create()/readiness-probe inject path was never modified. User: approved.
- **R2 (storage not persisting)** ✓ — caused by the reverted code-review remediation's `flush()` coalesce (dropped a mid-write mutation). Reverted to the approved-good `flush()`. The user's interim "still broken" was a STALE dev `npm start` MAIN process (dev hot-reloads the renderer only; main-process changes need a full `npm start` restart) — a fresh start picked up the revert and persistence works. User: "the storing is working."

LESSON: dev `npm start` does NOT reload main-process changes on the fly — always fully restart `npm start` when verifying a main-side fix, or the test runs against stale code.

The 4 code-review criticals (CR-01..CR-04 + WR-02) were REVERTED (commit fda9fe3/d765fa9) because the rushed remediation broke R2; they are deferred to `.planning/todos/pending/2026-06-09-redo-phase-06.1-code-review-criticals.md` to be redone with real-path tests + mandatory user re-verify. Temporary `[STORE-DEBUG]` instrumentation added during R2 diagnosis was removed (commit d439686). Final gates after all reverts/fixes: tsc clean, eslint(src/,tests/) clean, 260 unit GREEN, 14/14 smoke GREEN (pty-resize known flake clean on re-run). Phase 06.1 genuinely complete.
