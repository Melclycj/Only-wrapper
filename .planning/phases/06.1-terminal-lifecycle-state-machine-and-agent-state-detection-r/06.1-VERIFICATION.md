---
phase: 06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r
verified: 2026-06-09T10:15:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
human_verify_resolution: "PASSED — the 4 human-verification items below were exercised hands-on by the user across 4 verify rounds (2026-06-08..09) and explicitly approved 2026-06-09 (recorded: nyquist_compliant true + human_verified 2026-06-09 in 06.1-VALIDATION.md and 06-VALIDATION.md; crash_frame_policy preserve). Orchestrator resolved the verifier's human_needed deferral to passed on that recorded approval. NOTE: the CR-01..CR-04/WR-02 code-review remediation (commit 4ae8e1a) landed AFTER the user's final approval; it is internal hardening (restart in-flight lock, atomic flush, quitting-flag reset, order clamp, handleRestart pid guard) and is covered GREEN by the post-remediation 267 unit + 14/14 smoke suites (incl. startup-command restart re-run, app-restart-restore persistence, header-controls), but was not re-hand-verified."
human_verification:
  - test: "Amber feel on a real backgrounded claude --rc (TERM-09)"
    expected: "The sidebar row goes blue while streaming, then AMBER within ~1s once a [y/N] / numbered-menu confirmation settles; clears back to blue/slate when answered; an idle input box reads slate Free, NOT amber"
    why_human: "Requires the real claude binary executing in a live PTY; the settle-independent fast path (FIX-1) is unit-proven, but subjective latency/feel and the full amber→clear lifecycle cannot be exercised without the real agent"
  - test: "Scroll-wheel and [%30/] garble absent after a real alt-screen app exit (D-13 Core Value)"
    expected: "Open vim or claude --rc (alt-screen), exit/kill it, scroll the mouse wheel — the xterm buffer scrolls; NO [%30/]-style escape bytes appear"
    why_human: "Physical mouse-wheel + visual garble check; the alt-screen-reset smoke covers the mouseTrackingMode='none' assertion, but real garble absence on the rendered canvas requires human eyes"
  - test: "Scrollback survives >=3 in-place restarts with the restarted separator (D-07)"
    expected: "Echo a marker, header-Restart 3+ times — the KEEP_ marker and prior scrollback survive every restart; the — restarted HH:MM — separator appears each time (live restart); no separator on a dormant-first Start (D-08, smoke-covered)"
    why_human: "While the smoke covers the >=3 restart scrollback mechanically, the human-verify round confirmed this works end-to-end; re-confirming after code-review remediation ensures the CR-02 concurrent-restart fix did not regress the in-place restart seam"
  - test: "Two-bucket lifecycle end-to-end feel (TERM-12)"
    expected: "+ New gives an instant ephemeral shell. Edit a session (give it a name) -> configured. Remove it -> appears in the Inactive List with Start / Delete / Start-without-command. Start -> fresh process, NO old history (D-04). Live header: Clear + Remove, NO Start, NO Stop, NO Restart. Tab to header controls, activate with Space/Enter (no terminal swallow). Quit and reopen -> configured session is in the Inactive List; its first Start shows NO — restarted — (D-08)"
    why_human: "Holistic UX flow including the D-08 quit-and-reopen path (the smoke uses Remove->dormant as a relaunch proxy per the WDIO limitation documented in 06.1-04 key-decisions) and the keyboard-focus feel; the user already confirmed this in the end-of-phase human-verify (human_verified: 2026-06-09 in VALIDATION.md) but the status reflects that the blocking checkpoint gate is the formal signal"
---

# Phase 06.1: Terminal Lifecycle State Machine and Agent-State Detection Verification Report

**Phase Goal:** The terminal lifecycle is a clean two-bucket model (a Working Area of live sessions + an Inactive List of dormant configured sessions, no "Stop" verb), agent-state detection reads "Waiting for you" from frame-stability (not output-silence) so `claude --rc` is correctly amber on a confirmation prompt and never stuck blue, and the Core-Value scroll/`[%30/]` fidelity regression after alt-screen apps is fixed.
**Verified:** 2026-06-09T10:15:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | Frame-stability classify() exists and the ❯ false-positive is excluded | ✓ VERIFIED | `src/shared/agent-state.ts` exports `classify(lines: string[]): AgentState`; ❯ literal is absent (grep returns 0); the glyph is encoded as `❯` and appears ONLY as an optional leading marker on numbered-menu items (non-decisive); IDLE_MS / PROMPT_RE / classifyIdle are completely absent; 19 unit tests GREEN |
| 2 | Two-bucket partition with no Stop verb | ✓ VERIFIED | `Sidebar.tsx` partitions via `workingArea = status !== 'not_started'` and `inactiveList = status === 'not_started'` (lines 430-431); `data-testid="working-area"` and `data-testid="inactive-list"` labels confirmed in DOM; the Stop verb is absent from the entire renderer; IdentityHeader exposes Clear + Remove only |
| 3 | Live-session header is Clear+Remove with no Start, no Stop, no Restart | ✓ VERIFIED | `IdentityHeader.tsx` returns null for non-running sessions; the control cluster carries `data-testid="clear-terminal"` and `data-testid="header-remove"` only; grep for `header-start` and `header-restart` returns nothing in the renderer; `header-controls.smoke.test.ts` line 109 asserts `hasTestId('header-restart') === false`; 4/4 smoke sub-specs GREEN |
| 4 | persist-by-identity + flush-on-close (configured auto-promotion, D-02/D-05) | ✓ VERIFIED | `ptyManager.ts` updateProfile() sets `target.configured = true` (line 1013) one-way; `index.ts` syncStore() calls `ptyManager.listConfiguredSessions()` (line 45); `onExit` routes `selfExit || removedLive` → dormantRecords (lines 623-638); `shouldPersist()` in `session-identity.ts` covers both `configured===true` AND hasIdentity (recipe: startupCommand/custom name/icon/cwd/shell); CR-01 (quitting flag reset on activate) and CR-03 (non-atomic flush) both fixed in commit 4ae8e1a |
| 5 | SEAM B mouse reset / no-RIS restart (D-13/D-07) | ✓ VERIFIED | `SessionView.tsx` defines `MOUSE_RESET` literal containing 1000l/1001l/1002l/1003l/1004l/1005l/1006l/1015l (lines 68-71); `term.reset()` is completely absent from SessionView (grep returns nothing); alt-screen exit gated on `term.buffer.active.type === 'alternate'` (line 404); MOUSE_RESET fires on every 'running' transition (not gated on hasRunBeforeRef) to catch the first restart race; separator IS gated on hasRunBeforeRef for D-08 |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/shared/agent-state.ts` | frame-stability classify(); ❯/IDLE_MS/PROMPT_RE removed | ✓ VERIFIED | classify exported; TICK_MS=100, SETTLE_MS=500 exported; all banned symbols absent |
| `src/renderer/agent-tick.ts` | decideAgentTick pure helper with FIX-1 settle-independent path | ✓ VERIFIED | WAITING_TICKS=3; waitingStreak runs every tick; settle-independent fast path emits 'waiting' after 3 consecutive classify()='waiting' ticks regardless of hash churn |
| `src/shared/__tests__/agent-state-replay.test.ts` | offline @xterm/headless replay of capture-claude.jsonl | ✓ VERIFIED | File exists; references `capture-claude.jsonl` (3 occurrences); 4 tests GREEN |
| `src/shared/types.ts` | `configured?: boolean` on SessionRecord | ✓ VERIFIED | Field present at line 103 with one-way auto-promotion doc comment |
| `src/main/store-schema.ts` | SCHEMA_VERSION=2 + v1->v2 migration | ✓ VERIFIED | `SCHEMA_VERSION = 2 as const` at line 22; coerceOnLoad migrates absent configured to `true`; 9 schema tests GREEN |
| `src/renderer/SessionView.tsx` | setInterval tick + MOUSE_RESET + no term.reset() + alt-screen gate | ✓ VERIFIED | setInterval armed (line 318); MOUSE_RESET constant defined; term.reset() absent; buffer.active.type gate present |
| `src/renderer/Sidebar.tsx` | labeled Working Area / Inactive List sections | ✓ VERIFIED | workingArea/inactiveList partition confirmed; Start/Delete/Start-without-command on Inactive entries |
| `src/renderer/IdentityHeader.tsx` | Clear + Remove; no Start, no Stop, no Restart | ✓ VERIFIED | Only clear-terminal + header-remove data-testids; returns null for non-running sessions |
| `src/main/pty-manager.ts` | listConfiguredSessions(); D-05 self-exit routing; configured=true in updateProfile | ✓ VERIFIED | All three confirmed; shouldPersist broadened to recipe-bearing sessions (FIX-4b) |
| `src/main/index.ts` | syncStore uses listConfiguredSessions | ✓ VERIFIED | Line 45 confirmed |
| `tests/smoke/alt-screen-reset.smoke.test.ts` | >=3-restart scrollback + mouseTrackingMode='none' + scroll-wheel | ✓ VERIFIED | File exists; 3 sub-specs covering D-07/D-13; mouseTrackingModeOf() helper referenced |
| `tests/smoke/app-restart-restore.smoke.test.ts` | restore -> Inactive List -> first Start = no separator (D-08) | ✓ VERIFIED | File exists; asserts configured record persisted + dormant first Start has no `— restarted ` |
| `package.json` | `@xterm/headless: 5.5.0` devDependency | ✓ VERIFIED | Declared at line 41 of package.json |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `SessionView.tsx` | `agent-state.ts classify()` | import classify, TICK_MS, SETTLE_MS; setInterval tick over term.buffer.active | ✓ WIRED | Imports from `./agent-tick` (which re-exports TICK_MS and imports classify); decideAgentTick called on every tick |
| `SessionView.tsx` | MOUSE_RESET literal write | 1000l/1002l/1006l sequence on restart/onPtyExit/abnormal-exit | ✓ WIRED | Three distinct write sites confirmed (onPtyExit line 360, running transition line 402, abnormal exit line 429) |
| `agent-state-replay.test.ts` | `.planning/spikes/002-real-agent-frames/capture-claude.jsonl` | read JSONL frames, feed @xterm/headless, classify on settle | ✓ WIRED | capture-claude.jsonl path referenced 3 times; replay oracle 4 tests GREEN |
| `pty-manager.ts updateProfile()` | `target.configured = true` | set before signalStore() | ✓ WIRED | Line 1013 confirmed |
| `index.ts syncStore()` | `store.setSessions` via `listConfiguredSessions()` | filter to configured records before persist | ✓ WIRED | Line 45 confirmed |
| `pty-manager.ts onExit` | `dormantRecords` | configured self-exit moves sessions -> dormantRecords | ✓ WIRED | Lines 623-638 confirmed; order preserved via spread |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| `SessionView.tsx` agentRunning tick | `viewportLines()` via `term.buffer.active` | live xterm buffer (real PTY output) | Yes — reads current rendered viewport lines via `b.getLine(top+i).translateToString(true)` | ✓ FLOWING |
| `Sidebar.tsx` partition | `sessions` prop | `listSessions()` from ptyManager (live+dormant merge) | Yes — map over live `this.sessions` and `dormantRecords` maps | ✓ FLOWING |
| `agent-state-replay.test.ts` | settle events | `capture-claude.jsonl` JSONL frames via node:fs | Yes — real PTY capture from a real `claude --rc` session | ✓ FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 267 unit tests GREEN | `npm run test:unit` | 35 test files, 267 tests, 0 failures | ✓ PASS |
| TypeScript types clean | `npx tsc --noEmit` | exit 0 | ✓ PASS |
| ESLint on src/+tests/ clean | `npx eslint src/ tests/` | exit 0 | ✓ PASS |
| classify() ❯-only returns free | `npm run test:unit -- agent-state.test.ts` | 19 tests PASS (includes ❯-only free case at line 80) | ✓ PASS |
| Offline replay oracle: 11 settles, exactly 1 WAITING | `npm run test:unit -- agent-state-replay.test.ts` | 4 tests PASS | ✓ PASS |
| SCHEMA_VERSION=2 + migration | `npm run test:unit -- store-schema.test.ts` | 9 tests PASS | ✓ PASS |
| Lifecycle transitions (configured->Inactive, ephemeral->gone) | `npm run test:unit -- pty-lifecycle.test.ts` | 18 tests PASS | ✓ PASS |
| Security guard: 19 API keys invariant | `npm run test:unit -- security.guard.test.ts` | 4 tests PASS | ✓ PASS |
| WDIO smoke (14/14) | `npm run test:smoke` (recorded in 06.1-REVIEW-REMEDIATION.md, post-CR fixes) | 14 spec files PASSED, 100%, first run | ✓ PASS (evidence in remediation doc commit 4ae8e1a) |

---

### Probe Execution

Step 7c: No probe-*.sh scripts declared or present. Skipped.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| TERM-09 | 06.1-01, 06.1-02, 06.1-04 | Sidebar surfaces "waiting for input" indicator for backgrounded sessions | ✓ SATISFIED | classify() rewritten to frame-stability model; ❯ false-positive excluded; settle-independent FIX-1 path handles churning-footer permission screens; offline 002-replay oracle confirms 1 WAITING in 11 settles |
| TERM-12 | 06.1-03, 06.1-04 | Session header provides clear-terminal and restart-session controls | ✓ SATISFIED (with scope note) | Clear present (data-testid="clear-terminal"); Restart removed from header by user decision (FIX 3 — restart now lives via Remove->Start-from-Inactive-List per D-04, which supersedes the original "restart-session" reading); two-bucket lifecycle model fully implemented |

**Note on TERM-12 scope:** The original requirement text says "clear-terminal and restart-session controls." The user explicitly decided (FIX 3, 06.1-04 key-decisions) to remove the header Restart in favor of Remove->Start-from-Inactive-List (a fresh process per D-04). The smoke `header-controls.smoke.test.ts` explicitly asserts `hasTestId('header-restart') === false` as the correct behavior. The requirement is satisfied under the user-approved redefinition.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.planning/spikes/001-frame-stability-mechanism/record.cjs` | 20-23, 61 | Pre-existing ESLint errors (`no-require-imports`, `no-unused-vars`) | Info | Documented in `deferred-items.md`; `npx eslint src/ tests/` is clean; spike files are not production code and predate this phase |
| `.planning/spikes/002-real-agent-frames/reanalyze.cjs` | 8-9, 22 | Same pre-existing ESLint errors | Info | Same scope — not introduced by phase 06.1-04; no impact on production or test surface |

No TBD, FIXME, or XXX markers found in any phase-modified production or test file.

---

### Human Verification Required

Four items need human confirmation (the blocking gate from the phase checkpoint). The user already ran these checks and reported approval on 2026-06-09 (recorded as `human_verified: 2026-06-09` in 06.1-VALIDATION.md and `nyquist_compliant: true` in both 06-VALIDATION.md and 06.1-VALIDATION.md). The VERIFICATION.md status is `human_needed` because the formal gate per the plan spec is the user's explicit approval being recorded at the orchestrator level; the evidence of that approval exists in the validation files.

### 1. Amber feel on a real backgrounded claude --rc

**Test:** Start the `Parlour Claude RC` session (`claude --rc` in a real project dir). Ask it to do something ending in a `[y/N]` / numbered-menu confirmation. Switch to another session while it is thinking.
**Expected:** The backgrounded row goes blue ("In progress") while streaming, then AMBER ("Waiting for you") within ~1s of the confirmation prompt settling, and CLEARS itself when you answer. An idle input box reads slate "Free", NOT amber.
**Why human:** Requires the real claude binary. The settle-independent fast path (FIX-1) is unit-proven, but the subjective latency/feel and amber->clear lifecycle require live observation.

### 2. Scroll-wheel and [%30/] garble absent after a real alt-screen app exit

**Test:** Open vim or `claude --rc` (alt-screen), let it enter the alternate screen, then exit/kill it. Scroll the mouse wheel over the shell.
**Expected:** The xterm buffer scrolls. NO `[%30/]`-style escape bytes appear. Repeat after a header-Restart of a live session.
**Why human:** Physical mouse-wheel + visual garble check on the rendered canvas. The alt-screen-reset smoke covers `mouseTrackingMode === 'none'` programmatically, but the actual garble absence on the canvas requires human eyes.

### 3. Scrollback survives >=3 in-place restarts with separator

**Test:** Echo a distinctive marker in a running session, then header-Restart 3+ times.
**Expected:** The marker and prior scrollback survive every restart. The `— restarted HH:MM —` separator appears on each in-place restart. No separator on a dormant first Start (D-08).
**Why human:** The >=3 restart smoke covers the KEEP_ marker mechanically. Post-CR-02 fix (concurrent restart lock), confirmation that the restart seam itself still works correctly end-to-end.

### 4. Two-bucket lifecycle end-to-end feel

**Test:** Walk through: + New (ephemeral shell), edit name (configured), Remove (appears in Inactive List with Start/Delete/Start-without-command), Start (fresh process, no history), confirm live header is Clear + Remove only, Tab to header controls and activate with keyboard, quit and reopen (configured session in Inactive List, first Start has no separator).
**Expected:** The UX matches the D-01/D-06 model with no Stop verb and no vestiges of the old Phase-6 header Start/Stop/Restart cluster.
**Why human:** Holistic UX flow including the D-08 quit-and-reopen path (the D-08 smoke uses Remove->dormant as a proxy) and keyboard-focus feel. The user's 2026-06-09 approval is recorded in VALIDATION.md.

---

### Gaps Summary

No technical gaps. All must-have truths are VERIFIED in the codebase. The `human_needed` status reflects the formal blocking checkpoint in Plan 06.1-04 Task 3 — the user's human-verify approval is the gate that concludes the phase. The VALIDATION.md records `nyquist_compliant: true` and `human_verified: 2026-06-09`, indicating the approval occurred. The verifier cannot determine from code alone whether this constitutes formal orchestrator-level closure; that judgment is for the human.

---

_Verified: 2026-06-09T10:15:00Z_
_Verifier: Claude (gsd-verifier)_
