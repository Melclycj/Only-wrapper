---
phase: 06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r
plan: 04
type: execute
wave: 2
depends_on: [06.1-02, 06.1-03]
files_modified:
  - src/renderer/SessionManager.tsx
  - src/renderer/Sidebar.tsx
  - src/renderer/IdentityHeader.tsx
  - src/renderer/IdleCard.tsx
  - src/renderer/SessionView.tsx
  - tests/smoke/app-restart-restore.smoke.test.ts
autonomous: false
requirements: [TERM-12, TERM-09]
must_haves:
  truths:
    - "The sidebar shows two labeled sections — a Working Area (live sessions) and an Inactive List (dormant configured sessions); each Inactive entry has Start ▶, Delete, and a 'Start without command' affordance (D-01/D-06)"
    - "A live session's header shows Clear + Restart + a Remove affordance — NO Start, NO Stop (D-06; supersedes Phase-6 D-11)"
    - "Header controls are keyboard-focusable — Tab reaches Clear/Restart and Space/Enter activates them (the bug where the terminal swallowed Tab/Space is fixed)"
    - "Remove kills the PTY but keeps the recipe (→ Inactive List for a configured session); Delete (from the Inactive List) is permanent (D-03)"
    - "On app restart, a previously-live configured session returns in the Inactive List and its first Start shows NO '— restarted —' separator (D-08)"
  artifacts:
    - path: "src/renderer/Sidebar.tsx"
      provides: "labeled Working Area / Inactive List sections; Start ▶ / Delete / Start-without-command on Inactive entries"
      contains: "Inactive"
    - path: "src/renderer/IdentityHeader.tsx"
      provides: "live-only header: Clear + Restart + Remove (no Start, no Stop); keyboard-focusable"
      contains: "header-restart"
    - path: "tests/smoke/app-restart-restore.smoke.test.ts"
      provides: "restore → Inactive List → first Start has no separator (D-08)"
      contains: "restarted"
  key_links:
    - from: "src/renderer/SessionManager.tsx"
      to: "Working Area / Inactive List partition"
      via: "startedSessions (live) vs not_started (dormant) → labeled Sidebar sections"
      pattern: "startedSessions"
    - from: "src/renderer/IdentityHeader.tsx"
      to: "onRemove / handleCloseRequest"
      via: "Remove affordance on the live header"
      pattern: "remove|onClose"
    - from: "src/renderer/SessionManager.tsx"
      to: "presentation(status, agentState)"
      via: "the agent-state overlay (Plan 02) drives the row/header dot"
      pattern: "presentation\\("
---

<objective>
Axis 1 (TERM-12) renderer: surface the two-bucket lifecycle (D-01/D-06). Group the sidebar
into a labeled Working Area (live) and an Inactive List (dormant configured sessions);
reframe the live header to Clear + Restart + Remove (no Start, no Stop — supersedes Phase-6
D-11); move Start ▶ / Delete / "Start without command" onto every Inactive List entry; fix
the keyboard-focus bug so Tab/Space reach the header controls; and split the destructive
Close into Remove (keep recipe) vs Delete (permanent) per D-03. Start (an Inactive-List
entry, or `+ New`) always spawns a fresh process with NO scrollback/history restore (D-04). The agent-state overlay
(Plan 02) now drives the row/header dot through the unchanged `presentation()`.

Purpose: "Start is discoverable because it lives exactly where non-running sessions live."
North star: fewer steps for simple tasks. This is the user-facing payoff of Plans 02/03.
Output: Refactored `SessionManager.tsx`/`Sidebar.tsx`/`IdentityHeader.tsx`/`IdleCard.tsx`, a
new app-restart-restore smoke, and the end-of-phase human-verify that flips Phase-6
`nyquist_compliant: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-CONTEXT.md
@.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-RESEARCH.md
@.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-PATTERNS.md
@.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-VALIDATION.md
@.planning/DESIGN.md
@.planning/phases/06-robustness-flow-control-polish/06-UI-SPEC.md
@.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-02-SUMMARY.md
@.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-01/D-06 — two-bucket sidebar sections + live-only header (Clear+Restart+Remove) + keyboard-focus fix</name>
  <files>src/renderer/SessionManager.tsx, src/renderer/Sidebar.tsx, src/renderer/IdentityHeader.tsx, src/renderer/IdleCard.tsx, src/renderer/SessionView.tsx</files>
  <read_first>
    - src/renderer/SessionManager.tsx (the partition l.499–503: `activeIsCard`, `startedSessions`; handlers `handleRestart` l.149–160, `handleClear` l.169–174, `handleStart` l.183–202, `handleStartNoCmd` l.209–229; the destructive Close path `handleCloseRequest` l.119–120 / `confirmClose` l.128–141 calling `window.api.ptyClose`; the IdentityHeader render l.537–543 passing onStart; the ConfirmModal l.566+)
    - src/renderer/Sidebar.tsx (the row l.182–275; the per-row `presentation()` dot l.168–169; `dormant` test l.180; the dormant Start ▶ l.243–259 and the restart ↻ l.260–275; the SortableContext/list rendering — find where rows are mapped to group them into sections)
    - src/renderer/IdentityHeader.tsx (the control cluster l.58–119: Clear always l.74–87, Restart-when-running l.88–102, Start-when-not-running l.103–118 — D-06 DROPS the header Start; the props l.27–45 incl. `onStart`)
    - src/renderer/IdleCard.tsx (the dormant + error branches l.109–155; the dormant Start affordance l.147–154; the error card Edit/Retry l.126–144 — retained)
    - src/renderer/SessionView.tsx (l.176–187 attachCustomKeyEventHandler — the suspect for swallowing Tab/Space before they reach the header; the container focus order)
    - .planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-PATTERNS.md (§SessionManager.tsx D-01 partition l.241–256; §IdentityHeader.tsx D-06 l.260–278; §Sidebar.tsx D-01/D-06 l.282–297)
    - .planning/DESIGN.md (§"Status system (agent-state)" — the dot/badge inventory; keep the amber reserved for waiting)
    - .planning/phases/06-robustness-flow-control-polish/06-UI-SPEC.md (the header control cluster + error-card contract to reuse, adjusted for no-Stop/two-bucket)
  </read_first>
  <action>
    Per 06.1-PATTERNS.md, refactor the renderer for D-01/D-06 (reuse existing markup — D-14):
    (1) SECTIONS — In `Sidebar.tsx`, partition the rows into a labeled "Working Area" section
    (live: `status !== 'not_started' && status !== 'error'`, plus the `error` row which is a
    Working-Area card per D-05) and an "Inactive List" section (`status === 'not_started'`).
    UI placement is Claude's discretion (a labeled section header beneath the active rows is
    the recommended shape per CONTEXT.md discretion). Keep the existing `presentation()` dot
    (l.168–169) so the agent-state overlay from Plan 02 shows; keep the dormant Start ▶
    (l.243–259) on Inactive entries; add a Delete button and ensure the "Start without
    command" option (wired via `handleStartNoCmd`) is reachable on each Inactive entry (button
    or the existing context menu). (2) HEADER — In `IdentityHeader.tsx`, render the header
    ONLY for live sessions; show Clear (always) + Restart + a Remove affordance; DROP the
    contextual header Start branch (l.103–118) entirely (Start now lives on Inactive entries —
    D-06 supersedes Phase-6 D-11). Add a `onRemove` (or reuse the existing `onClose`/
    `handleCloseRequest`) prop wired to the Remove control. (3) REMOVE vs DELETE — In
    `SessionManager.tsx`, split the single destructive Close into: Remove (a live-session
    action) → for a CONFIGURED session, kill the PTY and keep the recipe so it lands in the
    Inactive List (the configured self-exit/stop transition from Plan 03 handles the dormant
    move; for a user-driven Remove issue the existing stop/close path that preserves the
    record when configured) — for an EPHEMERAL session, Remove → gone; and Delete (an
    Inactive-List action) → permanent (`window.api.ptyClose` removes the record entirely).
    Keep the ConfirmModal for the permanent Delete. (4) KEYBOARD FOCUS — fix the bug where
    Tab/Space were swallowed by the terminal before reaching the header controls: in
    `SessionView.tsx attachCustomKeyEventHandler` (l.176–187), return `true` (let the browser
    handle it) for Tab and for Space/Enter when the event target is NOT the terminal textarea
    (i.e. when focus is on a header `<button>`), so native button focus/activation works;
    confirm the header `<button>`s are in the natural Tab order (they already are native
    buttons with `:focus-visible`). Do NOT inject Tab/Space into the PTY when a control is
    focused. Update the IdentityHeader render call in SessionManager (l.537–543) to drop
    `onStart` and pass the Remove handler. Keep the `handleClear` client-side `term.clear()`
    (D-12) and the Clear chord (no new bridge key).
  </action>
  <verify>
    <automated>npm run test:unit && npx tsc --noEmit -p tsconfig.json && npx eslint src/renderer/SessionManager.tsx src/renderer/Sidebar.tsx src/renderer/IdentityHeader.tsx src/renderer/IdleCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi 'Inactive' src/renderer/Sidebar.tsx` (the Inactive List section label exists)
    - `grep -q 'header-start' src/renderer/IdentityHeader.tsx` returns nothing (the header Start branch is gone — D-06)
    - `grep -q 'header-restart' src/renderer/IdentityHeader.tsx` (Restart kept) and Clear (`clear-terminal`) kept
    - The header exposes a Remove control (grep a `data-testid` for remove/close on the header)
    - `npx tsc --noEmit` exits 0; `npx eslint` clean on the four files
    - `EXPECTED_API_KEYS` unchanged (window-config.ts NOT in this plan's diff)
    - Maps to 06.1-VALIDATION.md row: "Configured self-exit → Inactive List" (rendered presentation) and the TERM-12 header controls
  </acceptance_criteria>
  <done>The sidebar shows labeled Working Area + Inactive List sections; the live header is Clear+Restart+Remove (no Start/Stop); Start ▶/Delete/Start-without-command are on Inactive entries; Tab/Space reach header buttons; Remove keeps the recipe (configured)/Delete is permanent; tsc + eslint + unit GREEN; no new bridge key.</done>
</task>

<task type="auto">
  <name>Task 2: D-08 — app-restart restore lands in the Inactive List; first Start shows no separator (smoke)</name>
  <files>tests/smoke/app-restart-restore.smoke.test.ts</files>
  <read_first>
    - tests/smoke/persistence.smoke.test.ts (the existing persist/restore smoke — the relaunch pattern + how it reads restored rows; the MODEL for this new smoke)
    - tests/smoke/startup-command.smoke.test.ts (the restart-separator assertion pattern — `— restarted ` literal; how it drives Start)
    - tests/smoke/helpers/xterm-driver.ts (`readBufferOf`, `clickByTestId`, `clickSidebarRow`, `ensureSession`, the relaunch/reload helpers)
    - src/renderer/SessionView.tsx (after Plan 02 — `hasRunBeforeRef` starts false so a fresh dormant Start shows NO separator — Pitfall 8/D-08)
    - .planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-RESEARCH.md (§D-08 + Pitfall 8 — the dormant restore mounts a FRESH SessionView so hasRunBeforeRef is false)
    - .planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-VALIDATION.md (the D-08 smoke row)
  </read_first>
  <action>
    Create `tests/smoke/app-restart-restore.smoke.test.ts` (WDIO, real Electron + real PTY):
    create a session and CONFIGURE it (issue an edit via the edit modal / a profile change so
    `configured=true` — D-02) so it persists; lay down a distinctive primary-screen marker;
    relaunch/reload the app (the persistence.smoke pattern); assert the restored session
    appears in the Inactive List section as a dormant entry (not in the Working Area);
    click its Start ▶; wait for it to go running; read `readBufferOf(id)` and assert the
    buffer does NOT contain `— restarted ` on this FIRST Start (D-08 — the separator is only
    for in-place restart of a live session). Use deterministic waits (`browser.waitUntil` on
    the running status / a buffer marker) — NO `waitForTimeout`.
  </action>
  <verify>
    <automated>npx wdio run wdio.conf.ts --spec tests/smoke/app-restart-restore.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/smoke/app-restart-restore.smoke.test.ts` exists and asserts the restored row is in the Inactive List and the first-Start buffer has NO `— restarted `
    - `npx wdio run wdio.conf.ts --spec tests/smoke/app-restart-restore.smoke.test.ts` passes
    - The test uses `browser.waitUntil` (no `waitForTimeout`/`sleep`)
    - Maps to 06.1-VALIDATION.md row: "App-restart restore → Inactive List, first Start = no separator (D-08)"
  </acceptance_criteria>
  <done>A real relaunch restores the configured session into the Inactive List; its first Start spawns fresh with no `— restarted —` separator (D-08); the smoke is GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-of-phase human-verify — amber feel + scroll/garble fix + two-bucket lifecycle, then flip Phase-6 sign-off</name>
  <files>.planning/phases/06-robustness-flow-control-polish/06-VALIDATION.md, .planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-VALIDATION.md</files>
  <read_first>
    - .planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-VALIDATION.md (§Manual-Only Verifications — the amber-feel + scroll-wheel/garble checks; the sign-off checklist)
    - .planning/phases/06-robustness-flow-control-polish/06-VALIDATION.md (stays nyquist_compliant:false until this gate; the SC1–SC5 this phase finishes)
  </read_first>
  <action>
    PAUSE for the user. All automated suites (unit + offline 002 replay + smoke incl.
    alt-screen-reset and app-restart-restore) must be GREEN before presenting this gate. Drive
    the four hands-on checks in &lt;how-to-verify&gt; (amber feel, scroll/garble fix, scrollback
    survival across ≥3 restarts, two-bucket lifecycle) against the running app. On the user's
    "approved", set `nyquist_compliant: true` in BOTH
    `.planning/phases/06-robustness-flow-control-polish/06-VALIDATION.md` (Phase-6 sign-off this
    phase finishes) and
    `.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-VALIDATION.md`,
    and record the user's abnormal-exit frame choice (scrollback-preserving default vs blank
    crash frame — RESEARCH Open Q1). Do NOT flip the flags or proceed without explicit approval.
  </action>
  <what-built>
    The full Phase-6.1 refactor: frame-stability detection (Plan 02), mouse-reset/scrollback
    fidelity fix (Plan 02), the two-bucket lifecycle + main transitions (Plan 03), and the
    two-section sidebar + no-Stop header + Remove/Delete + keyboard-focus fix (this plan).
    All automated suites (unit + offline replay + smoke incl. alt-screen and app-restart-restore)
    are GREEN before this checkpoint.
  </what-built>
  <how-to-verify>
    1. AMBER FEEL (TERM-09): Start the canonical `🛋️ Parlour Claude RC` session (`claude --rc`
       in a real project dir). Ask it to do something ending in a `[y/N]` / numbered-menu
       confirmation. Switch to ANOTHER session while it's thinking. Confirm: the backgrounded
       row goes blue ("In progress") while it streams, then AMBER ("Waiting for you") within
       ~1s of the confirmation prompt settling, and CLEARS itself (back to blue/slate) when
       you answer — it never nags and never sticks blue forever. An idle input box (claude
       finished its turn) reads slate "Free", NOT amber.
    2. SCROLL / `[%30/]` GARBLE GONE (D-13, Core Value): Open vim (or `claude --rc`), let it
       enter the alt-screen, then exit/kill it. Scroll the mouse wheel over the shell — it
       MUST scroll the xterm buffer, with NO `[%30/]`-style escape garble echoed. Repeat after
       a header-Restart of a live session.
    3. SCROLLBACK SURVIVES RESTART (D-07): Echo a marker, header-Restart 3+ times — the marker
       and prior scrollback survive every restart; the `— restarted HH:MM —` separator appears
       only on in-place restart of a LIVE session.
    4. TWO-BUCKET LIFECYCLE (D-01/D-06): `+ New` gives an instant ephemeral shell. Edit a
       session (give it a name) → it becomes configured. Remove it → it appears in the Inactive
       List with Start ▶ / Delete / "Start without command". Start it → fresh process, NO old
       history (D-04 — the app never replays scrollback; real context continuity is claude --resume's job, out of scope). The live header shows Clear + Restart + Remove (no Start, no Stop). Tab to the
       header controls and activate Clear/Restart with the keyboard (no terminal swallow).
       Quit and reopen → the configured session is in the Inactive List; its first Start shows
       NO `— restarted —`.
  </how-to-verify>
  <verify>
    <human-check>The user runs the four checks above against the running app and types "approved" (or describes issues). On approval both VALIDATION.md files flip nyquist_compliant: true.</human-check>
  </verify>
  <resume-signal>Type "approved" (and confirm the abnormal-exit frame choice: scrollback-preserving is the default — say if you prefer a blank crash frame, RESEARCH Open Q1), or describe issues.</resume-signal>
  <done>The user has confirmed amber feel, scroll/garble fix, scrollback survival across ≥3 restarts, and the two-bucket lifecycle; both 06-VALIDATION.md and 06.1-VALIDATION.md are flipped to nyquist_compliant: true; the abnormal-exit frame choice is recorded.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (NEW symbols introduced in this plan)
- Working Area / Inactive List section markup + labels in `src/renderer/Sidebar.tsx`
- A Remove handler/affordance on `src/renderer/IdentityHeader.tsx` + a `Delete` action on Inactive entries
- `tests/smoke/app-restart-restore.smoke.test.ts` — NEW D-08 smoke
- The Tab/Space focus fix in `attachCustomKeyEventHandler` (SessionView — no new exported symbol)
- REMOVED (excluded from drift checks): the contextual header Start branch (`header-start`) from IdentityHeader; the `onStart` prop from the header call site
- UNCHANGED (D-14): `src/renderer/status-colors.ts` `presentation()`/`AGENT_STYLE` (the overlay resolver the row/header dot route through)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer UI → ptyClose / stop IPC (existing) | Remove/Delete cross into main via existing channels |
| persisted store → restored rows → renderer | Restored configured rows are presented in the Inactive List |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-12 | EoP | new bridge key | mitigate | NONE added — Remove reuses the existing stop/close channels; Delete reuses `ptyClose`; the agent-state dot reuses the renderer-local overlay. `security.guard.test.ts` (19 keys) stays green. |
| T-06.1-13 | Tampering | restored Inactive-List rows | accept | Restored records pass through the existing `coerceOnLoad` (status forced not_started, ptyPid cleared); the renderer only presents them — Start re-validates cwd/shell in main (existing SC2 path). |
| T-06.1-14 | DoS | Destructive Delete confirm | accept | Delete is gated behind the existing ConfirmModal (no accidental permanent loss); Remove (keep-recipe) is reversible via Start. |
| T-06.1-15 | Info disclosure | header/sidebar labels | mitigate | Labels are session metadata (name/status) the user set; no PTY content rendered in the chrome; the SC2 error notice stays `sanitizeNotice`-stripped (unchanged). |
</threat_model>

<verification>
- `npm run test:unit` GREEN; `npx tsc --noEmit` clean; `npx eslint` clean on the four renderer files.
- The new `app-restart-restore.smoke.test.ts` plus the Plan-02 `alt-screen-reset.smoke.test.ts` pass.
- `src/main/window-config.ts` NOT in this plan's diff (EXPECTED_API_KEYS stays 19).
- `src/renderer/status-colors.ts` NOT in this plan's diff (presentation()/AGENT_STYLE untouched — D-14).
- On human-verify approval, both VALIDATION.md files flip `nyquist_compliant: true`.
</verification>

<success_criteria>
- Two-section sidebar (Working Area / Inactive List); Start ▶/Delete/Start-without-command on Inactive entries; live header = Clear+Restart+Remove (no Start/Stop); keyboard-focusable controls; Remove keeps recipe / Delete permanent.
- Restore lands in the Inactive List; first Start has no separator (D-08).
- Human-verify confirms amber feel, scroll/garble fix, scrollback survival, and the two-bucket lifecycle; Phase-6 + Phase-6.1 sign-off flips to nyquist_compliant: true.
</success_criteria>

<output>
Create `.planning/phases/06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r/06.1-04-SUMMARY.md` when done.
</output>
