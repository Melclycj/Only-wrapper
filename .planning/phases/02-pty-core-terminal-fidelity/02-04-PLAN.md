---
phase: 02-pty-core-terminal-fidelity
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - src/renderer/TerminalPane.tsx
autonomous: false
requirements:
  - TERM-01
  - TERM-02
must_haves:
  truths:
    - "A 50MB+ cat does not freeze keyboard input or drop output — the watermark pauses and resumes the PTY stream (SC5)"
    - "Cmd+C copies the selection, Cmd+V and right-click paste using bracketed paste so multi-line paste does not auto-execute (SC2, D-03)"
    - "Ctrl+C still sends SIGINT (distinct from Cmd+C copy) (D-03)"
    - "vim, python REPL, ssh, htop, truecolor, and CJK/emoji render with native fidelity (human-verified, SC1/SC2/SC4)"
  artifacts:
    - path: "src/renderer/TerminalPane.tsx"
      provides: "flow-control watermark wiring + copy/paste + bracketed paste on top of the round-trip"
      contains: "ptyPause"
  key_links:
    - from: "src/renderer/TerminalPane.tsx"
      to: "window.api.ptyPause / ptyResume"
      via: "watermark HIGH/LOW backpressure on the onPtyData path"
      pattern: "ptyPause"
    - from: "src/renderer/TerminalPane.tsx"
      to: "term.paste / navigator.clipboard"
      via: "Cmd+C copy, Cmd+V + right-click bracketed paste"
      pattern: "paste"
---

<objective>
Layer terminal fidelity onto the working round-trip: renderer-driven flow-control backpressure so high-throughput output never freezes the UI or drops bytes (SC5), and native macOS copy/paste with bracketed paste so multi-line paste never auto-executes (SC2/D-03). Then gate the phase on a human-verify checkpoint covering the visual/interactive fidelity that cannot be fully automated (vim/python/ssh/htop/truecolor/CJK/paste).

Purpose: 02-03 proved the loop closes; this plan makes it production-faithful under stress and matches Terminal.app's copy/paste conventions — the last fidelity gaps before the Core Value is provably met. The watermark turns the throughput E2E smoke GREEN; the checkpoint confirms the criteria that only a human can judge.
Output: TerminalPane.tsx extended with the watermark (ptyPause/ptyResume on the data path) and copy/paste handlers; pty-throughput smoke GREEN; a human-verify fidelity checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pty-core-terminal-fidelity/02-RESEARCH.md
@CLAUDE.md
@.planning/phases/02-pty-core-terminal-fidelity/02-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (02-04) produces:
- src/renderer/TerminalPane.tsx (extended) — watermark flow-control on the onPtyData path (HIGH=100000 / LOW=10000; term.write(chunk, cb) drain then ptyResume below LOW; ptyPause above HIGH); Cmd+C copy-selection, Cmd+V paste, right-click paste, all via term.paste() so bracketed-paste mode is honored; Ctrl+C left untouched (SIGINT passthrough)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire the renderer flow-control watermark (turn pty-throughput smoke GREEN, SC5)</name>
  <files>src/renderer/TerminalPane.tsx</files>
  <read_first>
    @src/renderer/TerminalPane.tsx  (the onPtyData to term.write path from 02-03 to wrap with backpressure)
    @.planning/phases/02-pty-core-terminal-fidelity/02-RESEARCH.md  (Pattern 4 watermark: HIGH=100000 LOW=10000, term.write(chunk, cb), ptyPause/ptyResume; do NOT use node-pty handleFlowControl)
    @tests/smoke/pty-throughput.smoke.test.ts  (the RED E2E asserting responsiveness + no-drop under ~50MB)
    @src/shared/api-types.ts  (ptyPause/ptyResume signatures already on window.api)
  </read_first>
  <action>
    In src/renderer/TerminalPane.tsx, replace the plain onPtyData handler with the canonical xterm watermark (RESEARCH Pattern 4). Maintain a mutable watermark counter scoped to the effect. In the onPtyData callback: increment watermark by chunk.length; call term.write(chunk, callback) where the callback subtracts chunk.length from watermark (clamped at 0) and calls window.api.ptyResume(id) when watermark drops below LOW; after scheduling the write, call window.api.ptyPause(id) when watermark exceeds HIGH. Use const HIGH = 100000 and const LOW = 10000 (RESEARCH-documented; HIGH may be tuned down toward a 500K ceiling if keystrokes lag, but start at 100000). This pauses the node-pty stream in main when xterm's parse queue backs up and resumes it as the queue drains, keeping keystrokes responsive and losing no bytes. Do NOT enable node-pty handleFlowControl (XON/XOFF — wrong layer, RESEARCH Alternatives). Keep the rest of the round-trip wiring (term.onData to ptyWrite, resize, exit notice) intact.
  </action>
  <verify>
    <automated>npm run package</automated>
    <automated>npx wdio run wdio.conf.ts --spec tests/smoke/pty-throughput.smoke.test.ts</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/renderer/TerminalPane.tsx','utf8'); if(!s.includes('ptyPause')||!s.includes('ptyResume')){console.error('watermark wiring incomplete');process.exit(1);} if(s.includes('handleFlowControl')){console.error('forbidden node-pty XON/XOFF flow control');process.exit(1);} console.log('flow-control watermark OK');"</automated>
  </verify>
  <acceptance_criteria>
    - onPtyData uses term.write(chunk, callback); the callback drains the watermark and calls ptyResume(id) when total is below LOW (10000)
    - ptyPause(id) is called when the watermark exceeds HIGH (100000)
    - node-pty handleFlowControl (XON/XOFF) is NOT used
    - pty-throughput.smoke.test.ts PASSES: ~50MB output does not freeze input (sentinel echoes within budget) and no bytes are dropped
    - The existing round-trip (keystrokes, resize, exit notice) still works (pty-roundtrip + pty-resize remain GREEN)
  </acceptance_criteria>
  <done>The renderer watermark pauses/resumes the PTY under load; the throughput E2E smoke is GREEN and the round-trip is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add macOS copy/paste + bracketed paste (SC2, D-03)</name>
  <files>src/renderer/TerminalPane.tsx</files>
  <read_first>
    @src/renderer/TerminalPane.tsx  (the term instance + containerRef from 02-03 to attach key/context handlers)
    @.planning/phases/02-pty-core-terminal-fidelity/02-RESEARCH.md  (Pattern 6: attachCustomKeyEventHandler for Cmd+C/Cmd+V; term.paste() honors bracketed paste; Ctrl+C falls through to SIGINT; right-click contextmenu paste)
  </read_first>
  <action>
    In src/renderer/TerminalPane.tsx (within the mount effect, after term.open), attach copy/paste per D-03 (RESEARCH Pattern 6):
    - Call term.attachCustomKeyEventHandler with a handler that: returns true for non-keydown events; on Cmd (metaKey) + KeyC when term.hasSelection(), writes term.getSelection() to navigator.clipboard and returns false; on Cmd (metaKey) + KeyV, reads navigator.clipboard text and calls term.paste(text) then returns false; returns true otherwise. Returning true for everything else means Ctrl+C is NOT intercepted and xterm forwards the 0x03 byte to the PTY as SIGINT (D-03 — Cmd+C and Ctrl+C are distinct keys).
    - Right-click paste: add a contextmenu listener on containerRef.current that preventDefault then reads navigator.clipboard text and calls term.paste(text). Remove this listener in the effect cleanup.
    Use term.paste() (NOT feeding raw text to onData) so bracketed-paste mode (DECSET 2004) is honored and a multi-line paste does not auto-execute until the user presses Enter (SC2). No copy-on-select (D-03 — do not add a selection-change copy handler).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
    <automated>node -e "const s=require('fs').readFileSync('src/renderer/TerminalPane.tsx','utf8'); if(!s.includes('attachCustomKeyEventHandler')||!s.includes('term.paste')||!s.includes('contextmenu')){console.error('copy/paste wiring incomplete');process.exit(1);} console.log('copy/paste + bracketed paste OK');"</automated>
  </verify>
  <acceptance_criteria>
    - attachCustomKeyEventHandler copies the selection on Cmd+C (only when there is a selection) and pastes on Cmd+V via term.paste()
    - A contextmenu (right-click) listener pastes via term.paste() and is removed on cleanup
    - Ctrl+C is NOT intercepted (falls through to xterm → 0x03 → SIGINT)
    - Paste uses term.paste() (bracketed paste) so multi-line paste does not auto-execute; no copy-on-select handler is added
    - tsc --noEmit passes
  </acceptance_criteria>
  <done>Cmd+C/Cmd+V/right-click paste work via bracketed paste; Ctrl+C remains SIGINT; no copy-on-select; project type-checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify native terminal fidelity (SC1/SC2/SC4)</name>
  <files>(none — manual verification of the running app; no files modified)</files>
  <action>STOP and hand off to the human. Build the app (npm start) and present the verification steps in &lt;how-to-verify&gt; below. Do not auto-advance — this gate is blocking and is never auto-approvable (workflow.auto_advance is ignored for human-verify of Core Value fidelity).</action>
  <what-built>
    The complete single-session terminal: a full-window xterm pane auto-starting a login shell, with the live PTY round-trip, window-resize reflow, flow-control backpressure, copy/paste + bracketed paste, 10k scrollback, and a passive exit notice. The automated smoke suite (round-trip, resize, throughput) is GREEN. This checkpoint covers the SC1/SC2/SC4 fidelity that only a human can judge (per 02-VALIDATION Manual-Only Verifications).
  </what-built>
  <how-to-verify>
    1. Launch the app: run `npm start`. A full-window terminal with a live shell prompt should appear (auto-start, D-02), cwd = your home directory.
    2. PATH parity (SC1/TERM-03): run `echo $PATH` and `which claude` (or `which codex`) — they should match what you see in Terminal.app. If `claude` is installed, run `claude --rc` and confirm it launches interactively. (This is the canonical Core Value scenario.)
    3. Interactive fidelity (SC1): run `vim` (type, navigate with arrows, `:q`), then `python` (a couple of REPL lines, Ctrl+D to exit), then `ssh localhost` or any host (confirm the prompt/keyboard behave natively). Confirm Ctrl+C kills a running `sleep 100` and Ctrl+D closes the REPL; confirm arrow keys navigate shell history.
    4. Colors + Unicode (SC4): run `echo $TERM` (expect `xterm-256color`); run a truecolor test (e.g. a 24-bit color script) and confirm smooth gradients; run `htop` and confirm box-drawing borders are intact and aligned; print some CJK text and emoji (e.g. `echo "日本語 🛋️ 表示"`) and confirm characters occupy the correct cell widths with no overlap/clipping.
    5. Copy/paste (SC2, D-03): select text and press Cmd+C; paste elsewhere with Cmd+V — confirm the copied text. Copy a 3-line snippet from a text editor and paste it with Cmd+V (and again with right-click) — confirm it does NOT auto-execute until you press Enter (bracketed paste). Confirm Ctrl+C still sends SIGINT (it is a different key from Cmd+C).
    6. Resize (SC3): open `vim` or `htop`, resize the window — confirm the content reflows correctly within ~1 second.
    7. Throughput (SC5): run `cat` of a 50MB+ file (or `yes | head -n 2000000`) — confirm the UI does not freeze, keyboard input stays responsive, and no output is corrupted/dropped.
    8. Exit notice (D-04): type `exit` — confirm a passive "[process exited]" notice appears and the app does not auto-restart the shell.
  </how-to-verify>
  <resume-signal>Type "approved" if all fidelity checks pass, or describe which check failed (with the command and what you observed) so the gap can be addressed.</resume-signal>
  <verify><human-check>Human runs steps 1-8 in &lt;how-to-verify&gt; against the running app and replies "approved" or reports a failing check.</human-check></verify>
  <done>Human has confirmed native fidelity across PATH parity, vim/python/ssh, truecolor/CJK/emoji/htop, copy-paste/bracketed-paste, resize reflow, 50MB throughput, and the exit notice — and replied "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer flow-control → main pause/resume | Renderer-driven ptyPause/ptyResume control the main-process PTY stream; bounded by the same validated IPC handlers from 02-02 |
| clipboard → terminal (paste) | OS clipboard content is pasted into the PTY; bracketed paste prevents multi-line auto-execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Denial of Service | unbounded term.write under high throughput | mitigate | Watermark backpressure (HIGH=100000/LOW=10000) pauses node-pty when xterm's queue backs up; verified by the throughput E2E smoke. Without it a 50MB cat freezes the event loop (Pitfall 2). |
| T-02-12 | Tampering | multi-line paste auto-execution | mitigate | term.paste() honors bracketed-paste mode (DECSET 2004) so pasted newlines are not auto-executed by the shell (SC2). Raw text is never fed to onData. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; the package legitimacy gate was satisfied in 02-01 (all packages canonical, reviewed). No [ASSUMED]/[SUS] packages → no blocking-human install checkpoint. |
</threat_model>

<verification>
- pty-throughput.smoke.test.ts GREEN (SC5); round-trip + resize smoke remain GREEN
- TerminalPane uses term.write(chunk, cb) + ptyPause/ptyResume and does NOT use node-pty handleFlowControl
- attachCustomKeyEventHandler + term.paste() + contextmenu paste present; Ctrl+C not intercepted
- tsc --noEmit + npm run lint clean
- Human-verify checkpoint approved (vim/python/ssh/htop/truecolor/CJK/paste/resize/throughput/exit)
- Full suite: npm test GREEN (unit + all three smoke specs)
</verification>

<success_criteria>
- 50MB+ output stays responsive and lossless via the watermark (SC5)
- Cmd+C/Cmd+V/right-click paste with bracketed paste; Ctrl+C = SIGINT; no copy-on-select (SC2, D-03)
- Human-verified native fidelity across vim/python/ssh/htop/truecolor/CJK/emoji (SC1/SC4)
- Phase-2 Core Value proven end to end; full automated suite GREEN
</success_criteria>

<output>
Create `.planning/phases/02-pty-core-terminal-fidelity/02-04-SUMMARY.md` when done.
</output>
