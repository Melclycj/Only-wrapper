---
phase: 02-pty-core-terminal-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/main/shell-resolver.ts
  - src/main/__tests__/shell-resolver.test.ts
  - src/main/__tests__/flow-control.test.ts
  - tests/smoke/helpers/xterm-driver.ts
  - tests/smoke/pty-roundtrip.smoke.test.ts
  - tests/smoke/pty-resize.smoke.test.ts
  - tests/smoke/pty-throughput.smoke.test.ts
autonomous: true
requirements:
  - TERM-01
  - TERM-02
  - TERM-03
  - TERM-04
must_haves:
  truths:
    - "node-pty 1.1.0 and the @xterm 5.x stack are installed and node-pty rebuilds against the Electron 36.9.5 ABI without error"
    - "All Phase-2 Wave 0 test files exist and FAIL RED (referencing symbols not yet implemented)"
    - "The WDIO xterm-driver helper can send keystrokes and read the xterm buffer in-page"
  artifacts:
    - path: "package.json"
      provides: "node-pty 1.1.0 + @xterm/xterm 5.5.0 + 5.x addons pinned as dependencies"
      contains: "node-pty"
    - path: "src/main/shell-resolver.ts"
      provides: "resolveShell signature stub so the unit test imports it"
      contains: "resolveShell"
    - path: "src/main/__tests__/shell-resolver.test.ts"
      provides: "RED unit test for resolveShell login-flag + /bin/zsh fallback"
      contains: "resolveShell"
    - path: "src/main/__tests__/flow-control.test.ts"
      provides: "RED unit test for watermark HIGH/LOW accounting"
      contains: "watermark"
    - path: "tests/smoke/helpers/xterm-driver.ts"
      provides: "WDIO in-page xterm driver (sendKeys / readBuffer / waitForText)"
      contains: "readBuffer"
    - path: "tests/smoke/pty-roundtrip.smoke.test.ts"
      provides: "RED E2E test for PTY round-trip + TERM env + Ctrl+C"
      contains: "xterm-256color"
    - path: "tests/smoke/pty-resize.smoke.test.ts"
      provides: "RED E2E test for resize then tput cols"
      contains: "tput cols"
    - path: "tests/smoke/pty-throughput.smoke.test.ts"
      provides: "RED E2E test for 50MB throughput responsiveness + no-drop"
      contains: "throughput"
  key_links:
    - from: "src/main/__tests__/shell-resolver.test.ts"
      to: "src/main/shell-resolver.ts"
      via: "import resolveShell"
      pattern: "resolveShell"
    - from: "tests/smoke/pty-roundtrip.smoke.test.ts"
      to: "tests/smoke/helpers/xterm-driver.ts"
      via: "import driver helper"
      pattern: "xterm-driver"
---

<objective>
Install and pin the Phase-2 terminal stack (node-pty + the @xterm 5.x family) and create every Wave 0 RED test stub from 02-VALIDATION.md. This plan lays the failing-test scaffold that plans 02-02, 02-03, and 02-04 must turn GREEN, and locks the exact dependency versions confirmed during planning.

Purpose: Establish the test-first safety net (the Phase-1 Wave 0 convention) and the verified dependency set BEFORE any PTY wiring, so every later task has a concrete automated target. This is the foundation slice — it produces no user-facing capability yet, but every subsequent slice is measured against the tests created here.
Output: Pinned dependencies in package.json, a rebuilt node-pty native binary, a resolveShell signature stub, two RED unit tests, three RED E2E smoke tests, and the WDIO xterm-driver helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pty-core-terminal-fidelity/02-RESEARCH.md
@.planning/phases/02-pty-core-terminal-fidelity/02-VALIDATION.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan (02-01) produces:
- package.json — new deps: node-pty 1.1.0, @xterm/xterm 5.5.0, @xterm/addon-fit 0.10.0, @xterm/addon-webgl 0.18.0, @xterm/addon-canvas 0.7.0, @xterm/addon-web-links 0.11.0, @xterm/addon-unicode11 0.8.0
- src/main/shell-resolver.ts — resolveShell() signature stub; implemented in 02-02
- src/main/__tests__/shell-resolver.test.ts — RED unit test
- src/main/__tests__/flow-control.test.ts — RED unit test
- tests/smoke/helpers/xterm-driver.ts — sendKeys / readBuffer / waitForText WDIO helpers
- tests/smoke/pty-roundtrip.smoke.test.ts, pty-resize.smoke.test.ts, pty-throughput.smoke.test.ts — RED E2E tests
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install and pin the node-pty + @xterm 5.x terminal stack</name>
  <files>package.json</files>
  <read_first>
    @CLAUDE.md  (Technology Stack table; "Critical: node-pty Native Module Build Concerns"; "What NOT to Use")
    @.planning/phases/02-pty-core-terminal-fidelity/02-RESEARCH.md  (Standard Stack; addon-major pinning rule; Package Legitimacy Audit)
    @package.json  (existing scripts incl. postinstall electron-rebuild; existing deps)
    @vite.main.config.ts  (node-pty already marked external)
    @forge.config.ts  (AutoUnpackNativesPlugin already present)
  </read_first>
  <action>
    Install the exact versions confirmed during planning (do NOT use caret ranges that could drift onto the xterm-6 addon line; pin exact). Add as runtime dependencies: node-pty 1.1.0, @xterm/xterm 5.5.0, @xterm/addon-fit 0.10.0, @xterm/addon-webgl 0.18.0, @xterm/addon-canvas 0.7.0, @xterm/addon-web-links 0.11.0, @xterm/addon-unicode11 0.8.0. These are the highest STABLE non-beta addon minors compatible with @xterm/xterm 5.5 — the 0.11/0.19/0.7/0.12/0.9 latest addons are built for xterm 6 and must NOT be used (CLAUDE.md locks xterm 5.5 plus addon-canvas, which xterm 6 removed).
    Run install with npm so the existing postinstall (electron-rebuild) recompiles node-pty native code against the Electron 36.9.5 V8 ABI. If postinstall does not run automatically, invoke electron-rebuild explicitly scoped to node-pty. Confirm node-pty is already external in vite.main.config.ts (it is) — do not change that. Do NOT add node-pty to renderer or shared imports (ESLint already bans it there).
    Per D-01 and "What NOT to Use": node-pty install/postinstall build scripts (prebuild.js / post-install.js) are EXPECTED for a native module and were reviewed clean in 02-RESEARCH Package Legitimacy Audit — no checkpoint required.
  </action>
  <verify>
    <automated>node -e "const d=require('./package.json').dependencies||{}; const need=['node-pty','@xterm/xterm','@xterm/addon-fit','@xterm/addon-webgl','@xterm/addon-canvas','@xterm/addon-web-links','@xterm/addon-unicode11']; const miss=need.filter(n=>!d[n]); if(miss.length){console.error('MISSING',miss);process.exit(1);} if(!String(d['@xterm/xterm']).includes('5.5')){console.error('xterm not 5.5');process.exit(1);} console.log('deps OK');"</automated>
    <automated>test -d node_modules/node-pty/build/Release</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains all 7 packages at exact versions (node-pty 1.1.0, @xterm/xterm 5.5.0, addon-fit 0.10.0, addon-webgl 0.18.0, addon-canvas 0.7.0, addon-web-links 0.11.0, addon-unicode11 0.8.0)
    - @xterm/xterm resolves to 5.5.x (NOT 6.x); no xterm-6-line addons (fit 0.11 / webgl 0.19 / web-links 0.12 / unicode11 0.9) are installed
    - electron-rebuild completed with no compile error; node_modules/node-pty/build/Release/ exists
    - node-pty remains in the external array of vite.main.config.ts (unchanged)
  </acceptance_criteria>
  <done>All 7 packages pinned at exact versions; node-pty rebuilt against the Electron 36.9.5 ABI with no compile error; xterm pinned to 5.5.x.</done>
</task>

<task type="auto">
  <name>Task 2: Create RED unit tests, resolveShell signature stub, and the WDIO xterm-driver helper</name>
  <files>src/main/shell-resolver.ts, src/main/__tests__/shell-resolver.test.ts, src/main/__tests__/flow-control.test.ts, tests/smoke/helpers/xterm-driver.ts, tests/smoke/pty-roundtrip.smoke.test.ts, tests/smoke/pty-resize.smoke.test.ts, tests/smoke/pty-throughput.smoke.test.ts</files>
  <read_first>
    @.planning/phases/02-pty-core-terminal-fidelity/02-VALIDATION.md  (Wave 0 Requirements; Per-Task Verification Map)
    @.planning/phases/02-pty-core-terminal-fidelity/02-RESEARCH.md  (Pattern 3 resolveShell; Pattern 4 watermark flow-control)
    @src/shared/__tests__/identity.guard.test.ts  (Phase-1 RED-stub convention + Vitest import style)
    @tests/smoke/boot.smoke.test.ts  (Phase-1 WDIO smoke style; browser global; mocha describe/it)
    @wdio.conf.ts  (specs glob tests/smoke/**/*.smoke.test.ts; mocha; 30s timeout)
    @src/main/window-config.ts  (electron-free module convention so Vitest imports run standalone)
  </read_first>
  <action>
    Create src/main/shell-resolver.ts as a SIGNATURE STUB ONLY: export an interface ResolvedShell with fields shell of type string and args of type string array, and export a function resolveShell returning ResolvedShell whose body throws an Error with message 'not implemented — 02-02'. Keep the file electron-free and OS-agnostic (no electron import) so Vitest imports it directly. Implementation lands in 02-02.

    Create src/main/__tests__/shell-resolver.test.ts (Vitest): assert resolveShell returns args equal to an array containing exactly the single element '-l'; assert shell equals process.env.SHELL when set; assert it falls back to /bin/zsh when SHELL is unset (mutate-and-restore process.env.SHELL around the case). This test FAILS RED now (stub throws) and goes GREEN in 02-02. Covers TERM-03 and TERM-04 (D-01).

    Create src/main/__tests__/flow-control.test.ts (Vitest): import the not-yet-existing watermark accounting from src/main/flow-control.ts (a createWatermark(high, low) factory exposing add(n), drain(n), shouldPause(), shouldResume(), and a readable total). Assert: starts not paused; after add() pushes the running total above HIGH (100000), shouldPause() is true; after drain() brings it below LOW (10000), shouldResume() is true; the total never goes negative (drain past zero clamps at 0). Test FAILS RED now (module absent) and goes GREEN in 02-02. Covers SC5 accounting. Include the word watermark in a comment so the must-have grep matches.

    Create tests/smoke/helpers/xterm-driver.ts: a WDIO helper module exporting sendKeys(text) (focus the .xterm-helper-textarea and type, or use browser.keys), readBuffer() returning a Promise of the visible xterm buffer text (execute in-page JS concatenating .xterm-rows textContent, or read a window-exposed terminal handle), and waitForText(substr, timeoutMs) that polls readBuffer until the substring appears or times out. Keep selectors resilient to xterm 5.5 (rows under .xterm-rows; input textarea .xterm-helper-textarea).

    Create the three E2E smoke tests as RED stubs (mocha/WDIO, matching boot.smoke.test.ts style; they boot the packaged Electron app which has no terminal yet, so they FAIL RED until 02-03/02-04). Each MUST import and use tests/smoke/helpers/xterm-driver.ts:
    - tests/smoke/pty-roundtrip.smoke.test.ts — type echo hello then Enter, assert readBuffer contains hello (SC1/TERM-02); type echo of the TERM variable then Enter, assert buffer contains xterm-256color (SC4); start sleep 100, send the Ctrl+C byte (0x03), assert the shell prompt returns (SC1).
    - tests/smoke/pty-resize.smoke.test.ts — record initial tput cols, resize the browser window, type tput cols then Enter, assert the reported column count changed to match the new width within 1 second (SC3).
    - tests/smoke/pty-throughput.smoke.test.ts — run a command emitting roughly 50MB (e.g. head reading from /dev/urandom piped to base64, or yes piped to head), then immediately type a sentinel echo READY_nonce then Enter and assert the sentinel echoes back within a responsiveness budget (UI not frozen), and assert no output was dropped via a sentinel or line-count check (SC5). Include the word throughput in a comment so the must-have grep matches.

    Add a Wave-0 banner comment at the top of each new test file noting it is intentionally RED until its implementing plan (cite 02-02 / 02-03 / 02-04).
  </action>
  <verify>
    <automated>npx vitest run src/main/__tests__/shell-resolver.test.ts src/main/__tests__/flow-control.test.ts; if [ $? -eq 0 ]; then echo "UNEXPECTED: unit tests passed pre-implementation"; exit 1; else echo "unit tests RED as expected"; fi</automated>
    <automated>test -f tests/smoke/helpers/xterm-driver.ts; test -f tests/smoke/pty-roundtrip.smoke.test.ts; test -f tests/smoke/pty-resize.smoke.test.ts; test -f tests/smoke/pty-throughput.smoke.test.ts; echo "E2E stubs + driver present"</automated>
  </verify>
  <acceptance_criteria>
    - src/main/shell-resolver.ts exports interface ResolvedShell (shell:string, args:string[]) and function resolveShell(): ResolvedShell whose stub body throws 'not implemented — 02-02'; the file imports no electron module
    - npx vitest run on the two new unit tests exits NON-ZERO (RED): shell-resolver throws, flow-control module is absent
    - tests/smoke/helpers/xterm-driver.ts exports sendKeys, readBuffer, and waitForText
    - All three new E2E test files exist, import the xterm-driver helper, and carry a Wave-0 RED banner citing their implementing plan
    - shell-resolver.test.ts asserts args is ['-l'], SHELL passthrough, and /bin/zsh fallback; flow-control.test.ts asserts HIGH-pause, LOW-resume, and non-negative total
  </acceptance_criteria>
  <done>Two RED unit tests, three RED E2E smoke stubs, the resolveShell stub, and the xterm-driver helper all exist; unit tests fail RED; E2E tests reference the driver.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to local node_modules | Third-party package code (node-pty native build scripts) executes on install |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm installs (node-pty + @xterm/*) | mitigate | All 7 packages reviewed in 02-RESEARCH Package Legitimacy Audit: canonical Microsoft / xterm.js org packages, multi-year history, registry-verified. node-pty install/postinstall scripts (prebuild.js / post-install.js) are expected native-module build steps, inspected clean — no ASSUMED/SUS/SLOP packages, so no blocking-human checkpoint required. Pin exact versions (no caret) to prevent silent drift onto unreviewed xterm-6 addons. |
| T-02-01 | Tampering | node-pty native binary | accept | Native code compiled locally from source via electron-rebuild against the Electron 36 ABI; not a prebuilt download. Verified by build-dir presence check. |
</threat_model>

<verification>
- npm dependency pin check (node -e gate) green; xterm at 5.5.x
- node-pty native build directory present
- Two new Vitest unit tests fail RED (expected pre-implementation)
- Three E2E smoke stubs + driver helper present and reference the helper
- npm run lint passes (no new lint violations from the stub + helper)
</verification>

<success_criteria>
- 7 terminal-stack packages pinned at exact versions; xterm at 5.5.x
- node-pty rebuilt against the Electron 36.9.5 ABI without compile error
- resolveShell signature stub + flow-control unit test + shell-resolver unit test fail RED
- xterm-driver WDIO helper and three E2E smoke stubs created and wired to the helper
</success_criteria>

<output>
Create `.planning/phases/02-pty-core-terminal-fidelity/02-01-SUMMARY.md` when done.
</output>
