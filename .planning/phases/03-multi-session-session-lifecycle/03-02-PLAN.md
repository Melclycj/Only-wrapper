---
phase: 03-multi-session-session-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/preload/index.ts
  - src/renderer/SessionView.tsx
  - src/renderer/SessionManager.tsx
  - src/renderer/Sidebar.tsx
  - src/renderer/status-colors.ts
  - src/renderer/index.tsx
  - src/renderer/terminal.css
autonomous: true
requirements: [TERM-06, TERM-08]
must_haves:
  truths:
    - "3+ sessions run concurrently; switching tabs never pauses or kills a background PTY (a background printer keeps advancing while hidden)"
    - "Switching back to a hidden session shows current scrollback instantly — no blank or frozen frame and no main-side replay"
    - "Only the active session holds a WebGL context; switching detaches WebGL from the old view and attaches it to the new one"
    - "Each sidebar row shows icon + name + a status badge whose color matches the DESIGN.md status language (running=blue, exited=green, stopped/not_started=slate, error=derived red)"
    - "An 'add session' button spawns a new session with default name 'Session N' and a default icon, and switches to it"
  artifacts:
    - path: "src/renderer/SessionView.tsx"
      provides: "Per-session xterm instance kept alive (buffers while hidden), WebGL attach-on-active, hidden-pane-safe fit/resize, per-instance flow control"
      contains: "attachWebgl"
    - path: "src/renderer/SessionManager.tsx"
      provides: "Owns sessions[]+activeId; renders Sidebar + a viewport stack of all SessionViews (all kept mounted); switch/add wiring; pty:status subscription"
      contains: "activeId"
    - path: "src/renderer/Sidebar.tsx"
      provides: "Basic DESIGN.md session list (icon + name + status badge), click-to-switch, add-session button"
      contains: "data-session-id"
    - path: "src/renderer/status-colors.ts"
      provides: "SessionStatus → {label, accent} oklch map incl. derived red error ramp"
      contains: "STATUS_STYLE"
  key_links:
    - from: "src/renderer/SessionManager.tsx"
      to: "window.api.onPtyStatus / listSessions"
      via: "bridge subscription + initial list"
      pattern: "onPtyStatus|listSessions"
    - from: "src/renderer/Sidebar.tsx"
      to: "src/renderer/status-colors.ts STATUS_STYLE"
      via: "status → badge accent"
      pattern: "STATUS_STYLE"
    - from: "src/preload/index.ts"
      to: "EXPECTED_API_KEYS (window-config.ts)"
      via: "exact-key-set guard"
      pattern: "onPtyStatus"
---

<objective>
Deliver the multi-session renderer vertical slice: wire the 4 new preload bridge methods, refactor the single full-window `TerminalPane` into a per-session `SessionView` (one xterm per session kept alive, WebGL attached only when active, buffers correctly while hidden), and build the `SessionManager` + basic DESIGN.md `Sidebar` + `status-colors` so a user can open 3+ sessions, switch between them with no process death and current scrollback, add new sessions, and see live status badges. This is the thinnest end-to-end slice that makes SC1, SC2, and the SC4 badge demonstrable.

Purpose: This is the genuinely new, renderer-internal engineering D-01 anticipated — instance-per-session with WebGL hand-off. It closes TERM-06 (keep-alive on switch) and the visible half of TERM-08 (status badges) end to end against the producer side built in 03-01.
Output: Extended `preload/index.ts`; new `SessionView.tsx`, `SessionManager.tsx`, `Sidebar.tsx`, `status-colors.ts`; updated `index.tsx` (mounts SessionManager) + `terminal.css` (IDE layout + hidden-pane visibility rule).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN.md
@.planning/phases/03-multi-session-session-lifecycle/03-RESEARCH.md
@.planning/phases/03-multi-session-session-lifecycle/03-PATTERNS.md
@.planning/phases/03-multi-session-session-lifecycle/03-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols this PLAN creates (drift-verification excludes these as net-new):
- **Components:** `SessionView` (SessionView.tsx), `SessionManager` (SessionManager.tsx), `Sidebar` (Sidebar.tsx).
- **Renderer helpers:** `attachWebgl(term)`, `detachWebgl(webgl)` (in SessionView), `STATUS_STYLE` map + a `statusLabel(status)`/accent accessor (status-colors.ts).
- **Preload bridge wiring:** `ptyStop`, `ptyRestart`, `onPtyStatus`, `listSessions` (the runtime implementations of the contract from 03-01).
- **DOM contract (consumed by the xterm-driver N-pane helpers from 03-01):** per-session container `data-session-id={logicalId}`; sidebar row `data-session-id={logicalId}`; add button `data-testid="add-session"`; active container class `.session-view.active`, hidden `.session-view[hidden-pane]`.
- **CSS classes/tokens:** `.ide-layout`, `.sidebar`, `.sidebar-row`, `.status-badge`, `.viewport-stack`, `.session-view`, hidden-pane visibility rule.
- **Default add-session scheme:** name `Session N`, icon `{ type: 'emoji', value: '🖥️' }`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the 4 preload bridge methods (ptyStop, ptyRestart, onPtyStatus, listSessions) — guard goes GREEN</name>
  <behavior>
    - `ptyStop(id)` → fire-and-forget `ipcRenderer.send('pty:stop', id)` (mirrors `ptyPause`).
    - `ptyRestart(id)` → `ipcRenderer.invoke('pty:restart', id)` returning `Promise<PtyCreateResult>` (mirrors `ptyCreate`).
    - `listSessions()` → `ipcRenderer.invoke('pty:list')` returning `Promise<SessionRecord[]>`.
    - `onPtyStatus(id, cb)` → id-filtered `ipcRenderer.on('pty:status', listener)` returning an unsubscribe (mirrors `onPtyData` EXACTLY).
    - After wiring, `security.guard.test.ts` asserts the exposed surface equals the 12-key EXPECTED_API_KEYS and `not.toHaveProperty('ipcRenderer')` → GREEN.
  </behavior>
  <read_first>
    - src/preload/index.ts (FULL current file — the `api: ElectronAPI` object literal; `ptyPause` fire-and-forget send lines 31-33; `ptyCreate` invoke lines 20-21; `onPtyData` id-filtered subscribe-returns-unsubscribe lines 41-50 — the EXACT template for `onPtyStatus`)
    - src/shared/api-types.ts (the contract from 03-01: the 4 new method signatures + PtyStatusPayload — implement to match)
    - src/shared/__tests__/security.guard.test.ts (the exact-key-set + no-ipcRenderer assertion this task must turn GREEN)
    - src/main/window-config.ts (EXPECTED_API_KEYS — already 12 keys after 03-01; the preload surface must equal it exactly)
    - .planning/phases/03-multi-session-session-lifecycle/03-PATTERNS.md §"src/preload/index.ts (bridge) — MODIFY" (onPtyData is the exact template for onPtyStatus; ptyStop mirrors ptyPause; ptyRestart/listSessions mirror ptyCreate)
  </read_first>
  <action>
    Extend the `api: ElectronAPI` object in `src/preload/index.ts` with the 4 new methods, NEVER exposing raw `ipcRenderer` (keep the single typed object literal). Add `import type { PtyStatusPayload, SessionRecord }` to the type imports. `ptyStop: (id) => { ipcRenderer.send('pty:stop', id); }`. `ptyRestart: (id) => ipcRenderer.invoke('pty:restart', id)`. `listSessions: () => ipcRenderer.invoke('pty:list')`. `onPtyStatus: (id, cb) => { const listener = (_e, p: PtyStatusPayload) => { if (p.id === id) cb(p); }; ipcRenderer.on('pty:status', listener); return () => ipcRenderer.removeListener('pty:status', listener); }` — a verbatim structural copy of `onPtyData` with the payload shape swapped. Use the `PTY_CHANNELS` string values (`'pty:stop'`, `'pty:restart'`, `'pty:list'`, `'pty:status'`) matching main. No fenced code in extra prose — the shapes are in the file you read.
  </action>
  <acceptance_criteria>
    - `npm run test:unit -- security.guard` GREEN: the exposed surface equals the 12-key EXPECTED_API_KEYS and `not.toHaveProperty('ipcRenderer')`.
    - `grep -c "ipcRenderer" src/preload/index.ts` shows ipcRenderer is used ONLY inside the typed method bodies (never added as an exposed key) — the guard test enforces this.
    - `onPtyStatus` filters by `p.id === id` and returns an unsubscribe (`grep -c "removeListener" src/preload/index.ts` ≥ 3 — onPtyData, onPtyExit, onPtyStatus).
    - `npx tsc --noEmit` 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- security.guard && npx tsc --noEmit</automated>
  </verify>
  <done>The preload exposes exactly the 12 typed methods incl. the 4 new ones; onPtyStatus mirrors onPtyData's id-filtered subscribe/unsubscribe; the security guard is GREEN; no raw ipcRenderer leaks.</done>
</task>

<task type="auto">
  <name>Task 2: status-colors.ts + SessionView.tsx — per-session xterm kept alive, WebGL-on-active, hidden-pane-safe fit (DESIGN.md status language)</name>
  <read_first>
    - src/renderer/TerminalPane.tsx (FULL file — the WHOLE component is the template for SessionView: TERMINAL_THEME const lines 32-36, xterm construction lines 60-68, addon load order lines 70-95 incl. the WebGL try/canvas-fallback block 82-95, open+fit lines 99-100, copy/paste keymap 110-127, the PTY round-trip + watermark 135-192 incl. createWatermark + the explicit `paused` edge 169-184, the exit notice 187-189, the debounced onResize 194-205, the cleanup 207-219)
    - src/renderer/terminal.css (FULL — current full-window `.terminal-pane` rules; extend to the IDE layout + hidden-pane visibility rule)
    - .planning/DESIGN.md §"Design tokens" (oklch surface/ink/line tokens, `--radius`, Nunito UI font, `--term-*` palette + JetBrains Mono) AND §"Status system" (running=blue `oklch(0.62 0.14 248)`, exited/finished=green `oklch(0.60 0.13 150)`, stopped/not_started/free=slate `oklch(0.64 0.02 260)`; derive a red `error` ramp consistent with the palette)
    - .planning/phases/03-multi-session-session-lifecycle/03-RESEARCH.md §"Architecture Patterns" Pattern 7 (instance-per-session, attachWebgl/detachWebgl, keep mounted — do NOT dispose on hide) AND Pattern 8 + §"Common Pitfalls" 1-2 (display:none breaks fit → use visibility/off-screen; WebGL ~16-context cap → active-only) AND §"Code Examples" → "Status → DESIGN.md color (status-colors.ts)"
    - .planning/phases/03-multi-session-session-lifecycle/03-PATTERNS.md §"src/renderer/SessionView.tsx" (line-range mapping of every TerminalPane mechanism to keep verbatim vs. change) AND §"src/renderer/status-colors.ts" (the TERMINAL_THEME const-map is the structural precedent; map the 5-state union + derived red error ramp `oklch(0.58 0.16 25)`)
    - src/shared/types.ts (SessionStatus union, SessionIconSpec the row will render, LogicalId)
  </read_first>
  <action>
    Create `src/renderer/status-colors.ts`: export `STATUS_STYLE: Record<SessionStatus, { label: string; accent: string }>` mapping the 5-state union to DESIGN.md §"Status system" oklch accents — running `{ label:'Running', accent:'oklch(0.62 0.14 248)' }` (blue), exited `{ label:'Finished', accent:'oklch(0.60 0.13 150)' }` (green), stopped `{ label:'Stopped', accent:'oklch(0.64 0.02 260)' }` (slate), not_started `{ label:'Idle', accent:'oklch(0.64 0.02 260)' }` (slate), error `{ label:'Error', accent:'oklch(0.58 0.16 25)' }` (DERIVED red ramp, hue ~25, palette-consistent L/C — D-04, no mockup state). This is the const-map pattern from TERMINAL_THEME.
    Create `src/renderer/SessionView.tsx` by extracting TerminalPane's body into a per-session component taking props `{ id: LogicalId; cwd?: string; startupCommand?: string; active: boolean; isRestart?: boolean }`. Copy verbatim: xterm construction (scrollback 10000, allowProposedApi, JetBrains Mono, TERMINAL_THEME), the fit/web-links/unicode11 addons, copy/paste keymap, and the PTY round-trip + per-instance `createWatermark(FLOW_HIGH, FLOW_LOW)` + the explicit `paused` edge (term.write runs EVEN while hidden — that is what keeps SC1/SC2 buffers current). CHANGE FOUR THINGS: (1) Move the WebGL try/canvas-fallback block out of construction into an `attachWebgl(term)`/`detachWebgl(webgl)` pair; call `attachWebgl` when `active` becomes true and `detachWebgl` (which does `webgl?.dispose()` to free the GPU context, ≤16 cap) when it becomes false — drive this from an effect on the `active` prop. (2) On the `ptyCreate` call, pass `{ cols, rows, cwd, startupCommand, id }` so a restart reuses the existing id (the SessionManager supplies the id). (3) Guard `fit()` with `fit.proposeDimensions()` returning a value before calling it, and re-`fit()` + `window.api.ptyResize(id, term.cols, term.rows)` + `term.focus()` when the view becomes active (the existing onResize body is the template) — because the container may have resized while hidden (Pattern 8). (4) In cleanup, also `webgl?.dispose()`; do NOT dispose the term on hide — only on permanent removal (no delete-session UI this phase). Subscribe to `window.api.onPtyStatus(id, …)` and, when a `running` status arrives AND this view has run before (a restart, not first spawn), write the dim separator `\r\n\x1b[2m— restarted HH:MM —\x1b[0m\r\n` into the SAME instance preserving scrollback (D-03) — track a `hasRunBefore` ref. Expose the container with `data-session-id={id}` and toggle the `active`/`hidden-pane` class for the driver + CSS. (Restart/stop CONTROLS are wired in 03-03; this task only establishes the view + its status subscription + the separator hook.)
    Extend `src/renderer/terminal.css`: add the IDE layout (`.ide-layout` = flex row; `.sidebar` left rail styled from DESIGN.md tokens — `--surface`/`--line`/Nunito; `.viewport-stack` fills the rest, `position:relative`). Add the hidden-pane rule per Pattern 8: `.session-view{ position:absolute; inset:0; }`, `.session-view[hidden-pane]{ visibility:hidden; z-index:0; }`, `.session-view.active{ visibility:visible; z-index:1; }` (NOT display:none). Keep the `--term-bg` background on the viewport. No fenced code blocks in this prose; the exact token values are in DESIGN.md and the const examples you read.
  </action>
  <acceptance_criteria>
    - `src/renderer/status-colors.ts` exports `STATUS_STYLE` with all 5 keys; the `running` accent is `oklch(0.62 0.14 248)`, `exited` is `oklch(0.60 0.13 150)`, `stopped`/`not_started` are `oklch(0.64 0.02 260)`, and `error` is the derived red `oklch(0.58 0.16 25)` (`grep -c "oklch(0.58 0.16 25)" src/renderer/status-colors.ts` ≥ 1).
    - `SessionView.tsx` has an `attachWebgl`/`detachWebgl` pair driven by the `active` prop; `detachWebgl` calls `webgl?.dispose()` (`grep -c "dispose" src/renderer/SessionView.tsx` ≥ 1) and the term is NOT disposed on hide (only in cleanup).
    - `SessionView.tsx` calls `term.write(data, …)` in the `onPtyData` path unconditionally (no `active` guard around it) so hidden sessions keep buffering (SC1/SC2).
    - `SessionView.tsx` guards `fit()` with `proposeDimensions()` and re-fits + `ptyResize` on activate (`grep -c "proposeDimensions" src/renderer/SessionView.tsx` ≥ 1).
    - `terminal.css` uses `visibility:hidden` for hidden panes and does NOT use `display:none` on `.session-view` (`grep -c "display: *none" src/renderer/terminal.css` returns 0 for session-view rules).
    - The container carries `data-session-id` (`grep -c "data-session-id" src/renderer/SessionView.tsx` ≥ 1).
    - `npx tsc --noEmit` 0 errors; `npm run lint` 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && grep -c "oklch(0.58 0.16 25)" src/renderer/status-colors.ts</automated>
  </verify>
  <done>status-colors maps all 5 statuses to DESIGN.md accents incl. the derived red error ramp; SessionView keeps one xterm alive per session (buffers while hidden), attaches WebGL only when active, and hides panes via visibility (not display:none) with proposeDimensions-guarded re-fit on activate.</done>
</task>

<task type="auto">
  <name>Task 3: SessionManager.tsx + Sidebar.tsx + mount — multi-session container, switch/add, live status badges (TERM-06, TERM-08 display)</name>
  <read_first>
    - src/renderer/TerminalPane.tsx (the `useEffect` + subscribe/unsubscribe + cleanup discipline lines 53-220 — the model for SessionManager's effects)
    - src/renderer/index.tsx (FULL — currently mounts `<TerminalPane/>`; swap to `<SessionManager/>`)
    - src/renderer/SessionView.tsx + src/renderer/status-colors.ts (from Task 2 — SessionManager renders all SessionViews + Sidebar consumes STATUS_STYLE)
    - src/preload/index.ts (the `onPtyStatus`/`listSessions` wired in Task 1 — SessionManager subscribes per id + lists on first render/after add)
    - .planning/DESIGN.md §"v1 component inventory" (SessionCard/IdeSidebarRow: icon + name + status dot/label) AND §"Reconciliation notes" (the row must render all three SessionIconSpec kinds: emoji | preset | color — not just emoji) AND §"Aesthetic direction" (warm, rounded, cozy — Nunito UI, rounded rows)
    - .planning/phases/03-multi-session-session-lifecycle/03-RESEARCH.md §"Architecture Patterns" → the System Architecture Diagram RENDERER block (SessionManager owns sessions[]+activeId; ViewportStack mounts ALL SessionViews; Sidebar rows + Add button) AND §"Open Questions" 3 (default name `Session N`, default icon `🖥️`, focus-on-switch)
    - .planning/phases/03-multi-session-session-lifecycle/03-PATTERNS.md §"src/renderer/SessionManager.tsx" + §"src/renderer/Sidebar.tsx" (analogs + the status-subscription mirror of onPtyData)
    - src/shared/types.ts (SessionRecord, SessionStatus, SessionIconSpec, LogicalId — the renderer session-list shape) and src/shared/id-factory.ts is NOT importable here (renderer); new ids are minted in MAIN — the renderer adds a session by calling ptyCreate and using the returned id
  </read_first>
  <action>
    Create `src/renderer/SessionManager.tsx` owning renderer state `sessions: SessionRecord[]` and `activeId: LogicalId | null`. On mount, call `window.api.listSessions()` for the initial list (main is the source of truth — RESEARCH Open Q2); if empty, auto-add one default session so the app still boots into a live terminal. Render `<Sidebar sessions activeId onSelect onAdd />` plus a `.viewport-stack` containing one `<SessionView>` per session (ALL kept mounted; pass `active={s.logicalId === activeId}`) keyed by `logicalId`. Subscribe to `window.api.onPtyStatus(s.logicalId, …)` per session and update that session's `status` in state on every transition (drives the badge — SC4), cleaning up all subscriptions in the effect return (mirror the onPtyData subscribe/unsubscribe discipline). `onSelect(id)` sets `activeId` (the SessionView effect hands WebGL + focus to the new view). `onAdd()` mints a session by computing the default name `Session ${sessions.length + 1}` and default icon `{ type:'emoji', value:'🖥️' }`, calling `window.api.ptyCreate({ cols, rows })`, then appending a `SessionRecord` (with the returned `id`/`pid`, status 'running', the default name/icon, cwd from home/default) and setting it active. NOTE: the SessionView itself issues its own ptyCreate on mount in Task 2 — to avoid a double-spawn, choose ONE owner of the spawn call and document it: recommended — SessionManager creates the record metadata and SessionView owns the actual `ptyCreate` (passing its `id` once known), OR SessionManager spawns and passes the resolved id to a controlled SessionView; pick one, keep it consistent, and ensure exactly one PTY per session (no orphan). Auto-`term.focus()` is handled in SessionView's activate effect.
    Create `src/renderer/Sidebar.tsx`: a `.sidebar` list rendering one `.sidebar-row` per session with `data-session-id={s.logicalId}` — render the icon via a `switch` over `SessionIconSpec.type` (emoji → the glyph; preset → the preset token; color → a color badge), the `s.name`, and a `.status-badge` whose dot/label color comes from `STATUS_STYLE[s.status]` (label + accent). Clicking a row calls `onSelect(s.logicalId)`; highlight the active row. Add a `[+ Add session]` button with `data-testid="add-session"` calling `onAdd`. Style from DESIGN.md tokens (Nunito, `--surface`/`--line`, rounded rows, `--radius`). This is the BASIC tier (D-02) — no create/edit form, no rename, no collapse, no keyboard shortcuts (those are Phase 4).
    Update `src/renderer/index.tsx`: import and mount `<SessionManager/>` into `#root` instead of `<TerminalPane/>`. Leave `TerminalPane.tsx` in place as the extraction source (it is no longer mounted; do not delete it this task — 03-03 or a later cleanup can remove it once SessionView fully subsumes it). No fenced code in this prose.
  </action>
  <acceptance_criteria>
    - `src/renderer/index.tsx` mounts `<SessionManager/>` (`grep -c "SessionManager" src/renderer/index.tsx` ≥ 1) and no longer renders `<TerminalPane/>`.
    - `SessionManager.tsx` calls `window.api.listSessions()` and `window.api.onPtyStatus(...)` (`grep -E "listSessions|onPtyStatus" src/renderer/SessionManager.tsx` returns ≥ 2 matches) and keeps ALL SessionViews mounted (renders a SessionView per session, passing `active`).
    - `Sidebar.tsx` renders a row per session with `data-session-id`, an icon switch over all 3 `SessionIconSpec` kinds, the name, and a `.status-badge` colored from `STATUS_STYLE` (`grep -c "STATUS_STYLE" src/renderer/Sidebar.tsx` ≥ 1); the add button has `data-testid="add-session"`.
    - Exactly one PTY is spawned per session (no double-spawn) — documented owner of the `ptyCreate` call; verified by the keep-alive E2E in 03-03 not showing duplicate sessions.
    - `npx tsc --noEmit` 0 errors; `npm run lint` 0 errors.
    - `npm test` (full suite incl. WDIO): the `multi-session-keepalive` smoke (SC1/SC2) goes GREEN — 3 sessions open, background printer in A keeps advancing while B is active, switch back shows current buffer (no blank/frozen frame).
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm test 2>&1 | grep -E "multi-session-keepalive|passing|failing"</automated>
  </verify>
  <done>The app boots into the multi-session IDE layout: a DESIGN.md basic sidebar (icon + name + live status badge mapped to the status color language) + a viewport stack of kept-alive SessionViews; user can add sessions and switch with no process death and current scrollback; the multi-session-keepalive E2E (SC1/SC2) is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (contextBridge) | The renderer now drives N concurrent sessions through the typed bridge (incl. the 4 new methods wired this plan). Still the only renderer↔main seam; raw ipcRenderer never exposed. |
| renderer DOM (untrusted PTY output) | Each xterm renders raw PTY bytes; xterm is the trusted sanitizer (no innerHTML of raw output by app code). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Spoofing (surface drift) | Preload now exposes 4 more methods — an unreviewed key could leak | mitigate | The 4 methods are added ONLY inside the typed `api` object; `security.guard.test.ts` asserts the exact 12-key set and `not.toHaveProperty('ipcRenderer')` (turned GREEN in Task 1). |
| T-03-07 | DoS (GPU exhaustion) | One WebGL context per session would blow the Chromium ~16-context cap at ~15 sessions → terminals blank | mitigate | WebGL attached to the ACTIVE view only; `webgl.dispose()` on deactivate frees the context immediately (Pattern 7 / Pitfall 2). |
| T-03-08 | Information Disclosure | PTY output rendered/inspected via the test driver or logs | mitigate | Renderer never logs PTY bytes; xterm renders into its own buffer; the E2E driver reads the rendered DOM only for assertions (no persistence). |
| T-03-03 | Injection | `startupCommand` shown/typed in the UI later | accept | Carried from 03-01 — written as the user's own keystrokes into the user's own shell; no app-side eval. (Startup-command UI is 03-03.) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH §Package Legitimacy Audit); no install task. |

No HIGH-severity threat is left without a mitigation.
</threat_model>

<verification>
- `npm run test:unit -- security.guard` GREEN (12-key surface, no ipcRenderer).
- `npx tsc --noEmit` 0 errors; `npm run lint` 0 errors.
- `npm test` — `multi-session-keepalive` smoke GREEN (SC1 keep-alive + SC2 current-scrollback).
- Manual/visual (deferred to 03-03 human-verify): badge colors match DESIGN.md; WebGL hand-off has no "too many contexts" warning at ~15 sessions.
</verification>

<success_criteria>
- Preload exposes the 4 new typed methods; security guard GREEN.
- SessionView keeps one xterm per session alive (buffers while hidden), WebGL on active only, hidden-pane-safe fit; status-colors maps all 5 statuses incl. the derived red error ramp.
- SessionManager + basic DESIGN.md Sidebar deliver add-session + switch-without-process-death + live status badges; app boots into the IDE layout.
- `multi-session-keepalive` E2E GREEN — SC1 (background process survives switch) + SC2 (current scrollback, no blank frame).
</success_criteria>

<output>
Create `.planning/phases/03-multi-session-session-lifecycle/03-02-SUMMARY.md` when done.
</output>
