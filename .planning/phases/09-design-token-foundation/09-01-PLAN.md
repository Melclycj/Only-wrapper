---
phase: 09-design-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/renderer/tokens.css
  - src/renderer/index.tsx
autonomous: false
requirements: [UI-01]
user_setup: []

must_haves:
  truths:
    - "The app self-hosts Nunito + JetBrains Mono (no CDN); the fonts are imported in the renderer entry before the consuming stylesheet"
    - "A single src/renderer/tokens.css :root layer defines the complete design-token set (colors, status accents, radius, shadow, motion, font stacks, and the full --space-* scale) with value-preserving values carried verbatim from DESIGN.md / terminal.css / status-colors.ts"
    - "tokens.css is imported before terminal.css so :root vars exist when component styles parse"
  artifacts:
    - path: "src/renderer/tokens.css"
      provides: "The :root design-token layer (D-02/D-03/D-04/D-05)"
      contains: "--color-accent"
      min_lines: 50
    - path: "package.json"
      provides: "Exact-pinned @fontsource deps"
      contains: "@fontsource/nunito"
  key_links:
    - from: "src/renderer/index.tsx"
      to: "@fontsource/nunito + @fontsource/jetbrains-mono + ./tokens.css"
      via: "import statements (fonts first, tokens before terminal.css)"
      pattern: "import '@fontsource/nunito"
    - from: "src/renderer/index.tsx"
      to: "src/renderer/tokens.css"
      via: "import order (tokens.css before terminal.css)"
      pattern: "import './tokens.css'"
---

<objective>
Establish the Phase 9 design-token foundation: install the self-hosted fonts (D-05),
create the single-source-of-truth `src/renderer/tokens.css` `:root` layer (D-02/D-03/D-04),
and wire the renderer entry so fonts + tokens load before the consuming stylesheet.

This is Wave 1 — it produces the token layer and the loaded fonts that Plan 02's
migration consumes. Nothing in `terminal.css` or `status-colors.ts` is migrated here
yet (that is Plan 02); this plan only ADDS `tokens.css`, the font imports, and the deps.

Purpose: Per UI-01, the design tokens are "the foundation the per-surface requirements
build on." The 28× blue / 8× danger-red / unloaded fonts are the cross-cutting debt that
must be unified into one home before per-surface work (Phases 10–13). This plan creates
that home and makes the signature cozy typography finally render.

Output: `@fontsource/nunito@5.2.7` + `@fontsource/jetbrains-mono@5.2.8` installed
(exact-pinned), `src/renderer/tokens.css` (the complete `:root` token set), and
`src/renderer/index.tsx` importing fonts first → `tokens.css` → `terminal.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN.md
@.planning/phases/09-design-token-foundation/09-CONTEXT.md
@.planning/phases/09-design-token-foundation/09-RESEARCH.md
@.planning/phases/09-design-token-foundation/09-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Pre-install legitimacy verification of @fontsource packages (supply-chain gate, T-09-SC)</name>
  <read_first>
    - .planning/phases/09-design-token-foundation/09-RESEARCH.md §"Package Legitimacy Audit" (slopcheck was UNAVAILABLE — manual registry signals substituted; precautionary checkpoint required before install)
    - .planning/phases/09-design-token-foundation/09-RESEARCH.md §"Security Domain" (Supply chain V1/V14 family — exact-pin + human-verify is the one concrete control)
  </read_first>
  <what-built>
    Nothing executed yet. This is the precautionary supply-chain gate that MUST pass
    before `npm install` of the two new font packages. RESEARCH flagged that slopcheck
    0.6.1 exposes no usable CLI in this environment, so an automated legitimacy verdict
    was unavailable; the two packages were verified manually (official Fontsource org,
    high downloads, no postinstall) but are gated here as belt-and-suspenders. This
    checkpoint is NOT auto-approvable (workflow.auto_advance is ignored for legitimacy gates).
  </what-built>
  <how-to-verify>
    Confirm each package is the legitimate official Fontsource package before approving install:
    1. Run `npm view @fontsource/nunito@5.2.7 maintainers scripts` and confirm: maintainer
       includes `jwr1` (official Fontsource), `scripts.postinstall` is EMPTY/absent.
    2. Run `npm view @fontsource/jetbrains-mono@5.2.8 maintainers scripts` and confirm the
       same official maintainer and NO postinstall script.
    3. Open https://www.npmjs.com/package/@fontsource/nunito and
       https://www.npmjs.com/package/@fontsource/jetbrains-mono — confirm the repository
       points to github.com/fontsource/fontsource (official org) and the version matches.
    Expected: both are official Fontsource packages, exact versions exist, zero install-time scripts.
  </how-to-verify>
  <acceptance_criteria>
    - `npm view @fontsource/nunito@5.2.7 scripts.postinstall` returns empty (no postinstall)
    - `npm view @fontsource/jetbrains-mono@5.2.8 scripts.postinstall` returns empty
    - Both packages resolve on the npm registry at the exact pinned versions (5.2.7 / 5.2.8)
    - Maintainer/repo confirmed official Fontsource org (github.com/fontsource/fontsource)
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to install, or describe the legitimacy concern to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install @fontsource deps + create tokens.css + wire renderer-entry imports (D-02/D-03/D-04/D-05)</name>
  <files>package.json, package-lock.json, src/renderer/tokens.css, src/renderer/index.tsx</files>
  <read_first>
    - src/renderer/terminal.css lines 1-17 (the existing `:root` block to lift verbatim: --surface #ffffff, --bg-sunk oklch(0.955 0.01 85), --ink oklch(0.32 0.012 70), --ink-soft oklch(0.5 0.012 70), --ink-faint oklch(0.66 0.01 75), --line oklch(0.91 0.008 80), --line-soft oklch(0.94 0.006 80), --radius 18px, --term-bg #1e232c)
    - src/renderer/index.tsx (current 4-line import block — the migration target for import order)
    - .planning/DESIGN.md §"Design tokens" (the --bg oklch(0.975 0.008 85) NOT yet in terminal.css :root; --term-text oklch(0.90 0.012 250); --term-faint oklch(0.66 0.02 255))
    - .planning/phases/09-design-token-foundation/09-RESEARCH.md §"Code Examples > tokens.css skeleton" (lines 278-352 — the full value-preserving token skeleton)
    - .planning/phases/09-design-token-foundation/09-PATTERNS.md §"src/renderer/tokens.css (NEW)" and §"src/renderer/index.tsx (MODIFY)"
    - src/renderer/status-colors.ts lines 23-51 (the current literal accents to reflect in --accent-* tokens: running blue oklch(0.62 0.14 248), exited green oklch(0.60 0.13 150), stopped/not_started slate oklch(0.64 0.02 260), error red oklch(0.58 0.16 25), waiting amber oklch(0.66 0.15 60))
  </read_first>
  <action>
    Install both fonts exact-pinned (no caret): run `npm install @fontsource/nunito@5.2.7 @fontsource/jetbrains-mono@5.2.8`. In package.json `dependencies`, the entries land alphabetically — `@fontsource/jetbrains-mono` and `@fontsource/nunito` slot BETWEEN `@dnd-kit/sortable` and `@xterm/addon-canvas` (`@f` sorts before `@x`); verify the block stays alphabetical and both values are bare `"5.2.8"` / `"5.2.7"` with no caret. Do NOT touch forge.config.ts or vite.renderer.config.ts (fonts are renderer assets with a relative base — RESEARCH confirms no packaging change is needed; over-engineering here is a regression risk).

    Create src/renderer/tokens.css as the single `:root` design-token layer. Carry EVERY value verbatim (value-preserving — Phase 9 changes rendered geometry/color for NOTHING except the now-real fonts). Define these token families (use the RESEARCH skeleton lines 278-352 as the authoritative source):
    - Surface/ink/line lifted from terminal.css:6-17 PLUS `--bg: oklch(0.975 0.008 85)` from DESIGN.md (the warm-white interior, not yet in terminal.css :root). Keep `--surface: #ffffff`, `--bg-sunk`, `--ink`, `--ink-soft`, `--ink-faint`, `--line`, `--line-soft` exactly as the existing :root.
    - Terminal palette: keep `--term-bg: #1e232c`; ADD `--term-text: oklch(0.90 0.012 250)` and `--term-faint: oklch(0.66 0.02 255)` (D-03 — CSS gutter only; these are NOT routed into the xterm JS theme).
    - Brand: `--color-accent: oklch(0.62 0.14 248)`, `--color-accent-strong: oklch(0.58 0.14 248)`, `--color-danger: oklch(0.58 0.16 25)`, `--color-danger-strong: oklch(0.54 0.16 25)`.
    - Status accents (consumed by status-colors.ts in Plan 02): `--accent-running: var(--color-accent)`, `--accent-finished: oklch(0.60 0.13 150)`, `--accent-idle: oklch(0.64 0.02 260)`, `--accent-error: var(--color-danger)`, `--accent-waiting: oklch(0.66 0.15 60)` (amber, TERM-09 reserved).
    - Radius (value-preserving, from the distinct radii in terminal.css): `--radius-xs: 6px`, `--radius-sm: 7px`, `--radius-md: 8px`, `--radius-lg: 10px`, `--radius-xl: 12px`. KEEP `--radius: 18px` as an alias (recommended Discretion call per D-04 / RESEARCH A3 / Open-Q2 — minimizes churn at the 3 card sites that use `var(--radius)`). The 999px pills stay literal (fully-round, not a scale step).
    - Elevation (the 3 distinct shadows): `--shadow-pop: 0 6px 18px oklch(0.32 0.012 70 / 0.14)`, `--shadow-dialog: 0 18px 48px oklch(0.255 0.018 264 / 0.28)`, `--shadow-menu: 0 10px 30px oklch(0.255 0.018 264 / 0.22)`. The `0 0 0 2px var(--surface)` at terminal.css:388 is a ring NOT an elevation shadow — Plan 02 leaves it literal (do not add a token for it).
    - Motion: `--duration-fast: 120ms`, `--ease-standard: ease` (matches the existing `0.12s ease`; compositor-friendly only).
    - Fonts: `--font-ui: 'Nunito', system-ui, sans-serif`, `--font-mono: 'JetBrains Mono', monospace`.
    - Spacing scale — DEFINED ONLY (D-01; NOT applied to existing padding/gap/margin this phase): `--space-0_5: 2px`, `--space-1: 4px`, `--space-1_5: 6px`, `--space-2: 8px`, `--space-2_5: 10px`, `--space-3: 12px`, `--space-4: 16px`, `--space-5: 20px`, `--space-6: 24px`, `--space-8: 32px`, `--space-12: 48px`. Add a comment that the odd-ball 5/14/22px values stay one-offs retuned by Phases 10–13.
    Use the file's existing comment style (DESIGN.md §-references). Add a header comment stating the file is value-preserving and that the spacing scale is defined-not-applied this phase.

    Wire src/renderer/index.tsx import order (RESEARCH Pattern 1 / PATTERNS): after `import '../shared/api-types';`, add the five font imports FIRST — `@fontsource/nunito/400.css`, `@fontsource/nunito/600.css`, `@fontsource/nunito/700.css`, `@fontsource/jetbrains-mono/400.css`, `@fontsource/jetbrains-mono/700.css` (per-weight latin default with font-display:swap baked in; do NOT use `@fontsource-variable/*`). Then `import './tokens.css';` BEFORE the existing `import './terminal.css';`. Keep the `import { SessionManager } from './SessionManager';` line. The order matters so `@font-face` + `:root` vars are defined when terminal.css parses.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && node -e "const p=require('./package.json');if(p.dependencies['@fontsource/nunito']!=='5.2.7'||p.dependencies['@fontsource/jetbrains-mono']!=='5.2.8')process.exit(1)" && grep -q "import './tokens.css'" src/renderer/index.tsx && grep -q "@fontsource/nunito/400.css" src/renderer/index.tsx && grep -q -- "--color-accent:" src/renderer/tokens.css && grep -q -- "--font-mono:" src/renderer/tokens.css && grep -q -- "--space-12:" src/renderer/tokens.css && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"@fontsource/nunito": "5.2.7"` and `"@fontsource/jetbrains-mono": "5.2.8"` (exact, no caret), placed alphabetically between `@dnd-kit/*` and `@xterm/*`
    - `src/renderer/index.tsx` imports the 5 font CSS files BEFORE `import './tokens.css'`, which itself is BEFORE `import './terminal.css'`
    - `grep -c -- "--color-accent:" src/renderer/tokens.css` returns ≥1; the file defines `--color-accent`, `--color-danger`, all 5 `--accent-*`, `--term-text`, `--term-faint`, `--radius-xs`..`--radius-xl` + `--radius`, all 3 `--shadow-*`, `--duration-fast`, `--ease-standard`, `--font-ui`, `--font-mono`, and `--space-0_5`..`--space-12`
    - `npx tsc --noEmit` exits 0; `npm run lint` exits 0; `npm run test:unit` exits 0 (unchanged — no migration yet, so status-colors.test.ts is still GREEN against the existing literals)
    - forge.config.ts and vite.renderer.config.ts are unchanged (git diff shows no edits)
  </acceptance_criteria>
  <done>
    Fonts installed exact-pinned, tokens.css exists with the complete value-preserving
    token set, index.tsx loads fonts → tokens.css → terminal.css in that order, and the
    full unit suite + tsc + lint stay green. No literal has been migrated yet (Plan 02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Two new third-party font packages cross into the build (supply chain). |
| renderer CSS/JS → no new IPC | This phase adds NO bridge surface; the renderer↔main boundary is untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-SC | Tampering | `npm install @fontsource/*` (supply chain) | mitigate | Exact-pin verified versions (5.2.7 / 5.2.8); official Fontsource org confirmed; `npm view … scripts.postinstall` empty; Task 1 blocking-human legitimacy checkpoint BEFORE install (slopcheck unavailable per RESEARCH). woff2 are static renderer assets — no executable surface. |
| T-09-01 | Elevation | renderer bridge surface | accept | No new IPC/bridge key added — EXPECTED_API_KEYS stays 20 (enforced by the existing `security.guard.test.ts`, which stays GREEN). Nothing to mitigate; recorded for completeness. |
| T-09-02 | Spoofing | Google Fonts / CDN at runtime | accept | D-05 forbids CDN; fonts are self-hosted/bundled, so the app makes NO font network request (local-only constraint preserved). No new egress introduced. |

## Reliability & Cost Lens

| Failure mode | Component | Disposition | Note |
|--------------|-----------|-------------|------|
| Install-time script abuse | postinstall | mitigate | Both packages have NO postinstall (verified in Task 1); the repo's own `postinstall` (fix-node-pty) is unaffected. |
| Bundle bloat | woff2 emission | accept | Latin-subset weights only (400/600/700 Nunito; 400/700 JetBrains Mono). Vite emits hashed shared files (not base64-inlined) — expected, no action. |
| No runtime cost surface | — | n/a | Tokens are presentation-only; no retry/concurrency/cost path is added. |
</threat_model>

<verification>
- `npx tsc --noEmit` — clean (no type regression from the import additions).
- `npm run lint` — clean.
- `npm run test:unit` — full unit suite GREEN (no test changes this plan; status-colors.test.ts still asserts the existing literals and must stay green).
- `git diff --name-only` shows ONLY package.json, package-lock.json, src/renderer/tokens.css, src/renderer/index.tsx changed — forge.config.ts and vite.renderer.config.ts untouched.
</verification>

<success_criteria>
- The two @fontsource packages are installed exact-pinned and pass the Task 1 legitimacy gate.
- `src/renderer/tokens.css` defines the complete value-preserving token set (every family listed in the action).
- `src/renderer/index.tsx` loads fonts first, then `tokens.css`, then `terminal.css`.
- tsc + lint + unit suite stay green; packaging/Vite config untouched.
</success_criteria>

<output>
Create `.planning/phases/09-design-token-foundation/09-01-SUMMARY.md` when done.
</output>
