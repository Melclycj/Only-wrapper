---
phase: 10-sidebar-visual-polish
plan: 13
type: execute
wave: 10
depends_on: [10-12]
files_modified:
  - .planning/phases/10-sidebar-visual-polish/10-VALIDATION.md
  - .planning/phases/10-sidebar-visual-polish/10-REVIEW.md
autonomous: false
requirements: [UI-02]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "A gsd-code-review delta pass covers ALL gap-closure source deltas (10-07 / 10-08 / 10-09 / 10-11 + the round-4 10-12 deltas) and any Critical/High finding is fixed BEFORE the human gate (the round-3 deferred review obligation is discharged here)"
    - "The full automated suite (unit + tsc + lint + smoke) is GREEN against the PACKAGED app and a fresh packaged no-injection ui-lab capture (`npm run ui:shots:fresh`, a NEW UI_LAB_TAG) completes its surfaces with the name-completeness assertion ENFORCED and PASSING and the amended wash-only sidebar-waiting expectation"
    - "A human re-verifies the running app (attempt 4): GAP-10-H fixed live (a single Start affordance across dormant-row states), the AMENDED waiting look (wash only, no edge bar) confirmed, the active-row status-colored bar reading amber while waiting confirmed acceptable (or extended as a NEW item, not a defect), and a standing no-regression sweep (GAP-10-D/E/F/G stay closed) — then signs off with an EXPLICIT, UNQUALIFIED 'approved'"
    - "nyquist_compliant flips to true in 10-VALIDATION.md ONLY on that unqualified approval; on any qualification it stays false and the new items route to a fresh gap-closure round"
  artifacts:
    - path: ".planning/phases/10-sidebar-visual-polish/10-VALIDATION.md"
      provides: "The human-gate history row for attempt 4 + the nyquist_compliant flag state"
      contains: "nyquist_compliant"
    - path: ".planning/phases/10-sidebar-visual-polish/10-REVIEW.md"
      provides: "The delta code-review of rounds 2-4 source changes (10-07/08/09/11/12) with severity-tagged findings"
      contains: "10-12"
  key_links:
    - from: "the packaged app"
      to: "the ui-lab capture + the human gate"
      via: "npm run ui:shots:fresh produces the proof capture; the operator drives the live verification"
      pattern: "ui:shots:fresh"
---

<objective>
The closing gate for Phase 10's gap-closure rounds — attempt 4. Discharge the round-3 DEFERRED code-review obligation, re-run the full evidence chain after 10-12 (GAP-10-H Start-dedup fix + GAP-10-I D-09 wash-only amendment), then put the running app in front of the operator for a BLOCKING human re-verify.

Round-4 scope (authoritative from 10-VERIFICATION.md "Addendum 2"): gate attempt 3 (2026-06-12) was NOT_APPROVED_QUALIFIED — ITEM A (live amber, GAP-10-D), ITEM B (gutter, GAP-10-E), ITEM C (active-row name, GAP-10-F/G) were ALL live-confirmed CLOSED. Attempt 4 verifies ONLY (1) the GAP-10-H fix live (a single Start affordance across dormant-row states), (2) the AMENDED waiting look (GAP-10-I — wash only, no edge bar) live, (3) the active-row status-colored bar reading amber while waiting (D-05/D-06 deliberately UNAMENDED — operator confirms acceptable or extends the amendment as a NEW item, not a defect), and (4) a standing no-regression sweep that GAP-10-D/E/F/G stay closed.

The phase gate flips to compliant ONLY on an explicit, unqualified "approved" — no "partial", no "with adjustments". This is the FOURTH human gate (attempts 1, 2, 3 all returned NOT APPROVED). Automated green is NOT proof for a visual phase (the 06.1/08/10 precedent — the prior three gates each caught a live failure or a new defect the static capture passed). The ui-lab harness is the mandatory visual-evidence tool for this project (Phases 10-13). Do NOT relax any machine check; do NOT re-run 10-10 (this is a NEW gate plan).

Purpose: Close UI-02. The new defect (GAP-10-H Start dedup) and the sanctioned spec change (GAP-10-I wash-only) must be confirmed in the running app by a human, because no automated assertion can substitute for the live dormant-row interaction integrity or the "feels right" amended waiting look.

Output: A recorded gate verdict + a delta-review record. On unqualified approval: nyquist_compliant true, UI-02 complete, Phase 10 closeable. On any qualification: verbatim capture + route to a new gap-closure round.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sidebar-visual-polish/10-VALIDATION.md
@.planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md
@.planning/phases/10-sidebar-visual-polish/10-CONTEXT.md
@.planning/phases/10-sidebar-visual-polish/10-10-SUMMARY.md
@.planning/phases/10-sidebar-visual-polish/10-12-SUMMARY.md
@.planning/phases/10-sidebar-visual-polish/10-04-PLAN.md
@tests/ui-lab/README.md
@tests/ui-lab/DESIGN-RUBRIC.md
</context>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<artifacts_produced>
## Artifacts this phase produces (for source-grounding drift verification)

No NEW code symbols — this plan runs the delta code-review, runs the suite, produces a packaged ui-lab capture (artifacts under `artifacts/ui-lab/` are gitignored), and records the human-gate verdict + the review findings. It edits no `src/` files (any review-driven fix is applied via the normal edit path and re-verified, but the plan's deliverables are the verdict record + the review record). It edits:
- `.planning/phases/10-sidebar-visual-polish/10-VALIDATION.md` — the Human Gate History table (append attempt 4) + the `nyquist_compliant` frontmatter flag.
- `.planning/phases/10-sidebar-visual-polish/10-REVIEW.md` — the rounds 2-4 delta code-review record (severity-tagged findings + dispositions).
The verdict + review records ARE the deliverables (mirrors 10-10-SUMMARY.md / 10-REVIEW.md format).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: gsd-code-review delta pass over the rounds 2-4 source changes (10-07/08/09/11/12) — fix any Critical/High BEFORE the gate</name>
  <files>.planning/phases/10-sidebar-visual-polish/10-REVIEW.md</files>
  <read_first>
    - .planning/phases/10-sidebar-visual-polish/10-07-SUMMARY.md (the GAP-10-D agentGateOpen fix + the dev-only trace seam — confirm no unconditional console in the shipped agent path; the spike-003 instrumentation should be gone or dev-gated)
    - .planning/phases/10-sidebar-visual-polish/10-08-SUMMARY.md (the gutter + WR-01 box-sizing + WR-02 dormant-gap CSS deltas)
    - .planning/phases/10-sidebar-visual-polish/10-09-SUMMARY.md (the assertNameNotCrushed harness + WR-03 import fix + WR-04 cleanup + WR-05 waitUntil)
    - .planning/phases/10-sidebar-visual-polish/10-11-SUMMARY.md (the active-row zero-collapse CSS delta)
    - .planning/phases/10-sidebar-visual-polish/10-12-SUMMARY.md (the round-4 GAP-10-H fix + GAP-10-I wash-only deltas)
    - .planning/phases/10-sidebar-visual-polish/10-REVIEW.md (the prior 10-05 delta review format — 0 Critical / 5 Warning / 1 Info; mirror its severity-tagged table)
    - src/renderer/Sidebar.tsx, src/renderer/sidebar.css, src/renderer/SessionView.tsx, src/renderer/agent-tick.ts, src/shared/agent-state.ts, src/renderer/start-affordances.ts, tests/ui-lab/surfaces.ts (the changed source surfaces to review)
  </read_first>
  <action>
    Run a delta code-review over the gap-closure source changes from rounds 2-4 (the round-3 deferred obligation: "gsd-code-review delta pass on rounds 2+3 source changes (10-07/08/09/11) — deliberately deferred to round 4 so one review covers all gap-closure deltas before gate attempt 4" — extended here to ALSO cover the round-4 10-12 deltas). Scope the review to the files changed by 10-07, 10-08, 10-09, 10-11, and 10-12 (use `git log`/`git diff` against the round-1 baseline to enumerate the exact changed lines; the SUMMARYs name the commits).

    Review for: correctness of the agent-state fix (10-07 agentGateOpen — no race re-introduced, no unconditional console in the shipped agentTick path, the dev trace seam INERT in production), CSS correctness (10-08/10-11/10-12 — zero-collapse + waiting wash-only + the GAP-10-H scoping if CSS was touched, no dead rules left, tokens-only no migrated literals), harness robustness (10-09/10-12 — assertions throw not silently pass, cleanup hooks present, no leaked fabricated state), and the GAP-10-H fix from 10-12 (the diagnosed-link change is minimal and does not re-introduce a duplicate Start on any other state). Tag each finding by severity (Critical / High / Medium / Low / Info) using the project's code-review ramp.

    Write the findings to `.planning/phases/10-sidebar-visual-polish/10-REVIEW.md` (append a "Rounds 2-4 delta review" section if the file exists, else create it) as a severity-tagged table with file:line + a concrete remediation for each. FIX any Critical or High finding in-place via the normal edit path BEFORE proceeding to Task 2 (they must not reach the human gate), then re-run the affected unit/tsc to confirm green. Record Medium/Low/Info as deferred-or-noted with rationale (do not block the gate on them; route genuinely-deferred items to deferred-items.md if appropriate). Do NOT touch the FULLY CLOSED GAP-10-D/E/F/G behavior — review only; a regression there is a finding, not a re-design.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-sidebar-visual-polish/10-REVIEW.md && grep -q "10-12" .planning/phases/10-sidebar-visual-polish/10-REVIEW.md && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/10-sidebar-visual-polish/10-REVIEW.md` contains a rounds 2-4 delta-review section covering the 10-07/08/09/11/12 source deltas, with severity-tagged findings (file:line + remediation each) and references plan `10-12`.
    - Every Critical and High finding is FIXED in-place before the gate (the review section records each as resolved with the fix), and `npx tsc --noEmit` exits 0 after any fix.
    - Medium/Low/Info findings are recorded with a disposition (fixed / deferred-with-rationale); genuinely-deferred items are noted (and added to deferred-items.md if they are out of phase scope).
    - No edit to the FULLY CLOSED GAP-10-D/E/F/G behavior except a fix for an identified regression (none expected).
  </acceptance_criteria>
  <done>The deferred rounds 2-4 delta code-review is complete; all Critical/High findings are fixed pre-gate; the review record is written; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite GREEN against the packaged app + fresh no-injection ui-lab capture (NEW UI_LAB_TAG) rescored against the rubric</name>
  <files>(verification-only — no source files modified; produces gitignored capture artifacts)</files>
  <read_first>
    - tests/ui-lab/README.md (the loop + hard rules: injection is preview, packaged is proof; tokens first; suite stays green; no claim without a capture)
    - tests/ui-lab/DESIGN-RUBRIC.md (the screenshot-checkable expectations — SC1 active-distinct, SC2 name-legibility, D-09 AMENDED waiting wash-only no edge bar, D-11 dashed recipe cards, collapsed continuity, the dormant-row single-Start affordance)
    - .planning/phases/10-sidebar-visual-polish/10-10-SUMMARY.md (the prior gate's capture tag, the rubric rescore table format, the static-capture caveat that the human gate covers the live items; the verbatim-capture + routing pattern)
    - .planning/phases/10-sidebar-visual-polish/10-12-SUMMARY.md (the round-4 deltas to look for in the capture: single Start on the dormant/idle surfaces; wash-only waiting, no edge bar)
    - .planning/phases/10-sidebar-visual-polish/deferred-items.md (the 12 known pre-existing `.planning/spikes/*.cjs` lint errors that are tolerated — all Phase-10 source/test files must lint clean)
  </read_first>
  <action>
    Build the packaged app and run the full evidence chain against it (NOT the dev server). Run, in order, capturing real stdout for the SUMMARY:
    - `npm run test:unit` — the full vitest suite, all-green (incl. the 10-07 real-frame regression test, the agent-state-replay oracle at "exactly 1 WAITING", tokens-completeness scanning sidebar.css, the corrected sidebar-agent-attr import, AND the NEW 10-12 start-affordances truth-table case). The count must not drop below the round-3 baseline.
    - `npx tsc --noEmit` — exit 0.
    - `npm run lint` — only the known pre-existing `.planning/spikes/*.cjs` errors are tolerated (logged in deferred-items.md); all Phase-10 source/test files must lint clean, and no unconditional console in the shipped agent path.
    - `npm run test:smoke` — 15/15 spec files green against the packaged binary (keyboard-switch, reorder, session-edit, app-restart-restore, sidebar-collapse — the SC4 keep-alive guard).
    - `UI_LAB_TAG=p10-gapfix-round4-gate npm run ui:shots:fresh` — the packaged, no-injection capture with a NEW tag. It ENFORCES the 10-09 name-completeness assertion (FAILS if a name is crushed), the amended wash-only sidebar-waiting surface, and (if 10-12 added one) the dormant-selected single-Start assertion. The run must complete its full surface set (the round-3 run was 11/11; if 10-12 added a surface the count rises by that surface — confirm the manifest lists every surface as captured, none failed).

    Then rescore the rubric against the fresh capture with pixel-cited verdicts, focusing on: GAP-10-H (the dormant/idle-card surfaces show a SINGLE Start affordance — no duplicate ▶/⏵), GAP-10-I (the sidebar-waiting capture shows the amber WASH with NO amber left edge bar; the active row's own status-colored edge bar is still present and reads amber when the active row is waiting), and the standing no-regression items (SC1 active-distinct, SC2 name complete incl. the active row, collapsed continuity, dashed recipe cards). Record the static-capture caveat: the capture proves the resting/populated/waiting/dormant STYLING but cannot exercise the live dormant-row interaction or a real agent — that is what Task 3's human gate covers.
  </action>
  <verify>
    <automated>npm run test:unit && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` is all-green (stdout pasted in the SUMMARY), including the 10-07 real-frame regression test, the agent-state-replay oracle at "exactly 1 WAITING", and the NEW 10-12 start-affordances truth-table case; the count did not drop below the round-3 baseline.
    - `npx tsc --noEmit` exits 0; `npm run lint` clean on Phase-10 files (only the known pre-existing spike .cjs errors tolerated; no unconditional console in the shipped agent path).
    - `npm run test:smoke` 15/15 green against the packaged binary.
    - `UI_LAB_TAG=p10-gapfix-round4-gate npm run ui:shots:fresh` completes its FULL surface set (manifest lists every surface captured, none failed) with the name-completeness assertion ENFORCED and PASSING, the amended wash-only sidebar-waiting surface, and the dormant single-Start assertion if 10-12 added one.
    - The rubric is rescored with pixel-cited verdicts for GAP-10-H (single Start on dormant/idle surfaces), GAP-10-I (waiting wash-only, no edge bar; active bar preserved), and the no-regression items.
  </acceptance_criteria>
  <done>The full automated suite is green against the packaged app and a fresh no-injection capture completes its full surface set under a NEW tag (with the GAP-10-H single-Start + GAP-10-I wash-only deltas visible), rescored against the rubric.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING human re-verify of the running app (attempt 4) — unqualified "approved" required (phase gate)</name>
  <files>(no source files — human verification checkpoint; the verdict is recorded by Task 4 into 10-VALIDATION.md)</files>
  <read_first>
    - .planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md (the GAP-10-H + GAP-10-I gap entries — what the operator confirms; GAP-10-D/E/F/G are FULLY CLOSED, only the no-regression sweep applies to them)
    - .planning/phases/10-sidebar-visual-polish/10-CONTEXT.md (§"Gap-closure addendum 2" — the D-09 amendment + the SCOPE NOTE that D-05/D-06 are unamended, so the active-bar-amber question is a confirm-or-extend, not a defect)
  </read_first>
  <action>
    This is a BLOCKING human-verify checkpoint — do NOT auto-approve regardless of any auto-advance / yolo setting (visual phase gate; 06.1/08/10 precedent: automated green is not proof, and the prior three gates each caught a live failure or a new defect the static capture passed; the legitimacy/gate discipline is never auto-approvable). Build + launch the packaged app, present the how-to-verify script below to the operator, and WAIT for an explicit verdict. Capture the operator's response VERBATIM for Task 4. The checkpoint detail is in <what-built> / <how-to-verify> / <resume-signal> below.
  </action>
  <what-built>
    GAP-10-H (duplicate Start on inactive rows): the diagnosed broken state path was reproduced and root-caused (10-12 spike), then fixed so an inactive/dormant row shows exactly ONE Start affordance in every state — at rest, when selected, on hover, and right after you click Start — pinned by a deterministic regression check. GAP-10-I (waiting look): per your 2026-06-12 decision ("waiting dont show edge for now"), the waiting treatment is now an amber tint WASH ONLY — the amber left edge bar is removed (expanded rows and the collapsed rail; collapsed waiting shows the amber status dot). The active row's own status-colored edge bar is unchanged, so an active row that is waiting still reads amber on its active bar. All four prior items (live amber GAP-10-D, gutter GAP-10-E, name completeness GAP-10-F/G) stay closed. The deferred rounds 2-4 code-review delta pass ran and its Critical/High findings were fixed. The full suite is green against the packaged app and the fresh capture completes its surface set, rescored PASS.
  </what-built>
  <how-to-verify>
    Build + launch the PACKAGED app (`npm run make` then open `out/Just-Wrapper-darwin-arm64/Just-Wrapper.app`).

    ITEM H — Single Start affordance on inactive/dormant rows (GAP-10-H, the qualifying defect):
    1. Create a session, give it a saved startup command (e.g. `claude --rc`) via Edit, then Stop/Remove it so it lands in the Inactive List as a dormant recipe card.
    2. With the dormant row NOT selected: confirm it shows exactly ONE Start affordance (the sidebar ▶, plus the separate "Start without command" ⏵ for a recipe with a saved command — that ⏵ is a DIFFERENT, intentional control, not a duplicate of ▶).
    3. CLICK the dormant row to select it (its IdleCard appears in the terminal area with a big "▶ Start session"): confirm the row now shows NO duplicate sidebar Start — the IdleCard's "▶ Start session" is the SOLE Start surface; there is no second small ▶/⏵ on the selected row.
    4. Hover the selected dormant row, and a non-selected dormant row: confirm no extra Start appears on hover.
    5. Click Start: confirm that as the session starts there is no lingering/stale ▶ left behind (the row becomes a live running session with no Start control).

    ITEM I — Amended waiting look, wash only (GAP-10-I):
    6. Drive a session to a waiting state (a real `claude --rc` permission prompt, as in the prior gate). Confirm the waiting row shows the light amber WASH across the row but NO amber left edge bar. Check both expanded and the collapsed rail (collapsed should show the amber status DOT, not an amber bar).
    7. Active-bar-amber question (D-05/D-06 deliberately unamended): with the WAITING session ALSO the active/selected row, confirm the active row's own left edge bar reads amber. State whether that is acceptable as-is, or whether you want the active bar's amber dropped too (that would be a NEW item — not a defect — captured for a future round, not a gate block).

    Standing no-regression sweep (GAP-10-D/E/F/G stay closed):
    8. Live amber still fires at a real permission prompt (GAP-10-D); the leading gutter is still compact (GAP-10-E); medium names render complete on inactive AND the active row (GAP-10-F/G); SC1 active-distinct, SC3 collapsed identity + dots, SC4 click-to-switch / drag-reorder / keyboard-switch + terminal keep-alive all unregressed.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms ITEM H (an inactive/dormant row shows exactly one Start affordance in every state — rest / selected / hover / just-started), ITEM I (waiting = amber wash only, no edge bar, expanded + collapsed; and confirms the active-bar-amber as acceptable OR flags it as a NEW future item), and the standing no-regression sweep (GAP-10-D/E/F/G + SC1-SC4 unregressed) — then types an explicit, unqualified "approved".</human-check>
  </verify>
  <resume-signal>
    Type exactly "approved" (unqualified) to flip the phase gate. ANY qualification — "partial", "approved but…", "approved with adjustments", or a new defect/request — is NOT an approval: it is captured verbatim and routed to a new gap-closure round (round 5; the next fix plan would be 10-14). A request to ALSO drop the active-bar amber is a NEW design item (not a defect) — captured and routed, not a gate block, but it still means NOT an unqualified approval if raised as a required change. The Nyquist gate flips true ONLY on an explicit, unqualified "approved".
  </resume-signal>
  <done>The operator has run the live verification script and returned an explicit verdict (captured verbatim for Task 4); on an unqualified "approved" the phase gate is cleared.</done>
</task>

<task type="auto">
  <name>Task 4: Record the gate verdict in 10-VALIDATION.md (flip the flag only on unqualified approval)</name>
  <files>.planning/phases/10-sidebar-visual-polish/10-VALIDATION.md</files>
  <read_first>
    - .planning/phases/10-sidebar-visual-polish/10-VALIDATION.md (the frontmatter `nyquist_compliant: false` + the Human Gate History table with attempts 1, 2, 3; append attempt 4)
    - .planning/phases/10-sidebar-visual-polish/10-10-SUMMARY.md (the verbatim-capture + routing pattern to mirror on a non-approval)
  </read_first>
  <action>
    Record Task 3's verdict verbatim. On an explicit, UNQUALIFIED "approved": set `nyquist_compliant: true` in the 10-VALIDATION.md frontmatter, add a "4 | 10-13 | <date> | APPROVED | <verbatim quote> → UI-02 complete, Phase 10 closeable" row to the Human Gate History table, and update the "Current state" line to reflect the gate is closed. On ANY qualification or new required change (including a required drop of the active-bar amber): leave `nyquist_compliant: false`, add the attempt-4 row with the verbatim verdict and the routed items (kind / severity / fix-belongs-in — a new defect routes as a gap; a sanctioned new design item routes as a design item), and note the next fix plan is 10-14. Record the active-bar-amber answer (acceptable → no new item; or extend → a NEW design item captured, distinct from a defect). Do NOT touch STATE.md or ROADMAP.md (the orchestrator owns those writes). Do NOT attempt any code fix here — fixes belong to the next gap-closure round if the gate fails.
  </action>
  <verify>
    <automated>grep -Eq "nyquist_compliant:\s*(true|false)" .planning/phases/10-sidebar-visual-polish/10-VALIDATION.md && grep -q "10-13" .planning/phases/10-sidebar-visual-polish/10-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 10-VALIDATION.md has an attempt-4 row in the Human Gate History table citing plan 10-13 and the verbatim operator verdict.
    - `nyquist_compliant` is `true` IF AND ONLY IF the operator gave an explicit unqualified "approved"; otherwise it remains `false` with the routed items recorded (and the active-bar-amber answer captured as acceptable or as a new design item).
    - No edits to STATE.md or ROADMAP.md; no source code changed in this task.
  </acceptance_criteria>
  <done>The gate verdict is recorded; the Nyquist flag reflects the operator's exact verdict (true only on unqualified approval); the active-bar-amber answer is captured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live agent process → amber detection | The no-regression sweep drives a real claude --rc agent rendering attacker-influenceable TUI output through the classify() path — re-confirming the GAP-10-D fix on a real prompt (the one boundary the static capture cannot exercise). |
| live dormant-row interaction → Start dedup | The human gate drives the real selected/just-started dormant-row render that the pure predicate cannot fully exercise — the boundary GAP-10-H lives on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13-01 | Repudiation | the phase gate (accepting on automated green / a partial verdict) | mitigate | The gate is BLOCKING and human-only; nyquist_compliant flips ONLY on an explicit unqualified "approved" (06.1/08/10 precedent). A partial/new-request verdict is captured verbatim and routed, never silently accepted — this control caught GAP-10-D, GAP-10-G, and GAP-10-H at the prior gates. |
| T-10-13-02 | Tampering | the deferred code-review obligation (skipping it to reach the gate faster) | mitigate | Task 1 discharges the round-3 deferred delta review BEFORE the gate and fixes Critical/High pre-gate; a skipped review is a process repudiation the SUMMARY would expose. |
| T-10-13-03 | Denial of Service | classify() on the real agent prompt (the live amber no-regression check) | accept | Re-verifies the locked 10-07 bounded-region mitigation behaves on a real prompt; no new code here. |
| T-10-13-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan (review + suite run + verdict record only). No supply-chain surface. EXPECTED_API_KEYS stays 20. |
</threat_model>

<verification>
- Task 1: the rounds 2-4 delta code-review is recorded in 10-REVIEW.md; Critical/High findings fixed pre-gate; tsc green.
- Task 2: full suite green against the packaged app + a fresh no-injection capture (NEW tag) completing its full surface set with the name-completeness assertion enforced + the amended wash-only waiting + the dormant single-Start delta visible, rescored against the rubric.
- Task 3: BLOCKING human gate confirms GAP-10-H (single Start across dormant-row states), GAP-10-I (waiting wash-only), the active-bar-amber answer, and the GAP-10-D/E/F/G + SC1-SC4 no-regression sweep in the running app.
- Task 4: the Nyquist flag is set to match the operator's exact verdict; the active-bar-amber answer is captured.
</verification>

<success_criteria>
- On unqualified approval: nyquist_compliant true, UI-02 complete, Phase 10 closeable — the qualifying defect (GAP-10-H Start dedup) confirmed fixed in the running app and the amended waiting look (GAP-10-I) confirmed.
- On any qualification: verbatim verdict captured, flag stays false, items routed to 10-14 — no silent acceptance; the active-bar-amber answer recorded as acceptable or as a NEW design item.
- The deferred rounds 2-4 code-review obligation is discharged (Critical/High fixed pre-gate).
- The full automated suite + the packaged ui-lab capture (full surface set, new tag, machine-checks enforced) are the evidence backing the gate; the human verdict is the authoritative record; no machine check relaxed.
</success_criteria>

<output>
Create `.planning/phases/10-sidebar-visual-polish/10-13-SUMMARY.md` when done. Record: the rounds 2-4 delta-review outcome (findings by severity + the Critical/High fixes applied); the full-suite stdout summary; the rescored rubric + the full-surface-set capture confirmation under tag `p10-gapfix-round4-gate`; the verbatim human verdict mapped to ITEM H / ITEM I / the active-bar-amber answer / the no-regression sweep; the resulting nyquist_compliant state; and — on a non-approval — the routed items for round 5 (10-14).
</output>
