---
phase: 10-sidebar-visual-polish
plan: 06
type: execute
wave: 2
depends_on: ["10-05"]
gap_closure: true
files_modified: []
autonomous: false
requirements: ["UI-02"]

must_haves:
  truths:
    - "A fresh packaged no-injection ui-lab capture (npm run ui:shots:fresh) exists for the Phase-10 surfaces INCLUDING the now-drivable sidebar-waiting surface, scored line-by-line against DESIGN-RUBRIC.md with pixel-cited verdicts (closes the failed P04-T1)"
    - "SC2 (two-line hierarchy / name not crushed) and D-09 (amber waiting wash visible) are re-scored against the fresh capture and the sidebar-waiting PNG, replacing the prior FAILED/SkipSurface verdicts"
    - "The full automated suite is GREEN against the PACKAGED app: npm run test:unit + npx tsc --noEmit + scoped eslint (Phase-10 source clean) + npm run test:smoke (15/15 against the packaged binary)"
    - "A BLOCKING human re-verify covering EXACTLY the two 10-VERIFICATION.md 'Human Verification Required' items — (1) hover-state name legibility and (2) live amber waiting incl. backgrounded + cleared-after-exit — is run against the running app and its verdict recorded verbatim (closes the failed P04-T3)"
    - "nyquist_compliant stays FALSE until the operator explicitly approves; automated green alone does NOT pass this visual phase (06.1/08 precedent)"
  artifacts:
    - path: ".planning/phases/10-sidebar-visual-polish/10-06-SUMMARY.md"
      provides: "Phase-gate evidence: fresh packaged capture record, rescored rubric table (SC2 + D-09 + sidebar-waiting), full-suite results, verbatim human re-verify verdict, nyquist gate state"
  key_links:
    - from: "10-05 code fixes"
      to: "fresh packaged ui:shots:fresh capture"
      via: "no-injection packaged capture proves the resting/waiting surfaces"
      pattern: "ui:shots:fresh"
    - from: "fresh capture + running app"
      to: "human re-verify verdict"
      via: "blocking checkpoint on the two Human-Verification-Required items"
      pattern: "approved"
---

<objective>
End-of-gap-closure verification gate for Phase 10. Plan 10-05 closed the code-level gaps (GAP-10-A name crush, GAP-10-B amber waiting + WR-03 gate, WR-04 precedence, IN-05 capture seam). This plan proves the fixes in the PACKAGED app and re-runs the blocking human gate the phase failed on (10-04-SUMMARY.md: NOT_APPROVED).

A visual phase cannot pass on automated green alone. This plan: (1) re-runs the full suite against the packaged binary, (2) produces a fresh packaged no-injection capture INCLUDING the newly-drivable sidebar-waiting surface, (3) re-scores the previously-FAILED rubric lines (SC2, D-09), and (4) runs a BLOCKING human re-verify covering exactly the two 10-VERIFICATION.md "Human Verification Required" items. The Nyquist gate flips true ONLY on an explicit operator "approved".

Purpose: convert the closed gaps into recorded proof and an honest human sign-off so Requirement UI-02 can complete.
Output: 10-06-SUMMARY.md with the capture record, rescored rubric, suite results, and the verbatim human verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-sidebar-visual-polish/10-CONTEXT.md
@.planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md
@.planning/phases/10-sidebar-visual-polish/10-04-SUMMARY.md
@tests/ui-lab/README.md
@tests/ui-lab/DESIGN-RUBRIC.md
</context>

<artifacts_this_phase_produces>
Symbols / rules / seams this plan CREATES (exclude from source-drift verification):
- DOCS: `.planning/phases/10-sidebar-visual-polish/10-06-SUMMARY.md` (the gate evidence record).
- ARTIFACTS (gitignored, not committed): a fresh packaged ui-lab capture under `artifacts/ui-lab/<tag>/` (e.g. `p10-gapfix-after/`) with `manifest.json` and the per-surface PNGs incl. `sidebar-waiting.png`.
- No source symbols are created — this is a verification-only plan (`files_modified: []`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Full suite + fresh packaged no-injection capture + rubric rescore (closes P04-T1)</name>
  <files>(none — verification-only; capture artifacts are gitignored)</files>
  <read_first>
    - tests/ui-lab/README.md (the loop + hard rules — injection is preview, packaged is proof; structural changes need ui:shots:fresh; no claim without a capture)
    - tests/ui-lab/DESIGN-RUBRIC.md (the screenshot-checkable expectations; the SC2 two-line-hierarchy line and the D-09 amber-waiting line are the two to rescore)
    - .planning/phases/10-sidebar-visual-polish/10-04-SUMMARY.md (the prior gate's capture protocol: packaged app at out/Just-Wrapper-darwin-arm64, UI_LAB_TAG=p10-after npm run ui:shots:fresh, baseline-substitution disclosure)
    - .planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md (the failed truths SC2 + D-09 + P04-T1 this rescore must address)
    - .planning/phases/10-sidebar-visual-polish/deferred-items.md (the pre-existing .planning/spikes/*.cjs lint errors are out of scope — do not chase them)
  </read_first>
  <action>
    Build/package the app from the 10-05 HEAD so the capture is no-injection proof (mirror the 10-04 protocol: package to out/Just-Wrapper-darwin-arm64/Just-Wrapper.app). Run the full automated suite against the PACKAGED app and record exact results: `npm run test:unit` (must include tokens-completeness scanning sidebar.css AND the new sidebar-agent-attr test from 10-05, plus the agent-state-replay oracle still GREEN); `npx tsc --noEmit`; `npm run lint` (Phase-10 source files must be clean — the only permitted errors are the pre-existing `.planning/spikes/*.cjs` ones logged in deferred-items.md); `npm run test:smoke` (15/15 spec files against the packaged binary, incl. session-edit, app-restart-restore, keyboard-switch, reorder, sidebar-collapse, header-controls).

    Produce a FRESH packaged no-injection capture: `UI_LAB_TAG=p10-gapfix-after npm run ui:shots:fresh`. Confirm the run includes the `sidebar-waiting` surface PNG (now unblocked by 10-05 Task 3 — it must no longer SkipSurface) AND a `sidebar-populated` capture exercising a long-name running non-active row so the GAP-10-A name-reclaim is visible at rest. Record the manifest.json metadata (tag, gitSha, surface list).

    Re-score the rubric against the fresh capture with pixel-cited verdicts, focusing on the two previously-failed lines: SC2 / D-01·D-03 (two-line hierarchy — name NOT crushed; the control cluster reserves zero width at rest) and D-09 (amber left edge bar + amber wash now visible on the sidebar-waiting PNG). Note the static capture still cannot exercise the live hover state or a real agent process — those move to the human re-verify (Task 2). Use the phase9-baseline as the disclosed BEFORE pair (per 10-04 substitution note) for the name-crush before/after. Write the rescored rubric table into the SUMMARY.
  </action>
  <verify>
    <automated>ls artifacts/ui-lab/p10-gapfix-after/manifest.json && ls artifacts/ui-lab/p10-gapfix-after/sidebar-waiting.png && ls artifacts/ui-lab/p10-gapfix-after/sidebar-populated.png</automated>
  </verify>
  <acceptance_criteria>
    - A fresh capture dir `artifacts/ui-lab/p10-gapfix-after/` exists with `manifest.json`, `sidebar-waiting.png`, and `sidebar-populated.png`
    - `npm run test:unit` exits 0 with the new sidebar-agent-attr test and tokens-completeness + agent-state-replay all passing (paste the tail of stdout in the SUMMARY)
    - `npx tsc --noEmit` exits 0
    - `npm run lint` shows ZERO errors in Phase-10 source files (only the pre-existing `.planning/spikes/*.cjs` errors permitted)
    - `npm run test:smoke` passes all spec files against the packaged binary (paste the count)
    - The SUMMARY contains a rescored rubric table where SC2 (name not crushed) and D-09 (amber waiting on the sidebar-waiting PNG) are scored PASS with pixel-cited evidence, OR an honest FAIL routed back to a further fix
  </acceptance_criteria>
  <done>The packaged suite is GREEN, a fresh no-injection capture (incl. the unblocked sidebar-waiting surface) exists, and the two previously-failed rubric lines are rescored with pixel-cited evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: BLOCKING human re-verify of the two live-app items (closes P04-T3 / nyquist gate)</name>
  <files>(none — human-verify checkpoint; the verdict is recorded in 10-06-SUMMARY.md + 10-VALIDATION.md)</files>
  <read_first>
    - .planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md (§"Human Verification Required" — the EXACT two items this checkpoint re-verifies)
    - .planning/phases/10-sidebar-visual-polish/10-04-SUMMARY.md (the prior NOT_APPROVED verdict + the gate discipline: record verbatim, never flip nyquist without an explicit "approved")
    - .planning/phases/10-sidebar-visual-polish/10-CONTEXT.md (the GAP-10-C user decision — re-judge impression, do NOT treat the status-colored edge bar as a fail)
  </read_first>
  <action>
    Present the running packaged app and the two-item how-to-verify script below to the operator, capture their verdict VERBATIM, and record it mapped against ITEM 1 / ITEM 2 (plus the WR-04 and GAP-10-C notes) in 10-06-SUMMARY.md. Do NOT do the work the operator must do and do NOT infer "approved" from automated green. On an explicit "approved": set `nyquist_compliant` true in 10-VALIDATION.md and mark UI-02 complete. On any fail: capture the failing items verbatim, leave `nyquist_compliant` false, leave UI-02 open, and route the items to a 10-07 gap-closure plan.
  </action>
  <what-built>
    Plan 10-05 collapsed the hover-control cluster to zero width at rest (so the session name is no longer crushed — GAP-10-A), gated the amber waiting attribute on running rows and proved the emit→store→render chain (GAP-10-B / WR-02 / WR-03), made amber beat the active accent order-independently (WR-04), and unblocked the ui-lab amber capture (IN-05). Task 1 above produced a fresh packaged capture and re-scored the rubric. This checkpoint is the human re-verify the phase failed on at the 10-04 gate — it covers EXACTLY the two items in 10-VERIFICATION.md "Human Verification Required".
  </what-built>
  <how-to-verify>
    Launch the packaged app (out/Just-Wrapper-darwin-arm64/Just-Wrapper.app, built from the 10-05 HEAD).

    ITEM 1 — Name legibility in hover state (GAP-10-A re-verify):
    1. Create 3+ sessions with LONG names across both the Working Area and the Inactive List.
    2. Hover over a running, non-active row that shows the Edit/Restart/Close controls.
    3. EXPECTED: the session name on line 1 remains legible — NOT crushed to a few characters. The name is either fully visible or clearly ellipsis-truncated at a reasonable length; the controls reveal over the row's right end without permanently eating the name's width. Also confirm at rest (not hovering) the name uses the reclaimed width.
    4. Confirm the dormant recipe row's ▶ Start is still always-visible (not collapsed).

    ITEM 2 — Amber waiting treatment (GAP-10-B re-verify):
    5. Launch a `claude --rc` session and let it reach a permission prompt (waiting for input).
    6. EXPECTED: that row shows an amber left edge bar + a light amber wash; line 2 reads "Waiting for you".
    7. Switch to a DIFFERENT session while the first is still waiting — EXPECTED: the backgrounded waiting row STILL shows amber (it does not require being active).
    8. Let the waiting session finish/exit — EXPECTED: the amber DISAPPEARS (no stale amber on a finished row — WR-03).
    9. If a row is BOTH active AND waiting, confirm it reads amber (not the blue active accent) — WR-04.

    Note (GAP-10-C, user decision 2026-06-11): the edge bar is intentionally STATUS-colored and the per-session custom color lives on the icon tile — this is the locked spec and is NOT being changed. Re-judge the overall impression now that amber works; if it still reads wrong, that is a NEW design item for a future phase, not a fail of this gate.
  </how-to-verify>
  <verify>
    <human-check>The operator runs the 9-step script in the running packaged app and returns either "approved" or a verbatim list of failing items mapped to the step numbers above.</human-check>
  </verify>
  <resume-signal>Type "approved" to flip the Nyquist gate true and complete Phase 10, OR describe each failing item verbatim (which step, what you saw) so it can be routed to a further gap-closure plan. Do NOT flip the gate without an explicit "approved".</resume-signal>
  <acceptance_criteria>
    - The operator's verdict is recorded VERBATIM in 10-06-SUMMARY.md, mapped against ITEM 1 and ITEM 2 (and the WR-04 + GAP-10-C notes)
    - If "approved": `nyquist_compliant` is set true in 10-VALIDATION.md, the SUMMARY records the approval, and Requirement UI-02 is marked complete
    - If NOT approved: the failing items are captured verbatim and routed to a 10-07 gap-closure plan; nyquist_compliant stays FALSE; UI-02 stays open
    - The gate is NEVER passed on automated green alone — an explicit human "approved" is required (06.1/08 precedent)
  </acceptance_criteria>
  <done>The two live-app items are re-verified by the operator in the running app and the verdict (verbatim) plus the resulting Nyquist gate state are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification-only plan. No source code changes, no new input paths, no new dependencies. The packaged app under test is built from the already-reviewed 10-05 HEAD. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering | Packaged build under test | accept | The capture/gate runs the packaged binary built from reviewed source (10-05). No source edits in this plan; capture artifacts are gitignored and never shipped. Local-only Electron app, no network surface. |
| T-10-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs (verification-only; ui-lab and test toolchain already vetted — 10-UI-SPEC.md §Registry Safety). No legitimacy gate required. |
</threat_model>

<verification>
Automated: full suite GREEN against the packaged app (unit incl. sidebar-agent-attr + tokens-completeness + agent-state-replay; tsc 0; scoped eslint clean; smoke all-pass). Fresh packaged no-injection capture present with the sidebar-waiting PNG. Human: a BLOCKING re-verify of the two 10-VERIFICATION.md items, verdict recorded verbatim. The phase passes ONLY on an explicit operator "approved".
</verification>

<success_criteria>
- Fresh packaged no-injection capture exists (incl. the unblocked sidebar-waiting surface) and the previously-failed SC2 + D-09 rubric lines are rescored with pixel-cited evidence.
- Full automated suite GREEN against the packaged app.
- The two "Human Verification Required" items are re-verified in the running app; verdict recorded verbatim.
- On "approved": Nyquist gate true, UI-02 complete, Phase 10 closes. Otherwise: gaps routed to 10-07, gate stays false.
</success_criteria>

<output>
Create `.planning/phases/10-sidebar-visual-polish/10-06-SUMMARY.md` when done — incl. the fresh capture record, the rescored rubric table, the full-suite results, the verbatim human re-verify verdict, and the resulting Nyquist gate state.
</output>
