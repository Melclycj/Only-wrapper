---
phase: 10-sidebar-visual-polish
plan: 12
type: execute
wave: 9
depends_on: [10-11]
files_modified:
  - src/renderer/Sidebar.tsx
  - src/renderer/sidebar.css
  - src/renderer/__tests__/start-affordances.test.ts
  - tests/ui-lab/surfaces.ts
  - .planning/spikes/004-dormant-start-dup/README.md
autonomous: true
requirements: [UI-02]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "GAP-10-H: an inactive (dormant) row presents EXACTLY ONE Start affordance in every state — at rest, when selected/active, on hover, and immediately after the user clicks Start — no duplicate or stale ▶/⏵ Start co-renders (the R3 2026-06-09 startAffordances dedup contract holds across all enumerated dormant-row states)"
    - "The duplicate-Start repro state is reproduced and root-caused with captured evidence BEFORE any fix is written (diagnose-first, the proven 10-07 shape) — the README names WHICH two Start affordances co-render and on WHICH state path"
    - "A deterministic regression check pins the fix: a unit assertion over the startAffordances truth table (covering active dormant / non-active dormant / recipe-with-command / error-card states) AND, if the defect proved to be a render-path/state-flip rather than the pure predicate, a ui-lab dormant-selected surface assertion that no duplicate Start renders"
    - "GAP-10-I (D-09 AMENDMENT, sanctioned spec change — NOT check relaxation): a waiting row carries the amber tint WASH ONLY, with NO amber left edge bar — expanded rows AND the collapsed-rail mirror alike; the now-obsolete WR-04 compound precedence selector (.sidebar-row.active[data-agent='waiting']) is REMOVED, not left as dead rules; the active row's own status-colored edge bar (D-05/D-06) is UNCHANGED"
    - "The ui-lab sidebar-waiting surface expectation + any unit/CSS assertion on the waiting border-left are updated to the amended wash-only spec; assertNameNotCrushed is UNTOUCHED; the full suite stays green and ui:shots:fresh completes its surfaces"
  artifacts:
    - path: "src/renderer/Sidebar.tsx"
      provides: "The corrected Start-affordance render path (the startAffordances gate is honored across every dormant-row state; no duplicate Start co-renders)"
      contains: "startAffordances"
    - path: "src/renderer/sidebar.css"
      provides: "Waiting = amber wash only (no edge bar); WR-04 compound precedence selector removed; active edge bar (D-05/D-06) preserved"
      contains: "data-agent='waiting'"
    - path: "src/renderer/__tests__/start-affordances.test.ts"
      provides: "Truth-table regression pinning exactly-one-Start across the enumerated dormant-row states"
      contains: "totalStartCount"
    - path: ".planning/spikes/004-dormant-start-dup/README.md"
      provides: "The captured repro + per-state evidence diagnosing which two Start affordances co-rendered and on which path"
      contains: "GAP-10-H"
  key_links:
    - from: "src/renderer/Sidebar.tsx startAffordances() gate"
      to: "the sidebar ▶ (.row-control-start) + ⏵ (.row-control start-no-cmd) + the IdleCard ▶ render"
      via: "startCtl.sidebarStart / startCtl.startNoCmd flags gate which Start renders per row so exactly one Start-labeled control shows on a dormant row"
      pattern: "startCtl\\.(sidebarStart|startNoCmd)"
    - from: "src/renderer/sidebar.css [data-agent='waiting']"
      to: "the row's amber treatment"
      via: "the amber color-mix WASH stays; the border-left edge-bar rule + the WR-04 compound selector are removed (GAP-10-I)"
      pattern: "color-mix\\(in oklch, var\\(--accent-waiting\\)"
---

<objective>
Round 4 gap-closure code fixes for Phase 10. Two items, both renderer-scoped, both fixed here so they share `sidebar.css` without a write conflict:

1. **GAP-10-H (S2 defect — qualifies the gate).** At the round-3 BLOCKING human gate (10-10 attempt 3) the operator reported, verbatim: "the start button on the inactive task does not remove the original start button." A duplicate Start affordance renders on an inactive/dormant row. The R3 (2026-06-09) `startAffordances` dedup contract is: the ACTIVE dormant row's IdleCard "▶ Start session" is the SOLE Start surface (the sidebar ▶ AND the ⏵ "Start without command" are suppressed there); every NON-active dormant row keeps its single sidebar ▶ (+ its ⏵ when a startup command is saved). The pure `startAffordances()` reducer and its unit test are correct in isolation, so the live duplicate comes from a render-path / state-combination / status-flip path the pure test does not exercise. The exact repro state was NOT captured at the gate — so this plan is DIAGNOSE-FIRST (the proven 10-07 shape): reproduce → root-cause with evidence → fix the one broken link → lock with a deterministic regression check.

2. **GAP-10-I (S3 — sanctioned spec change, NOT a defect).** D-09 was AMENDED 2026-06-12 (operator verbatim: "waiting dont show edge for now. Later i will add more dynamic effect to show stage of agent."). Amended treatment: waiting = amber tint WASH ONLY, NO amber left edge bar — expanded rows AND the collapsed-rail mirror; collapsed waiting indication falls back to the amber status dot. The now-obsolete WR-04 compound precedence selector is REMOVED. D-05/D-06 (the ACTIVE row's own status-colored edge bar) are UNAMENDED and stay. Tests/captures asserting the waiting border-left update to THIS amended spec (sanctioned change, not relaxation; `assertNameNotCrushed` untouched).

Purpose: Close the two open gaps blocking the round-4 gate (10-13) so a dormant row shows exactly one Start in every state and the waiting look matches the amended D-09. Scope is renderer-only; the xterm/PTY data path and the 20-key bridge surface (EXPECTED_API_KEYS) are untouched.

Output: A corrected Start render path in `Sidebar.tsx` (+ `sidebar.css` if the defect is CSS-resurrected), a truth-table regression in `start-affordances.test.ts` (and a ui-lab dormant-selected assertion if the defect is a render-path one), the amended wash-only waiting CSS, and the updated sidebar-waiting harness expectation — plus a diagnosis README under `.planning/spikes/004-dormant-start-dup/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sidebar-visual-polish/10-CONTEXT.md
@.planning/phases/10-sidebar-visual-polish/10-UI-SPEC.md
@.planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md
@.planning/phases/10-sidebar-visual-polish/10-11-SUMMARY.md
@src/renderer/Sidebar.tsx
@src/renderer/SessionManager.tsx
@src/renderer/IdleCard.tsx
@src/renderer/start-affordances.ts
@src/renderer/__tests__/start-affordances.test.ts
@src/renderer/sidebar.css
@tests/ui-lab/surfaces.ts
</context>

<artifacts_produced>
## Artifacts this phase produces (for source-grounding drift verification)

This plan modifies four EXISTING files and creates ONE new spike doc directory. It introduces NO new exported code symbols (no new components, hooks, props, classes, or custom properties):

- **NEW directory + file:** `.planning/spikes/004-dormant-start-dup/README.md` — the GAP-10-H diagnosis trail (captured repro + per-state evidence). Documentation only; not a shipped code path. (A throwaway `.cjs` repro driver MAY be added under this dir if a real-app capture is needed — same class as the existing `.planning/spikes/*.cjs` scripts whose lint errors are tracked in `deferred-items.md`; do NOT add it to the shipped build.)
- **Modified `src/renderer/Sidebar.tsx`:** the existing `startAffordances`-gated render in `SortableSidebarRow` is corrected so the gate is honored on the diagnosed state path. NO new prop, component, or symbol — the fix reuses the existing `startCtl.sidebarStart` / `startCtl.startNoCmd` flags and the existing row-local `activeIsCard` predicate. If the diagnosis shows the row-local `activeIsCard` is computed differently than `SessionManager.activeIsCard`, the row-level predicate is aligned to the authoritative one (still no new symbol).
- **Modified `src/renderer/sidebar.css`:** the `[data-agent='waiting']` `border-left` edge-bar rule (expanded ~line 107-110 + collapsed mirror ~line 517-519) is removed; the amber `color-mix` WASH (~line 99-101, expanded; the collapsed mirror keeps the amber STATUS DOT, not a bar) stays; the WR-04 compound precedence selector `.sidebar-row.active[data-agent='waiting']` is removed in BOTH the expanded and collapsed blocks. No new selector or token is added. If GAP-10-H proves CSS-resurrected, a scoping fix to an EXISTING `.row-control-start` rule may be applied (no new selector class).
- **Modified `src/renderer/__tests__/start-affordances.test.ts`:** new assertions extend the EXISTING truth-table coverage (reusing the exported `startAffordances` / `StartAffordanceInput` symbols) for the diagnosed state(s). No new production symbol.
- **Modified `tests/ui-lab/surfaces.ts`:** the `sidebar-waiting` surface `expects` string updates to wash-only; if a dormant-selected duplicate-Start assertion is warranted, it reuses the existing driving utilities (`addSession`, `clickSidebarRow`, `hasTestId`, `browser.execute`) — no new exported helper unless a small `assertSingleStartAffordance(id)` local module function is added in the same style as `assertNameNotCrushed` (a local function, not a cross-file export).

REUSED existing symbols (NOT new): `startAffordances`, `StartAffordanceInput`, `StartAffordances`, `activeIsCard` (both the Sidebar row-local const and `SessionManager.activeIsCard`), `startCtl`, `rowAgentAttr`, `presentation`, `handleStart`, `handleStartNoCmd`, `assertNameNotCrushed`, `assertLongNameDegradesGracefully`, the `sidebar-waiting` surface, the `--accent-waiting` token.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce + root-cause GAP-10-H — enumerate dormant-row states and capture which two Start affordances co-render (DIAGNOSE-FIRST, no fix)</name>
  <files>.planning/spikes/004-dormant-start-dup/README.md</files>
  <read_first>
    - src/renderer/start-affordances.ts (the pure dedup reducer — the R3 contract: idleCardStart = dormant && isActive && activeIsCard; sidebarStart = dormant && !idleCardStart; startNoCmd = dormant && !idleCardStart && hasCommand; the active dormant card row must collapse to totalStartCount=1)
    - src/renderer/Sidebar.tsx (lines ~208-223: the row-local `dormant`, `activeIsCard`, and `startCtl = startAffordances({...})` computation; lines ~307-326: the sidebar ▶ `.row-control-start` render gated on `startCtl.sidebarStart`; lines ~343-364: the ⏵ `.row-control start-no-cmd` gated on `startCtl.startNoCmd`; the `isActive` prop source from `renderRow`/`s.logicalId === activeId`)
    - src/renderer/SessionManager.tsx (lines ~615-619: the GLOBAL `activeIsCard = activeRecord?.status === 'not_started' || activeRecord?.status === 'error'`; lines ~274-318: handleStart/handleStartNoCmd optimistic 'running' flip — the status-flip timing that could momentarily leave a row dormant-in-DOM while the IdleCard mounts/unmounts; line ~685: the `activeIsCard && activeRecord !== null` IdleCard render gate)
    - src/renderer/IdleCard.tsx (the IdleCard "▶ Start session" `.idle-start-button` data-testid="idle-start-session" — the SOLE Start for the active dormant row; the error branch shows Edit/Retry, not a Start)
    - src/renderer/sidebar.css (lines ~216-218 + ~264-272: the `[data-dormant] .row-control-start` ALWAYS-VISIBLE reserved-width exemption — verify it cannot resurrect a React-unrendered button; lines ~237-262: the at-rest zero-collapse + hover/focus reveal that 10-11 changed for the active row — check the interaction)
    - .planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md (the GAP-10-H entry: the 4 candidate hypotheses (a) activeIsCard predicate misfire (b) ⏵ co-renders where gated (c) stale ▶ after start (status-flip race) (d) 10-11 zero-collapse interaction)
    - .planning/phases/10-sidebar-visual-polish/10-07-PLAN.md (the proven diagnose-first shape: capture the real state, walk the chain link-by-link against evidence, name the ONE broken link before fixing)
  </read_first>
  <action>
    Create the diagnosis spike dir `.planning/spikes/004-dormant-start-dup/` with a `README.md`. Do NOT fix anything in this task — the deliverable is the reproduced state + an evidence-backed root cause.

    Enumerate the dormant-row states and, for EACH, record which Start-labeled affordances render (sidebar ▶ `start-session`, sidebar ⏵ `start-no-cmd-session`, IdleCard ▶ `idle-start-session`) by tracing `startAffordances(input)` for the row's exact `{status, isActive, activeIsCard, startupCommand}` AND, where the live DOM can be inspected, by reading the actual rendered `data-testid`s. The state matrix to cover: (1) dormant row at REST, non-active; (2) dormant row SELECTED/active (its IdleCard shows in the terminal area) — with and without a saved startupCommand; (3) dormant row on HOVER; (4) a row IMMEDIATELY AFTER the user clicks Start (the optimistic `setSessions(... status:'running')` flip vs. the IdleCard unmount timing — does a stale dormant render leave a ▶ while the card is still visible?); (5) the active row at REST after 10-11's zero-collapse (does the `[data-dormant] .row-control-start` always-visible CSS exemption combine with the 10-11 change to surface a control that React intended to suppress?). Cover both EXPANDED and COLLAPSED rail where relevant.

    Reproduce against the running packaged app where a state cannot be settled from the pure reducer alone (states 2/4/5 especially — they depend on `isActive`, `activeIsCard`, and render timing). If a deterministic DOM capture is needed, drive the app (a small throwaway `.cjs` under the spike dir is acceptable — same class as `.planning/spikes/003-...`; it is NOT a shipped path) or inspect via the existing ui-lab `browser.execute` vocabulary, and record per-state the set of rendered Start `data-testid`s. Identify the SINGLE state path on which TWO Start-labeled affordances co-render, with the captured rendered-testid set (or the traced `startAffordances` input + output) that proves it.

    Write the findings to the README: the per-state matrix, the ONE failing state path, which two affordances co-render there, and which of the 4 candidate hypotheses (or a 5th) is the confirmed root cause — with the line(s)/capture that prove it (e.g. "on state (2)-with-command, the row-local `activeIsCard` reads X while `SessionManager.activeIsCard` reads Y, so `startCtl.sidebarStart` was true while the IdleCard ▶ also rendered" OR "on state (4), the optimistic running flip and the IdleCard unmount race so for one frame both the sidebar ▶ and the IdleCard ▶ paint"). Classify the fix target: pure-predicate input mismatch (Sidebar passes a wrong `activeIsCard`), render-path gate, or status-flip-timing.
  </action>
  <verify>
    <automated>test -f .planning/spikes/004-dormant-start-dup/README.md && test -s .planning/spikes/004-dormant-start-dup/README.md && grep -qi "GAP-10-H" .planning/spikes/004-dormant-start-dup/README.md && grep -qiE "start-session|idle-start-session|start-no-cmd-session" .planning/spikes/004-dormant-start-dup/README.md && echo DIAGNOSED</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/spikes/004-dormant-start-dup/README.md` exists, is non-empty, and contains the string `GAP-10-H`.
    - The README documents a per-state matrix covering at least: non-active dormant at rest, ACTIVE/selected dormant (with and without startupCommand), hover, and just-after-Start — naming the rendered Start `data-testid`s for each state (`start-session` / `start-no-cmd-session` / `idle-start-session`).
    - The README names the SINGLE failing state path, identifies WHICH TWO Start affordances co-render there, and states the confirmed root cause mapped to one of the 4 candidate hypotheses (or a documented 5th), with the captured/traced evidence that proves it.
    - No source file under `src/` is modified in this task (diagnosis only) — `git status --porcelain src/` shows no change attributable to Task 1.
  </acceptance_criteria>
  <done>The duplicate-Start state is reproduced and the single broken state path is root-caused with captured evidence, written to the spike README — no fix applied yet.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the diagnosed GAP-10-H link + lock it with a deterministic regression check (truth-table unit + optional ui-lab dormant-selected assertion)</name>
  <files>src/renderer/Sidebar.tsx, src/renderer/sidebar.css, src/renderer/__tests__/start-affordances.test.ts, tests/ui-lab/surfaces.ts</files>
  <read_first>
    - .planning/spikes/004-dormant-start-dup/README.md (the Task-1 diagnosis — the fix target + the failing state path)
    - src/renderer/Sidebar.tsx (the `activeIsCard` row-local const at ~line 216-217 and the `startCtl` computation at ~218-223; the three Start render gates at ~311, ~349)
    - src/renderer/SessionManager.tsx (the authoritative `activeIsCard` definition at ~615-616 — if the row must mirror it, align to THIS exact predicate `status === 'not_started' || status === 'error'`, scoped by `isActive`)
    - src/renderer/start-affordances.ts (the contract being honored — if the defect is in the INPUT to startAffordances, fix the input not the reducer; the reducer's R3 logic is already correct per its unit test)
    - src/renderer/__tests__/start-affordances.test.ts (the existing truth-table — extend it; the existing cases assert primaryStartCount/totalStartCount across active/non-active/running/recipe states)
    - tests/ui-lab/surfaces.ts (the existing driving utilities + the `sidebar-populated` / `idle-card` surfaces — if a dormant-selected assertion is warranted, add it in the style of `assertNameNotCrushed` and wire it into an existing surface's prepare(), gated on observable DOM state, NOT a bare pause)
    - .planning/phases/10-sidebar-visual-polish/10-11-SUMMARY.md (confirm the 10-11 active-row zero-collapse + WR-04 + dormant exemption invariants this fix must not regress)
  </read_first>
  <action>
    Apply the SMALLEST change at the diagnosed broken link from Task 1, then lock it with a deterministic regression check. Do not refactor beyond the fix.

    If the root cause is a pure-predicate INPUT mismatch (the Sidebar row passes a wrong `activeIsCard` or `isActive` into `startAffordances`): correct the input in `Sidebar.tsx` so the row's `activeIsCard` mirrors `SessionManager.activeIsCard` exactly — `isActive && (s.status === 'not_started' || s.status === 'error')` — so the active dormant card row suppresses the sidebar ▶ and the ⏵ (the IdleCard ▶ is the sole Start). Do NOT alter the `startAffordances` reducer (its R3 logic is correct and unit-proven).

    If the root cause is a render-path gate (a Start renders outside the `startCtl.*` gate, e.g. an ungated `▶`/`⏵`): route that render through the existing `startCtl.sidebarStart` / `startCtl.startNoCmd` flag so exactly one Start-labeled control shows per dormant row state.

    If the root cause is a status-flip TIMING race (the optimistic `status:'running'` flip and the IdleCard mount/unmount momentarily co-render two Start surfaces): make the render deterministic so no frame paints two Starts — e.g. gate the duplicate surface on the same authoritative condition the IdleCard uses, so the sidebar ▶ cannot paint while the IdleCard ▶ is mounted for the same id.

    If the root cause is the CSS `[data-dormant] .row-control-start` always-visible exemption resurrecting a React-unrendered control after the 10-11 zero-collapse: scope that EXISTING `sidebar.css` exemption so it only applies to a rendered (`startCtl.sidebarStart`) Start — do NOT add a new selector class; tighten the existing rule. Preserve the D-13 standing-invitation spacing for the legitimately-rendered non-active dormant ▶.

    Then add the regression check (deterministic, no relaxation):
    - UNIT (always): extend `start-affordances.test.ts` with a truth-table assertion covering the diagnosed state(s) — assert `totalStartCount === 1` for the active dormant card row WITH and WITHOUT a saved startupCommand, and the existing non-active recipe case stays at `totalStartCount === 2`. If the just-started transition is representable as a reducer input, add it; otherwise cover it in the ui-lab assertion below. Reuse the exported `startAffordances` / `StartAffordanceInput`.
    - UI-LAB (only if the defect was a render-path/state-flip NOT captured by the pure predicate): add a local `assertSingleStartAffordance(id)` in `tests/ui-lab/surfaces.ts` (same style as `assertNameNotCrushed` — a local module function that throws a descriptive `Error`, NOT `SkipSurface`) that counts the rendered Start `data-testid`s (`start-session` + `start-no-cmd-session` + `idle-start-session`) within the row + the active terminal area and throws if > 1 for the selected dormant row; wire it into the `idle-card` surface prepare() (which already selects a dormant row and waits for `idle-card`), gated on the observable `idle-card` testid. Do NOT touch `assertNameNotCrushed` or `assertLongNameDegradesGracefully`.

    DO NOT change any `data-testid` or class name (the smoke + ui-lab selector contract is frozen). DO NOT touch the 20-key bridge surface. Use only `var(--token)` values for any CSS (tokens-completeness bans migrated literals).
  </action>
  <verify>
    <automated>npx vitest run src/renderer/__tests__/start-affordances.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/renderer/__tests__/start-affordances.test.ts` is GREEN with the NEW truth-table case(s): the active dormant card row (with AND without a saved startupCommand) asserts `totalStartCount === 1`; the existing non-active recipe row stays `totalStartCount === 2`.
    - The fix matches the Task-1 diagnosis: `git diff` shows a change ONLY at the diagnosed link (Sidebar input alignment, a render-path gate, a status-flip guard, or a scoped CSS exemption) — not a reducer rewrite and not a `data-testid`/class rename.
    - If a ui-lab assertion was added: `tests/ui-lab/surfaces.ts` contains a local `assertSingleStartAffordance`-style function wired into an existing dormant-selecting surface, gated on observable DOM state (no bare pause); `assertNameNotCrushed` and `assertLongNameDegradesGracefully` are byte-for-byte unchanged.
    - `npx tsc --noEmit` exits 0; `npx vitest run src/renderer/__tests__/tokens-completeness.test.ts` is GREEN (no banned literal reintroduced) if `sidebar.css` was touched.
    - EXPECTED_API_KEYS unchanged (no bridge edit) — `git diff src/main/window-config.ts` is empty.
  </acceptance_criteria>
  <done>The diagnosed GAP-10-H link is fixed with the minimal change; the active dormant row renders exactly one Start in the regression-pinned states; the truth-table unit (and a ui-lab dormant-selected assertion if the defect was a render-path one) green; no selector/bridge regression.</done>
</task>

<task type="auto">
  <name>Task 3: GAP-10-I — amend D-09 to wash-only (drop the amber edge bar; remove the obsolete WR-04 selector; preserve the active edge bar) + update the waiting tests/captures</name>
  <files>src/renderer/sidebar.css, tests/ui-lab/surfaces.ts</files>
  <read_first>
    - src/renderer/sidebar.css (lines ~96-110: the `[data-agent='waiting']` amber color-mix WASH + the `border-left` edge-bar rule + the WR-04 compound precedence selector `.sidebar-row.active[data-agent='waiting'], .sidebar-row[data-agent='waiting']`; lines ~511-520: the collapsed-rail `.sidebar.collapsed .sidebar-row.active` edge bar + the collapsed WR-04 mirror `.sidebar.collapsed .sidebar-row.active[data-agent='waiting'], .sidebar.collapsed .sidebar-row[data-agent='waiting']`; lines ~525-536: the collapsed-rail status dot that becomes the collapsed waiting indication; lines ~89-94: the ACTIVE row's own D-05/D-06 status-colored edge bar that stays)
    - .planning/phases/10-sidebar-visual-polish/10-CONTEXT.md (§"Gap-closure addendum 2": D-09 AMENDED — wash only, NO edge bar, expanded + collapsed mirror; collapsed waiting = the amber status dot; WR-04 becomes obsolete; D-05/D-06 NOT amended; "static, no pulsing/animation" lock stays until the future animation system)
    - .planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md (the GAP-10-I entry: drop the border-left rules (expanded + collapsed mirror), keep the wash, REMOVE the WR-04 compound selector rather than leaving dead rules, update the sidebar-waiting surface expectation + any waiting border-left assertion)
    - tests/ui-lab/surfaces.ts (the `sidebar-waiting` surface `expects` string — currently "amber left edge bar + a light amber tint wash"; the deterministic data-agent='waiting' poke + WR-05 waitUntil + WR-04 cleanup that stay)
    - src/renderer/__tests__/tokens-completeness.test.ts (scans sidebar.css for var() completeness + bans migrated literals — must stay green; do not introduce a raw color)
  </read_first>
  <action>
    Implement the D-09 amendment: waiting = amber tint WASH ONLY, NO amber left edge bar. Concrete edits in `src/renderer/sidebar.css`:

    1. KEEP the expanded amber wash rule `.sidebar-row[data-agent='waiting'] { background: color-mix(in oklch, var(--accent-waiting) 8%, transparent); }` (~line 99-101) unchanged.
    2. REMOVE the expanded amber edge-bar rule and its WR-04 compound precedence selector — delete the entire block `.sidebar-row.active[data-agent='waiting'], .sidebar-row[data-agent='waiting'] { border-left: 3px solid var(--accent-waiting); }` (~lines 107-110). With the bar gone there is no precedence to arbitrate, so the WR-04 compound selector is removed (not left as a dead rule) per GAP-10-I. Update the explanatory comment above the wash rule to state waiting is now wash-only (no edge bar) per the 2026-06-12 D-09 amendment, and that the WR-04 precedence selector was removed as obsolete.
    3. REMOVE the collapsed-rail amber edge-bar mirror — delete the block `.sidebar.collapsed .sidebar-row.active[data-agent='waiting'], .sidebar.collapsed .sidebar-row[data-agent='waiting'] { border-left: 3px solid var(--accent-waiting); }` (~lines 517-519). The collapsed waiting indication falls back to the amber status dot (the existing `.sidebar.collapsed .collapsed-status-dot { background: var(--accent); }` already carries the row's `--accent`, which is amber while waiting — no new rule needed). Update its comment to note the collapsed waiting bar was removed (D-09 amendment) and the amber status dot is the collapsed indication.
    4. DO NOT touch the ACTIVE row's own status-colored edge bar: `.sidebar-row.active { ... border-left: 3px solid var(--accent); }` (~lines 89-94) and the collapsed `.sidebar.collapsed .sidebar-row.active { border-left: 3px solid var(--accent); }` (~lines 511-513) STAY — D-05/D-06 are unamended, so an active waiting row's active bar still reads amber.

    Then update the harness/assertions to the amended spec (sanctioned spec change, NOT check relaxation):
    - In `tests/ui-lab/surfaces.ts`, change the `sidebar-waiting` surface `expects` string from "amber left edge bar + a light amber tint wash" to the amended "a light amber tint WASH ONLY (no edge bar) — static, no pulse" wording. Keep the deterministic data-agent='waiting' poke, the WR-05 `waitUntil`, and the WR-04 `cleanup` exactly as they are (they prove the wash CSS fires; they are not edge-bar assertions). DO NOT touch `assertNameNotCrushed` / `assertLongNameDegradesGracefully`.
    - If any unit/CSS assertion elsewhere asserts the waiting `border-left` (grep the repo for `border-left` near `waiting` and for `accent-waiting` in test files), update it to assert the wash is present and the edge bar is ABSENT, per the amended spec.

    Use only `var(--token)` values; reintroduce no raw color literal (tokens-completeness bans them). Change no `data-testid` or class name.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/renderer/sidebar.css','utf8'); const body=c.replace(/\/\*[\s\S]*?\*\//g,''); if(/\[data-agent='waiting'\][^{]*\{[^}]*border-left/.test(body)) {console.error('FAIL: a waiting border-left edge-bar rule still present'); process.exit(1);} if(/\.sidebar-row\.active\[data-agent='waiting'\]/.test(body)) {console.error('FAIL: obsolete WR-04 compound selector still present'); process.exit(1);} if(!/\[data-agent='waiting'\][^{]*\{[^}]*color-mix\(in oklch, var\(--accent-waiting\)/.test(body)) {console.error('FAIL: amber wash rule missing'); process.exit(1);} if(!/border-left: 3px solid var\(--accent\)/.test(body)) {console.error('FAIL: active row status-colored edge bar (D-05/D-06) was removed — must stay'); process.exit(1);} console.log('OK: waiting wash-only; WR-04 removed; active edge bar preserved');"</automated>
  </verify>
  <acceptance_criteria>
    - In `src/renderer/sidebar.css` (comment bodies ignored): NO rule selecting `[data-agent='waiting']` sets `border-left` (the verify node command checks this) — neither expanded nor the collapsed mirror.
    - The obsolete WR-04 compound selector `.sidebar-row.active[data-agent='waiting']` is ABSENT from the file (expanded and collapsed blocks both removed).
    - The expanded amber WASH rule `.sidebar-row[data-agent='waiting'] { background: color-mix(in oklch, var(--accent-waiting) 8%, transparent); }` is PRESENT and unchanged.
    - The ACTIVE row's status-colored edge bar `.sidebar-row.active { ... border-left: 3px solid var(--accent); }` (expanded) and the collapsed `.sidebar.collapsed .sidebar-row.active { border-left: 3px solid var(--accent); }` are PRESENT and unchanged (D-05/D-06 not amended).
    - The collapsed `.sidebar.collapsed .collapsed-status-dot { background: var(--accent); ... }` is PRESENT (the amber status dot is the collapsed waiting indication).
    - `tests/ui-lab/surfaces.ts` `sidebar-waiting` `expects` string says wash-only / no edge bar; the data-agent poke + WR-05 waitUntil + WR-04 cleanup are intact; `assertNameNotCrushed` and `assertLongNameDegradesGracefully` are byte-for-byte unchanged.
    - `npx vitest run src/renderer/__tests__/tokens-completeness.test.ts` is GREEN (no banned literal); `npx tsc --noEmit` exits 0; the Task-3 verify node command prints `OK: waiting wash-only; WR-04 removed; active edge bar preserved`.
  </acceptance_criteria>
  <done>Waiting renders as the amber wash only (no edge bar) in expanded + collapsed; the obsolete WR-04 selector is removed; the active row's status-colored edge bar (D-05/D-06) is preserved; the sidebar-waiting harness expectation + any waiting border-left assertion match the amended spec; tokens-completeness + tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Renderer-only change: a Start-render gate alignment + a CSS rule deletion. No new input parsing, IPC, PTY, persistence, network, or package install. The same already-vetted DOM nodes render with corrected gating + a different at-rest waiting style. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12-01 | Tampering | the frozen selector / data-testid contract (Sidebar.tsx + sidebar.css → smoke + ui-lab drivers) | mitigate | The fix routes existing renders through existing `startCtl.*` flags and deletes CSS rules; it touches no `data-testid` or class name. The smoke suite (15/15) + the ui-lab run are re-run in the 10-13 gate plan as the regression guard — a broken selector contract fails them. |
| T-10-12-02 | Repudiation | the GAP-10-H "fixed" claim without a captured repro | mitigate | Task 1 is diagnose-FIRST: the repro + per-state evidence + the named broken link land in the spike README BEFORE the fix, and Task 2's regression check pins the exact state so it cannot silently regress (the same discipline that closed GAP-10-D in 10-07). |
| T-10-12-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan (renderer edits + tests only). No supply-chain surface. EXPECTED_API_KEYS stays 20 — no bridge change. |
</threat_model>

<verification>
- Task 1: the duplicate-Start state is reproduced and the single broken state path is root-caused with captured evidence in `.planning/spikes/004-dormant-start-dup/README.md` (no source touched).
- Task 2: the minimal fix at the diagnosed link makes the active dormant row render exactly one Start; the `start-affordances.test.ts` truth-table is GREEN with the new case(s) (and a ui-lab dormant-selected assertion if the defect was a render-path one); tsc 0; tokens-completeness green; bridge unchanged.
- Task 3: waiting renders wash-only (no edge bar, expanded + collapsed); the obsolete WR-04 selector is removed; the active edge bar (D-05/D-06) is preserved; the sidebar-waiting harness expectation + any waiting border-left assertion match the amended spec; tokens-completeness + tsc green.
- The full regression suite (unit/tsc/lint/smoke) + the packaged `ui:shots:fresh` are run in the 10-13 gate plan (not duplicated here) — this plan's per-task gates are the unit/predicate/CSS proofs.
</verification>

<success_criteria>
- GAP-10-H closed: an inactive/dormant row presents exactly ONE Start affordance in every enumerated state (rest / selected-active / hover / just-started; expanded + collapsed), pinned by a deterministic regression check; the root cause is documented with captured evidence.
- GAP-10-I implemented: waiting = amber wash only (no edge bar) on expanded rows AND the collapsed-rail mirror; the obsolete WR-04 compound precedence selector is removed; the active row's status-colored edge bar (D-05/D-06) is unchanged; the sidebar-waiting capture expectation + any waiting border-left assertion are updated to the amended spec without relaxing `assertNameNotCrushed`.
- Renderer-only, value-respecting: no `data-testid`/class rename, no bridge change (EXPECTED_API_KEYS stays 20), no migrated literal (tokens-completeness green), tsc 0.
- The two fixes are isolated to the diagnosed link + the sanctioned spec change — no scope creep into the FULLY CLOSED GAP-10-D/E/F/G (which must NOT be touched).
</success_criteria>

<output>
Create `.planning/phases/10-sidebar-visual-polish/10-12-SUMMARY.md` when done. Record: the GAP-10-H per-state matrix + the single diagnosed broken state path with its evidence; the exact minimal fix applied (the diagnosed-link diff) + the new truth-table case(s) and any ui-lab dormant-selected assertion; the GAP-10-I CSS diff (removed waiting border-left rules + the removed WR-04 selector, with the wash + active edge bar confirmed preserved) + the updated sidebar-waiting expectation; and confirmation that tsc, tokens-completeness, and the start-affordances unit are green, the bridge is unchanged, and GAP-10-D/E/F/G were not touched. Note that the full suite + packaged ui:shots:fresh run in the 10-13 gate plan.
</output>
