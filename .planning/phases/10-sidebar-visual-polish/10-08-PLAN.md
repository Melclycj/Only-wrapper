---
phase: 10-sidebar-visual-polish
plan: 08
type: execute
wave: 6
depends_on: []
files_modified:
  - src/renderer/sidebar.css
autonomous: true
requirements: [UI-02]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "The leading gutter (drag-handle dots + icon tile) is visibly more compact so the session name gets more horizontal width (GAP-10-E)"
    - "An unrevealed hover control reserves ZERO layout width at rest — the border-width:0 zero-collapse actually wins the cascade (WR-01 closed: no residual ~2px per hidden control)"
    - "A dormant recipe row reserves no dead trailing space between the always-visible ▶ and the zero-collapsed hidden controls (WR-02 closed: the flex gap no longer applies between hidden items)"
  artifacts:
    - path: "src/renderer/sidebar.css"
      provides: "Compacted leading gutter + true zero-width-at-rest hover controls + dormant-row gap fix"
      contains: "row-drag-handle"
  key_links:
    - from: "src/renderer/sidebar.css .sidebar-row .row-control rest block"
      to: "the cascade winner over the compact-button border shorthand"
      via: "rest-state declaration placed AFTER the compact-button block (or box-sizing:border-box)"
      pattern: "border-width:\\s*0"
---

<objective>
Close GAP-10-E (operator design adjustment) plus the two related code-review WARNINGs WR-01 and WR-02 — one cohesive CSS-sizing concern in `sidebar.css`.

- **GAP-10-E:** "the space in the front is too wide. compact a bit to leave more sapace." The leading gutter (drag-handle ⠿ + the 34px icon tile + the 12px row gap) steals too much of the 220px rail from the session name. Compact it so the name gets more width. ITEM 1 was otherwise APPROVED — this is a sizing tweak, not a redesign.
- **WR-01:** `border-width: 0` at the `.row-control` rest block (sidebar.css line ~206) is dead code — clobbered by the later equal-specificity `border: 1px solid transparent` shorthand at line ~242, so each hidden control still renders ~2px. The "an unrevealed control consumes no layout width" comment is currently false.
- **WR-02:** Dormant rows restore `gap: var(--space-1)` on `.row-controls`, but flex gap is inserted between ALL flex items including the zero-collapsed hidden ones — ~12-18px of dead trailing space on every Inactive-List recipe card (the bucket with the longest secondary text). This directly serves GAP-10-E's goal.

Purpose: Maximize the horizontal width available to the session name (the name-legibility SC2 payoff), and make the zero-width-at-rest invariant actually true so future name-crush can't creep back in via residual control width.

Output: A compacted, correct leading gutter and trailing control cluster in `sidebar.css` — value-preserving everywhere else.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sidebar-visual-polish/10-REVIEW.md
@.planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md
@.planning/phases/10-sidebar-visual-polish/10-UI-SPEC.md
@.planning/phases/10-sidebar-visual-polish/10-CONTEXT.md
@.planning/phases/10-sidebar-visual-polish/10-PATTERNS.md
</context>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<artifacts_produced>
## Artifacts this phase produces (for source-grounding drift verification)

No NEW exported symbols or files — this plan edits existing CSS selectors in `src/renderer/sidebar.css` only. The selectors it touches already exist: `.sidebar-row` (the row gap), `.sidebar-row .row-drag-handle`, `.sidebar-row .row-icon`, `.sidebar-row .row-control` (rest block + compact-button block), `.sidebar-row[data-dormant] .row-controls`, `.sidebar-row .row-controls`. No tokens added (all values must resolve through existing `tokens.css` var()s — `tokens-completeness.test.ts` guards this).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Compact the leading gutter (GAP-10-E) — drag handle + icon tile + row gap reclaim name width</name>
  <files>src/renderer/sidebar.css</files>
  <read_first>
    - src/renderer/sidebar.css (the full file — specifically: `.sidebar-row` gap `var(--space-3)` line ~67; `.sidebar-row .row-icon` width/height 34px lines ~111-120; `.sidebar-row .row-drag-handle` width 12px / margin-left -2px lines ~307-321; `.sidebar-row .row-text` flex column lines ~124-130 — the name column that should reclaim the freed width)
    - .planning/phases/10-sidebar-visual-polish/10-UI-SPEC.md (Spacing Scale: --space-1=4px, --space-2=8px, --space-3=12px; the icon tile is a sized ELEMENT not a spacing step — UI-SPEC permits 32-36px; the 34px is at the upper end and may compact toward 30-32px; values must stay on the --space-* scale where they are gaps)
    - .planning/phases/10-sidebar-visual-polish/10-06-SUMMARY.md (screenshot-1 context: the drag-handle dots + large monitor icon tile occupy a wide leading gutter; the name renders "Ses…" — the exact symptom to fix)
    - .planning/phases/10-sidebar-visual-polish/10-CONTEXT.md (D-04 icon tile "~32-36px"; Claude's Discretion: row gaps/paddings retune onto --space-*; D-13 the dormant ▶ keeps reserved width)
  </read_first>
  <action>
    Reduce the leading gutter so the `.row-text` name column gains width on the 220px rail. Concretely, in `src/renderer/sidebar.css`:
    - Reduce the `.sidebar-row` flex `gap` from `var(--space-3)` (12px) to `var(--space-2)` (8px) so the icon↔text and handle↔icon gaps tighten. (Per UI-SPEC the row horizontal padding stays `var(--space-3)`; only the inter-item gap compacts — confirm against the ui-lab look after the change.)
    - Reduce the `.sidebar-row .row-icon` tile from `34px` to a smaller on-spec dimension within D-04's 32-36px band — use `var(--space-8)` (32px) for width AND height so the icon is a token-sourced element dimension rather than the literal `34px`, keeping the rounded `var(--radius-md)`. Adjust the emoji `font-size` down proportionally only if the ui-lab capture shows clipping (keep ~16-18px).
    - Tighten the `.row-drag-handle` footprint: keep the ⠿ glyph but reduce its `width` from `12px` to `var(--space-2)` (8px) and drop the `margin-left: -2px` negative nudge if it no longer reads aligned — the goal is the handle telegraphs draggability without eating width. Keep its hover-reveal (opacity 0 → 1) behavior unchanged.
    Do NOT change `.row-text { flex: 1 1 auto; min-width: 0 }` — it already claims the freed middle space; the point is to free MORE of it. Keep every selector name and the `.sidebar-row[data-dormant] .row-control-start` reserved-width exemption (D-13) intact. Value-preserve the collapsed-rail rules (the icon-only rail width 52px is a layout dimension, not touched).
  </action>
  <verify>
    <automated>npx vitest run src/renderer/__tests__/tokens-completeness.test.ts && grep -q "row-drag-handle" src/renderer/sidebar.css && grep -Eq "row-icon" src/renderer/sidebar.css</automated>
  </verify>
  <acceptance_criteria>
    - The `.sidebar-row` inter-item `gap` is reduced from `var(--space-3)` to `var(--space-2)` (grep: `gap: var(--space-2)` present on `.sidebar-row`).
    - The `.row-icon` tile dimension is reduced to an on-spec token value (`var(--space-8)` = 32px) for both width and height — no longer the literal `34px` (grep: `.sidebar-row .row-icon` no longer contains `34px`).
    - The `.row-drag-handle` width is reduced (no longer `12px`) and still has its hover-reveal opacity transition.
    - `tokens-completeness.test.ts` stays green (every value resolves through tokens.css var(); no banned literal reintroduced).
    - All selector names and the D-13 dormant ▶ reserved-width exemption are intact (grep: `.sidebar-row[data-dormant] .row-control-start` still present with `max-width: 24px`).
  </acceptance_criteria>
  <done>The leading gutter is compacted via on-spec token values, freeing horizontal width for the session name; tokens-completeness stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Make zero-width-at-rest real (WR-01) + kill dormant-row trailing gap (WR-02)</name>
  <files>src/renderer/sidebar.css</files>
  <read_first>
    - src/renderer/sidebar.css (the two `.sidebar-row .row-control` blocks: the GAP-10-A rest block at lines ~201-208 with `border-width: 0`, and the compact-button block at lines ~234-249 with `border: 1px solid transparent` — the LATER one wins the cascade at equal specificity 0,2,0; AND the `.sidebar-row[data-dormant] .row-controls` reveal rule at lines ~181-188 restoring `gap: var(--space-1)`; AND the `.sidebar-row .row-controls` rest block lines ~169-179)
    - .planning/phases/10-sidebar-visual-polish/10-REVIEW.md (WR-01 fix guidance: move the zero-collapse declarations AFTER the compact-button block OR add `box-sizing: border-box` to `.row-control`; WR-02 fix guidance: scope the dormant exemption so only the visible ▶ contributes layout — `gap: 0` on the dormant container + own spacing on `.row-control-start`)
    - .planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md (the Anti-Patterns table rows for sidebar.css 206-vs-242 and 169-188 — the precise line references and impact)
  </read_first>
  <action>
    Fix WR-01: make the `border-width: 0` zero-collapse actually win. Add `box-sizing: border-box` to the `.sidebar-row .row-control` rule so `max-width: 0` clamps the FULL box (border included) — this is the cleanest single-property fix and is order-independent. (Alternatively, if box-sizing has side effects on the 24px revealed buttons, instead MERGE the rest-collapse declarations into a single rule placed AFTER the compact-button block so the rest state is declared last and `border-width: 0` wins source-order — but prefer `box-sizing: border-box` as it is robust to future reordering.) After the fix, an unrevealed `.row-control` must occupy ZERO horizontal layout width (no residual 2px per hidden control). Update the now-accurate comment (the "24px = the .row-control width below" note is wrong — with content-box it was 26px; with border-box the revealed width IS 24px including border, so the comment becomes correct — verify and reword to state the box-sizing model explicitly).

    Fix WR-02: scope the dormant `.row-controls` gap restore so it does NOT insert space between the zero-collapsed hidden controls. Change `.sidebar-row[data-dormant] .row-controls` to `gap: 0` and give the always-visible ▶ its own leading spacing via `.sidebar-row[data-dormant] .row-control-start { margin-left: var(--space-1); }` (or equivalent). On hover/focus-within of a dormant row, restore `gap: var(--space-1)` so the revealed ✎/🗑 cluster spaces normally (mirror the existing `:hover .row-controls` / `:focus-within .row-controls` restore selectors). Net effect: a dormant recipe card at rest reserves only the ▶'s width + its own margin — no dead trailing gaps stacking up behind the startup-command secondary line.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/__tests__/tokens-completeness.test.ts && grep -Eq "box-sizing:\s*border-box" src/renderer/sidebar.css && grep -Eq "data-dormant\] \.row-controls" src/renderer/sidebar.css</automated>
  </verify>
  <acceptance_criteria>
    - `.sidebar-row .row-control` carries `box-sizing: border-box` (grep-confirmed), so `max-width: 0` clamps the bordered box and a hidden control reserves zero width — OR the rest-collapse block is the LAST `.sidebar-row .row-control` declaration in source order (whichever approach is taken, the zero-width-at-rest invariant is real, not clobbered).
    - The inaccurate comment about control width is corrected to state the box model and the true revealed width.
    - `.sidebar-row[data-dormant] .row-controls` is set to `gap: 0` at rest; the always-visible ▶ gets its own `margin-left` so it keeps standing spacing (D-13 preserved); hover/focus on a dormant row restores the inter-button gap.
    - `tokens-completeness.test.ts` stays green; all control `data-testid` selectors and class names are unchanged (no markup contract touched — this is CSS-only).
    - `npx tsc --noEmit` exits 0 (no TS touched, but confirms the build is clean).
  </acceptance_criteria>
  <done>WR-01 and WR-02 are closed: hidden controls reserve zero width and dormant rows reserve no dead trailing gap; the name reclaims the full freed width.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits presentation-only CSS in the renderer. No PTY data, no IPC, no untrusted-input parsing path is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08-01 | Tampering | sidebar layout (CSS-only restyle regressing a selector the smoke/ui-lab harness reads) | mitigate | No class names or data-testid selectors are renamed (CSS value/sizing edits only); `tokens-completeness.test.ts` stays green; the full smoke + ui-lab capture in plan 10-10 re-proves the selector contract and the keep-alive guard. |
| T-10-08-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan (pure CSS edit). No supply-chain surface. |
</threat_model>

<verification>
- tokens-completeness.test.ts green (no banned literal; every value resolves through tokens.css).
- tsc clean.
- The visual proof (compacted gutter, name no longer crushed, zero residual control width) is captured + scored in plan 10-10's packaged ui-lab run and confirmed at the human gate.
</verification>

<success_criteria>
- The leading gutter is visibly more compact (operator's GAP-10-E request satisfied).
- An unrevealed hover control reserves zero layout width (WR-01 closed).
- Dormant recipe rows reserve no dead trailing space (WR-02 closed).
- No selector contract broken; tokens-completeness + tsc clean; value-preserving elsewhere.
</success_criteria>

<output>
Create `.planning/phases/10-sidebar-visual-polish/10-08-SUMMARY.md` when done. Record the exact CSS deltas (gap, icon tile, drag handle, box-sizing, dormant gap), confirm tokens-completeness + tsc green, and note that the visual confirmation is owned by the 10-10 packaged capture + human gate.
</output>
