---
phase: 10-sidebar-visual-polish
plan: 15
type: execute
wave: 12
depends_on: [10-14]
files_modified:
  - .planning/phases/10-sidebar-visual-polish/10-VALIDATION.md
  - .planning/phases/10-sidebar-visual-polish/10-REVIEW.md
autonomous: false
requirements: [UI-02]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "A gsd-code-review delta pass covers the round-5 source deltas (the 10-14 GAP-10-J sampler + SessionView delegation, the GAP-10-K locale helper + spawn-env wiring, the GAP-10-K font-stack change) and any Critical/High finding is fixed BEFORE the human gate"
    - "The full automated suite (unit + tsc + lint + smoke) is GREEN against the PACKAGED app and a fresh packaged no-injection ui-lab capture (`npm run ui:shots:fresh`, a NEW UI_LAB_TAG) completes its surface set with the name-completeness assertion ENFORCED and PASSING and the wash-only sidebar-waiting expectation"
    - "A human re-verifies the running app (attempt 5): (a) scrolling the session history NO LONGER changes the sidebar status (GAP-10-J), (b) Chinese renders in the terminal (GAP-10-K — e.g. `echo 你好世界` shows correct glyphs, not tofu), and (c) a no-regression sweep confirms GAP-10-D/E/F/G/H/I stay closed — then signs off with an EXPLICIT, UNQUALIFIED 'approved'"
    - "nyquist_compliant flips to true in 10-VALIDATION.md ONLY on that unqualified approval; on any qualification it stays false and the new items route to a fresh gap-closure round (round 6; the next fix plan would be 10-16)"
  artifacts:
    - path: ".planning/phases/10-sidebar-visual-polish/10-VALIDATION.md"
      provides: "The human-gate history row for attempt 5 + the nyquist_compliant flag state"
      contains: "nyquist_compliant"
    - path: ".planning/phases/10-sidebar-visual-polish/10-REVIEW.md"
      provides: "The round-5 delta code-review record (GAP-10-J + GAP-10-K source deltas) with severity-tagged findings"
      contains: "10-14"
  key_links:
    - from: "the packaged app"
      to: "the ui-lab capture + the human gate"
      via: "npm run ui:shots:fresh produces the proof capture; the operator drives the live scroll + CJK verification"
      pattern: "ui:shots:fresh"
---

<objective>
The closing gate for Phase 10's gap-closure rounds — attempt 5. Run the round-5 delta code-review (10-14 GAP-10-J + GAP-10-K deltas), re-run the full evidence chain against the packaged app, then put the running app in front of the operator for a BLOCKING human re-verify of the two round-5 fixes plus a standing no-regression sweep.

Round-5 scope (authoritative from 10-VERIFICATION.md "Addendum 3"): gate attempt 4 (2026-06-12) was NOT_APPROVED_QUALIFIED — ITEM H (single Start, GAP-10-H) and ITEM I (waiting wash-only, GAP-10-I) were APPROVED and CLOSED; GAP-10-D/E/F/G remain closed. The qualification opened two NEW items, now fixed by 10-14: GAP-10-J (scrolling the session history flips the sidebar status — fixed by sampling the live tail at baseY) and GAP-10-K (Chinese/CJK does not render — fixed by a UTF-8 spawn locale + a CJK font fallback). Attempt 5 verifies (1) scrolling no longer changes the status, (2) Chinese renders in the terminal, and (3) a standing no-regression sweep that GAP-10-D/E/F/G/H/I stay closed.

The phase gate flips to compliant ONLY on an explicit, unqualified "approved" — no "partial", no "with adjustments". This is the FIFTH human gate (attempts 1-4 all returned NOT APPROVED / QUALIFIED). Automated green is NOT proof for this project (the 06.1/08/10/13 precedent — each prior gate caught a live failure or a new defect the static capture passed; attempt 4 itself passed all machine checks yet the operator found GAP-10-J + GAP-10-K live). The ui-lab harness is the mandatory visual-evidence tool (Phases 10-13). Do NOT relax any machine check; do NOT re-run 10-13 (this is a NEW gate plan).

Purpose: Close UI-02. The two round-5 fixes (scroll-invariant status + CJK rendering) must be confirmed in the running app by a human — the scroll-flip is a live interaction the static capture cannot exercise, and CJK glyph rendering depends on the Finder-launched runtime locale + the installed system fonts, which only the running packaged app exhibits.

Output: A recorded gate verdict + a delta-review record. On unqualified approval: nyquist_compliant true, UI-02 complete, Phase 10 closeable. On any qualification: verbatim capture + route to a new gap-closure round (round 6; next fix plan 10-16).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sidebar-visual-polish/10-VALIDATION.md
@.planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md
@.planning/phases/10-sidebar-visual-polish/10-CONTEXT.md
@.planning/phases/10-sidebar-visual-polish/10-13-SUMMARY.md
@.planning/phases/10-sidebar-visual-polish/10-14-SUMMARY.md
@.planning/phases/10-sidebar-visual-polish/10-04-PLAN.md
@tests/ui-lab/README.md
@tests/ui-lab/DESIGN-RUBRIC.md
</context>

<artifacts_produced>
## Artifacts this phase produces (for source-grounding drift verification)

No NEW code symbols — this plan runs the round-5 delta code-review, runs the suite, produces a packaged ui-lab capture (artifacts under `artifacts/ui-lab/` are gitignored), and records the human-gate verdict + the review findings. It edits no `src/` files (any review-driven Critical/High fix is applied via the normal edit path and re-verified, but the plan's deliverables are the verdict record + the review record). It edits:
- `.planning/phases/10-sidebar-visual-polish/10-VALIDATION.md` — the Human Gate History table (append attempt 5) + the `nyquist_compliant` frontmatter flag.
- `.planning/phases/10-sidebar-visual-polish/10-REVIEW.md` — the round-5 delta code-review record (severity-tagged findings + dispositions for the GAP-10-J + GAP-10-K deltas).
The verdict + review records ARE the deliverables (mirrors 10-13-SUMMARY.md / 10-REVIEW.md format).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: gsd-code-review delta pass over the round-5 source changes (10-14 GAP-10-J + GAP-10-K) — fix any Critical/High BEFORE the gate</name>
  <files>.planning/phases/10-sidebar-visual-polish/10-REVIEW.md</files>
  <read_first>
    - .planning/phases/10-sidebar-visual-polish/10-14-SUMMARY.md (the round-5 deltas: the GAP-10-J sampleAgentFrame helper + SessionView delegation, the GAP-10-K resolvePtyLocale helper + spawn-env wiring, the CJK font-stack change — the commits to enumerate via `git diff`)
    - .planning/phases/10-sidebar-visual-polish/10-REVIEW.md (the prior delta-review format — the rounds 2-4 section; mirror its severity-tagged table)
    - src/renderer/agent-tick.ts, src/renderer/SessionView.tsx (the GAP-10-J sampling-source change — verify sampleAgentFrame is pure/DOM-free, reads baseY not viewportY, and the SessionView delegation did not break the keep-alive mount or the data path)
    - src/main/pty-locale.ts, src/main/pty-manager.ts (the GAP-10-K locale resolver — verify it honors an existing UTF-8 LANG (never clobbers), defaults only when absent, returns {} on win32, and the spawn-env spread order keeps process.env's LANG authoritative; no unconditional console; no bridge change)
    - src/renderer/tokens.css, src/renderer/__tests__/agent-frame-sample.test.ts, src/main/__tests__/pty-locale.test.ts (the font-stack edit + the two new regression tests — verify the tests assert real behavior (scroll invariance; locale truth table), not tautologies)
  </read_first>
  <action>
    Run a delta code-review over the round-5 source changes from 10-14 (use `git log`/`git diff` against the round-4 baseline to enumerate the exact changed lines; the 10-14-SUMMARY names the commits). Scope the review to: `src/renderer/agent-tick.ts`, `src/renderer/SessionView.tsx`, `src/main/pty-locale.ts`, `src/main/pty-manager.ts`, `src/renderer/tokens.css`, and the two new test files.

    Review for: (GAP-10-J) the sampler reads `baseY` not `viewportY`, is pure/DOM-free, and the SessionView delegation left the keep-alive mount + the xterm/PTY data path untouched — and the scroll-invariance test genuinely fails on the old viewportY behavior (not a tautology); (GAP-10-K locale) resolvePtyLocale honors an inherited UTF-8 LANG (the broad-blast-radius mitigation — it must not clobber a user's `zh_CN.UTF-8`), defaults only when absent, returns `{}` on win32, and the spawn-env spread order is correct; no unconditional `console.*` in the shipped main path; (GAP-10-K font) JetBrains Mono stays primary, a CJK family is appended, `monospace` stays last, no font FILE was bundled (supply-chain note), and tokens-completeness is not tripped. Tag each finding by severity (Critical / High / Medium / Low / Info) using the project's code-review ramp.

    Write the findings to `.planning/phases/10-sidebar-visual-polish/10-REVIEW.md` (append a "Round-5 delta review" section) as a severity-tagged table with file:line + a concrete remediation for each. FIX any Critical or High finding in-place via the normal edit path BEFORE proceeding to Task 2 (they must not reach the human gate), then re-run the affected unit/tsc to confirm green. Record Medium/Low/Info as deferred-or-noted with rationale (route genuinely-deferred items to deferred-items.md if out of phase scope). Do NOT touch the CLOSED GAP-10-D/E/F/G/H/I behavior — review only; a regression there is a finding, not a re-design.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-sidebar-visual-polish/10-REVIEW.md && grep -q "10-14" .planning/phases/10-sidebar-visual-polish/10-REVIEW.md && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/10-sidebar-visual-polish/10-REVIEW.md` contains a round-5 delta-review section covering the 10-14 GAP-10-J + GAP-10-K source deltas, with severity-tagged findings (file:line + remediation each) and references plan `10-14`.
    - Every Critical and High finding is FIXED in-place before the gate (the review section records each as resolved with the fix), and `npx tsc --noEmit` exits 0 after any fix.
    - Medium/Low/Info findings are recorded with a disposition (fixed / deferred-with-rationale).
    - No edit to the CLOSED GAP-10-D/E/F/G/H/I behavior except a fix for an identified regression (none expected).
  </acceptance_criteria>
  <done>The round-5 delta code-review is complete; all Critical/High findings are fixed pre-gate; the review record is written; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite GREEN against the packaged app + fresh no-injection ui-lab capture (NEW UI_LAB_TAG) rescored against the rubric</name>
  <files>(verification-only — no source files modified; produces gitignored capture artifacts)</files>
  <read_first>
    - tests/ui-lab/README.md (the loop + hard rules: injection is preview, packaged is proof; tokens first; suite stays green; no claim without a capture)
    - tests/ui-lab/DESIGN-RUBRIC.md (the screenshot-checkable expectations — SC1 active-distinct, SC2 name-legibility, D-09 wash-only waiting, D-11 dashed recipe cards, collapsed continuity, the dormant-row single-Start affordance)
    - .planning/phases/10-sidebar-visual-polish/10-13-SUMMARY.md (the prior gate's capture tag + the known pre-existing skips/flakes to handle: the `idle-card` surface skip — "context menu has no Stop item" — is a PRE-EXISTING harness limitation that recurs identically every run; the `startup-command` smoke spec parallel-load FLAKE — confirmed GREEN on isolated re-run; treat both as the documented patterns, NOT new failures)
    - .planning/phases/10-sidebar-visual-polish/10-14-SUMMARY.md (the round-5 deltas — note GAP-10-J + GAP-10-K are NOT screenshot-checkable by the static capture: scroll-flip is a live interaction and CJK rendering needs the running runtime locale/fonts; the capture proves the sidebar STYLING is unregressed, the human gate proves the two fixes)
    - .planning/phases/10-sidebar-visual-polish/deferred-items.md (the known pre-existing `.planning/spikes/*.cjs` lint errors that are tolerated — all Phase-10 source/test files must lint clean)
  </read_first>
  <action>
    Build the packaged app and run the full evidence chain against it (NOT the dev server). Run, in order, capturing real stdout for the SUMMARY:
    - `npm run test:unit` — the full vitest suite, all-green, INCLUDING the two NEW 10-14 regression tests (the GAP-10-J scroll-invariance `agent-frame-sample.test.ts` and the GAP-10-K `pty-locale.test.ts`), plus the standing 10-07 real-frame regression, the agent-state-replay oracle, tokens-completeness scanning sidebar.css, and the 10-12 start-affordances truth-table. The count must not drop below the round-4 baseline (373) and should RISE by the two new test files' cases.
    - `npx tsc --noEmit` — exit 0.
    - `npm run lint` — only the known pre-existing `.planning/spikes/*.cjs` errors are tolerated (logged in deferred-items.md); all Phase-10 source/test files (incl. the new pty-locale.ts + agent-tick.ts sampler) must lint clean, and no unconditional console in the shipped main/agent path.
    - `npm run test:smoke` — 15/15 spec files green against the packaged binary (keyboard-switch, reorder, session-edit, app-restart-restore, sidebar-collapse — the SC4 keep-alive guard). If the `startup-command` spec flakes under parallel load (the documented 10-13 pattern), re-run it isolated and record the GREEN isolated result (10-14 touches no PTY DATA path; the locale env change is spawn-arg only).
    - `UI_LAB_TAG=p10-gapfix-round5-gate npm run ui:shots:fresh` — the packaged, no-injection capture with a NEW tag. It ENFORCES the 10-09 name-completeness assertion (FAILS if a name is crushed) and the wash-only sidebar-waiting surface. The run must complete its surface set (the `idle-card` skip is the documented pre-existing harness limitation — identical across all prior captures; confirm the SAME skip, not a new failure; every OTHER surface must be captured, none failed).

    Then rescore the rubric against the fresh capture with pixel-cited verdicts, focusing on the standing no-regression items (SC1 active-distinct, SC2 name complete incl. the active row, collapsed continuity, dashed recipe cards, GAP-10-H single-Start on dormant/idle surfaces, GAP-10-I wash-only waiting). Record the static-capture caveat EXPLICITLY: the capture proves the sidebar STYLING is unregressed but CANNOT exercise GAP-10-J (live scroll interaction) or GAP-10-K (CJK rendering needs the running runtime locale + fonts) — those two are the human gate's job in Task 3.
  </action>
  <verify>
    <automated>npm run test:unit && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` is all-green (stdout pasted in the SUMMARY), INCLUDING the NEW `agent-frame-sample.test.ts` (scroll invariance) and `pty-locale.test.ts` (locale truth table), plus the standing 10-07 real-frame regression and the 10-12 start-affordances case; the count rose above the round-4 baseline (≥ 373 + the new cases) and did not drop.
    - `npx tsc --noEmit` exits 0; `npm run lint` clean on Phase-10 files (only the known pre-existing spike .cjs errors tolerated; no unconditional console in the shipped main/agent path).
    - `npm run test:smoke` 15/15 green against the packaged binary (a `startup-command` parallel-load flake re-run isolated and recorded GREEN if it occurs — the documented 10-13 pattern).
    - `UI_LAB_TAG=p10-gapfix-round5-gate npm run ui:shots:fresh` completes its surface set under the NEW tag (the pre-existing `idle-card` skip is the SAME documented harness limitation, not a new failure; every other surface captured, none failed) with the name-completeness assertion ENFORCED and PASSING and the wash-only sidebar-waiting surface.
    - The rubric is rescored with pixel-cited verdicts for the no-regression items, and the static-capture caveat (GAP-10-J + GAP-10-K are NOT capture-checkable → human gate) is recorded.
  </acceptance_criteria>
  <done>The full automated suite is green against the packaged app (incl. the two new round-5 regression tests) and a fresh no-injection capture completes its surface set under a NEW tag, rescored against the rubric, with the GAP-10-J/K-need-human caveat recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING human re-verify of the running app (attempt 5) — unqualified "approved" required (phase gate)</name>
  <files>(no source files — human verification checkpoint; the verdict is recorded by Task 4 into 10-VALIDATION.md)</files>
  <read_first>
    - .planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md (Addendum 3: the GAP-10-J + GAP-10-K entries — what the operator confirms; GAP-10-D/E/F/G/H/I are CLOSED, only the no-regression sweep applies to them)
    - .planning/phases/10-sidebar-visual-polish/10-14-SUMMARY.md (the exact fixes to confirm: baseY sampling for scroll-invariance; the UTF-8 spawn locale + CJK font fallback)
  </read_first>
  <action>
    This is a BLOCKING human-verify checkpoint — do NOT auto-approve regardless of any auto-advance / yolo setting (visual + terminal-fidelity phase gate; the 06.1/08/10/13 precedent: automated green is not proof, and every prior gate caught a live failure or a new defect the static capture passed; the legitimacy/gate discipline is never auto-approvable). Build + launch the packaged app, present the how-to-verify script below to the operator, and WAIT for an explicit verdict. Capture the operator's response VERBATIM for Task 4. The checkpoint detail is in <what-built> / <how-to-verify> / <resume-signal> below.
  </action>
  <what-built>
    GAP-10-J (scrolling flipped the sidebar status): the agent-state classifier was reading the VISIBLE part of the terminal, which moves when you scroll up into history — so scrolling re-read old screens and the status changed (free → in-progress → waiting). Fixed: the classifier now always reads the LIVE bottom of the terminal (the real current output), so scrolling the history up no longer changes the status. GAP-10-K (Chinese did not render): two causes were fixed — (1) a Finder-launched app inherited no UTF-8 locale, so command-line tools fell back to ASCII and could not emit Chinese; the app now passes a UTF-8 locale to each session (without overriding a locale you've set yourself); (2) the terminal font (JetBrains Mono) has no Chinese glyphs, so a Chinese-capable system font was added as a fallback after it. Together, Chinese now renders in the terminal. All six prior items (live amber GAP-10-D, gutter GAP-10-E, name completeness GAP-10-F/G, single Start GAP-10-H, wash-only waiting GAP-10-I) stay closed. The round-5 code-review delta pass ran and its Critical/High findings were fixed. The full suite is green against the packaged app and the fresh capture completes its surface set, rescored PASS.
  </what-built>
  <how-to-verify>
    Build + launch the PACKAGED app (`npm run package` then open `out/Just-Wrapper-darwin-arm64/Just-Wrapper.app` — NOT `npm run make`, which has a known SEPARATE lowdb crash tracked as its own todo).

    ITEM J — Scrolling no longer flips the status (GAP-10-J, the qualifying defect):
    1. Start a `claude --rc` (or any) session and let it produce enough output to fill the terminal and create scrollback (run a few commands, or wait for an agent run).
    2. Note the session's sidebar status (e.g. "free" / "in progress" / "waiting for you").
    3. Scroll the terminal history UP into the scrollback (mouse wheel / trackpad) — past the live bottom, into older output.
    4. Confirm the sidebar status DOES NOT change while you scroll up and back down — it stays whatever the LIVE state is (it no longer flips to "in progress" / "waiting for you" just from scrolling). Try it both while the session is idle (free) and while it has a live permission prompt (waiting) — scrolling must not change the reported status in either case.

    ITEM K — Chinese renders in the terminal (GAP-10-K):
    5. In a session, run a command that emits Chinese, e.g. `echo 你好世界` (or `echo 中文测试 终端 渲染`). Confirm the Chinese characters render as REAL glyphs — not tofu boxes (□□□), not blanks, not mis-aligned overlap. Optionally check `locale` shows a UTF-8 LANG/LC_ALL.
    6. (Optional fidelity check) Open a CJK-heavy interactive tool or `vim` a file with Chinese content and confirm it reads correctly with no width misalignment.

    Standing no-regression sweep (GAP-10-D/E/F/G/H/I stay closed):
    7. Live amber still fires at a real permission prompt (GAP-10-D); the leading gutter is still compact (GAP-10-E); medium names render complete on inactive AND the active row (GAP-10-F/G); an inactive/dormant row shows exactly ONE Start affordance in every state (GAP-10-H); the waiting look is the amber WASH only, no edge bar, expanded + collapsed (GAP-10-I); SC1 active-distinct, SC3 collapsed identity + dots, SC4 click-to-switch / drag-reorder / keyboard-switch + terminal keep-alive all unregressed.
  </how-to-verify>
  <verify>
    <human-check>Operator confirms ITEM J (scrolling the session history up no longer changes the sidebar status — in both the idle/free and the live-waiting cases), ITEM K (Chinese renders as real glyphs in the terminal — `echo 你好世界` shows correct characters, not tofu), and the standing no-regression sweep (GAP-10-D/E/F/G/H/I + SC1-SC4 unregressed) — then types an explicit, unqualified "approved".</human-check>
  </verify>
  <resume-signal>
    Type exactly "approved" (unqualified) to flip the phase gate. ANY qualification — "partial", "approved but…", "approved with adjustments", or a new defect/request — is NOT an approval: it is captured verbatim and routed to a new gap-closure round (round 6; the next fix plan would be 10-16). The Nyquist gate flips true ONLY on an explicit, unqualified "approved".
  </resume-signal>
  <done>The operator has run the live verification script and returned an explicit verdict (captured verbatim for Task 4); on an unqualified "approved" the phase gate is cleared.</done>
</task>

<task type="auto">
  <name>Task 4: Record the gate verdict in 10-VALIDATION.md (flip the flag only on unqualified approval)</name>
  <files>.planning/phases/10-sidebar-visual-polish/10-VALIDATION.md</files>
  <read_first>
    - .planning/phases/10-sidebar-visual-polish/10-VALIDATION.md (the frontmatter `nyquist_compliant: false` + the Human Gate History table with attempts 1, 2, 3, 4; append attempt 5)
    - .planning/phases/10-sidebar-visual-polish/10-13-SUMMARY.md (the verbatim-capture + routing pattern to mirror on a non-approval)
  </read_first>
  <action>
    Record Task 3's verdict verbatim. On an explicit, UNQUALIFIED "approved": set `nyquist_compliant: true` in the 10-VALIDATION.md frontmatter, add a "5 | 10-15 | <date> | APPROVED | <verbatim quote> → UI-02 complete, Phase 10 closeable" row to the Human Gate History table, and update the "Current state" line to reflect the gate is closed. On ANY qualification or new required change: leave `nyquist_compliant: false`, add the attempt-5 row with the verbatim verdict and the routed items (kind / severity / fix-belongs-in — a new defect routes as a gap), and note the next fix plan is 10-16. Map the verdict to ITEM J (scroll-invariant status) / ITEM K (CJK rendering) / the no-regression sweep. Do NOT touch STATE.md or ROADMAP.md (the orchestrator owns those writes). Do NOT attempt any code fix here — fixes belong to the next gap-closure round if the gate fails.
  </action>
  <verify>
    <automated>grep -Eq "nyquist_compliant:\s*(true|false)" .planning/phases/10-sidebar-visual-polish/10-VALIDATION.md && grep -q "10-15" .planning/phases/10-sidebar-visual-polish/10-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 10-VALIDATION.md has an attempt-5 row in the Human Gate History table citing plan 10-15 and the verbatim operator verdict, mapped to ITEM J / ITEM K / the no-regression sweep.
    - `nyquist_compliant` is `true` IF AND ONLY IF the operator gave an explicit unqualified "approved"; otherwise it remains `false` with the routed items recorded (next fix plan 10-16).
    - No edits to STATE.md or ROADMAP.md; no source code changed in this task.
  </acceptance_criteria>
  <done>The gate verdict is recorded; the Nyquist flag reflects the operator's exact verdict (true only on unqualified approval); the round-5 items are mapped to ITEM J / ITEM K.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live agent process → scroll-invariant status | The no-regression sweep + ITEM J drive a real session whose scroll position the static capture cannot exercise — re-confirming the GAP-10-J fix that scrolling does not re-classify old frames (the one boundary the static capture cannot reach). |
| live runtime locale + system fonts → CJK rendering | ITEM K drives a real Finder-launched packaged app whose inherited locale + installed fonts determine whether Chinese renders — the boundary GAP-10-K lives on, unreachable by the static capture. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-15-01 | Repudiation | the phase gate (accepting on automated green / a partial verdict) | mitigate | The gate is BLOCKING and human-only; nyquist_compliant flips ONLY on an explicit unqualified "approved" (06.1/08/10/13 precedent). A partial/new-request verdict is captured verbatim and routed, never silently accepted — this control caught GAP-10-D, GAP-10-G, GAP-10-H, GAP-10-J, and GAP-10-K at the prior gates. |
| T-10-15-02 | Tampering | the round-5 code-review obligation (skipping it to reach the gate faster) | mitigate | Task 1 runs the round-5 delta review BEFORE the gate and fixes Critical/High pre-gate; a skipped review is a process repudiation the SUMMARY would expose. |
| T-10-15-03 | Denial of Service | classify() on the real agent prompt + the scroll path (the live no-regression + ITEM J check) | accept | Re-verifies the locked 10-07 bounded-region mitigation + the 10-14 baseY sampling behave on a real session; no new code here. |
| T-10-15-SC | Tampering | npm/pip/cargo installs + bundled fonts | accept | Zero package installs AND zero bundled fonts this plan (review + suite run + verdict record only; GAP-10-K's font fix used system-present families). No supply-chain surface. EXPECTED_API_KEYS stays 20. |
</threat_model>

<verification>
- Task 1: the round-5 delta code-review (GAP-10-J + GAP-10-K deltas) is recorded in 10-REVIEW.md; Critical/High findings fixed pre-gate; tsc green.
- Task 2: full suite green against the packaged app (incl. the two new round-5 regression tests) + a fresh no-injection capture (NEW tag) completing its surface set with the name-completeness assertion enforced + the wash-only waiting, rescored against the rubric, with the GAP-10-J/K-need-human caveat recorded.
- Task 3: BLOCKING human gate confirms GAP-10-J (scroll no longer flips status), GAP-10-K (Chinese renders), and the GAP-10-D/E/F/G/H/I + SC1-SC4 no-regression sweep in the running app.
- Task 4: the Nyquist flag is set to match the operator's exact verdict; the round-5 items are mapped.
</verification>

<success_criteria>
- On unqualified approval: nyquist_compliant true, UI-02 complete, Phase 10 closeable — the qualifying defect (GAP-10-J scroll→status) confirmed fixed and CJK rendering (GAP-10-K) confirmed in the running app.
- On any qualification: verbatim verdict captured, flag stays false, items routed to 10-16 — no silent acceptance.
- The round-5 code-review obligation is discharged (Critical/High fixed pre-gate).
- The full automated suite + the packaged ui-lab capture (full surface set, new tag, machine-checks enforced) are the evidence backing the gate; the human verdict is the authoritative record; no machine check relaxed.
</success_criteria>

<output>
Create `.planning/phases/10-sidebar-visual-polish/10-15-SUMMARY.md` when done. Record: the round-5 delta-review outcome (findings by severity + any Critical/High fixes applied); the full-suite stdout summary (incl. the two new regression tests passing); the rescored rubric + the full-surface-set capture confirmation under tag `p10-gapfix-round5-gate` + the GAP-10-J/K-need-human caveat; the verbatim human verdict mapped to ITEM J / ITEM K / the no-regression sweep; the resulting nyquist_compliant state; and — on a non-approval — the routed items for round 6 (10-16).
</output>
