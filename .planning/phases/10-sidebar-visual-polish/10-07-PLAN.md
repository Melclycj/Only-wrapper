---
phase: 10-sidebar-visual-polish
plan: 07
type: execute
wave: 6
depends_on: []
files_modified:
  - src/renderer/SessionView.tsx
  - src/shared/agent-state.ts
  - src/renderer/agent-tick.ts
  - .planning/spikes/003-live-amber-repro/README.md
  - .planning/spikes/003-live-amber-repro/capture-claude-websearch.jsonl
  - src/shared/__tests__/agent-state-replay.test.ts
autonomous: false
requirements: [UI-02]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "The exact live frame the operator's claude --rc Web Search permission prompt renders is captured deterministically (a real PTY frame stream / viewport snapshot), not assumed"
    - "The single broken link in the chain PTY data → viewportLines() → classify() → decideAgentTick → emitAgent → handleAgentState → row.agentState → rowAgentAttr → data-agent → amber CSS is identified with evidence (an instrumentation log or a failing-then-passing test on the real frame)"
    - "A regression test reproduces the captured real frame and asserts the chain now emits 'waiting' for it"
    - "Amber 'Waiting for you' fires in the running packaged app at a real claude --rc permission prompt — both when that session is active AND when it is backgrounded"
  artifacts:
    - path: ".planning/spikes/003-live-amber-repro/capture-claude-websearch.jsonl"
      provides: "The captured real Web Search permission-prompt frame stream (the screenshot-2 reproduction) used to drive the diagnosis + regression test"
    - path: "src/shared/__tests__/agent-state-replay.test.ts"
      provides: "Regression coverage extended with the real Web Search frame; asserts classify() (and/or decideAgentTick) yields 'waiting' for it"
  key_links:
    - from: "src/renderer/SessionView.tsx agentTick"
      to: "src/renderer/agent-tick.ts decideAgentTick"
      via: "viewportLines() → decideAgentTick → emitAgent (the live emission path)"
      pattern: "decideAgentTick\\("
    - from: "captured real frame"
      to: "classify()"
      via: "replay oracle reconstructs the real viewport and classifies it"
      pattern: "classify\\("
---

<objective>
Close GAP-10-D (the S1 blocker): the live amber "Waiting for you" treatment never fired at the 2nd human gate when a real `claude --rc` session reached a Web Search permission prompt — the sidebar row stayed BLUE (Running).

This is a DEBUG-FIRST plan. Static analysis is contradictory: `classify()` matches the idealized screenshot-2 frame (numberedMenu + claudeFooter both fire), `rowAgentAttr()` is running-gated and unit-tested (8/8 green), `decideAgentTick()` has a settle-INDEPENDENT fast path with green unit tests (4/4), and the CSS seam is proven by the ui-lab capture. Yet amber did not fire live. **The plan MUST NOT assume a root cause.** It must REPRODUCE the failure with the real frame, DIAGNOSE the actual broken link with evidence, THEN fix exactly that link, then prove amber fires in the running app.

Purpose: The product's soul is "which agent needs me." Amber waiting detection is the single most important behavior of this phase (D-06/D-09). The blocking human gate exists precisely because automated green did not predict this live failure — so the fix must be grounded in a captured real frame, not in another plausible-but-untested hypothesis.

Output: A captured real Web Search permission-prompt frame, an evidence-based diagnosis of the one broken link, a targeted fix at that link, a regression test on the real frame, and (in plan 10-10's gate) live confirmation that amber fires.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md
@.planning/phases/10-sidebar-visual-polish/10-06-SUMMARY.md
@.planning/phases/10-sidebar-visual-polish/10-05-SUMMARY.md
@.planning/phases/10-sidebar-visual-polish/10-PATTERNS.md
@.planning/spikes/002-real-agent-frames/README.md
</context>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<artifacts_produced>
## Artifacts this phase produces (for source-grounding drift verification)

New symbols / files created by this plan:
- `.planning/spikes/003-live-amber-repro/README.md` — diagnosis trail + how-to-capture doc (new spike dir)
- `.planning/spikes/003-live-amber-repro/capture-claude-websearch.jsonl` — captured real Web Search permission-prompt frame stream
- A new instrumentation helper inside `src/renderer/SessionView.tsx` (temporary or guarded debug-trace seam — see Task 1; if kept, it is a dev-only guarded log)
- New test cases inside `src/shared/__tests__/agent-state-replay.test.ts` reconstructing + classifying the real Web Search frame
- Possibly a new exported constant/regex in `src/shared/agent-state.ts` OR a new branch in `decideAgentTick`/`viewportLines` reading logic — the EXACT symbol depends on the Task-1 diagnosis and MUST NOT be predetermined

NOTE: This plan reuses existing symbols `classify`, `decideAgentTick`, `viewportLines` (local in SessionView), `rowAgentAttr`, `handleAgentState`, `presentation`, the `record.cjs` recorder, and the spike-002 capture — those are NOT new.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce + instrument — capture the REAL Web Search frame and trace the live chain</name>
  <files>.planning/spikes/003-live-amber-repro/README.md, .planning/spikes/003-live-amber-repro/capture-claude-websearch.jsonl, src/renderer/SessionView.tsx</files>
  <read_first>
    - src/renderer/SessionView.tsx (the live agentTick + viewportLines + agentRunning gate + emitAgent — the whole SEAM A block lines ~333-509; read it fully so instrumentation targets the real call sites)
    - src/renderer/agent-tick.ts (decideAgentTick decision order; the settle-independent fast path)
    - src/shared/agent-state.ts (classify() — the exact regexes: shellPrompt, trailingQuestion, ynBracket, numberedMenu, claudeFooter, passwordPrompt; note the `region = nonEmpty.slice(-4).join('\n')` window and the `shellPrompt && !trailingQuestion` early-return-FREE branch)
    - .planning/spikes/002-real-agent-frames/README.md (the existing recorder protocol + the kill-finding: only numbered_menu + claude_footer discriminate; ❯ is ambient)
    - .planning/spikes/002-real-agent-frames/capture.sh and .planning/spikes/001-frame-stability-mechanism/record.cjs (the existing capture tooling — reuse it; do NOT write a new recorder)
    - 10-06-SUMMARY.md (the exact screenshot-2 frame: "Tool use / Web Search(...) / Do you want to proceed? / ❯ 1. Yes / 2. Yes, and don't ask again... / 3. No" with bottom line "Esc to cancel · Tab to amend")
  </read_first>
  <action>
    Create the diagnosis spike dir `.planning/spikes/003-live-amber-repro/`. Capture the REAL frame stream of a `claude --rc` Web Search permission prompt using the EXISTING recorder (`.planning/spikes/002-real-agent-frames/capture.sh` wraps `001-.../record.cjs`); write the log to `capture-claude-websearch.jsonl` in the new dir. The capture MUST reach the exact screenshot-2 state (a Web Search tool-permission prompt with the "Do you want to proceed?" question, the ❯-numbered options, and the "Esc to cancel · Tab to amend" footer). If the recorder forensic format does not preserve enough of the rendered viewport to diagnose, additionally add an instrumentation seam in SessionView.tsx's `agentTick` callback that logs, per tick while `agentRunning`: (a) the full `viewportLines()` array, (b) the `classify()` verdict on it, (c) the `decideAgentTick` return, (d) the `agentRunning` flag. Gate this trace behind a dev-only flag (e.g. a `window.__AGENT_TRACE` boolean or an env-driven guard) so it is INERT in production — never an unconditional `console.log` in the shipped path (CLAUDE.md no-console rule). Run the real app, drive a real `claude --rc` to the Web Search prompt, and collect the trace.

    Then walk the chain link by link against the collected evidence and write the findings into the spike README: PTY data → `viewportLines()` (does the captured viewport actually CONTAIN the footer + numbered-menu lines, or is the footer below `viewportY+rows` / are option lines wrapped so the "N." anchor breaks / is the question line outside the last-4-non-empty `region` window?) → `classify()` (what does it return on the REAL line array — not the idealized one?) → `decideAgentTick` (does the streak reach WAITING_TICKS, or does `agentRunning` gate it, or does the never-settling footer churn interact differently than the synthetic test assumed?) → `emitAgent` → `handleAgentState` (is `row.status` actually 'running' when it arrives?) → `rowAgentAttr` → `data-agent` → CSS. Identify the SINGLE link that drops the "waiting" signal, with the trace/capture line(s) that prove it. Do NOT fix anything in this task — its deliverable is the captured frame + the evidence-based diagnosis written to the README.
  </action>
  <verify>
    <automated>test -f .planning/spikes/003-live-amber-repro/capture-claude-websearch.jsonl && test -s .planning/spikes/003-live-amber-repro/capture-claude-websearch.jsonl && grep -qiE "esc to (cancel|interrupt)|proceed|web search|numbered" .planning/spikes/003-live-amber-repro/capture-claude-websearch.jsonl && echo CAPTURED</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/spikes/003-live-amber-repro/capture-claude-websearch.jsonl` exists, is non-empty, and contains evidence of the Web Search permission prompt (footer / proceed / numbered-menu markers grep-confirmable).
    - `.planning/spikes/003-live-amber-repro/README.md` documents a per-link trace of the live chain with the specific captured line(s) that identify which ONE link drops the "waiting" signal.
    - Any instrumentation added to SessionView.tsx is behind a dev-only guard (grep shows no unconditional `console.` in the production agentTick path; `npm run lint` stays clean on SessionView.tsx).
    - The diagnosis names a concrete root cause hypothesis ANCHORED in captured evidence (e.g. "viewportLines() does not include the footer line because the prompt renders below viewportY+rows" or "classify() returns free because the real option lines wrap and the `\\d+\\.` anchor matches <2"), NOT a restatement of the unconfirmed 10-06 hypothesis.
  </acceptance_criteria>
  <done>The real Web Search frame is captured and the single broken link in the live chain is identified with captured-evidence backing, written to the spike README.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the diagnosed link + lock it with a regression test on the real frame</name>
  <files>src/shared/agent-state.ts, src/renderer/agent-tick.ts, src/renderer/SessionView.tsx, src/shared/__tests__/agent-state-replay.test.ts</files>
  <read_first>
    - .planning/spikes/003-live-amber-repro/README.md (the Task-1 diagnosis — the fix target)
    - .planning/spikes/003-live-amber-repro/capture-claude-websearch.jsonl (the captured real frame to reconstruct in the test)
    - src/shared/__tests__/agent-state-replay.test.ts (the existing oracle: reconstructFrame / viewportLines / classifySettleFrame helpers + the "11 settles → exactly 1 WAITING" assertions — extend it the same way; pinned @xterm/headless 5.5.0)
    - src/shared/agent-state.ts (classify regexes + the region window — only touch if Task 1 proves the recognizer is the broken link)
    - src/renderer/agent-tick.ts (decideAgentTick — only touch if Task 1 proves the settle/streak logic is the broken link)
    - src/renderer/SessionView.tsx (viewportLines / agentRunning gate / emitAgent — only touch if Task 1 proves the live reading or gating is the broken link)
  </read_first>
  <behavior>
    - RED: a new test in agent-state-replay.test.ts reconstructs the captured real Web Search frame (the multi-line "Do you want to proceed?" + ❯-numbered options + "Esc to cancel · Tab to amend" footer as the REAL viewportLines() would render it) and asserts the chain yields 'waiting' — and FAILS against the current code (reproducing GAP-10-D offline).
    - GREEN: after the targeted fix at the diagnosed link, the same test passes ('waiting').
    - The existing oracle assertions stay green: "11 settles → exactly 1 WAITING" and "every non-@29s settle classifies FREE" (no false-positive regression — the ❯ caret must STILL not be a waiting signal; a shell prompt on the active line must STILL be FREE).
    - The 4 existing decideAgentTick fast-path tests stay green (no regression of the churning-footer / single-transient-debounce / Thinking-never-waiting / settled-shell-free cases).
  </behavior>
  <action>
    Apply the SMALLEST fix that closes the diagnosed link from Task 1. Scope discipline: change ONLY the link Task 1 proved broken — do not opportunistically rewrite the whole detector. Concrete forms the fix may take depending on the diagnosis (pick the one the evidence supports, do NOT apply all):
    - If `viewportLines()` does not surface the footer/menu (footer below the visible viewport, or the question line outside the last-4-non-empty `region`): widen the live read window in SessionView.tsx `viewportLines()` and/or the `region` slice in `classify()` (src/shared/agent-state.ts line ~74) to the bounded extent the captured frame requires — keep it linear/anchored (ReDoS-safe, ASVS V5) and bounded by `rows`.
    - If the real option lines wrap so the `^\\s*[❯>]?\\s*\\d+\\.\\s+\\S` numbered-menu anchor matches <2: adjust the recognizer to the real wrapped shape while keeping it anchored and keeping the spike-002 "exactly 1 WAITING" count intact.
    - If `agentRunning` is false at the prompt (the gate never opened, e.g. the 'running' status raced or the backgrounded pane's tick path differs): fix the gate timing in SessionView.tsx without breaking the keep-alive xterm (effect keyed on `id` only — do not re-bind on `active`).
    - If `decideAgentTick` streak/settle interaction drops it on the real frame: fix the decision order in agent-tick.ts.
    Reconstruct the captured real frame in agent-state-replay.test.ts (mirror the existing `reconstructFrame`/`classifySettleFrame` @xterm/headless approach so the real viewport is genuinely parsed, not hand-shaped) and assert 'waiting'. Run RED first (must fail on current code), then GREEN. Keep classify() ReDoS-safe and the amber accent reserved exclusively for 'waiting'. Remove or permanently dev-gate any Task-1 instrumentation so the shipped agentTick path has no unconditional logging.
  </action>
  <verify>
    <automated>npx vitest run src/shared/__tests__/agent-state-replay.test.ts src/renderer/__tests__/agent-tick.test.ts src/shared/__tests__/agent-state.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The new regression test in agent-state-replay.test.ts reconstructs the captured real Web Search frame and asserts 'waiting'; it FAILED before the fix (RED evidence recorded in the SUMMARY) and PASSES after (GREEN).
    - The existing oracle assertions stay green: `expect(waitingCount).toBe(1)` and `expect(freeCount).toBe(10)` and the per-settle FREE sweep — no false-positive regression.
    - All 4 agent-tick.test.ts cases stay green; agent-state.test.ts stays green.
    - `npx tsc --noEmit` exits 0.
    - `npm run lint` is clean on the modified files (no unconditional console.* in the production agentTick path; grep `console\.` in SessionView.tsx returns only dev-guarded or zero hits in the agent path).
    - The change touches ONLY the link Task 1 diagnosed (the diff is scoped; the SUMMARY cites the diagnosed link and the one-place fix).
  </acceptance_criteria>
  <done>The diagnosed broken link is fixed with the minimal change, a real-frame regression test reproduces GAP-10-D and now passes, and no existing detection assertion regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PTY output → renderer xterm buffer | The terminal content classify() inspects is attacker-influenceable agent/program output (a malicious program could render text shaped like a confirmation prompt or a pathological wide/long line). |
| dev instrumentation → logs | The Task-1 trace seam logs raw viewport text (could contain cwd paths / command fragments) if left enabled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Denial of Service | classify()/region regexes on PTY output (any region-window widening) | mitigate | Every regex stays LINEAR + ANCHORED (no nested quantifiers, no catastrophic backtracking — ASVS V5 / T-06.1-01). Any widened `region`/viewport read stays bounded by `term.rows` (a rows×cols viewport, never a growing byte stream) so runtime is bounded. Re-assert the existing purity header invariant. |
| T-10-07-02 | Information Disclosure | dev instrumentation trace seam in SessionView | mitigate | The trace is dev-only-guarded (window flag / env), INERT in production; removed or permanently gated before the plan completes. No unconditional console.* in the shipped agentTick path (lint-enforced). The capture file lives under .planning/spikes (forensic, gitignored-or-reviewed) — operator drives the capture so no secret is auto-collected. |
| T-10-07-03 | Tampering | agent-state amber overlay (false amber / suppressed amber) | accept | The overlay is a renderer-only presentation hint, never persisted, never written to a PTY, never an IPC field (D-06/D-12). A mis-classification is a UX inaccuracy, not a security event. No new bridge key (EXPECTED_API_KEYS stays 20). |
| T-10-07-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan — reuses @xterm/headless (already pinned 5.5.0) + existing recorder. No supply-chain surface. |
</threat_model>

<verification>
- Real Web Search frame captured deterministically (Task 1).
- Single broken link diagnosed with captured-evidence backing (Task 1 README).
- Regression test reproduces GAP-10-D offline (RED) then passes after the fix (GREEN), with the spike-002 "1 WAITING" oracle still green (Task 2).
- tsc + lint clean; no unconditional console in the production agent path.
- Live confirmation that amber fires in the running app is deferred to plan 10-10's BLOCKING human gate (this plan is diagnosis + code fix; it cannot self-prove the live behavior — only a real agent process at a real prompt can, which is exactly what the human gate covers).
</verification>

<success_criteria>
- GAP-10-D root cause is DIAGNOSED (not assumed) and FIXED at the one broken link.
- The captured real Web Search frame is checked into `.planning/spikes/003-live-amber-repro/` and drives an offline regression test that reproduces then closes the defect.
- No false-positive regression: the spike-002 oracle still yields exactly 1 WAITING; the ❯ caret is still not a signal; a shell prompt is still FREE.
- EXPECTED_API_KEYS stays 20; the amber accent stays reserved exclusively for 'waiting'.
</success_criteria>

<output>
Create `.planning/phases/10-sidebar-visual-polish/10-07-SUMMARY.md` when done. Record: the captured real frame, the per-link trace, the SINGLE diagnosed broken link with its evidence, the one-place fix applied, the RED→GREEN regression-test evidence, and explicit confirmation that the spike-002 oracle + agent-tick + agent-state suites stayed green. Note that live amber confirmation is owned by the 10-10 human gate.
</output>
