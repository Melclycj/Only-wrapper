---
phase: 10-sidebar-visual-polish
plan: 05
type: execute
wave: 1
depends_on: ["10-02", "10-03"]
gap_closure: true
files_modified:
  - src/renderer/sidebar.css
  - src/renderer/Sidebar.tsx
  - src/renderer/row-secondary.ts
  - src/renderer/__tests__/sidebar-agent-attr.test.ts
  - tests/ui-lab/surfaces.ts
  - tests/ui-lab/helpers.ts
autonomous: true
requirements: ["UI-02"]

must_haves:
  truths:
    - "On a 220px rail, a long-name running non-active row shows the full session name (or a reasonably-long ellipsis truncation) at rest — the edit/restart/close control cluster reserves ZERO layout width until the row is hovered/focused (GAP-10-A / SC2 / D-01-D-03)"
    - "Hovering or keyboard-focusing a row reveals the controls; the controls remain in the DOM at all times so the E2E driver can address every data-testid (D-02)"
    - "The dormant recipe row's ▶ Start (.row-control-start) keeps its always-visible reserved width (D-13 exemption) — it does NOT collapse to zero"
    - "A running row carrying agentState='waiting' renders data-agent='waiting' (amber edge bar + amber wash); a non-running row (stopped/exited/error/not_started) NEVER carries data-agent even with a stale agentState (GAP-10-B / WR-03 / D-09)"
    - "Written evidence (a passing pure unit test plus a traced explanation) shows where the live emit→store→render chain surfaces amber for a 'waiting' agent state (GAP-10-B / WR-02)"
    - "A waiting row that is ALSO active renders the amber edge bar — amber wins over the active status accent regardless of CSS source order (WR-04 / locked Phase-6 amber-reserved contract)"
    - "The ui-lab sidebar-waiting surface no longer throws SkipSurface unconditionally — it sets data-agent='waiting' via a deterministic test-only DOM seam and produces a PNG (IN-05)"
  artifacts:
    - path: "src/renderer/sidebar.css"
      provides: "Zero-width-at-rest control cluster (GAP-10-A fix) + order-independent waiting-beats-active edge bar (WR-04 fix)"
      contains: "max-width: 0"
    - path: "src/renderer/Sidebar.tsx"
      provides: "data-agent JSX seam gated on s.status === 'running' (WR-03 fix)"
      contains: "status === 'running'"
    - path: "src/renderer/row-secondary.ts"
      provides: "Pure rowAgentAttr(status, agentState) helper backing the data-agent seam"
      contains: "rowAgentAttr"
    - path: "src/renderer/__tests__/sidebar-agent-attr.test.ts"
      provides: "Pure-function coverage that running+waiting yields the attribute and non-running never does"
    - path: "tests/ui-lab/surfaces.ts"
      provides: "sidebar-waiting surface driven via a deterministic data-agent seam (no permanent SkipSurface)"
  key_links:
    - from: "src/renderer/Sidebar.tsx"
      to: "src/renderer/sidebar.css"
      via: "data-agent='waiting' attribute → [data-agent='waiting'] amber rule"
      pattern: "data-agent"
    - from: "tests/ui-lab/surfaces.ts"
      to: "src/renderer/sidebar.css"
      via: "test-only DOM poke sets data-agent='waiting' → packaged capture shows amber"
      pattern: "sidebar-waiting"
---

<objective>
Close the two BLOCKER live-app gaps and the WR-04 fragility from the Phase-10 human gate (NOT_APPROVED, recorded in 10-04-SUMMARY.md), as code fixes only — no capture/gate here (that is 10-06).

1. GAP-10-A (S2, root cause WR-01): the hover-reveal control cluster reserves permanent layout width (`opacity:0` does not remove the layout box), crushing the session name even at rest on the 220px rail. Collapse the per-control box to zero width at rest and restore it on hover/focus, keeping every control in the DOM for the E2E driver and exempting the always-visible dormant ▶ Start.
2. GAP-10-B (S2, root causes WR-02 + WR-03 + IN-05): the amber "Waiting for you" treatment never appeared live. Gate the data-agent JSX seam on `status==='running'` (WR-03), investigate-and-prove the live emit→store→render chain surfaces amber (WR-02), add a pure unit test for the seam, and add a deterministic ui-lab test seam so the amber styling is capturable (IN-05).
3. WR-04 (WARNING): make waiting-beats-active edge-bar precedence explicit so a future CSS reorder cannot silently invert it.

Per the 2026-06-11 user decision in 10-CONTEXT.md, GAP-10-C requires NO code change — the edge bar stays STATUS-colored and per-session custom color stays on the icon tile. Do NOT touch edge-bar color semantics.

Purpose: restore name legibility and the amber "which agent needs me" signal — the product's core read — so Phase 10 can pass its human re-verify.
Output: edited sidebar.css + Sidebar.tsx + row-secondary.ts, a new pure unit test, an unblocked ui-lab waiting surface, and a written WR-02 chain trace in this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-sidebar-visual-polish/10-CONTEXT.md
@.planning/phases/10-sidebar-visual-polish/10-UI-SPEC.md
@.planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md
@.planning/phases/10-sidebar-visual-polish/10-REVIEW.md
@.planning/phases/10-sidebar-visual-polish/10-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols / rules / seams this plan CREATES (exclude from source-drift verification):
- CSS: `max-width:0` + `overflow:hidden` (+ zeroed padding/border-width) added to the existing `.sidebar-row .row-control` at-rest rule in `src/renderer/sidebar.css`; a `max-width` restore added to the existing `.sidebar-row:hover/.active/:focus-within/:focus-visible .row-control` reveal selectors.
- CSS: a new explicit waiting-beats-active edge-bar selector, e.g. `.sidebar-row.active[data-agent='waiting'], .sidebar-row[data-agent='waiting']` setting `border-left` to `var(--accent-waiting)` (plus its collapsed-rail mirror).
- TSX: tightened `data-agent` JSX seam in `src/renderer/Sidebar.tsx` (gated on `s.status === 'running' && agentState`).
- TS: a new exported pure helper `rowAgentAttr(status, agentState)` in `src/renderer/row-secondary.ts` returning the `data-agent` value or `undefined`.
- TEST: `src/renderer/__tests__/sidebar-agent-attr.test.ts` (new pure-function test file).
- UI-LAB: a deterministic test-only DOM seam used by the `sidebar-waiting` surface in `tests/ui-lab/surfaces.ts` (a `browser.execute` poke that sets `data-agent='waiting'` on a real row), plus any helper it needs in `tests/ui-lab/helpers.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse hover-control box to zero width at rest (GAP-10-A / WR-01) + make waiting-beats-active explicit (WR-04)</name>
  <files>src/renderer/sidebar.css</files>
  <read_first>
    - src/renderer/sidebar.css (CURRENT state — lines 161-242 for the .row-controls/.row-control/.row-control-start rules; 169-184 for the opacity-only reveal; 86-99 + 427-433 for the .active vs [data-agent='waiting'] edge-bar rules)
    - .planning/phases/10-sidebar-visual-polish/10-REVIEW.md (WR-01 root cause + the two viable CSS routes; WR-04 root cause + the explicit-precedence fix)
    - .planning/phases/10-sidebar-visual-polish/10-UI-SPEC.md (§Interaction Contract "Hover-reveal controls … No reserved width"; §Color "Amber … reserved EXCLUSIVELY for waiting … must win over the active status accent")
    - src/renderer/tokens.css (confirm --duration-fast / --ease-standard exist; max-width is layout-bound and must NOT be added to any transition)
  </read_first>
  <action>
    Edit the existing at-rest `.sidebar-row .row-control` rule (currently `opacity:0` + a single `transition: opacity …`) to ALSO collapse its layout box: add `max-width: 0; overflow: hidden; padding: 0; border-width: 0;` so an unrevealed control consumes zero horizontal width. Keep `opacity:0`. Keep the transition on `opacity` ONLY — do NOT add `max-width` to the transition list (max-width is layout-bound per web/performance.md; let it snap). Restore the box in the existing reveal block (`.sidebar-row:hover .row-control, .sidebar-row.active .row-control, .sidebar-row:focus-within .row-control, .sidebar-row:focus-visible .row-control`) by adding `max-width: 24px; padding: 0; border-width: 1px;` alongside the existing `opacity:1` (24px matches the existing `.row-control` width at sidebar.css:196). If the `.row-controls` container or its gap still reserves measurable width with all children at zero, neutralize that at rest too (e.g. drop the container's resting `gap`/`margin-left` until revealed) — but do NOT remove buttons from the DOM (sidebar.css:159-160 comment + the E2E driver require every data-testid control to stay present and addressable).

    EXEMPT the dormant ▶ Start: the existing `.sidebar-row[data-dormant] .row-control-start { opacity: 1 }` (sidebar.css:188-190) must keep its reserved width — add `max-width: 24px; padding: 0; border-width: 1px;` to that exemption rule and keep the circular `.row-control-start` styling (227-242) intact so the always-visible recipe ▶ is never collapsed (D-13 / 6.1 D-06). The `.row-drag-handle` reveal (279-287) is opacity-only on a 12px hint — leave it unless the resting capture shows the 12px still crushes the name, in which case collapse it the same way (`max-width:0`/`overflow:hidden` at rest, restored on the same hover/focus selectors) — executor's call after viewing the resting capture.

    WR-04 explicit precedence: remove the order-dependent reliance between `.sidebar-row.active` (border-left `var(--accent)`, sidebar.css:89) and `.sidebar-row[data-agent='waiting']` (border-left `var(--accent-waiting)`, sidebar.css:97). Add an explicit, order-independent rule `.sidebar-row.active[data-agent='waiting'], .sidebar-row[data-agent='waiting'] { border-left: 3px solid var(--accent-waiting); }` with a comment documenting the precedence ("amber waiting wins over the active status accent — locked Phase-6 contract; order-independent so a future reorder cannot invert it"). Mirror it in the collapsed-rail block (427-433) so `.sidebar.collapsed .sidebar-row.active[data-agent='waiting']` resolves to `var(--accent-waiting)`. Keep all values as `var()` tokens (tokens-completeness.test.ts bans migrated literals).
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*/\*' src/renderer/sidebar.css | grep -c 'max-width: 0' | grep -qv '^0$' && grep -q "data-agent='waiting'" src/renderer/sidebar.css && npm run test:unit -- tokens-completeness 2>&1 | tail -5 && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/sidebar.css` `.sidebar-row .row-control` at-rest rule contains `max-width: 0` and `overflow: hidden` (grep on non-comment lines finds them)
    - The reveal selectors block (`:hover`/`.active`/`:focus-within`/`:focus-visible`) sets `max-width: 24px` on `.row-control`
    - The `transition` on `.row-control` lists `opacity` and does NOT list `max-width`
    - `.sidebar-row[data-dormant] .row-control-start` retains a non-zero `max-width` (the ▶ Start stays reserved — D-13 exemption)
    - A selector matching both active+waiting AND non-active waiting sets `border-left: … var(--accent-waiting)` and a comment documents the precedence (WR-04)
    - `npm run test:unit -- tokens-completeness` exits 0; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The control cluster reserves zero width at rest (name reclaims the space), reveals on hover/focus with buttons still in the DOM, the dormant ▶ stays visible, and waiting beats active on the edge bar regardless of source order — all token-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate data-agent on running (WR-03), factor + unit-test the seam, and trace the WR-02 live chain (GAP-10-B)</name>
  <files>src/renderer/Sidebar.tsx, src/renderer/row-secondary.ts, src/renderer/__tests__/sidebar-agent-attr.test.ts</files>
  <read_first>
    - src/renderer/Sidebar.tsx (lines 194-302 — agentState read at 194, presentation() at 195, the data-agent seam at 242 currently `{...(agentState ? { 'data-agent': agentState } : {})}`)
    - src/renderer/row-secondary.ts (the existing pure module to add rowAgentAttr to — match its header-comment + pure-export shape; imports SessionStatus from ../shared/types)
    - src/renderer/SessionManager.tsx (lines 564-572 handleAgentState — stores agentState ONLY while row.status==='running'; lines 477-501 the onPtyStatus subscription)
    - src/renderer/apply-status-event.ts (line 80: `const agentState = event.status === 'running' ? row.agentState : undefined;` — the reducer ALREADY clears agentState when a row leaves running; the JSX gate is a belt-and-suspenders defense, not the sole guard)
    - src/renderer/agent-tick.ts + src/shared/agent-state.ts (the pure detection chain: decideAgentTick → classify → emits 'waiting')
    - src/shared/__tests__/agent-state-replay.test.ts (the ORACLE: replays the real .planning/spikes/002-real-agent-frames/capture-claude.jsonl through classify() and asserts exactly 1 WAITING — proves the recognizer already works; the WR-02 failure is therefore in PROPAGATION/render, not detection)
    - src/renderer/SessionView.tsx (lines 333-388 detector setInterval + emitAgent → onAgentStateRef.current(id,state); lines 480-510 agentRunning gate open/close on status transitions)
    - src/renderer/__tests__/start-affordances.test.ts (the pure-predicate table-test idiom to mirror — no testing-library, plain Node/Vitest)
    - .planning/phases/10-sidebar-visual-polish/10-REVIEW.md (WR-02 + WR-03 full text, incl. the suggested pure rowAgentAttrs(status, agentState) factoring)
  </read_first>
  <action>
    WR-03 JSX gate: change the Sidebar.tsx data-agent seam (line 242) from gating on `agentState` truthiness alone to gating on BOTH `s.status === 'running'` AND `agentState` truthy, so a finished/exited/stopped/not_started row can never render `data-agent='waiting'` even if a stale agentState lingered. Target shape: emit `data-agent` only when `s.status === 'running' && agentState`. Leave the `--accent` inline style and `presentation(s.status, agentState)` untouched.

    Factor the seam for unit-testability: add an exported pure function `rowAgentAttr(status: SessionStatus, agentState: AgentState | undefined): AgentState | undefined` to `src/renderer/row-secondary.ts` returning `agentState` when `status === 'running' && agentState` is truthy, else `undefined`. Reuse the proven pure-module Vitest idiom (no React/electron/xterm imports). Import the `AgentState` type from `../shared/agent-state` and `SessionStatus` from `../shared/types` as the existing modules do. Wire Sidebar.tsx to compute the data-agent value via `rowAgentAttr(s.status, agentState)` and emit the attribute only when the result is defined.

    WR-02 investigation (investigation-then-fix with explicit acceptance): the agent-state-replay oracle already proves `classify()` emits exactly 1 WAITING from the REAL claude --rc capture — the recognizer is NOT the failure. Trace the remaining chain end to end and identify where 'waiting' fails to reach the rendered attribute live: (a) SessionView `emitAgent` → `onAgentStateRef.current(id, state)` → SessionManager `handleAgentState(id, state)` stores it on the row ONLY while `row.status==='running'`; (b) the row object handed to Sidebar carries `agentState`; (c) the JSX seam emits `data-agent`. The most likely live defect is propagation/render (agentState not surviving onto the row prop, or a memo/identity issue), NOT detection. If the trace reveals an actual detector-emission bug (decideAgentTick / agentRunning gate / emitAgent change-only suppression), fix THAT code and note it; if the trace shows the chain is sound once WR-03 is applied and the only prior failure was the absent capture coverage (IN-05, Task 3) plus the missing running gate, state that conclusion with the evidence. Record the chain trace + conclusion in this plan's SUMMARY.

    Create `src/renderer/__tests__/sidebar-agent-attr.test.ts` mirroring start-affordances.test.ts (pure table test, no jsdom): assert `rowAgentAttr('running','waiting') === 'waiting'`; `rowAgentAttr('running','in-progress') === 'in-progress'`; `rowAgentAttr('running','free') === 'free'`; `rowAgentAttr('running', undefined) === undefined`; and that each non-running status (`'stopped'`, `'exited'`, `'error'`, `'not_started'`) with a stale `'waiting'` agentState returns `undefined` (the WR-03 invariant — a finished row never carries amber). Add a header comment documenting the WR-03 invariant being guarded.
  </action>
  <verify>
    <automated>npm run test:unit -- sidebar-agent-attr 2>&1 | tail -15 && npx tsc --noEmit && grep -q "status === 'running'" src/renderer/Sidebar.tsx && grep -q "rowAgentAttr" src/renderer/row-secondary.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/Sidebar.tsx` emits `data-agent` only when running is true — grep for `status === 'running'` near the data-agent seam succeeds
    - `rowAgentAttr(status, agentState)` is exported from `src/renderer/row-secondary.ts` and is the source of the data-agent value used by Sidebar.tsx
    - `src/renderer/__tests__/sidebar-agent-attr.test.ts` exists and `npm run test:unit -- sidebar-agent-attr` exits 0
    - The test asserts running+waiting → 'waiting'; running+undefined → undefined; each of stopped/exited/error/not_started + stale 'waiting' → undefined
    - `npx tsc --noEmit` exits 0
    - This plan's SUMMARY records the WR-02 end-to-end chain trace and an explicit conclusion (detector-bug-fixed OR chain-sound-once-WR-03+IN-05-applied) with evidence references (the replay oracle, the handleAgentState gate, apply-status-event line 80)
  </acceptance_criteria>
  <done>data-agent is running-gated (no stale amber on finished rows), the seam is a unit-tested pure function, and the WR-02 live chain is traced with a written, evidence-backed conclusion.</done>
</task>

<task type="auto">
  <name>Task 3: Unblock the ui-lab sidebar-waiting surface via a deterministic data-agent test seam (IN-05)</name>
  <files>tests/ui-lab/surfaces.ts, tests/ui-lab/helpers.ts</files>
  <read_first>
    - tests/ui-lab/surfaces.ts (lines 320-343 the sidebar-waiting surface that currently throws SkipSurface unconditionally; lines 344-400 inactive-recipes for the prepare-helper idiom — addSession, clickSidebarRow, browser.execute usage, waitForTestId)
    - tests/ui-lab/helpers.ts (helper exports — shoot, testid/selector helpers, browser.execute patterns; add a new helper here only if reused)
    - tests/ui-lab/README.md (harness rules — injection is preview, packaged is proof; a surface must produce a PNG)
    - src/renderer/sidebar.css (the [data-agent='waiting'] amber rule the seam must trigger — the DOM poke must satisfy the selector `.sidebar-row[data-agent='waiting']`)
    - .planning/phases/10-sidebar-visual-polish/10-REVIEW.md (IN-05: "Add a deterministic injection seam … a test-only applyCssOverride-style DOM poke that toggles the attribute, capturing the styling even though the real detector can't be driven")
  </read_first>
  <action>
    Replace the unconditional `throw new SkipSurface(...)` in the `sidebar-waiting` surface's `prepare` with a deterministic test-only seam that makes the amber styling capturable WITHOUT a real agent process: create a session (reuse `addSession(ctx)` like inactive-recipes), then use `browser.execute` to set `data-agent='waiting'` directly on that row's DOM node (select `.sidebar-row[data-session-id="<id>"]`, call `el.setAttribute('data-agent','waiting')`). This is a presentation-only DOM poke proving "does the CSS render amber" — it deliberately does NOT exercise the real detector (proven by the agent-state-replay oracle + the manual gate). Add a comment stating the seam captures STYLING ONLY and detector-emission is covered by the replay oracle + the WR-02 trace in 10-05.

    The [data-agent='waiting'] CSS rule fires on the attribute alone (it is the JSX seam in the app, not the CSS, that is running-gated), so the poke does not need the row to be `status='running'`; keep the seam minimal. Keep the surface's existing `id`, `title`, `designRefs` and update `expects` to note the deterministic styling-only seam. Ensure the surface produces a PNG via the standard capture path (no more SkipSurface throw). Add a shared helper to helpers.ts only if reused; otherwise inline the `browser.execute` poke.

    Do NOT modify any production renderer code from this task — harness-only changes inside tests/.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('tests/ui-lab/surfaces.ts','utf8'); const m=s.slice(s.indexOf(\"id: 'sidebar-waiting'\"), s.indexOf(\"id: 'inactive-recipes'\")); if(/throw new SkipSurface/.test(m)){console.error('FAIL: sidebar-waiting still throws SkipSurface');process.exit(1)} if(!/data-agent/.test(m)){console.error('FAIL: no data-agent seam');process.exit(1)} console.log('OK: sidebar-waiting unblocked with data-agent seam')" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The `sidebar-waiting` surface block in `tests/ui-lab/surfaces.ts` no longer contains `throw new SkipSurface`
    - The surface's prepare contains a `data-agent` DOM poke (sets `data-agent='waiting'` on a `.sidebar-row`)
    - The node guard command above exits 0 (prints "OK: sidebar-waiting unblocked with data-agent seam")
    - `npx tsc --noEmit` exits 0
    - No file under `src/` is modified by this task (harness-only)
  </acceptance_criteria>
  <done>The sidebar-waiting surface drives the amber styling deterministically and will produce a PNG in a packaged capture, giving the amber treatment automated regression coverage for the first time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-controlled session strings → DOM | Session names / cwd tails / startup commands are user-controlled strings rendered as React text nodes in the sidebar rows (auto-escaped). No new untrusted-input path is added by this plan. |
| PTY-rendered agent output → classify() | Agent terminal frames are attacker-influenceable and feed the (unchanged) pure `classify()` recognizer; this plan does not modify that path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Tampering | Session name / cwd / startupCommand rendered in `.row-name` / `.row-secondary` | accept | Already rendered as React text nodes (auto-escaped, UI-SPEC §Copy discipline / RESEARCH V7); this plan adds no `dangerouslySetInnerHTML` and no HTML interpolation. Local-only Electron app, no network surface. |
| T-10-05-02 | Information Disclosure | `data-agent` attribute on the row | accept | The attribute carries only the presentation overlay enum ('waiting'/'in-progress'/'free') — no secret or PTY content. WR-03 gate further restricts it to running rows. |
| T-10-05-03 | Tampering | ui-lab test-only DOM poke (`setAttribute('data-agent','waiting')`) | mitigate | The poke lives ONLY in `tests/ui-lab/surfaces.ts` (harness code, never shipped); Task 3 forbids touching `src/`. Cannot affect production behavior. |
| T-10-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (renderer-only CSS/TSX/test edits; zero new dependencies — confirmed by 10-UI-SPEC.md §Registry Safety). No legitimacy gate required. |
</threat_model>

<verification>
Per-task: `npm run test:unit` (incl. tokens-completeness scanning sidebar.css + the new sidebar-agent-attr test) and `npx tsc --noEmit` GREEN.
Per-plan (before SUMMARY): `npm run lint` clean for all Phase-10 source files (the pre-existing `.planning/spikes/*.cjs` errors stay out of scope per deferred-items.md); the agent-state-replay oracle still GREEN (it must not regress when the seam is factored). The packaged capture + smoke + blocking human re-verify are deferred to 10-06.
</verification>

<success_criteria>
- The control cluster reserves zero layout width at rest; on a 220px rail a long-name running non-active row shows the full name or a reasonable ellipsis (GAP-10-A closed at the CSS level; visual proof in 10-06).
- Every data-testid control remains in the DOM and reveals on hover/focus; the dormant ▶ Start stays always-visible.
- `data-agent` is emitted only on running rows (WR-03); the seam is a unit-tested pure function; the WR-02 live chain is traced with a written conclusion.
- Waiting beats active on the edge bar order-independently (WR-04).
- The ui-lab sidebar-waiting surface is unblocked and produces a PNG (IN-05).
- All token-clean (no banned literals), tsc/unit GREEN, GAP-10-C untouched.
</success_criteria>

<output>
Create `.planning/phases/10-sidebar-visual-polish/10-05-SUMMARY.md` when done — and it MUST include the WR-02 end-to-end chain trace and conclusion (Task 2 acceptance).
</output>
