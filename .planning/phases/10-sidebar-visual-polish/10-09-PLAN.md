---
phase: 10-sidebar-visual-polish
plan: 09
type: execute
wave: 6
depends_on: []
files_modified:
  - tests/ui-lab/surfaces.ts
  - src/renderer/__tests__/sidebar-agent-attr.test.ts
autonomous: true
requirements: [UI-02]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "The ui-lab harness deterministically asserts name completeness (scrollWidth <= clientWidth on .row-name, and/or a long-name fixture) so name-crush regressions are machine-checked, not eye-scored from PNGs (GAP-10-F)"
    - "The sidebar-agent-attr test imports SessionStatus from the correct path so its typed completeness guard is actually enforceable (WR-03)"
    - "The ui-lab sidebar-waiting surface cleans up its fabricated data-agent='waiting' attribute so amber does not leak into subsequent captures (WR-04)"
    - "The ui-lab sidebar-waiting surface asserts the data-agent attribute actually landed before capturing — a silent no-op can no longer produce a false-success screenshot (WR-05)"
  artifacts:
    - path: "tests/ui-lab/surfaces.ts"
      provides: "Name-completeness assertion + sidebar-waiting cleanup hook + waitUntil-the-attribute-landed guard"
      contains: "scrollWidth"
    - path: "src/renderer/__tests__/sidebar-agent-attr.test.ts"
      provides: "Corrected SessionStatus import path so the completeness type guard is enforceable"
      contains: "../../shared/types"
  key_links:
    - from: "tests/ui-lab/surfaces.ts sidebar-waiting prepare"
      to: "the captured row's data-agent attribute"
      via: "browser.waitUntil the attribute landed + cleanup removes it"
      pattern: "waitUntil"
    - from: "the name-completeness assertion"
      to: ".row-name overflow"
      via: "scrollWidth <= clientWidth check (or long-name fixture)"
      pattern: "scrollWidth"
---

<objective>
Close GAP-10-F (harness improvement the operator explicitly asked for: "why isnt the visualization harness used here to verify whether name is complete?") plus the three related ui-lab/test-quality code-review WARNINGs WR-03, WR-04, WR-05 — one cohesive harness/test-quality concern.

- **GAP-10-F:** The SC2 name-legibility verdict is currently eye-scored from PNGs only. Add a deterministic machine-checked assertion (`scrollWidth <= clientWidth` on `.row-name`, and/or a long-name fixture asserting no visible overflow) so future name-crush regressions fail automatically.
- **WR-03:** `sidebar-agent-attr.test.ts` imports `SessionStatus` from `'../shared/types'` (resolves to nonexistent `src/renderer/shared/types`); every sibling test uses `'../../shared/types'`. It passes silently (type-only import erased by esbuild + tsconfig excludes `__tests__`), so the typed completeness guard is unenforced.
- **WR-04:** The ui-lab `sidebar-waiting` surface pokes `data-agent='waiting'` onto a real row and never removes it — the amber leaks into the subsequent `inactive-recipes` and `sidebar-collapsed` captures, fabricating an amber-reserved signal no real state produced.
- **WR-05:** The same surface's `row?.setAttribute('data-agent','waiting')` has no assertion the attribute (or the amber paint) landed — a silent no-op produces a false-success "captured ok" screenshot of a plain row.

Purpose: Make name-crush machine-checked (the operator's direct request) and make the amber-evidence surface honest (no leak, no false success), so the visual-evidence chain the project relies on for Phases 10-13 is trustworthy.

Output: A self-checking ui-lab name-completeness assertion + a robust, cleaned-up, asserted sidebar-waiting surface + a corrected typed test import.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sidebar-visual-polish/10-REVIEW.md
@.planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md
@.planning/phases/10-sidebar-visual-polish/10-CONTEXT.md
@.planning/phases/10-sidebar-visual-polish/10-PATTERNS.md
</context>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<artifacts_produced>
## Artifacts this phase produces (for source-grounding drift verification)

New symbols / additions created by this plan:
- A name-completeness assertion helper inside `tests/ui-lab/surfaces.ts` (e.g. an `assertNameNotCrushed()` local utility) and a long-name fixture in the `sidebar-populated` surface (a session whose name exceeds the rail width, used to exercise the ellipsis budget)
- A `cleanup` hook added to the existing `sidebar-waiting` surface entry
- A `browser.waitUntil` attribute-landed guard added to the existing `sidebar-waiting` surface `prepare`

NOTE: Reuses existing harness symbols (`Surface`, `SurfaceContext`, `SkipSurface`, `addSession`, `rowIds`, `rowText`, `setInputByTestId`, `openEditModal`, `clickByTestId`, `waitForTestIdGone`, `browser.execute`) and the existing `rowAgentAttr`/`SessionStatus` types — those are NOT new. The `surfaces.ts` registry order and the existing surface ids are unchanged.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add a deterministic name-completeness assertion to the ui-lab harness (GAP-10-F)</name>
  <files>tests/ui-lab/surfaces.ts</files>
  <read_first>
    - tests/ui-lab/surfaces.ts (the full registry: the `sidebar-populated` surface lines ~184-221 where named sessions are created via `openEditModal` + `setInputByTestId('edit-name', ...)`; the local driving utilities `rowIds`, `rowText`, `addSession`, `setInputByTestId`; the `Surface` interface with `prepare`/`available`/`cleanup`)
    - tests/ui-lab/helpers.ts (the capture flow — `shoot`, `writeManifest`, `waitForFonts`; how surfaces are driven and recorded, so the assertion throws a clear failure rather than a silent skip)
    - src/renderer/sidebar.css (`.sidebar-row .row-name` lines ~132-139: `overflow: hidden; text-overflow: ellipsis; white-space: nowrap` — the ellipsis mechanism the assertion validates; the name is "complete" when scrollWidth <= clientWidth, i.e. NOT truncated)
    - .planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md (GAP-10-F missing-item: "scrollWidth <= clientWidth on .row-name, or a long-name fixture asserting no visible overflow")
    - $HOME/.claude/rules/testing-policy.md (wait on observable state, never sleep; assert observable DOM, not internals)
  </read_first>
  <action>
    Add a deterministic name-completeness check to the ui-lab harness so name-crush is machine-detected. Two complementary pieces:

    1. A reusable assertion utility in surfaces.ts, e.g. `assertNameNotCrushed(id: string)`, that runs `browser.execute` to read the row's `.row-name` element and returns `{ scrollWidth, clientWidth, text }`; assert that for a MEDIUM-length name (one that should fit) `scrollWidth <= clientWidth` (the name is fully shown, no truncation). For the canonical "Parlour Claude" session already created in `sidebar-populated`, the name MUST NOT be truncated at rest — assert it. Throwing a clear Error (not SkipSurface) when crushed makes the harness FAIL on a name-crush regression instead of eye-scoring.

    2. Add a LONG-name fixture: in `sidebar-populated` (or a dedicated step within it), create a session whose name is deliberately long (e.g. "Marketing Parlour Long Session Name For Overflow Test") and assert the overflow is handled by ELLIPSIS gracefully — the `.row-name` IS allowed to truncate, but only via ellipsis, with `clientWidth` bounded by the row's content box and no layout break. Concretely: assert the long-name row's `.row-name` `clientWidth` is > 0 and that the computed `text-overflow` is `ellipsis` (an overlong name degrades gracefully, never crushing the rest of the layout).

    Call the medium-name assertion at the point in `sidebar-populated.prepare` where the renamed "Parlour Claude" row is confirmed present (after the existing `waitUntil rowText includes 'Parlour Claude'`). Keep all existing surface behavior and the registry order intact. Do NOT add a `browser.pause`-only wait for the assertion — gate it on the observable rename landing.
  </action>
  <verify>
    <automated>grep -q "scrollWidth" tests/ui-lab/surfaces.ts && grep -Eq "row-name" tests/ui-lab/surfaces.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - surfaces.ts contains a name-completeness assertion using `scrollWidth`/`clientWidth` on `.row-name` (grep: `scrollWidth` present).
    - The medium-length "Parlour Claude" name is asserted NOT truncated (`scrollWidth <= clientWidth`) at the point its rename is confirmed in `sidebar-populated`.
    - A long-name fixture exists and asserts graceful ellipsis truncation (computed `text-overflow: ellipsis`, bounded clientWidth, no layout break) — NOT a hard crush.
    - The assertion throws a descriptive Error (not SkipSurface) on a crush so the run FAILS loudly.
    - `npx tsc --noEmit` exits 0; no `browser.pause`-only wait was added for the assertion (it gates on observable state).
  </acceptance_criteria>
  <done>The ui-lab harness machine-checks that the session name is complete (medium name not truncated; long name degrades to ellipsis, never crushed) — name-crush regressions now fail the run automatically.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the sidebar-waiting surface (WR-04 cleanup + WR-05 assert-landed) and fix the WR-03 import path</name>
  <files>tests/ui-lab/surfaces.ts, src/renderer/__tests__/sidebar-agent-attr.test.ts</files>
  <read_first>
    - tests/ui-lab/surfaces.ts (the `sidebar-waiting` surface lines ~320-348: the `data-agent='waiting'` poke via `row?.setAttribute`, the `browser.pause(300)` settle, and the MISSING cleanup hook + MISSING landed-assertion; the `inactive-recipes` and `sidebar-collapsed` surfaces that run AFTER it and inherit the leaked amber)
    - src/renderer/__tests__/sidebar-agent-attr.test.ts (line ~16: `import type { SessionStatus } from '../shared/types'` — the WRONG path; compare to siblings using `'../../shared/types'`)
    - src/renderer/__tests__/icon-spec.test.ts or src/renderer/__tests__/session-switch.test.ts (the correct `'../../shared/types'` import idiom to mirror)
    - .planning/phases/10-sidebar-visual-polish/10-REVIEW.md (WR-04 fix: add a `cleanup` hook removing `data-agent`; WR-05 fix: `browser.waitUntil` that `getAttribute('data-agent') === 'waiting'` before capture; IN-01 note: the WR-05 waitUntil subsumes most of the `pause(300)` hedge)
    - $HOME/.claude/rules/testing-policy.md (wait on observable state, never sleep)
  </read_first>
  <action>
    WR-03: In `sidebar-agent-attr.test.ts`, change the import from `import type { SessionStatus } from '../shared/types'` to `import type { SessionStatus } from '../../shared/types'` so the typed `const nonRunning: SessionStatus[]` completeness annotation actually resolves (matches every sibling test). The 8 cases must still pass.

    WR-05: In the `sidebar-waiting` surface `prepare`, capture the poked id into a local variable, and after `row.setAttribute('data-agent', 'waiting')` add a `browser.waitUntil` (timeout ~2000ms, interval ~100ms, descriptive timeoutMsg) that re-reads the row via `browser.execute` and returns true only when `getAttribute('data-agent') === 'waiting'` — so a silent no-op (selector miss / stale id) FAILS the surface instead of capturing a plain row. Once the waitUntil gates the paint, reduce or remove the bare `browser.pause(300)` per IN-01 (keep at most a short settle if WebGL paint timing demands it).

    WR-04: Add a `cleanup` hook to the `sidebar-waiting` Surface entry that removes the fabricated attribute using the recorded poked id: `browser.execute` to `querySelector('.sidebar-row[data-session-id="<poked id>"]')?.removeAttribute('data-agent')`. Do NOT use `ctx.ids[ctx.ids.length-1]` (the list may grow before cleanup runs) — use the id recorded in `prepare`. This stops the amber leaking into the `inactive-recipes` and `sidebar-collapsed` captures that run after it. Keep the surface id, title, designRefs, and expects unchanged; keep the registry order unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/__tests__/sidebar-agent-attr.test.ts && grep -q "'../../shared/types'" src/renderer/__tests__/sidebar-agent-attr.test.ts && grep -q "waitUntil" tests/ui-lab/surfaces.ts && grep -q "cleanup" tests/ui-lab/surfaces.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `sidebar-agent-attr.test.ts` imports `SessionStatus` from `'../../shared/types'` (grep-confirmed); the 8 cases still pass under `vitest run`.
    - The `sidebar-waiting` surface `prepare` records the poked id and has a `browser.waitUntil` asserting `getAttribute('data-agent') === 'waiting'` before capture (grep: `waitUntil` present in surfaces.ts; the bare `pause(300)` is reduced/removed per IN-01).
    - The `sidebar-waiting` Surface entry has a `cleanup` hook that `removeAttribute('data-agent')` on the recorded poked id (grep: `cleanup` present near the sidebar-waiting block; uses the recorded id, not `ctx.ids[length-1]`).
    - `npx tsc --noEmit` exits 0; no `console.*` added; no sleep-only wait added.
  </acceptance_criteria>
  <done>WR-03/WR-04/WR-05 are closed: the typed guard resolves, the waiting surface asserts the attribute landed and cleans it up so it never leaks amber into later captures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ui-lab harness fabricated state | The sidebar-waiting surface injects a styling-only attribute into the running app DOM; if it leaks it falsifies downstream visual evidence (a correctness/honesty concern, not an attack surface). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09-01 | Repudiation | ui-lab visual-evidence integrity (fabricated amber leaking / false-success capture) | mitigate | WR-04 cleanup removes the fabricated attribute so downstream captures reflect real state; WR-05 assert-landed prevents a silent no-op false-success screenshot. The amber-evidence chain becomes trustworthy (the project relies on it for Phases 10-13). |
| T-10-09-02 | Tampering | the test-only data-agent DOM poke | accept | The poke is harness code (never shipped) and is presentation-only (an attribute the CSS keys on). It does not touch the PTY, IPC, or persistence. Bounded to the ui-lab run. |
| T-10-09-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan (test/harness edits only). No supply-chain surface. |
</threat_model>

<verification>
- sidebar-agent-attr.test.ts green with the corrected import (8/8).
- tsc clean across the touched test/harness files.
- The name-completeness assertion + the hardened sidebar-waiting surface are exercised in plan 10-10's packaged ui-lab run (the run will now FAIL on a name crush and will not leak amber).
</verification>

<success_criteria>
- Name-crush is machine-checked in the ui-lab harness (GAP-10-F closed) — the operator's direct request satisfied.
- The typed completeness guard in sidebar-agent-attr.test.ts is enforceable (WR-03 closed).
- The sidebar-waiting surface cleans up after itself (WR-04 closed) and asserts the attribute landed (WR-05 closed).
- No shipped code changed; no selector contract broken; tsc + the unit test green.
</success_criteria>

<output>
Create `.planning/phases/10-sidebar-visual-polish/10-09-SUMMARY.md` when done. Record the name-completeness assertion shape (medium-not-truncated + long-name-ellipsis), the WR-03 import fix, the WR-04 cleanup hook, the WR-05 waitUntil, and confirm sidebar-agent-attr + tsc green. Note the assertions are exercised in the 10-10 packaged capture.
</output>
