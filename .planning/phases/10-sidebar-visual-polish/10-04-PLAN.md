---
phase: 10-sidebar-visual-polish
plan: 04
type: execute
wave: 3
depends_on: ["10-02", "10-03"]
files_modified: []
autonomous: false
requirements: [UI-02]

must_haves:
  truths:
    - "A full packaged, no-injection ui-lab capture exists for the Phase-10 surfaces and is scored against DESIGN-RUBRIC.md with pixel-cited verdicts"
    - "The full test suite (unit incl. tokens-completeness + tsc + eslint + smoke) is GREEN against the packaged app"
    - "A human verifies the running app against SC1-SC4 and signs off (no visual claim is accepted on automated green alone — the CONTEXT verification protocol)"
  artifacts:
    - path: ".planning/phases/10-sidebar-visual-polish/10-04-SUMMARY.md"
      provides: "The phase-gate evidence: before/after capture pairs, rubric scores, suite results, human sign-off"
  key_links:
    - from: "the packaged app"
      to: "artifacts/ui-lab/p10-after/"
      via: "npm run ui:shots:fresh — packaged no-injection capture (proof, not preview)"
      pattern: "ui:shots:fresh"
---

<objective>
Close Phase 10 with the mandatory verification protocol from 10-CONTEXT: a full packaged
no-injection ui-lab capture scored against DESIGN-RUBRIC.md, the full automated suite GREEN
against the packaged app, and a blocking human-verify in the running app. No visual claim is
accepted on automated green alone (the operator's "still ugly" Phase-9 verdict is exactly why
this gate exists).

Purpose: Plans 01-03 produced the code; this gate proves the three Phase-9 sidebar gaps are
closed (Gap 1 name-crush, Gap 4 context-menu position + danger, Gap 5 unanchored inactive rows)
and SC1-SC4 hold against real packaged pixels and a human's eyes, before the phase is marked
verified.

Output: `10-04-SUMMARY.md` with the before/after PNG pairs, the rubric gap table, the suite
results, and the recorded human sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sidebar-visual-polish/10-CONTEXT.md
@.planning/phases/10-sidebar-visual-polish/10-UI-SPEC.md
@.planning/phases/10-sidebar-visual-polish/10-02-SUMMARY.md
@.planning/phases/10-sidebar-visual-polish/10-03-SUMMARY.md
@tests/ui-lab/README.md
@tests/ui-lab/DESIGN-RUBRIC.md
</context>

<artifacts_this_phase_produces>
This plan creates no source symbols. It produces capture artifacts under
`artifacts/ui-lab/p10-after/` (gitignored) and the `10-04-SUMMARY.md` evidence record.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Full suite + packaged ui-lab capture scored against the rubric</name>
  <files>(no source files — runs the suite + capture harness)</files>
  <read_first>
    - tests/ui-lab/README.md (the self-evolve loop + Hard Rules: no claim without a capture; injection is preview, packaged is proof; the ui:shots:fresh requirement after TSX changes)
    - tests/ui-lab/DESIGN-RUBRIC.md (the per-surface checklist incl. the Phase-10 sections added in Plan 02 — score every line PASS/PARTIAL/FAIL with pixel evidence)
    - .planning/phases/10-sidebar-visual-polish/10-UI-SPEC.md (the SC1-SC4 contract values to score against)
    - .planning/phases/09-design-token-foundation/09-HUMAN-UAT.md (Gaps 1/4/5 — the BEFORE specifics this phase closes; the after-capture must show each gap resolved)
  </read_first>
  <action>
    Run the full automated suite against the packaged app and produce the scored visual evidence:
    1. `npm run test:unit` (incl. tokens-completeness scanning sidebar.css) — must be GREEN.
    2. `npx tsc --noEmit` and `npm run lint` — clean.
    3. `npm run test:smoke` — GREEN, with explicit attention to the selector-contract smokes
       (session-edit, app-restart-restore, keyboard-switch, reorder, sidebar-collapse) and the
       context-menu-driven smoke (header-controls).
    4. `UI_LAB_TAG=p10-after npm run ui:shots:fresh` — packaged, no-injection capture (proof).
    5. Read `artifacts/ui-lab/p10-after/manifest.json` then each PNG (multimodal Read). Score
       `sidebar-populated`, `sidebar-collapsed`, `context-menu`, `inactive-recipes` (+
       `sidebar-waiting` if it was drivable, else note the SkipSurface) against
       DESIGN-RUBRIC.md — one PASS/PARTIAL/FAIL line per rubric line WITH pixel evidence (never
       cite CSS source). Pair each against the `artifacts/ui-lab/before-phase10/` baseline from
       Plan 01.
    6. Build the gap table (surface | rubric line | verdict | evidence | severity | fix route).
       If S1/S2 gaps remain, iterate per the ui-lab loop (tokens/sidebar.css value tweaks via
       UI_LAB_LIVE_CSS preview, then re-prove with ui:shots:fresh) until the active-distinction
       (SC1), two-line hierarchy (SC2), collapsed continuity (SC3), recipe cards (D-11), and
       context-menu position+danger (D-14/D-15) all score PASS. Capture the final before/after
       pairs for the SUMMARY.
  </action>
  <verify>
    <automated>npm run test:unit && npx tsc --noEmit && npm run lint && npm run test:smoke</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` exits 0 (tokens-completeness GREEN, scanning sidebar.css)
    - `npx tsc --noEmit` exits 0 and `npm run lint` exits 0
    - `npm run test:smoke` exits 0 (all selector-contract + context-menu smokes GREEN)
    - `artifacts/ui-lab/p10-after/manifest.json` exists and the sidebar-populated / sidebar-collapsed / context-menu / inactive-recipes PNGs are present
    - A rubric gap table is recorded in 10-04-SUMMARY.md with pixel-cited verdicts; SC1 (active distinction), SC2 (two-line hierarchy / Gap 1), Gap 4 (menu position + danger), Gap 5 (recipe cards) score PASS
  </acceptance_criteria>
  <done>Full suite GREEN against the packaged app; packaged capture scored against the rubric with the three Phase-9 gaps shown closed; before/after pairs ready for the human gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: End-of-phase human-verify of the sidebar in the running app (SC1-SC4)</name>
  <files>(none — human inspection of the running app)</files>
  <read_first>
    - .planning/phases/10-sidebar-visual-polish/10-UI-SPEC.md (the SC1-SC4 contract being verified)
    - .planning/phases/09-design-token-foundation/09-HUMAN-UAT.md (the Gap 1/4/5 BEFORE state)
  </read_first>
  <action>
    Pause for a BLOCKING human verification of the running app. Present the before/after ui-lab
    capture pairs from Task 1, then walk the operator through the how-to-verify script below in
    the packaged (or `npm start`) app. Do NOT mark the phase verified or flip nyquist_compliant
    in 10-VALIDATION.md until the operator types "approved". If the operator reports gaps,
    capture them verbatim for a gap-closure plan rather than accepting on automated green
    (the 06.1/08 precedent — automated green is not proof for a visual phase).
  </action>
  <what-built>
    The Phase-10 sidebar restyle: two-line rows with a large icon tile + full-width name + a
    status dot/word secondary line (Gap 1 fixed); a filled, status-color-edge-barred, lifted
    active card (SC1); an amber "Waiting for you" treatment on waiting rows incl. non-active ones
    (D-09); dashed eggshell recipe cards with an always-visible ghost ▶ Start in the Inactive
    List (Gap 5 fixed); small-caps section labels with counts; a viewport-clamped context menu
    whose Remove/Delete item is danger-red (Gap 4 fixed); and a collapsed icon-only rail that
    keeps the active marker + per-row status dot legible (SC3). Click-to-switch / drag-reorder /
    keyboard switching / terminal keep-alive unchanged (SC4).
  </what-built>
  <how-to-verify>
    1. Launch the packaged app: `npm run make` then open the built app
       (`out/Just-Wrapper-darwin-arm64/Just-Wrapper.app` on macOS), OR `npm start` for a dev run.
    2. Create 4-5 sessions across both buckets — at least one running, one named/iconed
       (e.g. 🛋️ Parlour Claude), one finished (type `exit`), and one configured-then-Removed so
       it lands in the Inactive List as a recipe.
    3. SC1 — glance at the sidebar: can you instantly tell which session is ACTIVE? It must read
       as a filled card with a colored left edge bar + a subtle lift, clearly distinct from the
       flat inactive rows.
    4. SC2 / Gap 1 — every row shows its FULL name on line 1 (no longer crushed to one
       character) and a status dot + word (+ cwd tail / startup command) on line 2.
    5. D-09 — if you have a session waiting for input (e.g. a `claude --rc` permission prompt),
       its row reads amber with "Waiting for you", and an amber/backgrounded waiting session
       shows the amber even while you are on another session.
    6. Gap 5 / D-11/D-13 — the Inactive List rows are dashed "recipe" cards with an always-
       visible circular ▶ Start that fills blue on hover; section labels read WORKING AREA · n /
       INACTIVE · n.
    7. Gap 4 / D-14/D-15 — right-click a row near the TOP-LEFT of the sidebar: the menu opens at
       the cursor and stays fully on-screen (does NOT overlap the sidebar header); the
       Remove/Delete item is red.
    8. SC3 — collapse the rail (chevron): the active tile stays lifted with its edge bar, each
       row keeps a status dot, and no secondary text leaks; expand again.
    9. SC4 / Core Value — click between sessions, drag to reorder, use Cmd/Ctrl+1-9 to switch:
       switching is instant, the terminal content is preserved (keep-alive), and a running
       `claude --rc` / `vim` / REPL still behaves exactly like a native terminal. NOTHING about
       terminal fidelity may have regressed.
  </how-to-verify>
  <verify>
    <human-check>The operator confirms SC1-SC4 + the three Phase-9 gaps closed in the running app and types "approved"; gaps, if any, are recorded for a gap-closure plan.</human-check>
  </verify>
  <resume-signal>Type "approved" to flip the Phase-10 Nyquist gate and close the phase, or describe the gaps to route into a gap-closure plan.</resume-signal>
  <done>The operator has inspected the running app against the SC1-SC4 script and signed off (or named the gaps); nyquist_compliant flips true only on explicit approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | A verification-only plan: it runs the existing test suite and the ui-lab capture harness, then a human inspects the running app. No source change, no IPC, no new bridge key — `EXPECTED_API_KEYS` stays 20. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-19 | Spoofing | — | accept | No auth/identity surface (ASVS V2/V3 N/A). |
| T-10-20 | Tampering | — | accept | No code change; the ui-lab harness writes only to the gitignored isolated userData (JW_USER_DATA_DIR), never the dev's real session store. |
| T-10-21 | Repudiation | — | accept | The human sign-off is recorded in 10-04-SUMMARY.md (the 08-HUMAN-UAT precedent). |
| T-10-22 | Information disclosure | — | accept | Captures contain only locally-created test sessions; no secret. |
| T-10-23 | Denial of service | — | accept | One packaged build + one capture run; bounded. |
| T-10-24 | Elevation of privilege | — | accept | No main-process code touched; contextIsolation + the 20-key bridge stay frozen. |
| T-10-SC | Tampering (supply chain) | npm installs | accept | ZERO packages installed; no install task. |
</threat_model>

<verification>
- The automated gate: `npm run test:unit && npx tsc --noEmit && npm run lint && npm run test:smoke`
  all GREEN against the packaged app.
- The visual gate: a packaged no-injection `ui:shots:fresh` capture scored PASS on SC1-SC4 and
  the three Phase-9 gaps vs DESIGN-RUBRIC.md, with before/after pairs.
- The human gate (blocking): the operator signs off in the running app per the how-to-verify
  script. nyquist_compliant in 10-VALIDATION.md stays FALSE until this explicit approval (the
  06.1/08 precedent — automated green is not proof for a visual phase).
</verification>

<success_criteria>
- Full automated suite GREEN against the packaged app.
- Packaged ui-lab capture scored PASS on SC1 (active distinction), SC2 (two-line hierarchy /
  Gap 1 closed), Gap 4 (menu position + danger), Gap 5 (recipe cards), SC3 (collapsed
  continuity) — with pixel-cited evidence and before/after pairs.
- Human sign-off recorded; if gaps surface, they are captured for a gap-closure plan rather than
  silently accepted.
- Core Value intact: terminal fidelity unregressed; click-switch / drag-reorder / keyboard
  switching / keep-alive all confirmed working.
</success_criteria>

<output>
Create `.planning/phases/10-sidebar-visual-polish/10-04-SUMMARY.md` when done (include the
before/after PNG paths, the rubric gap table, the suite results, and the recorded human sign-off).
</output>
