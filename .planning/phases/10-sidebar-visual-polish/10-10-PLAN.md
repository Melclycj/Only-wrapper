---
phase: 10-sidebar-visual-polish
plan: 10
type: execute
wave: 8
depends_on: [10-07, 10-08, 10-09, 10-11]
files_modified:
  - .planning/phases/10-sidebar-visual-polish/10-VALIDATION.md
autonomous: false
requirements: [UI-02]
gap_closure: true
user_setup: []

must_haves:
  truths:
    - "The full automated suite (unit + tsc + lint + smoke) is GREEN against the PACKAGED app"
    - "A fresh packaged no-injection ui-lab capture is produced via `npm run ui:shots:fresh`, completing 11/11 surfaces with the now-asserting name-completeness check (GAP-10-G fix from 10-11 confirmed — the active-row medium name is no longer crushed) and the hardened sidebar-waiting surface (no leaked amber)"
    - "A human re-verifies the running app: live amber fires at a real claude --rc permission prompt (GAP-10-D), the leading gutter is compact (GAP-10-E), and names are complete on the active row (GAP-10-G) — and signs off with an EXPLICIT, UNQUALIFIED 'approved' (no 'partial', no 'with adjustments')"
    - "nyquist_compliant flips to true in 10-VALIDATION.md ONLY on that unqualified approval; on any qualification it stays false and the items route to a new gap-closure round"
  artifacts:
    - path: ".planning/phases/10-sidebar-visual-polish/10-VALIDATION.md"
      provides: "The human-gate history row for attempt 3 + the nyquist_compliant flag state"
      contains: "nyquist_compliant"
  key_links:
    - from: "the packaged app"
      to: "the ui-lab capture + the human gate"
      via: "npm run ui:shots:fresh produces the proof capture; the operator drives the live verification"
      pattern: "ui:shots:fresh"
---

<objective>
The closing gate for Phase 10's gap-closure rounds. Re-run the full evidence chain after 10-07 (GAP-10-D live amber fix), 10-08 (GAP-10-E gutter + WR-01/WR-02 CSS), 10-09 (GAP-10-F harness + WR-03/WR-04/WR-05), and 10-11 (GAP-10-G active-row name-crush CSS fix) land, then put the running app in front of the operator for a BLOCKING human re-verify.

GAP-10-G note (round-3 scope): the 10-09 `assertNameNotCrushed` machine check caught the active row crushing a medium name (controlsWidth=52px, "Parlour Claude" scrollWidth=98 > clientWidth=51) at this gate's first Task-1 run, blocking it 10/11. Plan 10-11 collapses the active row's permanently-revealed controls to zero reserved width at rest (D-02). This re-run's `npm run ui:shots:fresh` MUST now complete 11/11 surfaces with the name-completeness assertion passing on the active row — that is the precondition for even presenting the app to the human gate. The gate's BLOCKING human-verify semantics are unchanged.

The phase gate flips to compliant ONLY on an explicit, unqualified "approved" — no "partial", no "with adjustments". This is the third human gate (attempts 1 and 2 both returned NOT APPROVED). Automated green is NOT proof for a visual phase (the 06.1/08/10 precedent — the prior two gates each caught a live failure the static capture passed). The ui-lab harness is the mandatory visual-evidence tool for this project (Phases 10-13).

Purpose: Close UI-02. The blocking S1 item (GAP-10-D live amber) and the operator's adjustments (GAP-10-E gutter, GAP-10-F harness, GAP-10-G active-row name completeness) must all be confirmed in the running app by a human, because no automated assertion can substitute for the live-agent amber-detection behavior or the "feels right" gutter proportion.

Output: A recorded gate verdict. On unqualified approval: nyquist_compliant true, UI-02 complete, Phase 10 closeable. On any qualification: verbatim capture + route to a new gap-closure round.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sidebar-visual-polish/10-VALIDATION.md
@.planning/phases/10-sidebar-visual-polish/10-VERIFICATION.md
@.planning/phases/10-sidebar-visual-polish/10-06-SUMMARY.md
@.planning/phases/10-sidebar-visual-polish/10-04-PLAN.md
@tests/ui-lab/README.md
@tests/ui-lab/DESIGN-RUBRIC.md
</context>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<artifacts_produced>
## Artifacts this phase produces (for source-grounding drift verification)

No NEW code symbols — this plan runs the suite, produces a packaged ui-lab capture (artifacts under `artifacts/ui-lab/` are gitignored), and records the human-gate verdict by editing `10-VALIDATION.md` (the Human Gate History table + the `nyquist_compliant` frontmatter flag). It edits no `src/` files. The verdict record IS the deliverable (mirrors 10-06-SUMMARY.md).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Full suite GREEN against the packaged app + fresh no-injection ui-lab capture (11/11 surfaces) rescored against the rubric</name>
  <files>(verification-only — no source files modified; produces gitignored capture artifacts)</files>
  <read_first>
    - tests/ui-lab/README.md (the loop + hard rules: injection is preview, packaged is proof; tokens first; suite stays green; no claim without a capture)
    - tests/ui-lab/DESIGN-RUBRIC.md (the screenshot-checkable expectations the capture is scored against — specifically SC1 active-distinct, SC2 name-legibility, D-09 amber edge bar + wash, D-11 dashed recipe cards, collapsed continuity)
    - .planning/phases/10-sidebar-visual-polish/10-06-SUMMARY.md (the prior gate's capture tag `p10-gapfix-after`, the rubric rescore table format, the static-capture caveat that the human gate covers the live items)
    - .planning/phases/10-sidebar-visual-polish/10-07-SUMMARY.md (the GAP-10-D fix — what to expect live)
    - .planning/phases/10-sidebar-visual-polish/10-08-SUMMARY.md (the gutter/CSS deltas to look for in the capture)
    - .planning/phases/10-sidebar-visual-polish/10-09-SUMMARY.md (the name-completeness assertion the capture run now enforces)
    - .planning/phases/10-sidebar-visual-polish/10-11-SUMMARY.md (the GAP-10-G active-row control-collapse fix — the active-row name should now read full; read once 10-11 has run)
  </read_first>
  <action>
    Build the packaged app and run the full evidence chain against it (NOT the dev server). Run, in order, capturing real stdout for the SUMMARY:
    - `npm run test:unit` — the full vitest suite (must be all-green, including the 10-07 real-frame regression test, the 10-09 corrected sidebar-agent-attr import, tokens-completeness scanning sidebar.css, and the agent-state-replay oracle still at "exactly 1 WAITING").
    - `npx tsc --noEmit` — exit 0.
    - `npm run lint` — only the known pre-existing `.planning/spikes/*.cjs` errors are tolerated (logged in deferred-items.md); all Phase-10 source/test files must lint clean, and the 10-07 instrumentation must be gone or dev-gated (no unconditional console in the shipped agent path).
    - `npm run test:smoke` — 15/15 spec files green against the packaged binary (keyboard-switch, reorder, session-edit, app-restart-restore, sidebar-collapse — the SC4 keep-alive guard).
    - `UI_LAB_TAG=p10-gapfix-round3 npm run ui:shots:fresh` — the packaged, no-injection capture. The run ENFORCES the 10-09 name-completeness assertion (it FAILS if a name is crushed) and the hardened sidebar-waiting surface. With the 10-11 GAP-10-G fix landed, the run MUST now complete 11/11 surfaces (the prior round-2 run failed 10/11 on `sidebar-populated` because the active-row controls crushed "Parlour Claude").

    Then rescore the rubric against the fresh capture with pixel-cited verdicts, focusing on: SC2 name-legibility (the active-row medium name now reads in full — GAP-10-G; the gutter is compacted — GAP-10-E; the harness name-completeness assertion passed 11/11), D-09 amber on sidebar-waiting.png (still PASS, now with no leak into later shots), and the inactive-recipes / sidebar-collapsed captures showing NO fabricated amber. Record the static-capture caveat: the capture proves the resting/populated/waiting STYLING but cannot exercise a real agent process — that is exactly what Task 2's human gate covers (the live GAP-10-D amber).
  </action>
  <verify>
    <automated>npm run test:unit && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` is all-green (stdout pasted in the SUMMARY), including the 10-07 real-frame regression test and the agent-state-replay oracle at "exactly 1 WAITING".
    - `npx tsc --noEmit` exits 0; `npm run lint` clean on Phase-10 files (only the known pre-existing spike .cjs errors tolerated; no unconditional console in the shipped agent path).
    - `npm run test:smoke` 15/15 green against the packaged binary.
    - `UI_LAB_TAG=p10-gapfix-round3 npm run ui:shots:fresh` completes 11/11 surfaces with a manifest.json, the name-completeness assertion ENFORCED and PASSING on the active row (GAP-10-G closed — was 10/11 in round-2), and the sidebar-waiting surface asserting + cleaning up (no leaked amber in subsequent captures).
    - The rubric is rescored with pixel-cited verdicts for SC2 (active-row name complete + gutter compact), D-09 (amber, no leak), and the recipe/collapsed surfaces.
  </acceptance_criteria>
  <done>The full automated suite is green against the packaged app and a fresh no-injection capture completes 11/11 surfaces (with the new harness assertions active and the GAP-10-G active-row name fit confirmed), rescored against the rubric.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING human re-verify of the running app — unqualified "approved" required (phase gate)</name>
  <files>(no source files — human verification checkpoint; the verdict is recorded by Task 3 into 10-VALIDATION.md)</files>
  <action>
    This is a BLOCKING human-verify checkpoint — do NOT auto-approve regardless of any auto-advance / yolo setting (visual phase gate; 06.1/08/10 precedent: automated green is not proof, and the prior two gates each caught a live failure the static capture passed). Build + launch the packaged app, present the how-to-verify script below to the operator, and WAIT for an explicit verdict. Capture the operator's response VERBATIM for Task 3. The checkpoint detail is in <what-built> / <how-to-verify> / <resume-signal> below.
  </action>
  <what-built>
    GAP-10-D (live amber): the diagnosed broken link in the agent-state chain was fixed (10-07) so a real claude --rc permission prompt now turns the sidebar row amber. GAP-10-E (gutter): the leading drag-handle + icon-tile gutter was compacted (10-08) so the session name gets more width; WR-01/WR-02 made hidden-control width truly zero. GAP-10-F (harness): the ui-lab harness now machine-checks name completeness, and the amber-evidence surface is hardened (10-09). GAP-10-G (active-row name crush): the active row's permanently-revealed Edit/Close controls (52px) were collapsed to zero reserved width at rest (10-11), so a medium name reads in full on the active row too — they reveal on hover/keyboard focus. The full suite is green against the packaged app and the fresh capture completes 11/11 surfaces, rescored PASS.
  </what-built>
  <how-to-verify>
    Build + launch the PACKAGED app (`npm run make` then open `out/Just-Wrapper-darwin-arm64/Just-Wrapper.app`). Run the full verification script from 10-04-PLAN.md plus the gap items:

    ITEM A — Live amber waiting (GAP-10-D, the S1 blocker):
    1. Create a session and run `claude --rc` in a real project directory.
    2. Ask claude to do something that ends in a tool-permission prompt — e.g. trigger a Web Search permission prompt (the "Tool use / Web Search / Do you want to proceed? / ❯ 1. Yes / 2. Yes, don't ask again / 3. No / Esc to cancel · Tab to amend" screen).
    3. Within ~1-2 seconds of the prompt appearing, the sidebar row for that session must show an AMBER left edge bar + a light amber wash, and line 2 must read "Waiting for you".
    4. Switch to ANOTHER session (background the claude one) — the amber must STILL show on the backgrounded row in the tab list.
    5. Answer the prompt (or let it resolve) — the amber must clear back to blue/Running once claude resumes working.

    ITEM B — Leading gutter compaction (GAP-10-E):
    6. With sessions of medium-length names, look at the sidebar row left edge (drag handle + icon tile). It should be noticeably more compact than before, leaving more horizontal space for the session name; medium names should read fully (no "Ses…" crush).

    ITEM C — Name completeness, including the ACTIVE row (GAP-10-F / GAP-10-G):
    7. Confirm medium-length names render complete on BOTH inactive AND the ACTIVE row — click a session with a 12-14 char name to make it active; its name must NOT truncate to "Ses…" just because it is active (this was the round-3 GAP-10-G fix: the active row's Edit/Close controls no longer reserve width at rest; they appear on hover or keyboard focus). An overlong name truncates gracefully with an ellipsis (not a hard crush of the rest of the row). Confirm the Edit/Close controls still appear on hover and via keyboard focus on the active row.

    Also re-confirm the previously-approved items did not regress: active row unmistakable (SC1), 5-state status legible (SC2), collapsed rail identity + status dots (SC3), click-to-switch / drag-reorder / keyboard-switch + terminal keep-alive (SC4).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms ITEM A (live amber fires at a real claude --rc permission prompt, including when backgrounded, and clears after), ITEM B (gutter compact), ITEM C (names complete on inactive AND active rows; active-row controls reachable on hover/keyboard focus), and SC1-SC4 unregressed — then types an explicit, unqualified "approved".</human-check>
  </verify>
  <resume-signal>
    Type exactly "approved" (unqualified) to flip the phase gate. ANY qualification — "partial", "approved but…", "approved with adjustments", or a new request — is NOT an approval: it is captured verbatim and routed to a new gap-closure round (the next plan would be 10-12). The Nyquist gate flips true ONLY on an explicit, unqualified "approved".
  </resume-signal>
  <done>The operator has run the live verification script and returned an explicit verdict (captured verbatim for Task 3); on an unqualified "approved" the phase gate is cleared.</done>
</task>

<task type="auto">
  <name>Task 3: Record the gate verdict in 10-VALIDATION.md (flip the flag only on unqualified approval)</name>
  <files>.planning/phases/10-sidebar-visual-polish/10-VALIDATION.md</files>
  <read_first>
    - .planning/phases/10-sidebar-visual-polish/10-VALIDATION.md (the frontmatter `nyquist_compliant: false` + the Human Gate History table with attempts 1 and 2; append attempt 3)
    - .planning/phases/10-sidebar-visual-polish/10-06-SUMMARY.md (the verbatim-capture + routing pattern to mirror on a non-approval)
  </read_first>
  <action>
    Record Task 2's verdict verbatim. On an explicit, UNQUALIFIED "approved": set `nyquist_compliant: true` in the 10-VALIDATION.md frontmatter, add a "3 | 10-10 | <date> | APPROVED | <verbatim quote> → UI-02 complete, Phase 10 closeable" row to the Human Gate History table, and update the "Current state" line to reflect the gate is closed. On ANY qualification or new request: leave `nyquist_compliant: false`, add the attempt-3 row with the verbatim verdict and the routed items (kind / severity / fix-belongs-in), and note the next plan is 10-12. Do NOT touch STATE.md or ROADMAP.md (the orchestrator owns those writes). Do NOT attempt any code fix here — fixes belong to the next gap-closure round if the gate fails.
  </action>
  <verify>
    <automated>grep -Eq "nyquist_compliant:\s*(true|false)" .planning/phases/10-sidebar-visual-polish/10-VALIDATION.md && grep -q "10-10" .planning/phases/10-sidebar-visual-polish/10-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 10-VALIDATION.md has an attempt-3 row in the Human Gate History table citing plan 10-10 and the verbatim operator verdict.
    - `nyquist_compliant` is `true` IF AND ONLY IF the operator gave an explicit unqualified "approved"; otherwise it remains `false` with the routed items recorded.
    - No edits to STATE.md or ROADMAP.md; no source code changed.
  </acceptance_criteria>
  <done>The gate verdict is recorded; the Nyquist flag reflects the operator's exact verdict (true only on unqualified approval).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live agent process → amber detection | The human gate drives a real claude --rc agent rendering attacker-influenceable TUI output through the classify() path — the one boundary the static capture cannot exercise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10-01 | Repudiation | the phase gate (accepting on automated green / a partial verdict) | mitigate | The gate is BLOCKING and human-only; nyquist_compliant flips ONLY on an explicit unqualified "approved" (06.1/08/10 precedent). A partial/new-request verdict is captured verbatim and routed, never silently accepted — this is the control that caught GAP-10-D at the prior gate and GAP-10-G at this gate's Task-1. |
| T-10-10-02 | Denial of Service | classify() on the real agent prompt (the live amber path) | accept | Re-verifies the T-10-07-01 bounded-region mitigation behaves on a real prompt; no new code here. The region/regex bounds were locked in 10-07. |
| T-10-10-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs this plan (suite run + verdict record only). No supply-chain surface. EXPECTED_API_KEYS stays 20. |
</threat_model>

<verification>
- Full suite green against the packaged app (Task 1) with the new harness assertions enforced, completing 11/11 surfaces (GAP-10-G fix confirmed).
- Fresh no-injection capture rescored against the rubric (Task 1).
- BLOCKING human gate confirms live amber (GAP-10-D), compact gutter (GAP-10-E), complete names on inactive AND active rows (GAP-10-F / GAP-10-G) in the running app (Task 2).
- The Nyquist flag is set to match the operator's exact verdict (Task 3).
</verification>

<success_criteria>
- On unqualified approval: nyquist_compliant true, UI-02 complete, Phase 10 closeable — the phase's blocking S1 (live amber) confirmed in the running app and the active-row name confirmed complete.
- On any qualification: verbatim verdict captured, flag stays false, items routed to 10-12 — no silent acceptance.
- The full automated suite + the packaged ui-lab capture (11/11 surfaces, with the new machine-checks) are the evidence backing the gate; the human verdict is the authoritative record.
</success_criteria>

<output>
Create `.planning/phases/10-sidebar-visual-polish/10-10-SUMMARY.md` when done. Record: the full-suite stdout summary, the rescored rubric, the 11/11 surface confirmation, the verbatim human verdict mapped to ITEM A/B/C, the resulting nyquist_compliant state, and — on a non-approval — the routed gap items for 10-12.
</output>
