---
phase: 04-session-identity-sidebar-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/icon-spec.ts
  - src/renderer/session-switch.ts
  - src/renderer/session-edit.ts
  - src/renderer/emoji-set.ts
  - src/main/switch-keys.ts
  - src/shared/api-types.ts
  - src/main/window-config.ts
  - src/preload/index.ts
  - src/main/pty-manager.ts
  - src/renderer/__tests__/icon-spec.test.ts
  - src/renderer/__tests__/session-switch.test.ts
  - src/renderer/__tests__/session-edit.test.ts
  - src/main/__tests__/switch-keys.test.ts
  - src/main/__tests__/pty-update-profile.test.ts
  - src/shared/__tests__/security.guard.test.ts
  - tests/smoke/helpers/xterm-driver.ts
  - tests/smoke/keyboard-switch.smoke.test.ts
  - tests/smoke/session-edit.smoke.test.ts
  - tests/smoke/sidebar-collapse.smoke.test.ts
autonomous: true
requirements: [SESS-01, SESS-03, NAV-05, SESS-02, SESS-04]
must_haves:
  truths:
    - "Pure module matchSwitchKey resolves Cmd/Ctrl+1-9, Cmd/Ctrl+Shift+] and Cmd/Ctrl+Shift+[ to the correct SwitchIntent and returns null for non-chords"
    - "Pure module resolveSwitch maps (sessions, activeId, intent) to the correct next activeId with next/prev wraparound and out-of-range position safety"
    - "Pure module icon-spec builds emoji/color SessionIconSpec and produces the color-initial badge letter"
    - "Pure module session-edit splits a form payload into the live half (name/icon) and the restart half (cwd/shell/startupCommand)"
    - "window.api exposes exactly 15 keys (13 prior + ptyUpdateProfile + onSwitchSession); security guard is GREEN and rejects any extra key or raw ipcRenderer"
    - "PtyManager.updateProfile writes type-guarded name/icon/cwd/shell/startupCommand onto the kept record; unknown id is a no-op; startupCommand is stored, never executed"
    - "After updateProfile sets a non-empty shell, a restart respawns the PTY using that shell (falling back to resolveShell when empty)"
  artifacts:
    - path: "src/main/switch-keys.ts"
      provides: "matchSwitchKey pure matcher + SwitchIntent/KeyInput types (NAV-05, D-12/D-13)"
      exports: ["matchSwitchKey", "SwitchIntent", "KeyInput"]
    - path: "src/renderer/session-switch.ts"
      provides: "resolveSwitch reducer (intent -> next activeId)"
      exports: ["resolveSwitch"]
    - path: "src/renderer/icon-spec.ts"
      provides: "emojiSpec/colorSpec/COLOR_INITIAL icon-spec builders (SESS-03, D-09)"
      exports: ["emojiSpec", "colorSpec", "COLOR_INITIAL"]
    - path: "src/renderer/session-edit.ts"
      provides: "splitEdit live-vs-restart field reducer (D-02)"
      exports: ["splitEdit"]
    - path: "src/renderer/emoji-set.ts"
      provides: "curated emoji array + warm color swatch list (D-08/D-09)"
      exports: ["CURATED_EMOJI", "COLOR_SWATCHES"]
    - path: "src/shared/api-types.ts"
      provides: "ptyUpdateProfile + onSwitchSession on ElectronAPI"
      contains: "ptyUpdateProfile"
    - path: "src/main/window-config.ts"
      provides: "EXPECTED_API_KEYS extended to 15 keys"
      contains: "ptyUpdateProfile"
    - path: "src/main/pty-manager.ts"
      provides: "PtyManager.updateProfile + pty:update-profile channel + create() honoring record.shell"
      contains: "updateProfile"
  key_links:
    - from: "src/preload/index.ts"
      to: "pty:update-profile / session:switch channels"
      via: "ipcRenderer.send + ipcRenderer.on"
      pattern: "pty:update-profile|session:switch"
    - from: "src/main/pty-manager.ts"
      to: "record.shell"
      via: "create() prefers record.shell when non-empty"
      pattern: "record\\.shell"
    - from: "src/shared/__tests__/security.guard.test.ts"
      to: "EXPECTED_API_KEYS"
      via: "Object.keys(exposed) === EXPECTED_API_KEYS"
      pattern: "EXPECTED_API_KEYS"
---

<objective>
Lay the Phase 4 foundation: all Wave 0 RED test stubs (Nyquist), the five React/xterm/electron-free pure modules every later slice consumes, and the ONE atomic bridge-lockstep change that adds `ptyUpdateProfile` + `onSwitchSession` to the contextBridge surface plus the main-side `PtyManager.updateProfile` record write that makes edited cwd/shell/startupCommand take effect on restart.

This is the interface-first plan: it defines the contracts (pure-module signatures + the two new bridge keys + the 15-key guard set) that Plans 02–04 build against, so executors never reverse-engineer them from the codebase.

Purpose: Unblock the create/edit, keyboard-switch, and collapse slices with tested, type-checked seams; satisfy SESS-01's "all fields functional" by persisting restart-applied fields into main (Pitfall 3); satisfy NAV-05's switch math with a Node-testable matcher + reducer (de-risking assumption A1 via the E2E stub).
Output: 5 pure modules, the 15-key bridge surface (api-types + window-config + preload + pty-manager channel triple + guard GREEN), `PtyManager.updateProfile`, `create()` honoring `record.shell`, and all Wave 0 RED stubs + extended E2E driver helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-session-identity-sidebar-ui/04-CONTEXT.md
@.planning/phases/04-session-identity-sidebar-ui/04-RESEARCH.md
@.planning/phases/04-session-identity-sidebar-ui/04-PATTERNS.md
@.planning/phases/04-session-identity-sidebar-ui/04-VALIDATION.md
@src/shared/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 RED test stubs + extended E2E driver helpers (Nyquist)</name>
  <files>src/renderer/__tests__/icon-spec.test.ts, src/renderer/__tests__/session-switch.test.ts, src/renderer/__tests__/session-edit.test.ts, src/main/__tests__/switch-keys.test.ts, src/main/__tests__/pty-update-profile.test.ts, tests/smoke/helpers/xterm-driver.ts, tests/smoke/keyboard-switch.smoke.test.ts, tests/smoke/session-edit.smoke.test.ts, tests/smoke/sidebar-collapse.smoke.test.ts</files>
  <read_first>
    - .planning/phases/04-session-identity-sidebar-ui/04-RESEARCH.md §"Validation Architecture" (the Wave 0 gap list + the Phase Requirements -> Test Map) and §"Assumptions Log" A1
    - .planning/phases/04-session-identity-sidebar-ui/04-VALIDATION.md (the Per-Task Verification Map + Wave 0 Requirements sections you will populate)
    - src/renderer/session-close.ts (pure-reducer test target shape)
    - src/main/pty-manager.ts:61-70,107-113,291-401 (clampDimension/isStringData/deriveStatus/stop/close — the id-validation + type-guard precedents pty-update-profile.test.ts asserts)
    - tests/smoke/helpers/xterm-driver.ts:104-211 (the data-session-id/data-testid selector contract + clickSidebarRow/clickAddSession/readBufferOf to extend)
    - tests/smoke/multi-session-keepalive.smoke.test.ts (the existing WDIO smoke test structure to mirror)
  </read_first>
  <action>
    Write FAILING (RED) unit stubs that import the not-yet-built pure modules: `icon-spec.test.ts` (asserts emojiSpec/colorSpec shape + COLOR_INITIAL returns uppercased first letter, fallback bullet for empty name), `session-switch.test.ts` (position in-range/out-of-range, next/prev wraparound, empty list returns activeId unchanged), `session-edit.test.ts` (splitEdit returns {live:{name,icon}, restart:{cwd,shell,startupCommand}}), `switch-keys.test.ts` (matchSwitchKey: Cmd+1 -> position index 0, Ctrl+9 -> index 8, meta+shift+']' -> next, control+shift+'[' -> prev, alt-held -> null, no-modifier -> null, type!=='keyDown' -> null; write A1-defensively so a later key-OR-code tweak is one line), `pty-update-profile.test.ts` (construct a PtyManager, call updateProfile on an unknown id -> no throw/no-op; verify non-string fields are ignored; verify a stored non-empty shell drives the next create/restart spawn — assert against the record, mocking node-pty as the existing pty-manager tests do).
    Extend `tests/smoke/helpers/xterm-driver.ts` with exported helpers used by the E2E stubs: `openContextMenu(id)` (right-click `.sidebar-row[data-session-id]`), `clickMenuItem(label)` (click `.context-menu-item` by text), `toggleCollapse()` (click the collapse toggle `[data-testid="sidebar-collapse"]`), `pressSwitchChord(intent)` (drive `browser.keys` for the chord), `readIdentityHeader()` (read `.identity-header` text). Keep the existing selector contract.
    Write three E2E smoke stubs marked to drive the real flows (they will go GREEN as Plans 02/03/04 land): `keyboard-switch.smoke.test.ts` (NAV-05 — add 2 sessions, press the position chord, assert active `data-session-id` + identity-header changed; THIS E2E is the A1 proof per RESEARCH), `session-edit.smoke.test.ts` (SESS-01/02/04 — open context menu, Edit, change name, assert live update + SAME data-session-id), `sidebar-collapse.smoke.test.ts` (NAV-01/02 — toggle collapse, assert `.row-name` hidden but `.row-icon` + status dot present).
    Finally, POPULATE 04-VALIDATION.md: fill the Per-Task Verification Map (one row per task across all four plans, with Requirement/TestType/Automated Command) and the Wave 0 Requirements checklist; do NOT yet flip `nyquist_compliant` (Plan 04 closes it).
  </action>
  <acceptance_criteria>
    - `src/renderer/__tests__/icon-spec.test.ts`, `session-switch.test.ts`, `session-edit.test.ts`, `src/main/__tests__/switch-keys.test.ts`, `pty-update-profile.test.ts` all exist and FAIL (RED — modules/methods not yet implemented)
    - `tests/smoke/helpers/xterm-driver.ts` exports `openContextMenu`, `clickMenuItem`, `toggleCollapse`, `pressSwitchChord`, `readIdentityHeader`
    - `tests/smoke/keyboard-switch.smoke.test.ts`, `session-edit.smoke.test.ts`, `sidebar-collapse.smoke.test.ts` exist
    - `.planning/phases/04-session-identity-sidebar-ui/04-VALIDATION.md` Per-Task Verification Map + Wave 0 Requirements sections are populated (no placeholder text)
    - `npm run test:unit` shows the five new unit files RED (the rest of the suite stays GREEN)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit 2>&1 | grep -E "switch-keys|session-switch|icon-spec|session-edit|pty-update-profile" | grep -qi "fail" && echo "RED confirmed"</automated>
  </verify>
  <done>All five pure-module unit stubs are RED, the three E2E smoke stubs + five driver helpers exist, and 04-VALIDATION.md's task map + Wave 0 list are populated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Five React/xterm/electron-free pure modules (GREEN)</name>
  <files>src/renderer/icon-spec.ts, src/renderer/session-switch.ts, src/renderer/session-edit.ts, src/renderer/emoji-set.ts, src/main/switch-keys.ts</files>
  <behavior>
    - matchSwitchKey: meta||control primary modifier (covers macOS Cmd + Windows Ctrl with one rule, D-12/D-13); 1-9 with no shift -> position index n-1; shift+']' -> next; shift+'[' -> prev; alt-held or no-primary or non-keyDown -> null
    - resolveSwitch: position -> sessions[index]?.logicalId ?? activeId; next/prev -> wraparound over current index; empty sessions -> activeId unchanged
    - emojiSpec(value) -> {type:'emoji',value}; colorSpec(value) -> {type:'color',value}; COLOR_INITIAL(spec,name) -> uppercased first grapheme of name for a color spec, bullet fallback, '' for non-color
    - splitEdit(payload) -> {live:{name,icon}, restart:{cwd,shell,startupCommand}} (no side effects; caller does setSessions + ptyUpdateProfile)
    - CURATED_EMOJI is the cozy dev/tool set incl. the canonical 🛋️ 🖥️; COLOR_SWATCHES is the warm DESIGN.md palette
  </behavior>
  <read_first>
    - src/renderer/session-add.ts:1-14 (the pure-module header rationale: imports NOTHING from React/xterm so it unit-tests in Node)
    - src/renderer/session-close.ts (the pure-reducer shape + defensive unknown handling to mirror)
    - src/renderer/status-colors.ts (the exported-const-map convention emoji-set.ts mirrors)
    - .planning/phases/04-session-identity-sidebar-ui/04-PATTERNS.md §"src/main/switch-keys.ts", §"session-switch.ts", §"icon-spec.ts", §"session-edit.ts", §"emoji-set.ts" (exact signatures + line-cited analogs)
    - .planning/DESIGN.md §"Status system" + §"Aesthetic direction" (the warm color swatch values + cozy emoji palette per D-08/D-09)
    - src/shared/types.ts:46-49 (SessionIconSpec discriminated union — DO NOT reshape)
  </read_first>
  <action>
    Implement the five modules so Task 1's unit stubs go GREEN. Each imports ONLY `../shared/types` (type-only) — never React, xterm, or electron (`switch-keys.ts` is main-side but stays electron-free; main/index.ts casts the Electron Input to KeyInput). `matchSwitchKey` returns `SwitchIntent | null` per D-12 (Cmd/Ctrl+1-9, Cmd/Ctrl+Shift+] next, Cmd/Ctrl+Shift+[ prev) — write the `[`/`]`/digit match A1-defensively (accept `key` OR `code` e.g. `BracketRight`/`Digit1`) so the NAV-05 E2E can confirm without a structural change. `resolveSwitch` mirrors `session-close.ts`'s defensive return-activeId-unchanged for unknown/out-of-range. `icon-spec.ts` exports `emojiSpec`/`colorSpec`/`COLOR_INITIAL`; store the full free-text emoji grapheme verbatim (never split — Pitfall 6). `session-edit.ts` exports `splitEdit` (live half name/icon per D-02; restart half cwd/shell/startupCommand). `emoji-set.ts` exports `CURATED_EMOJI` + `COLOR_SWATCHES` drawn from DESIGN.md (your discretion on the exact set per D-08/D-09 Claude's-discretion note). Do NOT surface the `preset` icon kind (D-07 — it stays in the type, unsurfaced).
  </action>
  <acceptance_criteria>
    - `src/renderer/__tests__/icon-spec.test.ts`, `session-switch.test.ts`, `session-edit.test.ts`, `src/main/__tests__/switch-keys.test.ts` are GREEN
    - `src/main/switch-keys.ts` imports neither `electron` nor `node-pty` (grep confirms)
    - `src/renderer/{icon-spec,session-switch,session-edit,emoji-set}.ts` import only `../shared/types` (grep confirms no react/xterm/electron import)
    - `emoji-set.ts` exports `CURATED_EMOJI` (array including '🛋️' and '🖥️') and `COLOR_SWATCHES` (array)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit 2>&1 | grep -E "switch-keys|session-switch|icon-spec|session-edit" | grep -qiv "fail" && grep -L "from 'electron'\|from 'node-pty'\|from 'react'" src/main/switch-keys.ts src/renderer/icon-spec.ts src/renderer/session-switch.ts src/renderer/session-edit.ts src/renderer/emoji-set.ts >/dev/null && echo OK</automated>
  </verify>
  <done>Four pure-module unit suites GREEN; all five modules are React/xterm/electron-free and type-check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Atomic bridge lockstep — ptyUpdateProfile + onSwitchSession + updateProfile + create() honors record.shell</name>
  <files>src/shared/api-types.ts, src/main/window-config.ts, src/preload/index.ts, src/main/pty-manager.ts, src/shared/__tests__/security.guard.test.ts</files>
  <behavior>
    - PtyManager.updateProfile(id, fields): unknown/forged id -> no-op; only typeof==='string' fields (name/cwd/shell/startupCommand) and a present icon are written onto record; startupCommand stored only (no PTY write — TERM-05 deferred)
    - After updateProfile sets record.shell non-empty, the next create()/restart() spawns with that shell; empty/absent shell falls back to resolveShell() (A2 guard)
    - The contextBridge exposes EXACTLY 15 keys; the guard asserts the exact set and rejects raw ipcRenderer
    - onSwitchSession(cb) subscribes to 'session:switch' and returns an unsubscribe fn
  </behavior>
  <read_first>
    - src/shared/api-types.ts:81-102 (ptyClose fire-and-forget + onPtyStatus subscribe shapes — the exact templates for the two new keys)
    - src/main/window-config.ts:51-65 (EXPECTED_API_KEYS — the 13-key array + the 02-02/03-01/03-03 expansion doc-comment style to extend)
    - src/preload/index.ts:82-116 (ptyClose send + onPtyStatus on/removeListener — verbatim templates)
    - src/main/pty-manager.ts:24-40,146-185,316-345,391-401,430-496 (PTY_CHANNELS, create()'s resolveShell + record build, restart() respawn, close() id-guard, registerIpc/unregisterIpc symmetry)
    - src/shared/__tests__/security.guard.test.ts:42-53 (the key-set equality assertion — goes RED->GREEN automatically when preload + EXPECTED_API_KEYS match)
    - .planning/phases/04-session-identity-sidebar-ui/04-PATTERNS.md §"pty-manager.ts" + §"api-types.ts" + §"preload" + §"window-config + security.guard" (the full 5-point lockstep with line cites)
    - src/main/shell-resolver.ts (resolveShell() return shape for the A2 fallback)
  </read_first>
  <action>
    Do ALL of the following in ONE atomic task (CLAUDE.md hard rule — the guard asserts the exact key set, so a partial change breaks the build):
    1. `api-types.ts`: add `ptyUpdateProfile: (id: LogicalId, fields: { name?: string; icon?: SessionIconSpec; cwd?: string; shell?: string; startupCommand?: string }) => void` (fire-and-forget, mirrors ptyClose) and `onSwitchSession: (cb: (intent: SwitchIntent) => void) => () => void` (subscribe, mirrors onPtyStatus). Import `SwitchIntent` (from `../main/switch-keys`) and `SessionIconSpec` as type-only.
    2. `window-config.ts`: append `'ptyUpdateProfile'` and `'onSwitchSession'` to `EXPECTED_API_KEYS` (-> 15 keys) with a doc-comment in the 03-03 expansion style noting the threat ref.
    3. `preload/index.ts`: wire `ptyUpdateProfile` as `ipcRenderer.send('pty:update-profile', id, fields)` and `onSwitchSession` as `ipcRenderer.on('session:switch', listener)` returning `removeListener` (verbatim from ptyClose/onPtyStatus).
    4. `pty-manager.ts`: add `PTY_CHANNELS.updateProfile: 'pty:update-profile'`; add `updateProfile(id, fields)` (id-validated against the sessions Map -> no-op on miss; typeof==='string' guard each of name/cwd/shell/startupCommand before writing to record; assign icon if present); register `ipcMain.on(PTY_CHANNELS.updateProfile, (_e, id, fields) => this.updateProfile(id, fields))` in `registerIpc` and `ipcMain.removeAllListeners(PTY_CHANNELS.updateProfile)` in `unregisterIpc` (mirror the close channel triple). Extend `create()` to prefer `prior?.record.shell` when non-empty (`record.shell && record.shell.length`), else `resolveShell()` (A2 guard); have `restart()` continue threading record fields. KEEP startupCommand stored only — no code path writes it to a PTY (TERM-05 deferred).
    5. Run the guard test: it goes RED (15 expected vs 13 exposed) then GREEN once preload + EXPECTED_API_KEYS agree.
    Threat refs (this plan's `<threat_model>`): T-04-01 (forged id -> no-op), T-04-02 (non-string field -> type-guard), T-04-03 (key leak -> guard), T-04-SC.
  </action>
  <acceptance_criteria>
    - `src/main/window-config.ts` EXPECTED_API_KEYS has exactly 15 entries including 'ptyUpdateProfile' and 'onSwitchSession'
    - `src/shared/__tests__/security.guard.test.ts` passes (`Object.keys(exposed).sort()` equals the 15-key set; no ipcRenderer)
    - `src/main/__tests__/pty-update-profile.test.ts` is GREEN (unknown-id no-op; non-string ignored; stored shell drives respawn)
    - `src/main/pty-manager.ts` create() references `record.shell` with a non-empty guard and an `resolveShell()` fallback (grep `record\.shell`)
    - `src/main/pty-manager.ts` has no code path writing `startupCommand` to `pty.write`/`pty.spawn` args (grep confirms startupCommand only assigned to record)
    - `npm run test:unit` is fully GREEN; `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit 2>&1 | tail -5 | grep -qi "fail" && echo FAIL || (grep -c "ptyUpdateProfile\|onSwitchSession" src/main/window-config.ts | grep -q 2 && echo OK)</automated>
  </verify>
  <done>The 15-key bridge surface is wired in lockstep (api-types + EXPECTED_API_KEYS + preload + pty-manager channel triple), the guard + pty-update-profile suites are GREEN, create() honors a stored shell with a fallback, and startupCommand is stored-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer (window.api) -> main (ipcMain) | Untrusted renderer-supplied args (id, profile fields) cross into the main process before reaching the in-memory record store |
| main (before-input-event) -> renderer (session:switch) | Main pushes a switch intent into the renderer (read-only inbound event) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `PtyManager.updateProfile` (forged/unknown id) | mitigate | Validate `id` against the live `sessions` Map; no-op on miss (mirrors `close()`/T-03-01) |
| T-04-02 | Tampering | `PtyManager.updateProfile` (non-string cwd/shell/startupCommand/name) | mitigate | `typeof === 'string'` guard each field before writing to the record (mirrors `isStringData`) |
| T-04-03 | Elevation of Privilege | new contextBridge keys leaking raw `ipcRenderer` | mitigate | `security.guard.test.ts` asserts exact 15-key `EXPECTED_API_KEYS`; lockstep update in one task |
| T-04-04 | Misuse | stored `startupCommand` unexpectedly executed | mitigate | TERM-05 auto-run deferred — `updateProfile` only writes to the record; no PTY-write code path (grep-verified in acceptance) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A this phase — Phase 4 installs ZERO packages (RESEARCH §Package Legitimacy Audit); no install task exists |
</threat_model>

<verification>
- `npm run test:unit` GREEN after Task 3 (guard + all five pure-module suites + pty-update-profile).
- `npx tsc --noEmit` passes (the two new bridge keys type-check across renderer + main).
- Wave 0 RED stubs from Task 1 for E2E remain pending (they go GREEN in Plans 02–04); unit RED stubs are closed by Tasks 2–3.
</verification>

<success_criteria>
- Five pure modules implemented + unit-GREEN, all React/xterm/electron-free.
- 15-key bridge surface live; security guard GREEN; no raw ipcRenderer.
- `PtyManager.updateProfile` id-validated + type-guarded; `create()` honors a stored shell with a `resolveShell()` fallback; `startupCommand` stored only.
- 04-VALIDATION.md per-task map + Wave 0 list populated.
</success_criteria>

<output>
Create `.planning/phases/04-session-identity-sidebar-ui/04-01-SUMMARY.md` when done.
</output>
