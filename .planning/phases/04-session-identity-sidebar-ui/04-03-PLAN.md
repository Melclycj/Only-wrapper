---
phase: 04-session-identity-sidebar-ui
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - src/main/index.ts
  - src/renderer/SessionManager.tsx
autonomous: true
requirements: [NAV-05, NAV-03]
must_haves:
  truths:
    - "Pressing Cmd/Ctrl+1 through Cmd/Ctrl+9 switches to the session in that sidebar position without touching the mouse"
    - "Pressing Cmd/Ctrl+Shift+] switches to the next session and Cmd/Ctrl+Shift+[ to the previous, wrapping around"
    - "The switch chords never reach the focused terminal — they work even while vim/tmux/fzf has focus (app always wins, D-13)"
    - "Switching updates the active terminal panel and identity header immediately; the previously active session keeps running and is visible again on re-activation"
  artifacts:
    - path: "src/main/index.ts"
      provides: "before-input-event interceptor -> matchSwitchKey -> webContents.send('session:switch')"
      contains: "before-input-event"
  key_links:
    - from: "src/main/index.ts"
      to: "matchSwitchKey (src/main/switch-keys.ts)"
      via: "before-input-event handler resolves the intent then preventDefault + send"
      pattern: "matchSwitchKey"
    - from: "src/renderer/SessionManager.tsx"
      to: "resolveSwitch + window.api.onSwitchSession"
      via: "onSwitchSession subscription applies resolveSwitch -> setActiveId"
      pattern: "onSwitchSession|resolveSwitch"
---

<objective>
Deliver the keyboard session-switching vertical slice end-to-end: intercept the NAV-05 chords in the MAIN process via `webContents.on('before-input-event')` (the only mechanism that guarantees "app wins" over a focused xterm on Windows Ctrl combos — D-13), resolve them with the Plan 01 pure `matchSwitchKey`, forward a `SwitchIntent` over the Plan 01 `onSwitchSession` bridge, and apply it in SessionManager with the pure `resolveSwitch` -> `setActiveId`.

Per D-12 the chord scheme is positions Cmd/Ctrl+1-9 + next/prev Cmd/Ctrl+Shift+] / Cmd/Ctrl+Shift+[. Per D-14 keyboard coverage is SWITCHING ONLY — this plan adds NO Cmd/Ctrl+T (new) or Cmd/Ctrl+W (close) bindings; create stays on the "+ Add" button and close stays on the context menu (Plan 02).

Purpose: Satisfy NAV-05 (positions + next/prev, no mouse) and D-12/D-13 (app-reserved, terminal-safe). De-risk assumption A1 (exact `key` strings) via the NAV-05 E2E gate. Confirm NAV-03 click-switch remains non-destructive (regression).
Output: the `before-input-event` wiring in `createWindow`, the `onSwitchSession` subscription effect in SessionManager, and the GREEN NAV-05 E2E.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-session-identity-sidebar-ui/04-CONTEXT.md
@.planning/phases/04-session-identity-sidebar-ui/04-RESEARCH.md
@.planning/phases/04-session-identity-sidebar-ui/04-PATTERNS.md
@src/main/index.ts
@src/renderer/SessionManager.tsx
@.planning/phases/04-session-identity-sidebar-ui/04-01-SUMMARY.md
@.planning/phases/04-session-identity-sidebar-ui/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Main-process before-input-event interception (D-13, app-wins)</name>
  <files>src/main/index.ts</files>
  <read_first>
    - src/main/index.ts:17-45 (createWindow — construct win, registerIpc, the win.on('closed') wiring where the interceptor is added)
    - src/main/switch-keys.ts (Plan 01 — matchSwitchKey + the KeyInput type the Electron Input is cast to)
    - .planning/phases/04-session-identity-sidebar-ui/04-PATTERNS.md §"src/main/index.ts" (the exact before-input-event wiring + the Menu-accelerator anti-pattern)
    - .planning/phases/04-session-identity-sidebar-ui/04-RESEARCH.md §"Pattern 1" + §"Anti-Patterns to Avoid" (preventDefault, NOT globalShortcut, NOT a Menu accelerator — Electron #19279)
  </read_first>
  <action>
    In `createWindow()` (after `win` is constructed, alongside the existing `win.on('closed', ...)`), add `win.webContents.on('before-input-event', (event, input) => { const intent = matchSwitchKey(input as unknown as KeyInput); if (intent) { event.preventDefault(); win.webContents.send('session:switch', intent); } })`. Import `matchSwitchKey` + the `KeyInput` type from `./switch-keys`. `preventDefault()` stops the chord ever reaching the renderer/xterm/PTY (D-13). DO NOT register these chords as Menu accelerators (preventDefault would also suppress menu accelerators — Electron #19279) and DO NOT use `globalShortcut` (system-wide, silent-fail, macOS layout bug). Send on the `session:switch` channel that the Plan 01 `onSwitchSession` preload subscription listens on. Keep all existing createWindow logic (registerIpc, loadURL/loadFile, closed cleanup) intact.
  </action>
  <acceptance_criteria>
    - `src/main/index.ts` calls `win.webContents.on('before-input-event', ...)` inside createWindow and imports `matchSwitchKey` from `./switch-keys`
    - The handler calls `event.preventDefault()` and `win.webContents.send('session:switch', intent)` only when matchSwitchKey returns non-null
    - No `globalShortcut` import or Menu-accelerator registration for these chords (grep absent `globalShortcut`)
    - `npx tsc --noEmit` passes; `npm run test:unit` stays GREEN
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "before-input-event" src/main/index.ts && grep -q "matchSwitchKey" src/main/index.ts && ! grep -q "globalShortcut" src/main/index.ts && echo OK</automated>
  </verify>
  <done>The main process intercepts the switch chords before renderer dispatch, resolves the intent with the pure matcher, and forwards it on session:switch — without a Menu accelerator or globalShortcut.</done>
</task>

<task type="auto">
  <name>Task 2: SessionManager onSwitchSession subscription + apply resolveSwitch (NAV-05 E2E GREEN)</name>
  <files>src/renderer/SessionManager.tsx</files>
  <read_first>
    - src/renderer/SessionManager.tsx:136-192 (the onPtyStatus subscription useEffect to mirror + the onSelect/setActiveId switch path resolveSwitch feeds)
    - src/renderer/session-switch.ts (Plan 01 — resolveSwitch(sessions, activeId, intent) -> next activeId)
    - .planning/phases/04-session-identity-sidebar-ui/04-PATTERNS.md §"SessionManager.tsx" (host the onSwitchSession sub mirroring the onPtyStatus sub; apply with resolveSwitch + setActiveId)
    - .planning/phases/04-session-identity-sidebar-ui/04-RESEARCH.md §"Assumptions Log" A1 + §"Open Questions" Q1 (the NAV-05 E2E is the A1 proof; adjust matchSwitchKey if key strings differ)
    - tests/smoke/keyboard-switch.smoke.test.ts + tests/smoke/helpers/xterm-driver.ts (Plan 01 — pressSwitchChord/readIdentityHeader the E2E drives)
  </read_first>
  <action>
    Add a `useEffect` in SessionManager that subscribes via `window.api.onSwitchSession((intent) => { setActiveId((cur) => resolveSwitch(sessionsRef.current, cur, intent)); })` and returns the unsubscribe fn (mirror the onPtyStatus sub cleanup). Because `resolveSwitch` needs the current sessions list and the subscription must not re-bind on every sessions change, read sessions through a ref (a `sessionsRef` kept in sync via an effect, or pass sessions + activeId into a stable callback) — match the existing subscription-cleanup discipline so no stale closure or listener leak occurs. Applying the intent calls `setActiveId` which already drives the non-destructive switch (TERM-06 path — SessionView activate effect hands WebGL+focus over; the previously active session keeps running, NAV-03). The identity header (Plan 02) updates because it reads the active record by activeId. Per D-14 this effect handles SWITCH intents only — no new/close intent exists. Run the WDIO `keyboard-switch.smoke.test.ts`: drive the real chord via `pressSwitchChord`, assert the active `data-session-id` + `readIdentityHeader()` changed. If the E2E reveals the `[`/`]`/digit `key` strings differ from the matcher's assumption (A1), apply the one-line `key`-OR-`code` fix in `src/main/switch-keys.ts` and re-run (the matcher is pure — Plan 01 wrote it defensively).
  </action>
  <acceptance_criteria>
    - `src/renderer/SessionManager.tsx` has a useEffect calling `window.api.onSwitchSession(...)` that applies `resolveSwitch` and returns the unsubscribe fn
    - The subscription does not leak listeners (cleanup returned) and reads the current sessions without re-binding per render (ref or stable-callback pattern)
    - `tests/smoke/keyboard-switch.smoke.test.ts` passes (active session + identity header change on the chord) — the A1 proof
    - `npx tsc --noEmit` passes; `npm run test:unit` GREEN
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "onSwitchSession" src/renderer/SessionManager.tsx && grep -q "resolveSwitch" src/renderer/SessionManager.tsx && echo OK</automated>
    <human-check>Run `npm start` with 3+ sessions: press Cmd/Ctrl+1, Cmd/Ctrl+2, Cmd/Ctrl+3 — the active terminal + identity header change with no mouse; run `vim` in one session, focus it, then press the switch chord and confirm it switches sessions instead of reaching vim (D-13 app-wins); press Cmd/Ctrl+Shift+] / [ to cycle next/previous with wraparound; confirm the background session kept running.</human-check>
  </verify>
  <done>NAV-05 works end-to-end: position + next/prev chords switch sessions with no mouse, the chords never reach the terminal, switching is non-destructive, and the NAV-05 E2E is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS keyboard -> main before-input-event | Raw keydown events reach main before the renderer; only exact switch chords are acted on |
| main -> renderer session:switch | Main pushes a typed SwitchIntent (read-only inbound) the renderer applies to local activeId |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Denial of Service (usability) | before-input-event swallowing legitimate keys | mitigate | matchSwitchKey returns null for anything but the exact chords; `preventDefault()` ONLY on a match — all other keys pass through to the renderer/PTY unchanged |
| T-04-08 | Tampering | forged/malformed switch intent reaching the renderer | mitigate | Intent originates ONLY from main's matchSwitchKey (a pure, bounded matcher); resolveSwitch defensively returns activeId unchanged for out-of-range/empty |
| T-04-09 | Elevation of Privilege | switch interception broadening the bridge surface | mitigate | onSwitchSession is a reviewed bridge key (Plan 01, in EXPECTED_API_KEYS); no raw ipcRenderer; guard stays GREEN |
| T-04-SC | Tampering | npm installs | mitigate | N/A — Phase 4 installs zero packages |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run test:unit` GREEN after each task.
- WDIO `keyboard-switch.smoke.test.ts` GREEN (NAV-05 + A1 proof).
- Manual human-check (Task 2) confirms app-wins over vim/tmux and non-destructive switching.
</verification>

<success_criteria>
- Cmd/Ctrl+1-9 select sidebar positions; Cmd/Ctrl+Shift+]/[ cycle next/prev with wraparound; all mouse-free (D-12).
- Chords never reach the focused terminal (D-13).
- Switching-only — no new/close bindings (D-14).
- Switching is non-destructive (background session keeps running; TERM-06 path reused; NAV-03 intact).
- NAV-05 E2E GREEN; A1 confirmed (matcher adjusted if needed).
</success_criteria>

<output>
Create `.planning/phases/04-session-identity-sidebar-ui/04-03-SUMMARY.md` when done.
</output>
