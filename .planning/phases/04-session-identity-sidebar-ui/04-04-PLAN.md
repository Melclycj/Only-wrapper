---
phase: 04-session-identity-sidebar-ui
plan: 04
type: execute
wave: 4
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - src/renderer/Sidebar.tsx
  - src/renderer/SessionManager.tsx
  - src/renderer/terminal.css
  - .planning/phases/04-session-identity-sidebar-ui/04-VALIDATION.md
autonomous: true
requirements: [NAV-02, NAV-01, SESS-03]
must_haves:
  truths:
    - "A pinned chevron toggle folds the sidebar to an icon-only rail and expands it back; the state stays where the user puts it"
    - "When collapsed, session names and status badges are hidden but the icon (emoji or color badge-with-initial) still identifies each session"
    - "Each collapsed rail item shows a small status-color dot so running/stopped status stays legible while collapsed (NAV-01)"
    - "Hovering a collapsed rail item reveals a tooltip with the session name (and status label)"
    - "Right-clicking a collapsed rail item opens the context menu (the only control surface when collapsed)"
  artifacts:
    - path: "src/renderer/Sidebar.tsx"
      provides: "collapse prop + .collapsed class + collapse toggle + collapsed status dot + rail tooltip"
      contains: "collapsed"
  key_links:
    - from: "src/renderer/SessionManager.tsx"
      to: "Sidebar collapsed prop"
      via: "collapsed local state + onToggleCollapse"
      pattern: "collapsed"
    - from: "src/renderer/terminal.css"
      to: ".sidebar.collapsed rail rules"
      via: "hide .row-name/.status-badge/.row-controls; show .collapsed-status-dot + .rail-tooltip"
      pattern: "sidebar\\.collapsed|collapsed-status-dot|rail-tooltip"
---

<objective>
Deliver the collapsible-sidebar vertical slice end-to-end: a pinned chevron toggle folds the expanded session list into an icon-only rail and back, where each rail item keeps its identifying icon (emoji or the Plan 02 color-badge-with-initial) plus a status-color dot and a hover tooltip, and the right-click context menu (Plan 02) is the control surface. This closes the phase's Nyquist validation contract.

Purpose: Satisfy NAV-02 (expanded/collapsed; icon identifies when collapsed) and NAV-01 (status legible in both modes) and the SESS-03 "icon stays visible when collapsed" clause (D-10/D-11). Collapse state is component-local (persistence is Phase 5 — D-11).
Output: the Sidebar collapse toggle + rail rendering + collapsed status dot + custom tooltip, the SessionManager `collapsed` state, the CSS rail rules, the GREEN collapse E2E, and `nyquist_compliant: true` in 04-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-session-identity-sidebar-ui/04-CONTEXT.md
@.planning/phases/04-session-identity-sidebar-ui/04-RESEARCH.md
@.planning/phases/04-session-identity-sidebar-ui/04-PATTERNS.md
@.planning/DESIGN.md
@src/renderer/Sidebar.tsx
@src/renderer/SessionManager.tsx
@src/renderer/status-colors.ts
@.planning/phases/04-session-identity-sidebar-ui/04-02-SUMMARY.md
@.planning/phases/04-session-identity-sidebar-ui/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar collapse toggle + rail rendering + status dot + tooltip</name>
  <files>src/renderer/Sidebar.tsx, src/renderer/terminal.css</files>
  <read_first>
    - src/renderer/Sidebar.tsx:60-155 (the nav/row render tree to add the collapse class, toggle, status dot, and tooltip to; renderIcon is already color-badge-with-initial from Plan 02)
    - src/renderer/status-colors.ts:22-31 (STATUS_STYLE.accent for the collapsed dot color)
    - .planning/phases/04-session-identity-sidebar-ui/04-PATTERNS.md §"Sidebar.tsx" + §"terminal.css" (the .sidebar.collapsed rules, .collapsed-status-dot, .rail-tooltip — custom CSS tooltip over native title=)
    - .planning/phases/04-session-identity-sidebar-ui/04-RESEARCH.md §"Pattern 6" (collapse + rail + dot + tooltip CSS sketch) + §"Common Pitfalls" Pitfall 5 (context menu reachable when collapsed) + Pitfall 6 (emoji box sizing)
    - .planning/DESIGN.md §"v1 component inventory" (RailIcon/IconTile collapsed identity) + §"Aesthetic direction" (warm tooltip)
  </read_first>
  <action>
    Add a `collapsed: boolean` prop + an `onToggleCollapse` callback prop to Sidebar. When `collapsed`, put a `.collapsed` class on `.sidebar`. Add a pinned chevron toggle button at the top of the nav (`data-testid="sidebar-collapse"`, `aria-pressed={collapsed}`, `aria-label="Collapse sidebar"` / "Expand sidebar"). On each row, add a `.collapsed-status-dot` element whose color is `STATUS_STYLE[s.status].accent` (so running/stopped stays legible collapsed — NAV-01) and a custom-tooltip element (`.rail-tooltip` reading the session name + status label, shown on hover/focus-visible — preferred over native `title=` per D-11/RESEARCH Pattern 6; native `title` is the acceptable cheap fallback if needed). The row's `onContextMenu` (Plan 02) is ALREADY at the `.sidebar-row` level so it works collapsed (Pitfall 5) — confirm it is not gated behind expanded-only markup. Add `terminal.css`: `.sidebar.collapsed { flex-basis: ~52px }`, `.sidebar.collapsed .row-name, .sidebar.collapsed .status-badge, .sidebar.collapsed .row-controls { display: none }`, `.sidebar.collapsed .sidebar-row { justify-content: center; position: relative }`, `.collapsed-status-dot { position:absolute; ... background: var(--accent) }`, `.rail-tooltip` (warm --surface/--radius/Nunito, shown on :hover/:focus-visible). Do NOT touch `.session-view`/`.viewport-stack` visibility — only the sidebar collapses (Anti-Pattern: collapse must not display:none the panes). Keep `.row-icon` 20x20 + line-height:1 (Pitfall 6).
  </action>
  <acceptance_criteria>
    - `src/renderer/Sidebar.tsx` accepts a `collapsed` prop, renders a toggle with `data-testid="sidebar-collapse"` and `aria-pressed`, and adds `.collapsed` to `.sidebar` when set
    - Each row renders a `.collapsed-status-dot` colored from `STATUS_STYLE` and a `.rail-tooltip` with the name
    - `onContextMenu` remains at the `.sidebar-row` level (works collapsed — grep confirms it is on the row div, not control-button-gated)
    - `src/renderer/terminal.css` contains `.sidebar.collapsed`, `.collapsed-status-dot`, `.rail-tooltip` and hides `.row-name`/`.status-badge`/`.row-controls` when collapsed
    - `.session-view`/`.viewport-stack` rules are unchanged (no new display:none on panes)
    - `npx tsc --noEmit` passes; `npm run test:unit` GREEN
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "sidebar-collapse" src/renderer/Sidebar.tsx && grep -q "collapsed-status-dot" src/renderer/terminal.css && grep -q "rail-tooltip" src/renderer/terminal.css && grep -q "sidebar.collapsed" src/renderer/terminal.css && echo OK</automated>
  </verify>
  <done>The sidebar collapses to an icon-only rail with a status dot + tooltip; the context menu still reaches collapsed rows; the terminal panes are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire collapse state in SessionManager + close Nyquist (collapse E2E GREEN)</name>
  <files>src/renderer/SessionManager.tsx, .planning/phases/04-session-identity-sidebar-ui/04-VALIDATION.md</files>
  <read_first>
    - src/renderer/SessionManager.tsx:39-46,201-210 (the useState block where `collapsed` joins editingId/menuState/closingId + the Sidebar mount where the prop is passed)
    - .planning/phases/04-session-identity-sidebar-ui/04-PATTERNS.md §"SessionManager.tsx" (host collapsed like closingId; persistence is Phase 5 — component-local now)
    - .planning/phases/04-session-identity-sidebar-ui/04-CONTEXT.md D-10/D-11 (pinned toggle, component-local state, collapsed control surface is the context menu)
    - tests/smoke/sidebar-collapse.smoke.test.ts + tests/smoke/helpers/xterm-driver.ts (Plan 01 — toggleCollapse the E2E drives)
    - .planning/phases/04-session-identity-sidebar-ui/04-VALIDATION.md (the Per-Task Verification Map + Validation Sign-Off + nyquist_compliant frontmatter to finalize)
  </read_first>
  <action>
    Add `const [collapsed, setCollapsed] = useState(false)` to SessionManager (component-local — persistence is Phase 5 per D-11) and pass `collapsed={collapsed}` + `onToggleCollapse={() => setCollapsed((c) => !c)}` to `<Sidebar>`. Run the WDIO `sidebar-collapse.smoke.test.ts`: `toggleCollapse()` then assert `.row-name` is hidden while `.row-icon` + `.collapsed-status-dot` are present, and that a right-click on a collapsed rail row opens the context menu (Pitfall 5). Then FINALIZE `04-VALIDATION.md`: verify the Per-Task Verification Map rows for all four plans are accurate (commands + requirements), confirm every Wave 0 RED stub is now GREEN, check the Validation Sign-Off boxes (all tasks have automated verify or Wave 0 deps; no 3 consecutive tasks without automated verify; Wave 0 covers all MISSING refs; no watch-mode flags), and set `nyquist_compliant: true` + `wave_0_complete: true` + `status: complete` in the frontmatter. Run the full unit suite + the three new E2E smoke tests to confirm GREEN before sign-off.
  </action>
  <acceptance_criteria>
    - `src/renderer/SessionManager.tsx` has `collapsed` state and passes `collapsed` + `onToggleCollapse` to Sidebar
    - `tests/smoke/sidebar-collapse.smoke.test.ts` passes (names hidden, icons + status dots present, context menu opens on a collapsed row)
    - `.planning/phases/04-session-identity-sidebar-ui/04-VALIDATION.md` has `nyquist_compliant: true` and `wave_0_complete: true`; the Per-Task Verification Map has no pending placeholder rows; the Validation Sign-Off boxes are checked
    - `npm run test:unit` fully GREEN; the three Phase-4 E2E smoke tests (keyboard-switch, session-edit, sidebar-collapse) all GREEN
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "collapsed" src/renderer/SessionManager.tsx && grep -q "nyquist_compliant: true" .planning/phases/04-session-identity-sidebar-ui/04-VALIDATION.md && echo OK</automated>
    <human-check>Run `npm start`: click the chevron toggle — the sidebar folds to an icon-only rail; confirm each session is still identifiable by its emoji or color-badge-with-initial, a status dot shows running/stopped, and hovering shows the name tooltip. Right-click a collapsed rail item and confirm the Edit/Restart/Close menu opens. Toggle back to expanded.</human-check>
  </verify>
  <done>Collapse works end-to-end: the toggle folds/expands the sidebar, collapsed rows stay identifiable with a status dot + tooltip + context menu, the collapse E2E is GREEN, and the phase's Nyquist contract is signed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM events (toggle click, right-click, hover) -> renderer | Untrusted UI events drive collapse + tooltip + menu state; no process or bridge surface crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Information Disclosure | rail tooltip exposing session name | accept | The session name is already shown in the expanded sidebar + identity header; a hover tooltip on a local single-user app exposes nothing new (no PII, local-only) |
| T-04-11 | Denial of Service (usability) | collapse hiding the only control surface | mitigate | The context menu is wired at the `.sidebar-row` level (Plan 02), so Edit/Restart/Close stay reachable when collapsed (Pitfall 5) — verified by the collapse E2E |
| T-04-12 | Tampering | collapse accidentally hiding terminal panes (display:none) | mitigate | Collapse touches ONLY `.sidebar` rules; `.session-view`/`.viewport-stack` visibility is untouched (grep-verified in Task 1 acceptance) — preserves the keep-alive invariant |
| T-04-SC | Tampering | npm installs | mitigate | N/A — Phase 4 installs zero packages |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npm run test:unit` GREEN after each task.
- WDIO `sidebar-collapse.smoke.test.ts` GREEN (NAV-01/02); the two prior Phase-4 E2E (keyboard-switch, session-edit) remain GREEN.
- 04-VALIDATION.md signed off with `nyquist_compliant: true`.
- Manual human-check (Task 2) confirms collapsed identifiability + the context menu reachable when collapsed.
</verification>

<success_criteria>
- Pinned chevron toggle folds/expands the sidebar; state persists where the user leaves it (component-local).
- Collapsed: names + badges hidden, icon (emoji / color-badge-with-initial) + status dot + tooltip remain (NAV-01/02, SESS-03).
- Context menu reaches collapsed rows.
- Terminal panes untouched (keep-alive intact).
- All three Phase-4 E2E smoke tests GREEN; Nyquist contract signed off.
</success_criteria>

<output>
Create `.planning/phases/04-session-identity-sidebar-ui/04-04-SUMMARY.md` when done.
</output>
