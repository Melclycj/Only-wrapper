---
phase: 12-session-form-polish-edit-ux
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - tests/smoke/session-edit.smoke.test.ts
  - tests/smoke/helpers/xterm-driver.ts
  - .planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md
autonomous: false
requirements: [UI-04, SESS-05, SESS-06]
user_setup: []

must_haves:
  truths:
    - "The session-edit smoke proves the SESS-05 edit→Save→reopen cwd+startup round-trip on the running app"
    - "The smoke's Save click matches the new 'Save changes' label (Rule-1 lockstep with Plan 02)"
    - "A packaged no-injection ui:shots:fresh capture of edit-modal + edit-modal-validation is scored against the updated DESIGN-RUBRIC"
    - "The operator confirms LIVE on the packaged app: SC1 one-designed-surface, SESS-05 prefill incl. O-1 first-open default, SESS-06 Browse… fills + CR-01 gates"
    - "nyquist_compliant flips true ONLY on the explicit unqualified operator approval"
  artifacts:
    - path: "tests/smoke/session-edit.smoke.test.ts"
      provides: "SESS-05 prefill round-trip assertion + the 'Save changes' label update"
      contains: "edit-cwd"
  key_links:
    - from: "tests/smoke/session-edit.smoke.test.ts"
      to: "tests/smoke/helpers/xterm-driver.ts"
      via: "clickMenuItem('Save changes')"
      pattern: "Save changes"
---

<objective>
The Phase-12 gate. Extend the session-edit smoke to prove the SESS-05 edit→Save→reopen
cwd+startup round-trip on the running app (the automated half of SC2), update the smoke's
Save click to the new 'Save changes' label (Rule-1 lockstep with Plan 02's relabel), run the
full suite GREEN, capture a packaged no-injection `ui:shots:fresh` of the edit-modal +
edit-modal-validation surfaces scored against the updated DESIGN-RUBRIC, then a BLOCKING
end-of-phase human-verify covering SC1 (one cohesive designed surface), SESS-05 O-1
(first-open default cwd), and SESS-06 O-2 (Browse… on the packaged app, CR-01 still gates).
The Nyquist gate flips ONLY on the operator's explicit unqualified "approved" (mirrors
Phases 10/11).

Purpose: prove the polish + the two SESS round-trips work LIVE on the packaged app, not just
in green units — the verify half of this verify-and-polish phase (D-01). The edit-only form (D-05)
and the unchanged 20-key bridge (SC4) are confirmed live, not re-architected.
Output: the extended smoke, the packaged capture evidence, the operator sign-off, and the
flipped VALIDATION frontmatter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-session-form-polish-edit-ux/12-CONTEXT.md
@.planning/phases/12-session-form-polish-edit-ux/12-RESEARCH.md
@.planning/phases/12-session-form-polish-edit-ux/12-UI-SPEC.md
@.planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md
@.planning/phases/12-session-form-polish-edit-ux/12-02-SUMMARY.md
@tests/smoke/session-edit.smoke.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the session-edit smoke — SESS-05 round-trip + 'Save changes' relabel (Rule-1 lockstep)</name>
  <files>tests/smoke/session-edit.smoke.test.ts, tests/smoke/helpers/xterm-driver.ts</files>
  <read_first>
    - tests/smoke/session-edit.smoke.test.ts (the current name-edit live test; the setEditName helper :44-56; the clickMenuItem('Save') call :73)
    - tests/smoke/helpers/xterm-driver.ts:265-275 (clickMenuItem — exact-trim text match on .context-menu-item; this is WHY the 'Save' → 'Save changes' relabel ripples here)
    - src/renderer/SessionEditModal.tsx (Plan 02 — confirm the Save visible label is now 'Save changes' and the edit-cwd/edit-startup testids are intact)
    - 12-RESEARCH.md §"Smoke assertion (the live round-trip)" (the open→set cwd+startup→Save→reopen→assert-DOM-values shape; the helpers already present)
    - 12-UI-SPEC.md §Interaction Contract "Prefill on open (SESS-05)" (what the round-trip must show)
  </read_first>
  <action>
    Update the Save click contract first: in session-edit.smoke.test.ts change every
    `clickMenuItem('Save')` to `clickMenuItem('Save changes')` (Plan 02 relabelled the visible
    Save text; clickMenuItem matches visible text exactly, so 'Save' no longer matches — this is
    the Rule-1 lockstep). Add helpers (or reuse setEditName's pattern) to set `edit-cwd` and
    `edit-startup` by dispatching the 'input' event through React's onChange path, and to read a
    testid's DOM `.value`. Add a NEW test case to the existing describe block: add a session →
    open Edit (context menu → Edit) → set edit-cwd to a KNOWN absolute dir (e.g. the smoke's
    working dir or a stable path that exists) + set edit-startup to a known string → click
    'Save changes' → reopen Edit on the SAME row → assert the `edit-cwd` and `edit-startup` DOM
    values equal what was saved (proving rehydrateProfiles + the seed effect round-tripped main's
    persisted truth — SESS-05). Use `browser.waitUntil` for the reopen-and-read (avoid fixed
    timeouts). Do NOT assert on the native Browse… dialog (that is the human-gate O-2). Keep the
    existing live-rename + logicalId-stable assertions.
  </action>
  <verify>
    <automated>npm run test:smoke 2>&1 | grep -E "Session edit smoke|session-edit"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "clickMenuItem('Save')" tests/smoke/session-edit.smoke.test.ts` == 0 AND `grep -c "Save changes" tests/smoke/session-edit.smoke.test.ts` >= 1 (relabel lockstep done).
    - The new round-trip case references both fields: `grep -Ec "edit-cwd|edit-startup" tests/smoke/session-edit.smoke.test.ts` >= 2.
    - `npm run test:smoke` session-edit spec GREEN against the packaged/running app (re-run isolated if the known parallel-load timing flake appears — STATE notes 3/3 isolated pass is the bar).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The smoke proves the SESS-05 cwd+startup round-trip and clicks the new 'Save changes' button; no native-dialog automation introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite GREEN + packaged ui:shots:fresh capture scored vs DESIGN-RUBRIC</name>
  <files>.planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md</files>
  <read_first>
    - 12-UI-SPEC.md §Verification Protocol (structural TSX change → MUST use ui:shots:fresh, NOT live-CSS injection; the packaged-no-injection-is-proof rule; the two surfaces to score)
    - tests/ui-lab/DESIGN-RUBRIC.md §edit-modal + §edit-modal-validation (the scoring contract from Plan 01)
    - tests/ui-lab/README.md (the run mechanics + tag convention)
    - 12-VALIDATION.md §Per-Task Verification Map (the table to populate as tasks complete)
  </read_first>
  <action>
    Run the full automated suite: `npm run test:unit && npm run test:smoke` GREEN +
    `npx tsc --noEmit` clean. Because Plan 02 was a STRUCTURAL TSX change, run
    `npm run ui:shots:fresh` (= package + ui:shots — packaged no-injection is the proof; do NOT
    use UI_LAB_LIVE_CSS for this capture, it previews stale markup) with a NEW tag (e.g.
    `UI_LAB_TAG=p12-form-gate`). Confirm the run captures both the `edit-modal` and the new
    `edit-modal-validation` surfaces. Score the captures against the updated
    `DESIGN-RUBRIC.md §edit-modal` (two-group structure, blue 'Save changes', designed picker,
    even rhythm) and `§edit-modal-validation` (neutral hints in --ink-faint, danger red only on a
    genuine error). Populate the 12-VALIDATION.md Per-Task Verification Map rows for the Phase-12
    tasks (mark unit/smoke ✅ green; mark the UI-04 visual + SESS-06 Browse rows as Manual-Only
    pending the human gate). Record the capture tag + gitSha in the SUMMARY. If any rubric line
    fails, STOP and route a fix (do not present a known-failing app to the operator — the Phase-10
    T-10-10-01 discipline).
  </action>
  <verify>
    <automated>npm run test:unit && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit && npm run test:smoke` GREEN; `npx tsc --noEmit` exits 0.
    - `security.guard.test.ts` GREEN (EXPECTED_API_KEYS === 20) and `tokens-completeness.test.ts` GREEN.
    - `npm run ui:shots:fresh` completes and emits captures for BOTH `edit-modal` and `edit-modal-validation` (the capture artifacts exist under the ui-lab output dir for the new tag).
    - The captures are scored against the updated rubric and PASS every line (recorded in the SUMMARY with the tag + gitSha); no rubric line is left failing.
    - 12-VALIDATION.md Per-Task Verification Map is populated (no placeholder row remains).
  </acceptance_criteria>
  <done>Full suite GREEN, packaged capture of both surfaces scored PASS vs the updated rubric, VALIDATION map populated — the app is gate-ready.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: BLOCKING end-of-phase human-verify — SC1 + SESS-05 (O-1) + SESS-06 (O-2) LIVE on the packaged app</name>
  <files>.planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md</files>
  <read_first>
    - 12-VALIDATION.md §Manual-Only Verifications (the three live items + their test instructions)
    - 12-UI-SPEC.md §Verification Protocol (the Nyquist-flips-only-on-explicit-approval rule)
    - 12-CONTEXT.md §Open questions O-1 / O-2 (first-open default cwd; Browse… packaged end-to-end)
  </read_first>
  <action>
    Build the packaged app (`npm run make`), present it to the operator, and walk the
    how-to-verify script below. Do NOT present a known-failing app — Task 2 must be GREEN with a
    passing rubric capture first. On the explicit unqualified "approved", set
    `nyquist_compliant: true` in 12-VALIDATION.md and record the approval date; on any
    qualified/failed verdict, leave it false and capture the defect for a `--gaps` plan.
  </action>
  <verify>
    <human-check>Operator confirms LIVE on the packaged app: SC1 one-designed-surface, SESS-05 prefill incl. O-1 first-open default cwd, SESS-06 Browse… fills an absolute path + CR-01 gates a bad path. nyquist flips true only on explicit unqualified "approved".</human-check>
  </verify>
  <what-built>
    The restructured edit-session form on the packaged app: two semantic groups (Identity /
    Launch · Applies on restart) in a single column with subheads + a hairline divider; a designed
    emoji/color picker with hover/selected/focus states; inline validation (neutral hints for empty
    name + non-absolute cwd; main's 'Working directory not found' rejection inline under cwd in the
    danger ramp); a constructive accent-blue 'Save changes' button. SESS-05 prefill and the SESS-06
    Browse… picker are wired (Phase 6) and now visually composed. All automated checks are GREEN and
    the packaged ui:shots:fresh captures scored PASS vs the rubric.
  </what-built>
  <how-to-verify>
    Run `npm run make` and open the packaged app (macOS). Then:
    1. SC1 (one cohesive designed surface): add a session, open Edit. Confirm the form reads as ONE
       designed surface — two clearly separated groups with subheads + a divider, a tidy picker, an
       accent-blue 'Save changes' button (NOT red), even spacing. The canonical scan-test:
       "does this look like one intentional designed form, not a raw stack of inputs?"
    2. SESS-05 O-1 (first-open default cwd): on a BRAND-NEW session's FIRST Edit-open, look at the
       Working directory field — confirm it shows main's resolved home path (clear) rather than an
       empty field, OR report which reads clearer.
    3. SESS-05 round-trip: edit a session — set a cwd + a startup command — Save → reopen Edit →
       confirm both fields show the saved values (not empty).
    4. SESS-06 O-2 (Browse… live): in Edit, click Browse… → confirm the native folder dialog opens →
       pick a directory → confirm the Working directory field fills with the chosen ABSOLUTE path.
       Then hand-type a non-existent path and Start → confirm main rejects it ('Working directory
       not found' surfaced inline) — CR-01 still gates.
    5. Validation tone: clear the name → confirm a CALM neutral hint (not red); type a relative cwd →
       confirm a CALM neutral format hint. Red appears only on a genuine main rejection.
  </how-to-verify>
  <resume-signal>
    Type "approved" (unqualified) to flip nyquist_compliant true and close the phase. If any item
    fails or is qualified, describe the issue — it routes to a gap-closure plan; nyquist stays false.
  </resume-signal>
  <acceptance_criteria>
    - The operator runs the packaged app and confirms ALL of SC1, SESS-05 (incl. O-1), and SESS-06 (O-2) LIVE.
    - On explicit unqualified "approved": set `nyquist_compliant: true` in 12-VALIDATION.md frontmatter and record the approval date.
    - On any qualified/failed verdict: nyquist_compliant stays false; the reported defect is captured for a `--gaps` plan.
  </acceptance_criteria>
  <done>The operator has confirmed the three live behaviors on the packaged app and explicitly approved; nyquist_compliant flipped true.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native OS dialog → renderer field (SESS-06) | Browse… returns only existing dirs from the OS dialog; the chosen path fills edit-cwd via the EXISTING pickDirectory key |
| renderer cwd → main (Start) | the form value reaches pty.spawn cwd via the EXISTING create() path, gated by CR-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | cwd path (Browse… OR free-text) → pty.spawn cwd | accept (mitigated by existing CR-01) | The human-verify EXPLICITLY exercises CR-01 (hand-type a non-existent path → confirm main rejects). The guard in `pty-manager.ts` create() is unchanged and is the validator of record. No code change in this plan touches main. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — zero packages installed (no install task in this plan). |

This plan adds NO bridge key (EXPECTED_API_KEYS stays 20, asserted GREEN), NO IPC channel, and
touches NO PTY/xterm/fit path. It is test-extension + verification only. ASVS V5 trivial;
V1/V2/V3/V4/V6 N/A.
</threat_model>

<verification>
- `npm run test:unit && npm run test:smoke` GREEN (including the new SESS-05 round-trip case).
- `npx tsc --noEmit` exits 0.
- `npm run ui:shots:fresh` captures both surfaces; scored PASS vs the updated DESIGN-RUBRIC.
- `security.guard.test.ts` GREEN (EXPECTED_API_KEYS === 20); `tokens-completeness.test.ts` GREEN; fidelity smokes GREEN.
- Operator approval recorded; 12-VALIDATION.md nyquist_compliant true (only on explicit unqualified approval).
</verification>

<success_criteria>
- The smoke proves the SESS-05 cwd+startup round-trip and uses the 'Save changes' label.
- Full suite GREEN; packaged capture of edit-modal + edit-modal-validation scored PASS.
- The operator confirms SC1 + SESS-05 (O-1) + SESS-06 (O-2) LIVE on the packaged app.
- nyquist_compliant flips true only on explicit unqualified approval; UI-04/SESS-05/SESS-06 satisfied.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

| Symbol / Path | Kind | Notes |
|---------------|------|-------|
| SESS-05 round-trip test case | new smoke test | in `tests/smoke/session-edit.smoke.test.ts`; edit→Save→reopen cwd+startup |
| `clickMenuItem('Save changes')` | modified call | Rule-1 lockstep with Plan 02's Save relabel |
| edit-cwd / edit-startup smoke helpers | new helpers | set + read field values via the input-event path |
| `p12-form-gate` ui-lab capture | evidence | packaged no-injection capture of edit-modal + edit-modal-validation |
| populated 12-VALIDATION Per-Task Map | modified | unit/smoke ✅; UI-04 visual + SESS-06 Manual-Only |
| `nyquist_compliant: true` | modified frontmatter | flipped ONLY on explicit operator approval |
</artifacts_produced>

<output>
Create `.planning/phases/12-session-form-polish-edit-ux/12-03-SUMMARY.md` when done.
</output>
