---
phase: 12-session-form-polish-edit-ux
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/renderer/SessionEditModal.tsx
  - src/renderer/IconPicker.tsx
  - src/renderer/SessionManager.tsx
  - src/renderer/form.css
  - src/renderer/terminal.css
  - src/renderer/index.tsx
autonomous: true
requirements: [UI-04, SESS-05, SESS-06]
user_setup: []

must_haves:
  truths:
    - "The edit form reads as two semantic groups (Identity / Launch · Applies on restart) in a single column with subheads + a hairline divider"
    - "The Save button is constructive accent-blue (not danger-red), labelled 'Save changes', with the edit-save testid and .context-menu-item class intact"
    - "An empty name shows a neutral hint; a non-absolute cwd shows a neutral format hint; main's 'Working directory not found' rejection shows inline under the cwd field in the danger ramp"
    - "The IconPicker reads as a designed grid + swatch row with token-driven hover/selected/focus states"
    - "rehydrateProfiles delegates to mergeAuthoritativeProfiles so re-opening Edit prefills main's persisted cwd/startup (SESS-05)"
    - "EXPECTED_API_KEYS stays 20 and every CSS value resolves from tokens.css"
  artifacts:
    - path: "src/renderer/SessionEditModal.tsx"
      provides: "Two-group D-02 layout, blue Save, inline validation display, errorMessage prop"
      contains: "modal-btn-save"
    - path: "src/renderer/IconPicker.tsx"
      provides: "Polished designed picker (CSS-class + a11y states; emoji|color model unchanged)"
    - path: "src/renderer/SessionManager.tsx"
      provides: "rehydrateProfiles via mergeAuthoritativeProfiles; errorMessage threaded into the modal"
      contains: "mergeAuthoritativeProfiles"
    - path: "src/renderer/form.css"
      provides: "Extracted form/modal CSS — subheads, divider, validation notice, blue Save, picker polish"
      contains: ".modal-btn-save"
  key_links:
    - from: "src/renderer/SessionManager.tsx"
      to: "src/renderer/merge-profiles.ts"
      via: "import { mergeAuthoritativeProfiles }"
      pattern: "mergeAuthoritativeProfiles"
    - from: "src/renderer/SessionEditModal.tsx"
      to: "src/renderer/validate-session-form.ts"
      via: "import { validateSessionForm }"
      pattern: "validateSessionForm"
    - from: "src/renderer/index.tsx"
      to: "src/renderer/form.css"
      via: "import './form.css'"
      pattern: "form\\.css"
---

<objective>
The headline UI-04 work: compose the locked Switchboard tokens into ONE cohesive designed
edit-session surface. Restructure SessionEditModal into the D-02 two-group single-column
layout (Identity / Launch · Applies on restart) with section subheads + a hairline divider;
polish IconPicker into a designed grid + swatch row (D-03, emoji|color model unchanged); make
Save constructive accent-blue (D-04 Pitfall 1 — the required blue-Save fix); display the D-04
inline validation (neutral hints from validateSessionForm + main's CR-01 'Working directory
not found' rejection threaded inline under cwd via the existing row.errorMessage, zero new IPC);
and rewire rehydrateProfiles to delegate to mergeAuthoritativeProfiles (SESS-05). Extract the
form/modal CSS into form.css (the <800-line discretion call) and snap the legacy 6px/10px/22px
literals to clean --space-* steps.

Purpose: this is the verify-and-polish deliverable (per D-01: UI-04 polish + verify-and-finish
SESS-05/06, NOT greenfield) — the form must scan as one designed surface (SC1) on the locked
chassis, with SESS-05 prefill and the picker/validation visible. Per D-05 this stays the single
EDIT modal (instant-spawn-then-edit; no create-first form is introduced). The two SESS round-trips
are ALREADY wired (Phase 6); this plan only restructures the presentation and rewires rehydrate
through the tested reducer.
Output: the restructured modal + polished picker + form.css + the SESS-05 rewire, all renderer-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-session-form-polish-edit-ux/12-CONTEXT.md
@.planning/phases/12-session-form-polish-edit-ux/12-RESEARCH.md
@.planning/phases/12-session-form-polish-edit-ux/12-PATTERNS.md
@.planning/phases/12-session-form-polish-edit-ux/12-UI-SPEC.md
@.planning/phases/12-session-form-polish-edit-ux/12-01-SUMMARY.md
@src/renderer/SessionEditModal.tsx
@src/renderer/IconPicker.tsx
@src/renderer/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract form CSS to form.css + add subheads, divider, validation notice, blue Save, picker polish (tokens-first)</name>
  <files>src/renderer/form.css, src/renderer/terminal.css, src/renderer/index.tsx</files>
  <read_first>
    - src/renderer/terminal.css (the form block ~L160-489 to move: .modal-dialog-edit / .edit-field / .edit-label / .edit-input / .edit-select / .edit-cwd-row / .edit-browse-button / .edit-restart-group / .applies-on-restart-hint / .modal-actions / .modal-btn* / .icon-picker / .emoji-grid / .emoji-cell / .emoji-input / .color-swatches / .color-swatch; the .context-menu-item-danger danger-ramp idiom ~L271-279)
    - src/renderer/tokens.css (the --space-*, --color-accent, --color-accent-strong, --color-danger, --ink-faint, --line, --line-soft, --bg-sunk, --radius-* tokens; confirm --space-1_5/2_5 are the legacy aliases NOT to use)
    - src/renderer/index.tsx (the existing CSS import order: fonts → tokens.css → terminal.css — add form.css after terminal.css)
    - src/renderer/__tests__/tokens-completeness.test.ts (what counts as a banned literal + which files it scans — confirm form.css is included or extend the scan list IN THE SAME plan)
    - 12-PATTERNS.md §"terminal.css (MODIFY ... candidate form.css extract)" (the literal-snap table + the new CSS rules: .edit-group-subhead, .edit-group-divider, .edit-field-notice--hint/--error, .modal-btn-save)
    - 12-UI-SPEC.md §Spacing + §Color + §Typography (the formal scale; the accent-reserved + danger-reserved lists; the 3-size / 2-weight type contract)
  </read_first>
  <action>
    Create src/renderer/form.css and MOVE the form/modal CSS block out of terminal.css into it
    (the discretion extract — terminal.css is at 493/800 lines and Phase 13 adds more). Add
    `import './form.css'` in index.tsx immediately after the terminal.css import. If
    tokens-completeness.test.ts scans an explicit file list, add form.css to that list IN THIS
    TASK so the guard covers the new file. Then add the new tokens-first rules: `.edit-group-subhead`
    (shared visual with the existing `.applies-on-restart-hint` — 12px/700/uppercase/0.02em
    tracking/--ink-faint); `.edit-group-divider` (hairline border-top: 1px solid var(--line-soft),
    --space-5 vertical margin); `.edit-field-notice` with `--hint` (color: var(--ink-faint)) and
    `--error` (color: var(--color-danger)) modifiers (12px/400/1.3); and `.modal-btn-save`
    (background + border-color: var(--color-accent); hover → var(--color-accent-strong); focus-visible
    → 2px var(--color-accent) ring) as a CONSTRUCTIVE-blue button distinct from the red
    `.modal-btn-confirm` (Pitfall 1 — required, not discretion). Polish the picker CSS: emoji-cell +
    color-swatch hover (--bg-sunk wash) / selected (ring via --line or --color-accent) / focus-visible
    (2px var(--color-accent)) states; snap the swatch gap and input vertical padding off the legacy
    6px/10px aliases to clean --space-2 (8px) / --space-3 (12px) steps; snap the 22px dialog top-pad
    and 2px restart-group pad to the nearest clean --space-* step. Every value must be a `var()`
    except element dimensions (dialog width min(...), tile aspect-ratio/sizes) and the single
    `#ffffff` button text where contrast requires it. No new motion system — reuse --duration-fast /
    --ease-standard for control transitions on compositor-friendly properties only.
  </action>
  <verify>
    <automated>npm run test:unit 2>&1 | grep -E "tokens-completeness" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `tokens-completeness.test.ts` GREEN (every var(--token) in form.css resolves; no banned literals; the new file is in the scan).
    - `grep -c "modal-btn-save" src/renderer/form.css` >= 1; `.modal-btn-save` references `var(--color-accent)` (`grep -c "color-accent" src/renderer/form.css` >= 1).
    - The legacy aliases are NOT introduced as canonical steps in the new rules: `grep -Ec "var\\(--space-1_5\\)|var\\(--space-2_5\\)" src/renderer/form.css` == 0 (snapped to clean steps).
    - `grep -c "form.css" src/renderer/index.tsx` == 1 (imported).
    - terminal.css no longer contains the moved form block (`grep -c "edit-restart-group" src/renderer/terminal.css` == 0) and stays under 800 lines.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The form CSS lives in form.css with the subhead/divider/validation/blue-Save/picker rules, tokens-first, guard GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: SessionEditModal — D-02 two-group restructure + IconPicker polish + D-04 inline validation display</name>
  <files>src/renderer/SessionEditModal.tsx, src/renderer/IconPicker.tsx</files>
  <read_first>
    - src/renderer/SessionEditModal.tsx (the current flat-stack JSX :138-291; the seed effect :68-75 to PRESERVE; the Esc/focus a11y :93-105 to PRESERVE; the handleSave DOM-read logic :109-136 to PRESERVE; the Save button :279-286 carrying modal-btn-confirm + context-menu-item)
    - src/renderer/IconPicker.tsx (the emoji grid + free-text emoji input + color-swatch JSX :39-92 — preserve structure, CSS-class polish only; the renderIcon preview)
    - src/renderer/validate-session-form.ts (Plan 01 — the validateSessionForm signature + FieldNotice shape to consume)
    - 12-PATTERNS.md §SessionEditModal.tsx (the D-02 restructure target JSX; the errorMessage prop addition; the validation-display pattern under the cwd field; the FROZEN testid list)
    - 12-PATTERNS.md §IconPicker.tsx (the CSS-class polish scope — JSX preserved)
    - 12-UI-SPEC.md §Form Structure Contract + §Copywriting Contract + §Interaction Contract (the group ordering, the FROZEN copy 'Save changes' / 'Launch · Applies on restart' / the two hint strings / 'Working directory not found', the picker/focus interaction states)
  </read_first>
  <action>
    Restructure the modal JSX into the D-02 layout WITHOUT renaming any frozen testid. Wrap the
    Name + Icon fields in a Group A container with an `.edit-group-subhead` reading `Identity`.
    Keep the existing `.edit-restart-group` as Group B but give its `.applies-on-restart-hint`
    (testid `applies-on-restart` — REUSED, not minted) the copy `Launch · Applies on restart` and
    style it as the group subhead; add the `.edit-group-divider` hairline between the two groups
    (rule vs recessed-surface treatment per UI-SPEC §Form Structure — tune in the ui-lab loop in
    Plan 03's gate, but ship a divider here). Change the Save button class from
    `modal-btn modal-btn-confirm context-menu-item` to `modal-btn modal-btn-save context-menu-item`
    (KEEP `.context-menu-item` — the smoke's clickMenuItem('Save') depends on it; KEEP the
    `edit-save` testid) and change its visible label from `Save` to `Save changes`. Add a new prop
    `errorMessage?: string` to SessionEditModalProps. Import validateSessionForm; compute notices
    from the live form values (name/cwd/startupCommand) and render the per-field notice under the
    Name field and under the cwd field as `<span className="edit-field-notice edit-field-notice--{tone}">`.
    When `errorMessage` matches the CR-01 cwd-rejection shape (starts with 'Working directory not
    found'), render it under the cwd field as the `--error`-tone notice (do NOT echo the raw path
    beyond what main already sanitized). PRESERVE the seed effect, the Esc/focus a11y, and the
    handleSave DOM-read logic verbatim. In IconPicker.tsx, leave the JSX structure intact (emoji
    grid, free-text input, swatch row, preview) and rely on the new form.css picker rules for the
    designed hover/selected/focus states — no model change (emoji|color stays). Do NOT add the
    real-glyph icon system (D-03 backlog boundary).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "modal-btn-confirm" src/renderer/SessionEditModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "modal-btn-save" src/renderer/SessionEditModal.tsx` == 1 AND `grep -c "modal-btn-confirm" src/renderer/SessionEditModal.tsx` == 0 (Save is the new constructive class; no inherited red).
    - `grep -c "context-menu-item" src/renderer/SessionEditModal.tsx` >= 1 (smoke contract intact) and the visible Save label is `Save changes` (`grep -c "Save changes" src/renderer/SessionEditModal.tsx` == 1).
    - All frozen testids survive: `grep -Ec "session-edit-modal|edit-name|edit-cwd|browse-cwd|edit-shell|edit-startup|applies-on-restart|edit-save|edit-cancel" src/renderer/SessionEditModal.tsx` == 9 (each present).
    - `grep -c "validateSessionForm" src/renderer/SessionEditModal.tsx` >= 1 and `grep -c "edit-field-notice" src/renderer/SessionEditModal.tsx` >= 1 (validation display wired).
    - `grep -c "errorMessage" src/renderer/SessionEditModal.tsx` >= 1 (the new prop) and the 'Identity' subhead is present (`grep -c "Identity" src/renderer/SessionEditModal.tsx` >= 1).
    - IconPicker emoji|color model unchanged: `grep -Ec "SVG|icon-font|glyphSet" src/renderer/IconPicker.tsx` == 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The modal renders two designed groups, a blue 'Save changes' button, inline validation, and a polished picker — all frozen testids preserved.</done>
</task>

<task type="auto">
  <name>Task 3: SessionManager — rehydrate via mergeAuthoritativeProfiles (SESS-05) + thread errorMessage into the modal</name>
  <files>src/renderer/SessionManager.tsx</files>
  <read_first>
    - src/renderer/SessionManager.tsx:380-405 (the inline rehydrateProfiles useCallback to rewire as a thin wrapper)
    - src/renderer/SessionManager.tsx:407-454 (handleSaveProfile + onAdd — both already call rehydrateProfiles; confirm no behavior change needed)
    - src/renderer/SessionManager.tsx:~695-720 (where activeRecord.errorMessage is passed to IdleCard at :699; the modal wiring onSaveLive/onSaveProfile/onCancel — pass the active row's errorMessage as the new modal prop)
    - src/renderer/merge-profiles.ts (Plan 01 — the mergeAuthoritativeProfiles signature)
    - 12-PATTERNS.md §merge-profiles.ts ("After extraction — rehydrateProfiles becomes" thin wrapper)
    - 12-RESEARCH.md §"How main's CR-01 rejection reaches the form WITHOUT a new IPC" (the errorMessage prop path; keep the IdleCard card too — Open Q2)
  </read_first>
  <action>
    Import `mergeAuthoritativeProfiles` from './merge-profiles'. Rewrite the rehydrateProfiles
    useCallback body to a thin wrapper: read `const authoritative = await window.api.listSessions()`,
    then `setSessions((prev) => mergeAuthoritativeProfiles(prev, authoritative))`. DELETE the inline
    Map/map block (it now lives in the tested reducer); keep the explanatory comment about main being
    the truth source + status/errorMessage being owned by onPtyStatus. Do NOT change handleSaveProfile
    or onAdd — they already invoke rehydrateProfiles (the SESS-05 round-trip is unchanged behavior,
    now flowing through the tested reducer). At the SessionEditModal wiring site, pass the active
    row's errorMessage as the new `errorMessage` prop (read defensively like the IdleCard does:
    `errorMessage={activeRecord?.errorMessage}` or the editing-target row's errorMessage — match the
    modal's `session` target). KEEP the IdleCard errorMessage card at :699 (Open Q2 — both surfaces
    fire at different moments). No new bridge key, no IPC change.
  </action>
  <verify>
    <automated>npm run test:unit && npx tsc --noEmit && npm run test:smoke 2>&1 | grep -E "session-edit|security"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mergeAuthoritativeProfiles" src/renderer/SessionManager.tsx` >= 1 (imported + used) and the inline `new Map(authoritative` block is gone (`grep -c "new Map(authoritative" src/renderer/SessionManager.tsx` == 0).
    - The modal receives the errorMessage prop: `grep -c "errorMessage=" src/renderer/SessionManager.tsx` >= 2 (the existing IdleCard pass + the new modal pass).
    - `npm run test:unit` GREEN (merge-profiles + all reducers) and `security.guard.test.ts` GREEN (EXPECTED_API_KEYS === 20 — no bridge change).
    - The session-edit smoke still passes (or is the documented parallel-load timing flake per STATE — re-run isolated to confirm 3/3): `npm run test:smoke` session-edit spec GREEN.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>rehydrateProfiles flows through the tested reducer; the modal surfaces main's CR-01 rejection inline; no bridge change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer form → main (existing channels) | cwd/shell/startup persist via the EXISTING ptyUpdateProfile; cwd reaches pty.spawn via the EXISTING create() path; Browse… uses the EXISTING pickDirectory |
| main → renderer (existing notice) | the CR-01 'Working directory not found' rejection rides the EXISTING onPtyStatus notice → row.errorMessage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | cwd path (free-text OR Browse…) → pty.spawn cwd | accept (mitigated by existing CR-01) | Main's CR-01 guard in `pty-manager.ts` create() (absolute + statSync().isDirectory(), errors 'Working directory not found' rather than spawning in ~) is UNCHANGED and stays the validator of record. The form's renderer pre-check is convenience only. Verify the guard is untouched (no edit to pty-manager.ts in this plan). |
| T-12-02 | Tampering/XSS | inline error string under cwd | mitigate | The error string is main's already-sanitized notice (`sanitizeNotice()`); the renderer renders it as a React text node (auto-escaped), no `dangerouslySetInnerHTML`. The CR-01-rejection match is a prefix check, not an eval. |
| T-12-03 | Elevation | new IPC surface | mitigate | NONE added — rehydrate reuses listSessions, the rejection reuses the onPtyStatus notice, persist reuses ptyUpdateProfile, Browse… reuses pickDirectory. EXPECTED_API_KEYS stays 20, asserted GREEN by security.guard.test.ts. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — zero packages installed (RESEARCH §Package Legitimacy Audit: not applicable). No install task. |

ASVS V5 applies trivially (escaped text nodes + fixed/ sanitized literals). V1/V2/V3/V4/V6 do
not apply (no auth/session/access-control/crypto change). No PTY/xterm/fit path touched — terminal
fidelity safe by construction; the fidelity smokes prove it stays GREEN.
</threat_model>

<verification>
- `npm run test:unit` GREEN (the two reducers + all guards).
- `npx tsc --noEmit` exits 0.
- `tokens-completeness.test.ts` GREEN (form.css tokens-first; no literals).
- `security.guard.test.ts` GREEN (EXPECTED_API_KEYS === 20).
- `npm run test:smoke` — session-edit smoke GREEN (re-run isolated if the known parallel-load flake appears); fidelity smokes GREEN.
- `pty-manager.ts` / `window-config.ts` unchanged (no main edit, no bridge change).
</verification>

<success_criteria>
- The modal renders as two designed groups in a single column with subheads + a divider (D-02).
- Save is constructive accent-blue, labelled 'Save changes', testid + .context-menu-item intact.
- Empty-name + non-absolute-cwd neutral hints render; the CR-01 rejection renders inline under cwd in the danger ramp.
- IconPicker reads as a designed grid + swatch row (D-03), emoji|color model unchanged.
- rehydrateProfiles delegates to the tested mergeAuthoritativeProfiles (SESS-05).
- All frozen testids preserved; EXPECTED_API_KEYS stays 20; suite GREEN; tsc clean.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

| Symbol / Path | Kind | Notes |
|---------------|------|-------|
| `src/renderer/form.css` | new file | extracted form/modal CSS + subhead/divider/validation/blue-Save/picker rules |
| `.edit-group-subhead` | CSS class | section subhead (Identity / Launch) |
| `.edit-group-divider` | CSS class | hairline between the two groups |
| `.edit-field-notice` / `--hint` / `--error` | CSS classes | inline validation notice tones |
| `.modal-btn-save` | CSS class | constructive accent-blue Save (replaces inherited red for this surface) |
| `errorMessage?: string` | new prop | added to `SessionEditModalProps` (threads the CR-01 rejection inline) |
| restructured `SessionEditModal` JSX | modified | two-group D-02 layout, 'Save changes', validation display |
| polished IconPicker states | modified (CSS-driven) | designed grid/swatch hover/selected/focus (model unchanged) |
| `rehydrateProfiles` (thin wrapper) | modified | delegates to `mergeAuthoritativeProfiles` |
</artifacts_produced>

<output>
Create `.planning/phases/12-session-form-polish-edit-ux/12-02-SUMMARY.md` when done.
</output>
