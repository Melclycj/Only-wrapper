---
phase: 12-session-form-polish-edit-ux
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/renderer/merge-profiles.ts
  - src/renderer/__tests__/merge-profiles.test.ts
  - src/renderer/validate-session-form.ts
  - src/renderer/__tests__/validate-session-form.test.ts
  - tests/ui-lab/surfaces.ts
  - tests/ui-lab/DESIGN-RUBRIC.md
autonomous: true
requirements: [SESS-05, UI-04]
user_setup: []

must_haves:
  truths:
    - "mergeAuthoritativeProfiles merges main's cwd/shell/startupCommand/configured back into rows by logicalId without touching status/errorMessage"
    - "validateSessionForm returns the frozen neutral hints for empty-name and non-absolute-cwd, and nothing for valid fields"
    - "The ui-lab edit-modal-validation surface exists so the D-04 inline-validation visual claim has a capture to score"
    - "The DESIGN-RUBRIC edit-modal section asserts the two-group structure and the blue Save"
  artifacts:
    - path: "src/renderer/merge-profiles.ts"
      provides: "Pure mergeAuthoritativeProfiles reducer (SESS-05 merge)"
      exports: ["mergeAuthoritativeProfiles"]
    - path: "src/renderer/__tests__/merge-profiles.test.ts"
      provides: "RED→GREEN tests for the merge reducer (4 cases)"
    - path: "src/renderer/validate-session-form.ts"
      provides: "Pure validateSessionForm reducer + isAbsolutePathish (D-04 rules)"
      exports: ["validateSessionForm", "FieldTone", "FieldNotice", "FormFieldValues"]
    - path: "src/renderer/__tests__/validate-session-form.test.ts"
      provides: "RED→GREEN tests for the validation rules (5 cases)"
    - path: "tests/ui-lab/surfaces.ts"
      provides: "New edit-modal-validation surface"
      contains: "edit-modal-validation"
    - path: "tests/ui-lab/DESIGN-RUBRIC.md"
      provides: "Updated edit-modal rubric + new edit-modal-validation rubric section"
      contains: "edit-modal-validation"
  key_links:
    - from: "src/renderer/__tests__/merge-profiles.test.ts"
      to: "src/renderer/merge-profiles.ts"
      via: "import { mergeAuthoritativeProfiles }"
      pattern: "from '\\.\\./merge-profiles'"
    - from: "src/renderer/__tests__/validate-session-form.test.ts"
      to: "src/renderer/validate-session-form.ts"
      via: "import { validateSessionForm }"
      pattern: "from '\\.\\./validate-session-form'"
---

<objective>
Wave-0 interface-first foundation for Phase 12. Extract the two pure reducers the
renderer composition (Plan 02) will consume, with RED→GREEN tests so the SESS-05 merge
logic and the D-04 validation rules are unit-testable in the Node/Vitest env (no jsdom).
In the same wave, add the ui-lab evidence scaffolds — the new `edit-modal-validation`
surface and the updated `DESIGN-RUBRIC.md §edit-modal` lines — so the UI-04 visual SC has
a capture and a rubric to score against before any markup changes.

Purpose: define the contracts (reducer signatures + the surface/rubric) so Plan 02 builds
against them rather than discovering them, and so the SESS-05/D-04 logic has automated
coverage that runs in CI without a DOM.
Output: 2 new pure reducer modules, 2 new RED→GREEN test files, 1 new ui-lab surface, an
updated DESIGN-RUBRIC.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-session-form-polish-edit-ux/12-CONTEXT.md
@.planning/phases/12-session-form-polish-edit-ux/12-RESEARCH.md
@.planning/phases/12-session-form-polish-edit-ux/12-PATTERNS.md
@.planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Pure mergeAuthoritativeProfiles reducer (SESS-05) — RED→GREEN</name>
  <files>src/renderer/merge-profiles.ts, src/renderer/__tests__/merge-profiles.test.ts</files>
  <read_first>
    - src/renderer/session-edit.ts (the file-header style + module shape to mirror — imports ONLY ../shared/types, no React/xterm/electron)
    - src/renderer/__tests__/session-edit.test.ts (the Wave-0 RED stub header + describe/it/expect shape to copy)
    - src/renderer/SessionManager.tsx:380-405 (the inline rehydrateProfiles map this reducer extracts; the exact 4 fields it copies: cwd/shell/startupCommand/configured; the status/errorMessage invariant comment at :387)
    - src/shared/types.ts (the SessionRecord shape — confirm logicalId/cwd/shell/startupCommand/configured fields)
    - 12-PATTERNS.md §merge-profiles.ts (the required module shape + the 4 required test cases)
    - 12-RESEARCH.md §"Reducer 1 — mergeAuthoritativeProfiles" (the RED→GREEN case definitions)
  </read_first>
  <behavior>
    - Test (a) fresh-spawn: a row minted with cwd:'' + an authoritative record carrying main's resolved cwd (e.g. a home path) → merged row shows main's cwd, not ''.
    - Test (b) edited-then-saved: a row with a stale submitted cwd + an authoritative record carrying a trimmed/validated cwd → merged row shows main's persisted truth.
    - Test (c) status/errorMessage untouched: a row with status:'running', errorMessage:'x' → after merge both are byte-identical (===) to the input.
    - Test (d) unknown id: a row whose logicalId is absent from authoritative → returned unchanged (object-equal to input).
    - configured: when truth.configured is undefined, falls back to row.configured (truth.configured ?? row.configured).
  </behavior>
  <action>
    Write the RED test FIRST at src/renderer/__tests__/merge-profiles.test.ts importing
    `mergeAuthoritativeProfiles` from '../merge-profiles' (mirror the session-edit.test.ts
    Wave-0 RED header; cite this plan + SESS-05). Encode the four behavior cases above plus
    the configured-fallback assertion. Run `npm run test:unit` and confirm it FAILS RED
    (module does not exist). THEN create src/renderer/merge-profiles.ts: a RENDERER-ONLY
    pure module importing only `import type { SessionRecord } from '../shared/types'`, with
    a header mirroring session-edit.ts. Export
    `mergeAuthoritativeProfiles<T extends SessionRecord>(rows: readonly T[], authoritative: readonly SessionRecord[]): T[]`
    that builds a `Map` keyed by logicalId, maps each row, returns the row unchanged when no
    truth is found, and otherwise spreads the row and overwrites EXACTLY four fields —
    cwd, shell, startupCommand, and `configured: truth.configured ?? row.configured`. Do NOT
    spread the full authoritative record and do NOT touch status/errorMessage/agentState
    (Pitfall 5). Run `npm run test:unit` and confirm GREEN. Do NOT yet rewire
    SessionManager.rehydrateProfiles — that wiring is Plan 02 (keeps this wave disjoint from
    SessionManager.tsx).
  </action>
  <verify>
    <automated>npm run test:unit 2>&1 | grep -E "merge-profiles" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - merge-profiles.test.ts is GREEN under `npm run test:unit` (all 5 assertions pass).
    - `npx tsc --noEmit` exits 0.
    - `grep -c "from '../shared/types'" src/renderer/merge-profiles.ts` == 1 and the file
      contains no `import ... from 'react'` / 'xterm' / 'electron' (run
      `grep -Ec "from 'react'|xterm|electron" src/renderer/merge-profiles.ts` == 0).
    - The merge body copies only cwd/shell/startupCommand/configured: `grep -Ec "status|errorMessage|agentState" src/renderer/merge-profiles.ts` == 0.
    - SessionManager.tsx is unchanged by this task (`git diff --name-only` does not list it).
  </acceptance_criteria>
  <done>The pure SESS-05 merge reducer exists with passing tests; SessionManager still owns its inline rehydrate (rewired in Plan 02).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Pure validateSessionForm reducer (D-04) — RED→GREEN</name>
  <files>src/renderer/validate-session-form.ts, src/renderer/__tests__/validate-session-form.test.ts</files>
  <read_first>
    - src/renderer/session-edit.ts (the module-shape + header style to mirror)
    - src/renderer/__tests__/session-edit.test.ts (the RED stub + assertion shape)
    - 12-PATTERNS.md §validate-session-form.ts (the required module shape, the isAbsolutePathish regex, the FROZEN copy strings)
    - 12-UI-SPEC.md §Copywriting Contract (the FROZEN literals: empty-name = 'Keeps the current name'; cwd format = 'Enter an absolute path, or use Browse…')
    - 12-RESEARCH.md §2 (the RED→GREEN cases + the isAbsolutePathish cross-platform note)
  </read_first>
  <behavior>
    - empty name ('') → { name: { tone: 'hint', message: 'Keeps the current name' } }
    - whitespace-only name ('   ') → same hint (trim before checking length)
    - non-absolute cwd ('relative/path') → { cwd: { tone: 'hint', message: 'Enter an absolute path, or use Browse…' } }
    - POSIX absolute cwd ('/Users/me/project') → {} (no cwd notice)
    - Windows absolute cwd ('C:\\Users\\me') → {} (no cwd notice)
    - empty cwd ('') → no cwd notice (only checked when non-empty)
    - empty startupCommand → {} (startup is optional; never validated)
  </behavior>
  <action>
    Write the RED test FIRST at src/renderer/__tests__/validate-session-form.test.ts importing
    `validateSessionForm` from '../validate-session-form' (mirror the Wave-0 RED header; cite
    this plan + D-04). Encode the seven behavior cases above using the EXACT frozen literals.
    Run `npm run test:unit` → confirm RED. THEN create src/renderer/validate-session-form.ts:
    a RENDERER-ONLY pure module importing nothing external (no ../shared/types needed — the
    field shape is local). Export `type FieldTone = 'hint' | 'error'`, interfaces
    `FieldNotice { tone: FieldTone; message: string }` and
    `FormFieldValues { name: string; cwd: string; startupCommand: string }`, and
    `validateSessionForm(f: FormFieldValues): Partial<Record<keyof FormFieldValues, FieldNotice>>`.
    Empty/whitespace name → the 'hint'-tone 'Keeps the current name' notice. Non-empty,
    non-absolute cwd → the 'hint'-tone 'Enter an absolute path, or use Browse…' notice.
    startupCommand → never a notice. Add a private `isAbsolutePathish(s: string): boolean`
    using the renderer-safe regex (POSIX `^/`, Windows `^[A-Za-z]:[/\\]`, UNC `^\\\\`) — a
    FORMAT check only, never an existence check; main's CR-01 stays the validator of record.
    Run `npm run test:unit` → confirm GREEN.
  </action>
  <verify>
    <automated>npm run test:unit 2>&1 | grep -E "validate-session-form" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - validate-session-form.test.ts is GREEN under `npm run test:unit` (all 7 assertions pass).
    - The frozen literals are present verbatim: `grep -c "Keeps the current name" src/renderer/validate-session-form.ts` == 1 and `grep -c "Enter an absolute path, or use Browse" src/renderer/validate-session-form.ts` == 1.
    - No external import: `grep -Ec "from 'react'|xterm|electron|from '\\.\\./shared" src/renderer/validate-session-form.ts` == 0.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The pure D-04 validation reducer exists with passing tests; Plan 02 wires it into the modal display.</done>
</task>

<task type="auto">
  <name>Task 3: ui-lab evidence scaffolds — edit-modal-validation surface + DESIGN-RUBRIC update</name>
  <files>tests/ui-lab/surfaces.ts, tests/ui-lab/DESIGN-RUBRIC.md</files>
  <read_first>
    - tests/ui-lab/surfaces.ts:104-132 (the helpers: openEditModal, setInputByTestId, clickByTestId, waitForTestId, waitForTestIdGone)
    - tests/ui-lab/surfaces.ts:400-417 (the existing edit-modal surface entry — the exact shape to copy for the validation variant)
    - tests/ui-lab/DESIGN-RUBRIC.md:108-119 (the current §edit-modal rubric lines to replace + the inactive-recipes "same DNA as edit-modal" reference at :119, which must keep resolving)
    - 12-PATTERNS.md §"tests/ui-lab/surfaces.ts + DESIGN-RUBRIC.md" (the exact new surface block + the rubric replacement text)
    - 12-UI-SPEC.md §Verification Protocol (the structural-change → ui:shots:fresh rule; the two rubric additions: D-02 grouping + blue Save)
  </read_first>
  <action>
    In tests/ui-lab/surfaces.ts, ADD a new surface entry with id `edit-modal-validation`
    immediately after the existing `edit-modal` entry, modeled byte-for-byte on its shape
    (designRefs citing 12-UI-SPEC.md §Color + 12-CONTEXT.md D-04; an `expects` one-liner about
    the danger-ramp helper under cwd + the neutral name hint). Its `prepare` opens the modal
    via `openEditModal(ctx.ids[0])`, then drives the renderer hints by
    `setInputByTestId('edit-cwd', 'not-absolute')` and `setInputByTestId('edit-name', '')`,
    then `browser.pause(300)`; its `cleanup` clicks `edit-cancel` and
    `waitForTestIdGone('session-edit-modal')`. Use ONLY the existing helpers (do not invent new
    testids — the validation display testids land in Plan 02; the surface still captures the
    pre-validation modal state until then, which is acceptable for the Wave-0 scaffold). In
    tests/ui-lab/DESIGN-RUBRIC.md, REPLACE the §edit-modal checklist (current lines 108-115) with
    the updated lines that assert (a) the two-group "Identity" / "Launch · Applies on restart"
    structure with subheads + hairline divider, single column; (b) Save = accent-blue fill
    (constructive, NOT danger-red) with label "Save changes"; (c) the picker hover/selected/grid
    treatment; (d) even field rhythm + clear inter-group break — per 12-PATTERNS.md. APPEND a new
    `## edit-modal-validation` rubric section (the cwd format hint in --ink-faint, the empty-name
    hint in --ink-faint, danger red absent unless main rejects, per-field not modal-wide). Leave
    the inactive-recipes "same DNA as edit-modal" reference (:119) intact and resolving.
  </action>
  <verify>
    <automated>grep -c "edit-modal-validation" tests/ui-lab/surfaces.ts && grep -c "edit-modal-validation" tests/ui-lab/DESIGN-RUBRIC.md && grep -c "Save changes" tests/ui-lab/DESIGN-RUBRIC.md && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id: 'edit-modal-validation'" tests/ui-lab/surfaces.ts` == 1.
    - The new surface uses only existing helpers: `grep -Ec "openEditModal|setInputByTestId|clickByTestId|waitForTestIdGone" tests/ui-lab/surfaces.ts` is unchanged-or-higher and no new helper function is defined.
    - DESIGN-RUBRIC.md §edit-modal asserts the two-group structure (`grep -Ec "Identity|Launch|two-group|two group" tests/ui-lab/DESIGN-RUBRIC.md` >= 1) and the blue Save (`grep -Ec "accent-blue|accent blue|blue fill" tests/ui-lab/DESIGN-RUBRIC.md` >= 1).
    - DESIGN-RUBRIC.md contains a `## edit-modal-validation` section (`grep -c "## edit-modal-validation" tests/ui-lab/DESIGN-RUBRIC.md` == 1).
    - `npx tsc --noEmit` exits 0 (surfaces.ts is TS and must still compile).
  </acceptance_criteria>
  <done>The validation surface + the updated rubric exist so Plan 02's structural change has a fresh-capture target and a scoring contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer string → form state | Renderer-local validation strings; no value crosses the IPC bridge in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | cwd path → eventual pty.spawn cwd | accept | Main's existing CR-01 guard (`pty-manager.ts` create() — absolute + statSync().isDirectory()) is the validator of record and is UNCHANGED. This plan only adds a renderer-cheap FORMAT hint (isAbsolutePathish), never an existence check; it grants no capability and crosses no boundary. |
| T-12-02 | Information disclosure | validation copy strings | accept | The two validation strings are FIXED literals (no path echo); rendered later by Plan 02 as React text nodes (auto-escaped). No `dangerouslySetInnerHTML`. |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — zero packages installed this plan (RESEARCH §Package Legitimacy Audit: not applicable). No install task exists. |

This plan adds NO bridge key (EXPECTED_API_KEYS stays 20), NO IPC channel, and touches NO
PTY/xterm/fit path. ASVS V5 applies trivially (fixed literals); V1/V2/V3/V4/V6 do not apply.
</threat_model>

<verification>
- `npm run test:unit` GREEN including the two new reducer test files.
- `npx tsc --noEmit` exits 0.
- `security.guard.test.ts` GREEN (EXPECTED_API_KEYS === 20 — untouched; no bridge change).
- `tokens-completeness.test.ts` GREEN (no CSS touched this plan).
- surfaces.ts compiles; the new validation surface is registered.
</verification>

<success_criteria>
- mergeAuthoritativeProfiles + validateSessionForm exist as pure modules with GREEN tests.
- The merge reducer copies exactly 4 fields and never touches status/errorMessage.
- The validation reducer emits the frozen literals and only the documented hints.
- The edit-modal-validation surface + the updated DESIGN-RUBRIC exist for Plan 02's capture.
- No bridge key added; suite GREEN; tsc clean.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

| Symbol / Path | Kind | Notes |
|---------------|------|-------|
| `src/renderer/merge-profiles.ts` | new file | exports `mergeAuthoritativeProfiles` (SESS-05 merge reducer) |
| `mergeAuthoritativeProfiles` | function | `<T extends SessionRecord>(rows, authoritative) => T[]`; copies cwd/shell/startupCommand/configured only |
| `src/renderer/__tests__/merge-profiles.test.ts` | new test | 4 cases + configured-fallback |
| `src/renderer/validate-session-form.ts` | new file | exports `validateSessionForm`, `FieldTone`, `FieldNotice`, `FormFieldValues` |
| `validateSessionForm` | function | `(f: FormFieldValues) => Partial<Record<keyof FormFieldValues, FieldNotice>>` |
| `isAbsolutePathish` | private function | renderer-safe POSIX/Windows/UNC format check |
| `src/renderer/__tests__/validate-session-form.test.ts` | new test | 7 cases, frozen literals |
| `edit-modal-validation` | new ui-lab surface | in `tests/ui-lab/surfaces.ts` |
| `## edit-modal-validation` | new rubric section | in `tests/ui-lab/DESIGN-RUBRIC.md`; §edit-modal lines updated for D-02 + blue Save |
</artifacts_produced>

<output>
Create `.planning/phases/12-session-form-polish-edit-ux/12-01-SUMMARY.md` when done.
</output>
