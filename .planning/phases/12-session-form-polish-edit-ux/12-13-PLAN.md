---
phase: 12-session-form-polish-edit-ux
plan: 13
type: execute
wave: 2
depends_on: ["12-11", "12-12"]
files_modified:
  - .planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md
autonomous: false
gap_closure: true
requirements: [UI-04, SESS-05, SESS-06]
user_setup: []

must_haves:
  truths:
    - "The freshly PACKAGED app (npm run package) is verified by the OPERATOR on a COLD Dock launch (`sudo purge` then cold launch from Finder/Dock) — the ONLY condition that reproduces the marker-loss; a warm/dev run will NOT reproduce it."
    - "GAP-12-B is explicitly approved: on a cold-launch Restart-to-apply AND a cold-launch dormant Start with a saved command, the new startup command AUTO-RUNS (no 'shell wasn't ready in time')."
    - "GAP-12-C is explicitly approved: a failed spawn (deleted-cwd Start AND Restart) surfaces a VISIBLE error (IdleCard + 'Working directory not found') where the user acted — it does NOT silently 'return to home'."
    - "GAP-12-E is explicitly approved: an invalid/non-existent absolute path at Save shows the inline reminder, KEEPS the modal open, and SUPPRESSES the restart prompt (the prompt does NOT pop first)."
    - "nyquist_compliant flips true ONLY on the operator's explicit UNQUALIFIED approval of all three (B + C + E). Any qualified/partial result keeps it false and routes to a round-4 gap closure."
    - "It is stated plainly in the verify steps that automated GREEN is NOT proof here — the DEBT-02 blind spot bit twice (the cold marker-loss never reproduces in the warm wdio/dev env)."
  artifacts:
    - path: ".planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md"
      provides: "The round-3 re-gate result appended (operator verdict per B/C/E + the nyquist_compliant decision)."
      contains: "Re-Gate 3"
  key_links:
    - from: "the packaged app cold Dock launch (sudo purge)"
      to: "the operator's B/C/E verdict"
      via: "the BLOCKING human-verify checkpoint"
      pattern: "cold"
    - from: "the operator's unqualified approval"
      to: "nyquist_compliant: true"
      via: "the re-gate decision recorded in 12-VERIFICATION.md"
      pattern: "nyquist_compliant"
---

<objective>
The round-3 re-gate. The two code plans (12-11 marker re-send + DIAG removal; 12-12 GAP-12-E precedence) have
landed and the automated chain is GREEN — but automated GREEN is NOT proof: the cold marker-loss (GAP-12-B) only
reproduces inside the PACKAGED app on a COLD Dock launch and has slipped the warm wdio/dev env TWICE (the DEBT-02
blind spot). This plan packages the app and runs the BLOCKING operator human-verify on a cold Dock launch,
covering GAP-12-B + GAP-12-C + GAP-12-E live, then records the verdict and the nyquist decision.

Purpose: prove the marker-loss fix on the only condition that reproduces it, and confirm the precedence +
error-surfacing fixes live — flipping nyquist_compliant true ONLY on the operator's explicit unqualified approval.
Output: the packaged app + the operator's recorded B/C/E verdict in 12-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/gap-12-b-restart-probe-timeout.md
@.planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md
</context>

<artifacts_this_phase_produces>
New symbols / files introduced by THIS plan:

- No code artifacts. This plan produces:
  - A freshly packaged app at `out/Just-Wrapper-darwin-arm64/Just-Wrapper.app` (rebuilt from the fix branch).
  - An appended `## Re-Gate 3 Result` section in `12-VERIFICATION.md` recording the operator's per-item verdict
    (GAP-12-B / GAP-12-C / GAP-12-E) and the `nyquist_compliant` decision (true ONLY on unqualified approval).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Package the fix branch + run the full automated chain (the floor, NOT the proof)</name>
  <files>out/Just-Wrapper-darwin-arm64/Just-Wrapper.app (built artifact)</files>
  <read_first>
    - package.json (the package / test:unit / test:integration / test:integration:marker-loss scripts)
    - .planning/phases/12-session-form-polish-edit-ux/12-11-SUMMARY.md (confirm the marker re-send + DIAG removal landed)
    - .planning/phases/12-session-form-polish-edit-ux/12-12-SUMMARY.md (confirm the GAP-12-E precedence fix landed)
    - .planning/debug/gap-12-b-restart-probe-timeout.md (§"Round 3 — APP-SIDE ROOT CAUSE" — the cold-Dock-launch reproduction recipe + the DEBT-02 caveat that warm runs do NOT reproduce)
  </read_first>
  <action>
    Run the full automated floor: `npx tsc --noEmit`, `npx eslint .`, `npm run test:unit`, `npm run test:integration`
    (both the heavy-init and the new marker-loss regressions), and `npm run package`. Confirm the packaged
    `out/Just-Wrapper-darwin-arm64/Just-Wrapper.app` is freshly built from the fix branch (mtime newer than the
    12-11/12-12 commits) and that the DIAG is GONE from the bundled main (grep the packaged app.asar to confirm
    `JW_PROBE_DIAG` / `jw-probe-timeline` are absent — the probe path is clean). This is the FLOOR for the
    human-verify, NOT proof of the fix — state that in the SUMMARY. Do NOT flip nyquist here.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint . && npm run test:unit && npm run test:integration && npm run package && test -d out/Just-Wrapper-darwin-arm64/Just-Wrapper.app && ! grep -aq 'JW_PROBE_DIAG' out/Just-Wrapper-darwin-arm64/Just-Wrapper.app/Contents/Resources/app.asar && echo PACKAGED-CLEAN</automated>
  </verify>
  <done>tsc + lint + unit + both integration regressions GREEN; the app is freshly packaged from the fix branch; the DIAG is absent from the bundled app.asar (prints PACKAGED-CLEAN). The SUMMARY states plainly that this automated GREEN is the floor, not proof — the cold marker-loss only reproduces in the packaged cold-launch human-verify.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: BLOCKING cold-Dock-launch operator human-verify (GAP-12-B + GAP-12-C + GAP-12-E)</name>
  <files>(none — operator-run verification against the packaged app)</files>
  <what-built>
    Claude packaged the fix branch (12-11 marker re-send + DIAG removal; 12-12 GAP-12-E precedence). The cold
    marker-loss (GAP-12-B) reproduces ONLY in the packaged app on a COLD Dock launch — it slipped the warm
    wdio/dev env TWICE (DEBT-02 blind spot). Automated GREEN is NOT proof; this checkpoint is the real gate.
  </what-built>
  <action>
    PAUSE for the operator. Present the steps in <how-to-verify> and WAIT for their explicit per-item B/C/E
    verdict (the <resume-signal>). Do NOT auto-advance (gate="blocking-human"); do NOT flip nyquist_compliant
    without an explicit unqualified approval of all three. Claude performs NO file edits in this task — it only
    presents the cold-launch repro instructions and records the operator's response (Task 3 writes the verdict).
  </action>
  <how-to-verify>
    Run on the operator's OWN macOS machine, against the FRESHLY PACKAGED app, on a COLD launch (a warm/dev run
    will NOT reproduce marker-loss):

    0. PREP — cold the OS disk caches, then cold-launch from Finder/Dock (NOT `npm start`, NOT a warm re-open):
       - In a terminal: `sudo purge`
       - Then open `out/Just-Wrapper-darwin-arm64/Just-Wrapper.app` from Finder/Dock.

    1. GAP-12-B — Restart-to-apply auto-runs the new command (cold):
       - Add a session, let it run. Edit it → set Startup command to e.g. `echo HELLO-RESTART` AND set Working
         directory to a REAL existing dir (e.g. ~/Thesis/Thesis-Work) → Save → "Restart to apply?" → Restart now.
       - EXPECT: the session respawns and `HELLO-RESTART` AUTO-RUNS (its output appears). NO "Startup command
         didn't auto-run — shell wasn't ready in time."
    2. GAP-12-B — dormant Start auto-runs the new command (cold, first session after launch):
       - On a freshly cold-launched app, add a session with a saved Startup command and Start it.
       - EXPECT: the command AUTO-RUNS on the first cold spawn (the marker-loss case). Try it 2–3× — the prior
         build failed the FIRST cold session ~3× then worked warm; this should now auto-run cold too.
    3. GAP-12-C — a failed spawn surfaces visibly (NOT "returned to home"):
       - Point a session's cwd at a real dir, then DELETE that dir on disk, then Start it (and separately, on a
         live session, Restart it).
       - EXPECT: a VISIBLE error where you acted — the IdleCard with "Working directory not found" — NOT a silent
         fall-back to the welcome/home view, NOT a stale green "running" row.
    4. GAP-12-E — an invalid path BLOCKS Save (inline reminder, modal stays open, no restart prompt first):
       - Edit a RUNNING session → change a launch field (e.g. Startup command) AND set Working directory to a
         non-existent absolute path (e.g. /x/nope) → click Save.
       - EXPECT: an inline reminder under the cwd field ("That folder doesn't exist — kept the previous
         directory"), the modal STAYS OPEN, and the "Restart to apply?" prompt does NOT pop first. Then fix the
         path to a real dir → Save → the modal closes and (since a live launch field changed) the restart prompt
         appears.

    REMINDER (state plainly): automated tests are GREEN, but they do NOT prove this — the cold marker-loss never
    reproduces in the warm wdio/dev env (the DEBT-02 blind spot bit twice). Your cold-launch result is the gate.
  </how-to-verify>
  <verify>
    <human-check>The operator runs the cold-Dock-launch repro (steps 0–4) and returns an explicit per-item B/C/E verdict. There is no automated proof for the cold marker-loss — this human-check IS the gate.</human-check>
  </verify>
  <done>The operator has returned an explicit per-item verdict for GAP-12-B (cold Restart + cold Start), GAP-12-C (visible failed-spawn error), and GAP-12-E (invalid path blocks Save, prompt suppressed). An unqualified approval of all three → nyquist_compliant true; otherwise the reopened gap(s) are captured for round 4.</done>
  <resume-signal>
    Approve each of B / C / E explicitly (e.g. "B approve, C approve, E approve") or describe what failed. Only an
    explicit UNQUALIFIED approval of ALL THREE flips nyquist_compliant true; any qualified/partial result keeps it
    false and routes to a round-4 gap closure.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the round-3 re-gate verdict + the nyquist decision in 12-VERIFICATION.md</name>
  <files>.planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md (the frontmatter `nyquist_compliant` field + the prior "Re-Gate 1 / Re-Gate 2 Result" sections — append a "Re-Gate 3 Result" in the same shape)
    - the operator's Task-2 verdict (the per-item B/C/E results + any qualifications)
  </read_first>
  <action>
    Append a `## Re-Gate 3 Result (12-13) — <date>` section to `12-VERIFICATION.md` in the SAME shape as the
    Re-Gate 1 / Re-Gate 2 sections: a verbatim per-item operator verdict table (GAP-12-B Restart + GAP-12-B Start +
    GAP-12-C + GAP-12-E), the automated-chain note (tsc/lint/unit/integration GREEN + packaged-clean), and the
    decision. Set the frontmatter `nyquist_compliant`: true ONLY if the operator gave an explicit UNQUALIFIED
    approval of ALL THREE (B + C + E); otherwise keep it false, record the reopened gap(s) with the operator's
    words, and state the route is a round-4 gap closure (`/gsd-plan-phase 12 --gaps`). Do NOT overstate — if any
    item is qualified/partial, it is NOT an approval. Update `status` accordingly (verified vs gaps_found).
  </action>
  <verify>
    <automated>grep -q 'Re-Gate 3' .planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md && grep -Eq 'nyquist_compliant: (true|false)' .planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md && echo RECORDED</automated>
  </verify>
  <acceptance_criteria>12-VERIFICATION.md has a `## Re-Gate 3 Result` section with the verbatim per-item B/C/E verdict; the frontmatter `nyquist_compliant` reflects the operator's decision (true ONLY on unqualified approval of all three, else false with the reopened gaps + the round-4 route).</acceptance_criteria>
  <done>12-VERIFICATION.md has a `## Re-Gate 3 Result` section with the verbatim per-item B/C/E verdict; the frontmatter `nyquist_compliant` reflects the operator's decision (true ONLY on unqualified approval of all three, else false with the reopened gaps + the round-4 route) (prints RECORDED).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the packaged app (cold Dock launch) → the operator | The only environment that reproduces the cold marker-loss; the human-verify exercises the real trust boundary the warm test env cannot |
| the operator's verdict → nyquist_compliant | The release-gate signal; only an unqualified human approval flips it (no auto-advance) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13-01 | Repudiation / false-pass | trusting automated GREEN as proof of the cold fix | mitigate | The checkpoint is BLOCKING + cold-launch-only; the verify steps state plainly that automated GREEN is NOT proof (DEBT-02 bit twice). nyquist flips ONLY on the operator's explicit unqualified approval |
| T-12-13-02 | Tampering | the human-verify run against a stale/warm build | mitigate | Task 1 re-packages from the fix branch and asserts the app.asar is DIAG-free + freshly built; the prep step requires `sudo purge` + a cold Dock launch (not `npm start`, not a warm re-open) |
| T-12-13-03 | Elevation | auto-advancing past the gate | accept | gate="blocking-human" — workflow.auto_advance is ignored for a blocking-human checkpoint; the operator must respond |
| T-12-13-04 | Tampering (supply chain) | npm install of a new package | accept | NO new dependency — only package + test scripts already present; no package-manager install task |

## Reliability & Cost (§6.5)
No retry mechanism is added here (this plan verifies the 12-11 marker re-send rather than adding retries).
(1) NO RETRY STORM — the operator runs the cold-launch repro a handful of times by hand; no automated loop.
(2) NO RESOURCE RUNAWAY — packaging + a manual run; no timers/subscriptions added. (3) NO COST/TOKEN/LLM.
(4) NO UI STALL — n/a (verification). (5) The marker re-send under test is already bounded by the hard ceiling
(12-11 R&C); this plan CONFIRMS the never-ready fallback is acceptable on the operator's real machine.
(6) CORE-VALUE — the human-verify explicitly checks the working dormant-Start auto-run + terminal fidelity are
unregressed (GAP-12-B steps exercise `claude --rc`-style auto-run).
</threat_model>

<verification>
- Task 1: tsc + lint + unit + both integration regressions GREEN; the app is freshly packaged from the fix
  branch; the DIAG is absent from the bundled app.asar.
- Task 2: the BLOCKING operator human-verify is answered with an explicit per-item B/C/E verdict.
- Task 3: 12-VERIFICATION.md has a `## Re-Gate 3 Result` section + the frontmatter `nyquist_compliant` reflects
  the operator's decision (true ONLY on unqualified approval of all three).
</verification>

<success_criteria>
- The fix branch is packaged clean (no DIAG) and the full automated chain is GREEN.
- The operator verifies GAP-12-B (cold Restart + cold Start auto-run), GAP-12-C (visible failed-spawn error), and
  GAP-12-E (invalid path blocks Save, prompt suppressed) on a COLD Dock launch.
- nyquist_compliant flips true ONLY on the operator's explicit unqualified approval of all three; any
  qualified/partial result keeps it false and routes to a round-4 gap closure.
- The round-3 re-gate verdict is recorded in 12-VERIFICATION.md.
</success_criteria>

<output>
Create `.planning/phases/12-session-form-polish-edit-ux/12-13-SUMMARY.md` when done.
</output>
