---
phase: 12-session-form-polish-edit-ux
plan: 10
type: execute
wave: 2
depends_on: ["12-08", "12-09"]
files_modified:
  - .planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md
autonomous: false
gap_closure: true
requirements: [UI-04, SESS-05, SESS-06]
user_setup: []

must_haves:
  truths:
    - "Full automated chain GREEN against the gap-closure round-2 fixes: npx tsc --noEmit, npx eslint ., npm run test:unit, and npm run test:smoke (isolate-retry on the known flakes)."
    - "The DEBT-02 real-timing regression runs GREEN: npm run test:integration (the spike-005 heavy-init driver proves the dual-deadline budget injects under a heavy rc that defeats the old 4000ms wall, and a never-ready stream still hits the 15000ms ceiling)."
    - "The BLOCKING operator human-verify is run ON THE OPERATOR'S OWN MACHINE (GAP-12-B's trigger is their real rc-init latency) covering: GAP-12-B restart-to-apply auto-runs the new command, GAP-12-C a failed/bad restart surfaces visibly, GAP-12-E a non-existent cwd at Save shows the inline drop notice (no silent close), and the already-passed GAP-12-A/D/E-name-hint + SESS-06 Browse + chords remain unregressed."
    - "nyquist_compliant flips true in 12-VALIDATION.md ONLY on the operator's explicit, unqualified approval of GAP-12-B + GAP-12-C + GAP-12-E — never auto-flipped."
  artifacts:
    - path: ".planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md"
      provides: "The round-2 re-gate evidence chain + the operator verdict; nyquist_compliant recorded"
      contains: "nyquist_compliant"
  key_links:
    - from: "the gap-closure plans 12-08 (main probe budget) + 12-09 (renderer surfaces)"
      to: "the round-2 re-gate verdict"
      via: "full suite + the test:integration real-timing regression + the BLOCKING operator human-verify on their machine"
      pattern: "human-verify"
---

<objective>
Re-gate Phase 12 (gap-closure round 2) after the two fix plans land: 12-08 (the GAP-12-B dual-deadline readiness
budget + the spike-005 real-timing regression) and 12-09 (GAP-12-C visible restart-error surface + GAP-12-E
no-silent-cwd-drop). Produce the full evidence chain, then run the BLOCKING operator human-verify ON THE
OPERATOR'S OWN MACHINE — because GAP-12-B's trigger is their real login-shell rc-init latency, which the dev box
and the fast wdio smoke do NOT reproduce (DEBT-02). The Nyquist gate (`nyquist_compliant`) flips true ONLY on the
operator's explicit, unqualified approval of the three reopened gaps.

Purpose: prove the three confirmed gaps are closed on real hardware without regressing the suite, terminal
fidelity, the working dormant-Start auto-run, or the already-passed items — and obtain the operator sign-off.
Output: the round-2 re-gate evidence + verdict recorded in 12-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md
@.planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md
@.planning/debug/gap-12-b-restart-probe-timeout.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated evidence chain (unit + smoke + integration + tsc + lint) against the round-2 fixes</name>
  <read_first>
    - .planning/phases/12-session-form-polish-edit-ux/12-08-SUMMARY.md (the main probe-budget fix outcome)
    - .planning/phases/12-session-form-polish-edit-ux/12-09-SUMMARY.md (the renderer surface fixes outcome)
    - .planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md (the re-gate criterion + the round-1 known-flake handling)
    - package.json (the test / test:unit / test:smoke / test:integration / ui:shots scripts)
  </read_first>
  <action>
    Run and capture terminal evidence (paste real stdout — never claim green without it) for: `npx tsc --noEmit`,
    `npx eslint .`, `npm run test:unit`, `npm run test:integration` (the DEBT-02 real-timing regression from 12-08
    Task 3 — it must pass on this POSIX host), and `npm run test:smoke` (isolate-retry the known flakes per the
    round-1 protocol; record the isolated pass counts). The visual gate is NOT re-scored here — GAP-12-A/D and the
    Save-button-in-frame capture already PASSED the round-1 re-gate and are NOT touched by 12-08/12-09; note that
    explicitly. Write the automated evidence into 12-VALIDATION.md under a new "Re-Gate 2 (round 2) — Automated
    chain" section, including the exact commands, pass counts, and any flake-isolation notes. Do NOT flip
    nyquist_compliant in this task.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint . && npm run test:unit && npm run test:integration</automated>
  </verify>
  <done>tsc + lint + unit + integration all GREEN with stdout captured into 12-VALIDATION.md; smoke run recorded (isolated where flaky); nyquist_compliant NOT yet changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: BLOCKING operator human-verify on the operator's machine — GAP-12-B / GAP-12-C / GAP-12-E</name>
  <read_first>
    - .planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md (the round-1 operator results + the reopened-gap fix directions)
    - .planning/debug/gap-12-b-restart-probe-timeout.md (the blind-spot note: the operator's real rc-init latency was NOT measured — this live re-verify on their machine is the real check)
  </read_first>
  <what-built>
    - GAP-12-B: the TERM-05 readiness budget now idle-extends on probe progress (8000ms base) with a hard 15000ms
      ceiling, so a heavy login-shell rc no longer false-times-out and the restart-to-apply auto-run injects.
    - GAP-12-C: a failed/bad restart (pid<=0, deleted-dir/corrupt-store edge) now flips the row to a visible
      error/IdleCard with the 'Working directory not found' notice instead of a stale-running row.
    - GAP-12-E: a non-existent absolute working directory at Save now shows an inline "that folder doesn't exist —
      kept the previous directory" notice and KEEPS the modal open (no silent close).
  </what-built>
  <how-to-verify>
    Run on YOUR OWN machine (the rc-init latency that triggered GAP-12-B is machine-specific):
    1. Build/run the PACKAGED app (the real-fidelity surface).
    2. GAP-12-B: Add a session, let it run, Edit → set Startup command (e.g. `echo HELLO-RESTART`) AND set Working
       directory to a REAL existing project dir (ideally one with heavy per-dir init — direnv/conda/nvm), Save →
       "Restart to apply?" → Restart now. EXPECT: the session respawns and `HELLO-RESTART` auto-runs (its output
       appears) — NOT "shell wasn't ready in time."
    3. GAP-12-E: Edit a session → type a non-existent ABSOLUTE path (e.g. `/no/such/dir/xyz`) in Working directory
       → Save. EXPECT: the modal STAYS OPEN with an inline "that folder doesn't exist — kept the previous
       directory" notice, and the field shows the prior directory — NOT a silent close.
    4. GAP-12-C (best-effort edge): if you can produce a deleted-after-save directory, a restart-to-apply on it
       should land a visible error (IdleCard + "Working directory not found") rather than a stuck green/running row.
    5. Unregressed: confirm GAP-12-A blue Save + spacing, GAP-12-D Cmd+A/clipboard/overlay, GAP-12-E name hint,
       SESS-06 Browse…, and the Cmd+1/2/K/F chords all still work; confirm a normal dormant Start still auto-runs.
  </how-to-verify>
  <resume-signal>Reply "approved" (unqualified) to flip nyquist_compliant true, or describe the failing item(s).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the round-2 verdict in 12-VALIDATION.md (flip nyquist_compliant ONLY on explicit approval)</name>
  <read_first>
    - .planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md (the Re-Gate 2 automated section from Task 1 + the existing front-matter nyquist_compliant field)
    - .planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md (the gate criterion — explicit unqualified approval required; do NOT edit this file — the orchestrator owns it)
  </read_first>
  <action>
    Record the operator's Task-2 verdict verbatim (per-item) in 12-VALIDATION.md under the "Re-Gate 2" section.
    If — and ONLY if — the operator replied with an explicit, unqualified approval of GAP-12-B + GAP-12-C +
    GAP-12-E (and no regression of the already-passed items), set `nyquist_compliant: true` in 12-VALIDATION.md
    front-matter and note the closure. If the operator reported ANY failure or qualified the approval, keep
    `nyquist_compliant: false`, record the residual gap(s) with the operator's words, and note the route
    (re-diagnose if a new root cause, or re-plan a further gap-closure round). Do NOT touch 12-VERIFICATION.md
    (orchestrator-owned). Do NOT create or switch branches (shared dev branch).
  </action>
  <verify>
    <human-check>12-VALIDATION.md shows the Re-Gate 2 per-item operator verdict, and nyquist_compliant reflects the operator's explicit decision (true only on unqualified approval).</human-check>
  </verify>
  <done>The round-2 verdict + per-item operator words are recorded in 12-VALIDATION.md; nyquist_compliant is true ONLY if the operator explicitly + unqualifiedly approved, else false with the residual gap(s) + route noted; 12-VERIFICATION.md untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated evidence → the gate verdict | Test stdout must be real (pasted), not asserted — a false-green would ship an unfixed lifecycle defect |
| operator human judgment → nyquist_compliant | The gate flips on a human signal that cannot be auto-synthesized (GAP-12-B's trigger is the operator's machine) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10-01 | Repudiation | a false "tests passed" claim without evidence | mitigate | Task 1 pastes real terminal stdout into 12-VALIDATION.md (CLAUDE.md "no green claim without terminal evidence"); the DEBT-02 integration test is run explicitly, not assumed |
| T-12-10-02 | Tampering | nyquist_compliant flipped without genuine human approval | mitigate | The checkpoint is gate="blocking-human" and NEVER auto-approvable; Task 3 flips true ONLY on an explicit unqualified operator reply; a qualified/failed reply keeps it false |
| T-12-10-03 | Spoofing | the dev box / fast smoke standing in for the operator's machine | mitigate | The human-verify is mandated ON THE OPERATOR'S OWN MACHINE because GAP-12-B's trigger is their rc-init latency (the diagnosis blind-spot) — the green smoke is explicitly insufficient (DEBT-02) |

## Reliability & Cost (§6.5)
This is a gate/verification plan — no production code changes. (1) NO RUNTIME COST: it runs the existing suites
once + a one-time human-verify. (2) The DEBT-02 integration test spawns real shells with bounded sleeps and a
hard 15s ceiling — no unbounded wait, no retry storm. (3) NO TOKEN/LLM COST. (4) The verdict is a single human
signal; no polling, no concurrency. Terminal fidelity is verified, not modified.
</threat_model>

<verification>
- `npx tsc --noEmit`, `npx eslint .`, `npm run test:unit`, `npm run test:integration` all GREEN with stdout in 12-VALIDATION.md.
- `npm run test:smoke` recorded (isolate-retry on the known flakes).
- The BLOCKING operator human-verify run on the operator's machine; per-item verdict recorded.
- `nyquist_compliant` in 12-VALIDATION.md reflects the operator's explicit decision (true ONLY on unqualified approval).
- 12-VERIFICATION.md NOT modified by this plan.
</verification>

<success_criteria>
- The full automated chain (incl. the DEBT-02 real-timing integration regression) is GREEN with captured evidence.
- The operator confirms on their machine that GAP-12-B auto-runs, GAP-12-C surfaces a failed restart, and GAP-12-E
  shows the inline drop notice — with no regression of the already-passed items or the dormant-Start auto-run.
- nyquist_compliant flips true ONLY on the operator's explicit, unqualified approval.
</success_criteria>

<output>
Create `.planning/phases/12-session-form-polish-edit-ux/12-10-SUMMARY.md` when done.
</output>
