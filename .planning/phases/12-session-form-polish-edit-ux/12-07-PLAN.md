---
phase: 12-session-form-polish-edit-ux
plan: 07
type: execute
wave: 3
depends_on: ["12-04", "12-05", "12-06"]
files_modified:
  - .planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md
  - .planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md
autonomous: false
gap_closure: true
requirements: [UI-04, SESS-05, SESS-06]
user_setup: []

must_haves:
  truths:
    - "Full suite GREEN: npm run test:unit + npx tsc --noEmit + npm run lint, and npm run test:smoke (isolate-retry on the known flakes, 3/3 isolated)."
    - "A fresh ui:shots:fresh capture INCLUDES the Save button and the rubric scores the blue Save + even spacing PASS."
    - "The BLOCKING operator human-verify covering GAP-12-A..E passes with an explicit unqualified 'approved'."
    - "nyquist_compliant flips true in 12-VALIDATION.md ONLY on that explicit approval."
  artifacts:
    - path: ".planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md"
      provides: "Re-gate evidence chain + the operator verdict; nyquist_compliant updated"
      contains: "nyquist_compliant"
  key_links:
    - from: "the gap-closure plans 12-04/05/06"
      to: "the re-gate verdict"
      via: "full suite + packaged ui:shots + BLOCKING human-verify"
      pattern: "human-verify"
---

<objective>
Re-gate Phase 12 after the gap-closure plans (12-04 renderer/CSS + META, 12-05 main Menu, 12-06 restart-to-apply
+ visible rejection) land. Produce the full evidence chain against the PACKAGED app, run a fresh `ui:shots:fresh`
that now captures the Save button, then run the BLOCKING operator human-verify covering GAP-12-A..E. The Nyquist
gate (`nyquist_compliant`) flips true ONLY on the operator's explicit, unqualified "approved" — exactly as the
first gate (12-03 Task 3) required.

Purpose: prove the five gaps are closed without regressing the suite or terminal fidelity, and obtain the
operator sign-off that the form is one cohesive designed surface with a working live-edit→restart flow.
Output: the re-gate evidence + verdict recorded in 12-VALIDATION.md; 12-VERIFICATION.md updated to reflect the
closure; the phase ready for verify/complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md
@.planning/phases/12-session-form-polish-edit-ux/12-UI-SPEC.md
@tests/ui-lab/DESIGN-RUBRIC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full evidence chain against the packaged app + fresh ui:shots capturing the Save button</name>
  <read_first>
    - tests/ui-lab/README.md (the loop: injection is preview, packaged is proof; no claim without capture)
    - tests/ui-lab/surfaces.ts (the edit-modal surface 12-04 updated to capture the Save button + the edit-modal-validation surface)
    - tests/ui-lab/DESIGN-RUBRIC.md (§edit-modal incl. the blue-Save line 12-04 added; §edit-modal-validation)
    - .planning/STATE.md (the KNOWN parallel-load smoke flakes on session-edit/startup-command/pty-resize — isolate-retry, 3/3 isolated bar)
  </read_first>
  <action>
    Run the verification gate in order; paste each command's terminal evidence into 12-VALIDATION.md (no claim
    without stdout — testing-policy):
    1. `npx tsc --noEmit` → 0 errors.
    2. `npm run lint` → clean (note any pre-existing deferred spike .cjs lint items already tracked in deferred-items.md; do NOT let new src/tests lint errors through).
    3. `npm run test:unit` → all GREEN (incl. the new app-menu / session-restart-prompt / session-lifecycle-actions tests + tokens-completeness + security.guard at 20 keys).
    4. `npm run test:smoke` → GREEN; if the KNOWN session-edit/startup-command/pty-resize parallel-load flake hits, isolate-retry each failing spec (run it alone) and record a 3/3 isolated-pass bar (per STATE.md gotcha). The session-edit smoke now includes the 12-06 Restart-to-apply specs.
    5. `npm run ui:shots:fresh` (= npm run package && npm run ui:shots) → a PACKAGED, no-injection capture. Record the tag + gitSha.
    Score the fresh capture against tests/ui-lab/DESIGN-RUBRIC.md §edit-modal + §edit-modal-validation. The
    edit-modal frame MUST now show the Save button (12-04 META fix); confirm from the PNG (visible pixel evidence,
    not CSS source) that: the Save button is in-frame, renders accent-BLUE, the two-group structure + even spacing
    read PASS, and the validation hints render calm (ink-faint) in the validation surface. If ANY rubric line is
    FAIL/PARTIAL, STOP — do NOT present a known-failing app to the operator (T-10-10-01 precedent: never present a
    known-failing app to the human gate). Route the gap back to 12-04/05/06 for a fix round before Task 2.

    Write the full evidence chain (the five command outputs + the rubric score table + tag/gitSha) into
    12-VALIDATION.md. Do NOT flip nyquist_compliant yet — that is Task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm run test:unit</automated>
  </verify>
  <done>
    tsc + lint + unit GREEN; smoke GREEN (isolate-retry bar recorded for any known flake); a fresh packaged
    ui:shots capture exists with the Save button IN-FRAME and the §edit-modal / §edit-modal-validation rubric
    lines scored PASS from pixel evidence; the evidence chain is written into 12-VALIDATION.md; nyquist_compliant
    NOT yet flipped.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: BLOCKING operator human-verify — GAP-12-A..E on the packaged app</name>
  <action>Present the packaged app to the operator and walk the how-to-verify steps below covering GAP-12-A..E (+ SESS-05 O-1 / SESS-06 O-2). Do NOT proceed to Task 3 until the operator responds with the resume-signal. This is a blocking-human checkpoint — never auto-advance.</action>
  <what-built>
    The five gap fixes against the packaged app:
    - GAP-12-A: the Save button now renders accent-BLUE (the context-menu-item override removed; click driven by data-testid); form spacing snapped to the --space-* rhythm.
    - GAP-12-B: after Save, a LIVE session whose cwd/shell/startup changed shows a "Restart to apply?" prompt → Restart now / Later (Restart now reuses the retained ptyRestart; same logicalId, new ptyPid; scrollback kept).
    - GAP-12-C: a bad working directory on the restart-to-apply surfaces "Working directory not found" where the user acted (row / IdleCard); main stays the validator of record.
    - GAP-12-D: an application Menu with the standard Edit roles is set, so Cmd+A selects the name-field text (no longer closes the modal) and Cmd+C/V/X work in inputs; the overlay no longer closes on a drag-select overshoot; the Cmd+1-9 / Cmd+K / Cmd+F chords still work.
    - GAP-12-E: the empty-name and non-absolute-cwd hints render visibly and CALM (ink-faint, not red); red only on a genuine main rejection.
  </what-built>
  <how-to-verify>
    Run the PACKAGED app: `npm run make` (or use the out/ build from Task 1's ui:shots:fresh), open Just-Wrapper.app.
    1. GAP-12-A — Add a session, open Edit (double-click the row or right-click → Edit). Confirm the "Save changes" button is a BLUE pill (constructive), Cancel is quiet neutral, and the field/group spacing reads even and intentional (not cramped/uneven).
    2. GAP-12-D — In the Name field, type some text, press Cmd+A. EXPECT: the field text is SELECTED and the modal STAYS OPEN (it must NOT close). Try Cmd+C / Cmd+V / Cmd+X in the Name and Working-directory fields — they must work. Then click-drag to select inside an input and release the mouse over the dim backdrop — the modal must STAY OPEN (no accidental close).
    3. GAP-12-E — Clear the Name field: a CALM grey hint "Keeps the current name" appears (not red). Type a non-absolute cwd (e.g. `foo/bar`): a calm grey hint "Enter an absolute path, or use Browse…" appears (not red).
    4. SESS-06 / O-2 — Click Browse…: the native folder picker opens; pick a directory; the Working-directory field fills with the absolute path.
    5. GAP-12-B — With the session RUNNING, change the Startup command to `echo HELLO-RESTART` and/or change the Working directory to another real dir; click Save changes. EXPECT: a "Restart to apply?" prompt appears → click "Restart now". The session restarts in place (scrollback kept) and the new startup command runs (you see HELLO-RESTART) / the new cwd is in effect. Repeat and click "Later": no restart; reopen Edit and confirm the value is still saved.
    6. GAP-12-C — Edit a running session, set the Working directory to a path, Save, Restart now, then (to force the rejection) you can set a directory that you then delete, OR trust the operator's judgment: confirm that when a restart fails on a missing directory, "Working directory not found" is shown where you can see it (on the session row / idle card), not silently swallowed.
    7. Confirm the existing chords still work: Cmd+1 / Cmd+2 switch sessions, Cmd+K clears the active terminal, Cmd+F opens find — none of these should have broken from the new Menu.
    8. SC1 scan-test: does the form read as ONE cohesive designed surface (grouped fields, clear labels, real picker, calm validation, blue Save)?
  </how-to-verify>
  <resume-signal>
    Type "approved" for an unqualified pass (nyquist_compliant flips true), or describe the remaining issues
    (the verdict stays QUALIFIED FAIL and routes another gap-closure round).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the verdict + flip nyquist_compliant on explicit approval</name>
  <files>.planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md, .planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/12-session-form-polish-edit-ux/12-VERIFICATION.md (the gaps_found verdict + the gap table — to mark each gap closed)
    - .planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md (the structure the first gate wrote — append the re-gate row)
  </read_first>
  <action>
    Record the Task-2 human-verify result verbatim in 12-VALIDATION.md (date, the operator's words, per-gap
    GAP-12-A..E pass/fail, the tag/gitSha from Task 1). 
    - If the operator gave an explicit unqualified "approved": set `nyquist_compliant: true` in 12-VALIDATION.md
      front-matter, and update 12-VERIFICATION.md to mark `status:` closed/verified and each of GAP-12-A..E +
      the META-finding as CLOSED (with the closing plan id). Note SESS-05 O-1 / SESS-06 O-2 confirmed live.
    - If the operator reported remaining issues: keep `nyquist_compliant: false`, record the new gap(s) verbatim
      in 12-VERIFICATION.md (same table shape), and return `## GAPS REMAIN` so the orchestrator routes another
      `/gsd-plan-phase 12 --gaps` round. Do NOT flip the flag on a qualified verdict.
    Do NOT edit any source file in this task — it is verdict-recording only. Leave the requirement traceability
    in REQUIREMENTS.md to the orchestrator's phase.complete step.
  </action>
  <verify>
    <automated>grep -nE "nyquist_compliant" .planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md | head -1</automated>
  </verify>
  <done>
    The re-gate verdict + per-gap closure is recorded in 12-VALIDATION.md and 12-VERIFICATION.md; nyquist_compliant
    is true ONLY if the operator explicitly approved, otherwise it stays false with the new gaps recorded for the
    next round.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated gate → operator gate | Never present a known-failing app to the human (T-10-10-01); the rubric must score PASS first |
| operator verdict → nyquist flag | The flag flips true ONLY on an explicit unqualified approval — a qualified verdict keeps it false |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07-01 | Repudiation | the gate verdict | mitigate | The operator's exact words + date + tag/gitSha are recorded verbatim in 12-VALIDATION.md; the flag is auditable to a specific approval |
| T-12-07-02 | Tampering | premature nyquist flip | mitigate | nyquist_compliant flips true ONLY in Task 3 and ONLY on an explicit "approved"; the checkpoint is `blocking-human`, never auto-advanced |
| T-12-07-SC | Tampering | npm installs during make | accept | This phase adds ZERO new packages (12-UI-SPEC §Registry Safety: not applicable); `npm run make` builds the existing, vetted dependency set — no new install to audit |

## Reliability & Cost (§6.5) — light
A verification + sign-off plan. The only runtime cost is one `npm run make` + `npm run ui:shots` + `npm run test:*`
run (developer machine, no production cost). No retry storms / cron / LLM cost / concurrency. The restart path
exercised in the human-verify is a single user-initiated restart (covered by 12-06's R&C analysis).
</threat_model>

<verification>
- Task 1: tsc + lint + unit + smoke GREEN; packaged ui:shots:fresh with the Save button in-frame; rubric scored PASS.
- Task 2: BLOCKING operator human-verify covering GAP-12-A..E (+ SESS-05 O-1 / SESS-06 O-2 live).
- Task 3: verdict recorded; nyquist_compliant flips true ONLY on explicit unqualified approval.
</verification>

<success_criteria>
- Full suite GREEN against the packaged app (isolate-retry bar for the known smoke flakes).
- ui:shots:fresh capture includes the Save button; §edit-modal + §edit-modal-validation rubric lines PASS.
- The operator confirms GAP-12-A..E live and gives an explicit unqualified "approved".
- nyquist_compliant: true in 12-VALIDATION.md; GAP-12-A..E + META marked CLOSED in 12-VERIFICATION.md.
- EXPECTED_API_KEYS stayed 20 throughout; terminal fidelity unregressed.
</success_criteria>

<output>
Update `.planning/phases/12-session-form-polish-edit-ux/12-VALIDATION.md` and `12-VERIFICATION.md`; create
`.planning/phases/12-session-form-polish-edit-ux/12-07-SUMMARY.md` when done.
</output>
