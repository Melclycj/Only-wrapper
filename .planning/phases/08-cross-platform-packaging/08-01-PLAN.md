---
phase: 08-cross-platform-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/os-gate.ts
  - src/main/__tests__/os-gate.test.ts
  - src/main/index.ts
  - forge.config.ts
  - package.json
  - wdio.conf.ts
  - tests/smoke/pty-roundtrip.smoke.test.ts
  - assets/icon.png
  - assets/icon.icns
  - assets/icon.ico
  - assets/README.md
  - docs/PACKAGING.md
autonomous: true
requirements: [PKG-01]
user_setup: []

must_haves:
  truths:
    - "npm run make produces a runnable out/Just-Wrapper-darwin-<arch>/Just-Wrapper.app on macOS with no manual post-processing (SC1)"
    - "A pre-1809 Windows host (os.release() build < 17763) is detected as unsupported by a pure function before any node-pty spawn (SC4 logic)"
    - "The packaged macOS .app round-trips a PTY echo from inside the ASAR archive — spawn-helper loads from app.asar.unpacked (SC3)"
    - "Forge resolves a base icon and a Windows setupIcon from assets/ (icon pipeline wired, D-07)"
    - "macOS sign/notarize config slots are present but skip cleanly when no APPLE_* env is set — no secret is committed (D-04)"
    - "The security guard still asserts exactly 20 EXPECTED_API_KEYS — packaging adds zero bridge keys"
  artifacts:
    - path: "src/main/os-gate.ts"
      provides: "Pure parseWindowsBuild + isUnsupportedWindows + MIN_WINDOWS_BUILD (D-05/SC4)"
      contains: "MIN_WINDOWS_BUILD"
      exports: ["MIN_WINDOWS_BUILD", "parseWindowsBuild", "isUnsupportedWindows"]
    - path: "src/main/__tests__/os-gate.test.ts"
      provides: "Fixture-string unit tests for the OS gate (Wave 0 → GREEN this plan)"
      contains: "parseWindowsBuild"
    - path: "forge.config.ts"
      provides: "packagerConfig icon + appBundleId + env-gated osxSign/osxNotarize slots; MakerSquirrel setupIcon (D-04/D-07)"
      contains: "appBundleId"
    - path: "package.json"
      provides: "appId-equivalent metadata: author + a config.forge appId field (D-07)"
      contains: "author"
    - path: "wdio.conf.ts"
      provides: "OS-conditional appBinaryPath (macOS .app / Windows .exe) — D-08"
      contains: "win32"
    - path: "docs/PACKAGING.md"
      provides: "Local-open path for the unsigned macOS app + make/CI overview (D-04)"
      contains: "quarantine"
    - path: "assets/icon.icns"
      provides: "Placeholder macOS app icon (D-07 placeholder)"
  key_links:
    - from: "src/main/index.ts"
      to: "src/main/os-gate.ts"
      via: "isUnsupportedWindows(process.platform, os.release()) gate inside app.whenReady before store.load()/createWindow()"
      pattern: "isUnsupportedWindows"
    - from: "forge.config.ts"
      to: "assets/icon"
      via: "packagerConfig.icon = 'assets/icon' (no extension) + MakerSquirrel setupIcon 'assets/icon.ico'"
      pattern: "assets/icon"
    - from: "wdio.conf.ts"
      to: "out/Just-Wrapper-<plat>-<arch>"
      via: "process.platform ternary on appBinaryPath"
      pattern: "appBinaryPath"
---

<objective>
Deliver the macOS-buildable end-to-end packaging slice: a real `npm run make` produces a
runnable, icon-bearing, metadata-complete `.app`, the pre-1809 Windows OS gate is wired
into the boot sequence (firing BEFORE any node-pty spawn), and the packaged-app PTY
round-trip smoke runs against an OS-conditional binary path. After this plan a developer
can build the macOS distributable and prove (via the existing WDIO smoke against the
packaged binary) that a PTY round-trips from inside the ASAR archive — the SC1(mac) /
SC3(mac) / SC4(logic) capabilities are real and verifiable on the dev box, with the
Windows leg deferred to the CI matrix (Plan 03).

This is a vertical slice (MVP_MODE): config + boot-gate + icon + smoke move together so the
app stays buildable and the new capability (a real packaged, gated, icon-bearing app) is
end-to-end demonstrable, not a horizontal "config only" layer.

Purpose: Turn the `npm start`-only app into a real macOS distributable with the OS-safety
gate in place, while keeping the proven node-pty ASAR mechanics (D-06) untouched.
Output: `src/main/os-gate.ts` (+ test), an extended `forge.config.ts`/`package.json`/
`wdio.conf.ts`/`index.ts`, `assets/icon.{icns,ico,png}`, `docs/PACKAGING.md`, and an
extended `pty-roundtrip.smoke.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cross-platform-packaging/08-CONTEXT.md
@.planning/phases/08-cross-platform-packaging/08-RESEARCH.md
@.planning/phases/08-cross-platform-packaging/08-PATTERNS.md
@CLAUDE.md

# Files this plan reads/extends (analogs + targets)
@src/main/shell-resolver.ts
@src/main/index.ts
@forge.config.ts
@package.json
@wdio.conf.ts
@tests/smoke/pty-roundtrip.smoke.test.ts
@scripts/fix-node-pty.cjs
@src/main/window-config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure os-gate module + ConPTY pre-window boot gate (D-05 / SC4)</name>
  <files>src/main/os-gate.ts, src/main/__tests__/os-gate.test.ts, src/main/index.ts</files>
  <read_first>
    - .planning/phases/08-cross-platform-packaging/08-RESEARCH.md Pattern 3 (lines 190-229) — the exact MIN_WINDOWS_BUILD=17763, parseWindowsBuild regex /(\d+)\.(\d+)\.(\d+)/ group 3, isUnsupportedWindows signature, AND the index.ts wiring block + the 17763-vs-18309 note (lines 229).
    - .planning/phases/08-cross-platform-packaging/08-PATTERNS.md "os-gate.ts" section (lines 115-149) — analog is shell-resolver.ts; the index.ts insertion point (index.ts:165-170) + required new imports.
    - src/main/shell-resolver.ts (lines 1-37) — the electron-free pure-module banner convention to mirror (no electron import → Vitest imports it directly).
    - src/main/index.ts (lines 1-19, 165-170) — the existing `import { app, ..., dialog, ... }` line, the module-scope `if (started) app.quit()` squirrel guard, and the `app.whenReady().then(async () => { const data = await store.load(); ... createWindow(); })` boot block where the gate inserts.
    - src/main/__tests__/shell-discovery.test.ts (lines 1-39) — the pure-helper fixture-table test style to mirror for os-gate.test.ts.
  </read_first>
  <behavior>
    - parseWindowsBuild('10.0.17763') → 17763; parseWindowsBuild('10.0.17134') → 17134; parseWindowsBuild('10.0.22631.1') → 22631 (group 3, ignores a 4th component); parseWindowsBuild('garbage') → null; parseWindowsBuild('') → null.
    - isUnsupportedWindows('win32','10.0.17134') → true (below 17763); isUnsupportedWindows('win32','10.0.17763') → false (at the floor, inclusive-OK); isUnsupportedWindows('win32','10.0.22631') → false; isUnsupportedWindows('darwin','25.0.0') → false (non-win32 never gated); isUnsupportedWindows('win32','unparseable') → false (an unparseable release does NOT block — fail-open so a parse quirk never bricks a supported host).
    - MIN_WINDOWS_BUILD === 17763.
  </behavior>
  <action>
    Create src/main/os-gate.ts as an electron-free pure module mirroring shell-resolver.ts's banner ("no electron import lets Vitest import it directly"). Export `MIN_WINDOWS_BUILD = 17763` (Windows 10 1809 ConPTY floor, CLAUDE.md), `parseWindowsBuild(release: string): number | null` using the regex `/(\d+)\.(\d+)\.(\d+)/` and returning `Number(m[3])` (the BUILD component — node-pty parses os.release() with the same regex), and `isUnsupportedWindows(platform: NodeJS.Platform, release: string): boolean` returning true ONLY when `platform === 'win32'` AND the parsed build is non-null AND `< MIN_WINDOWS_BUILD`. PLANNING NOTE (surface, do NOT silently change): RESEARCH Open Q1 — node-pty's OWN `_useConpty` gate is `>= 18309`, so builds 17763..18308 launch under THIS gate but run winpty internally; the locked CONTEXT value is 17763 (D-05/CLAUDE.md) — keep 17763, add a one-line code comment noting the 18309 discrepancy for the human to confirm later, never change the constant without an explicit decision.
    Write src/main/__tests__/os-gate.test.ts as a Vitest pure-helper fixture table (mirror shell-discovery.test.ts style) covering every case in <behavior>.
    Wire the gate into src/main/index.ts: add `import os from 'node:os';` and `import { isUnsupportedWindows } from './os-gate';` (`dialog` and `app` are already imported on line 1 — add no new electron import). Insert at the TOP of the existing `app.whenReady().then(async () => { ... })` callback (index.ts:165), BEFORE `const data = await store.load();`: if `isUnsupportedWindows(process.platform, os.release())`, call `dialog.showErrorBox('Windows 10 build 1809 or later required', 'Just-Wrapper needs Windows 10 build 1809 (10.0.17763) or newer for its terminal engine (ConPTY). Please update Windows and try again.')`, then `app.quit();` then `return;` so store.load()/hydrate()/createWindow() never run (the gate precedes every node-pty spawn path — D-05). The module-scope squirrel `if (started) app.quit()` guard (index.ts:17-19) stays first; this gate is inside whenReady, after it.
  </action>
  <verify>
    <automated>npm run test:unit -- src/main/__tests__/os-gate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/os-gate.ts` exports `MIN_WINDOWS_BUILD`, `parseWindowsBuild`, `isUnsupportedWindows` and has NO `import ... from 'electron'`.
    - `npm run test:unit -- src/main/__tests__/os-gate.test.ts` exits 0 (all <behavior> cases GREEN).
    - `src/main/os-gate.ts` source contains the literal `17763` and a comment mentioning `18309` (the discrepancy note).
    - `src/main/index.ts` contains `isUnsupportedWindows(process.platform, os.release())` inside the `app.whenReady` callback, positioned textually BEFORE `await store.load()`, and the gate branch calls `dialog.showErrorBox(` then `app.quit()` then `return`.
    - `npx tsc --noEmit` (or the project typecheck) reports no new errors from index.ts/os-gate.ts.
  </acceptance_criteria>
  <done>os-gate.test.ts is GREEN; the pre-1809 gate is wired into index.ts before any node-pty spawn; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Icon pipeline + metadata + env-gated sign slots (D-04 / D-07)</name>
  <files>forge.config.ts, package.json, assets/icon.png, assets/icon.icns, assets/icon.ico, assets/README.md, docs/PACKAGING.md</files>
  <read_first>
    - .planning/phases/08-cross-platform-packaging/08-RESEARCH.md "forge.config.ts EXTENSION" (lines 351-380) — the exact osxSign/osxNotarize env-gated slot shape + `icon: 'assets/icon'` (no extension) + MakerSquirrel setupIcon; and "macOS placeholder icon generation" (lines 336-349) — the iconutil/sips workflow + the "real multi-size .ico, not a renamed PNG" rule.
    - .planning/phases/08-cross-platform-packaging/08-PATTERNS.md "forge.config.ts" section (lines 152-157) — EXTEND ONLY: do NOT touch asar.unpackDir, the ignore keep-clause, or rebuildConfig.onlyModules:[] (D-06).
    - forge.config.ts (lines 1-78, ALL of it) — the existing packagerConfig (asar/ignore lines 10-48), rebuildConfig no-op (lines 55-57), makers array (lines 58-63). EXTEND in place.
    - package.json (lines 1-21) — productName 'Just-Wrapper', version, author:"" (fill it), scripts.
    - CLAUDE.md "What NOT to Use" — do not reintroduce winpty / .node-from-ASAR / mandatory rebuild; D-04/D-07 are additive only.
  </read_first>
  <action>
    Generate placeholder icon assets in a new `assets/` dir (a simple JW / 🛋️ motif placeholder; D-07 placeholder — a nicer icon is a later file-swap). On macOS use the host toolchain (verified present): build an `assets/icon.iconset/` of sips-resized PNGs from a 1024px placeholder, then `iconutil -c icns assets/icon.iconset -o assets/icon.icns`, plus `assets/icon.png` (512px base). Produce a REAL multi-size `assets/icon.ico` (256x256 included) — NOT a renamed PNG (Forge fails on a fake .ico); if a true-ICO writer is unavailable on the dev box, commit a prebuilt placeholder .ico into assets/. Add `assets/README.md` noting these are placeholders (swap later, config untouched — D-07). Clean up the temporary `.iconset` and `placeholder-1024.png` scratch files (do not commit the iconset dir).
    EXTEND forge.config.ts packagerConfig (alongside the existing `asar`/`ignore`, do NOT modify those or rebuildConfig): add `name: 'Just-Wrapper'`, `appBundleId: 'com.justwrapper.app'`, `icon: 'assets/icon'` (NO extension — Forge appends .icns/.ico per platform), and env-gated slots `osxSign: process.env.APPLE_IDENTITY ? {} : undefined` and `osxNotarize: process.env.APPLE_ID ? { appleId: process.env.APPLE_ID, appleIdPassword: process.env.APPLE_PASSWORD!, teamId: process.env.APPLE_TEAM_ID! } : undefined`. Change the maker line `new MakerSquirrel({})` → `new MakerSquirrel({ setupIcon: 'assets/icon.ico' })`. Leave `windowsSign` UNSET (Pitfall 4 — a stray cert config hangs CI; unsigned is the D-04 lock). NEVER commit any Apple credential — everything reads from process.env.
    EDIT package.json: set `"author": "Just-Wrapper"` (replace the empty string) and add a top-level `"config": { "forge": "./forge.config.ts" }` only if not already implied — if Forge already auto-discovers forge.config.ts (it does for `electron-forge`), instead record the appId via the forge packagerConfig appBundleId (already added) and add a `"appId": "com.justwrapper.app"` field to package.json for downstream traceability per the deep-work-rules artifact list. Do NOT alter scripts or pinned dep versions.
    Create docs/PACKAGING.md: document (1) `npm run make` builds the macOS `.app` under `out/`, (2) the unsigned-app local-open path — right-click → Open, or `xattr -dr com.apple.quarantine <path-to>.app` (Pitfall 5, D-04), (3) that signing/notarization is OFF by default and is enabled later by setting APPLE_IDENTITY / APPLE_ID / APPLE_PASSWORD / APPLE_TEAM_ID env vars (env-gated, never committed), (4) a one-line pointer that Windows artifacts are produced by the CI matrix (Plan 03).
  </action>
  <verify>
    <automated>test -f assets/icon.icns && test -f assets/icon.ico && test -f assets/icon.png && grep -q "appBundleId" forge.config.ts && grep -q "osxNotarize" forge.config.ts && grep -q "setupIcon" forge.config.ts && grep -q "quarantine" docs/PACKAGING.md && ! grep -RnE "APPLE_(ID|PASSWORD|TEAM_ID|IDENTITY)\s*[:=]\s*['\"][^'\"]+['\"]" forge.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `assets/icon.icns`, `assets/icon.ico`, `assets/icon.png` all exist; `assets/icon.ico` is a real multi-size ICO, not a renamed PNG (`file assets/icon.ico` reports an MS Windows icon resource, NOT a PNG).
    - `forge.config.ts` contains `appBundleId: 'com.justwrapper.app'`, `icon: 'assets/icon'`, `osxSign`, `osxNotarize`, and `setupIcon: 'assets/icon.ico'`; the existing `asar.unpackDir`, the `ignore` keep-clause, and `rebuildConfig.onlyModules: []` are byte-for-byte unchanged.
    - No hardcoded Apple credential string appears anywhere in forge.config.ts (the negative grep in verify passes); all sign/notarize values come from `process.env.*`.
    - `package.json` contains `"appId": "com.justwrapper.app"` and a non-empty `"author"`; scripts and dep versions are unchanged.
    - `docs/PACKAGING.md` documents the `xattr -dr com.apple.quarantine` local-open path and the env-gated signing flip.
    - `npx tsc --noEmit` reports no new errors from forge.config.ts.
  </acceptance_criteria>
  <done>Icon assets exist (real .ico), forge.config.ts has icon/appId/env-gated sign slots with proven mechanics untouched, package.json metadata set, PACKAGING.md documents local-open + signing flip; no secret committed.</done>
</task>

<task type="auto">
  <name>Task 3: OS-conditional packaged smoke + macOS make-and-smoke proof (D-08 / SC1 mac / SC3)</name>
  <files>wdio.conf.ts, tests/smoke/pty-roundtrip.smoke.test.ts</files>
  <read_first>
    - .planning/phases/08-cross-platform-packaging/08-RESEARCH.md "wdio.conf.ts OS-conditional binary" (lines 382-392) — the exact ternary on process.platform + os.arch() + the CI stand-in (no `claude` on runners) note.
    - .planning/phases/08-cross-platform-packaging/08-PATTERNS.md "wdio.conf.ts" section (lines 160-172) — the hardcoded macOS path at wdio.conf.ts:22-24 is the slot to make os-conditional; the smoke-extend analog at pty-roundtrip.smoke.test.ts:27-39.
    - wdio.conf.ts (lines 1-33, ALL) — the `'wdio:electronServiceOptions': { appBinaryPath: './out/Just-Wrapper-darwin-arm64/...' }` slot.
    - tests/smoke/pty-roundtrip.smoke.test.ts (lines 1-52, ALL) — existing `echo hello` + `echo $TERM` round-trip that already proves PTY-inside-ASAR on macOS; delete its stale RED banner only if it is now GREEN.
    - tests/smoke/helpers/xterm-driver.ts (lines 1-60) — ensureSession/sendKeys/readBuffer/waitForText the smoke uses.
  </read_first>
  <action>
    Make wdio.conf.ts `appBinaryPath` OS-conditional: add `import os from 'node:os';` and replace the hardcoded macOS string with a ternary — `process.platform === 'win32' ? './out/Just-Wrapper-win32-' + os.arch() + '/Just-Wrapper.exe' : './out/Just-Wrapper-darwin-' + os.arch() + '/Just-Wrapper.app/Contents/MacOS/Just-Wrapper'`. The verified macOS dir is `out/Just-Wrapper-darwin-arm64`; the Windows path mirrors Forge's `out/Just-Wrapper-win32-<arch>/Just-Wrapper.exe` naming (the CI Windows leg in Plan 03 consumes this). Do NOT remove the existing macOS deterministic-path comment; keep maxInstances/services/framework unchanged.
    Extend tests/smoke/pty-roundtrip.smoke.test.ts so its PTY round-trip stand-in is OS-appropriate (D-08 — CI runners lack `claude`, so the smoke proves PTY-works-inside-ASAR with a shell echo, NOT `claude`). The existing `echo hello` + `echo $TERM` cases already prove SC3 on POSIX (macOS/Git Bash/WSL). Add a guarded note (and, where a shell-specific assertion is needed, branch on `process.platform`) so the round-trip uses a cross-shell-safe command: `echo hello` round-trips in cmd/PowerShell/bash alike; keep `echo $TERM` → `xterm-256color` as the POSIX SC4 stand-in but do NOT assert `$TERM` on win32 (cmd has no `$TERM`) — guard that one case to non-win32. Keep the SIGINT/Ctrl+C case POSIX-only as well (guard to non-win32) since the smoke's primary SC3 proof is the PTY echo round-trip from inside ASAR, which is the cross-platform invariant. This task does NOT change any bridge key — confirm EXPECTED_API_KEYS stays 20.
    Then run a real local macOS proof: `npm run make` to produce `out/Just-Wrapper-darwin-arm64/Just-Wrapper.app`, then `npm run test:smoke` so the packaged-app PTY round-trip (SC3) runs against the freshly-built, icon-bearing binary. If `make` or smoke surfaces a packaging regression (e.g. spawn-helper exec bit, ASAR unpack), fix forward within the proven mechanics (do NOT touch rebuildConfig — D-06; the fix is almost certainly the postinstall chmod path already in scripts/fix-node-pty.cjs).
  </action>
  <verify>
    <automated>grep -q "win32" wdio.conf.ts && grep -q "Just-Wrapper.exe" wdio.conf.ts && grep -q "os.arch()" wdio.conf.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `wdio.conf.ts` contains a `process.platform === 'win32'` ternary on `appBinaryPath` producing `./out/Just-Wrapper-win32-<arch>/Just-Wrapper.exe` for win32 and the darwin `.app/Contents/MacOS/Just-Wrapper` path otherwise, using `os.arch()`.
    - `npm run make` produces `out/Just-Wrapper-darwin-arm64/Just-Wrapper.app` (the file/dir exists after make) with the placeholder icon applied — manual/CLI check: `test -d out/Just-Wrapper-darwin-arm64/Just-Wrapper.app`.
    - `npm run test:smoke` passes the pty-roundtrip case (`echo hello` round-trips through the PTY inside the packaged ASAR app — SC3) on macOS; paste the smoke spec-reporter stdout into the SUMMARY.
    - The win32-only guards exist: the `$TERM`/`xterm-256color` and Ctrl+C/SIGINT assertions are gated to `process.platform !== 'win32'` (or otherwise not asserted on win32).
    - `grep -c '== 0' ...` is not used; EXPECTED_API_KEYS unchanged — `grep -c "'" src/main/window-config.ts` region for EXPECTED_API_KEYS still lists 20 keys (security.guard stays GREEN; do not edit window-config.ts).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>wdio.conf.ts is OS-conditional; the macOS `.app` builds via `npm run make`; the packaged PTY round-trip smoke (SC3) is GREEN on macOS with real stdout captured; win32-specific assertions guarded; zero new bridge keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine / CI → committed repo | Apple signing/notarize credentials must NEVER cross into git; only env vars at build time |
| OS host → app boot | A pre-1809 Windows host must be rejected by the native dialog gate before any node-pty spawn |
| npm tarball → packaged app | node-pty native prebuilds are shipped pinned, loaded from app.asar.unpacked, never downloaded at runtime |
| renderer → main (contextBridge) | unchanged — exactly 20 EXPECTED_API_KEYS; this plan adds zero |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | osxSign/osxNotarize config (forge.config.ts) | mitigate | Env-gated slots `process.env.APPLE_* ? {...} : undefined`; never commit a credential; negative-grep in Task 2 verify asserts no hardcoded Apple secret; CI needs no secrets this phase (D-04) |
| T-08-02 | Denial of Service (self-inflicted) | pre-1809 Windows boot | mitigate | D-05 native `dialog.showErrorBox` gate fires inside app.whenReady BEFORE store.load()/createWindow()/any node-pty spawn, so a pre-1809 host gets a clear message + clean app.quit() instead of a crashing ConPTY spawn (SC4) |
| T-08-03 | Tampering | node-pty native `.node`/spawn-helper | mitigate | Ship the exact-pinned node-pty 1.1.0 prebuild from the locked tarball; rebuildConfig.onlyModules:[] stays a no-op (D-06, no runtime download); proven ASAR unpack mechanics untouched |
| T-08-04 | Elevation of Privilege | renderer IPC surface | accept (no change) | Packaging adds ZERO bridge keys; EXPECTED_API_KEYS stays 20 (security.guard invariant), node-pty stays in main — Task 3 confirms, never edits, the key list |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs ZERO new packages (RESEARCH Package Legitimacy Audit: no new installs); no [ASSUMED]/[SUS]/[SLOP] package gate needed |

## Reliability & Cost Lens
- The pre-1809 gate is fail-OPEN on an unparseable os.release() (isUnsupportedWindows returns false) so a parse quirk never bricks a supported host — the cost of a false-block (user locked out) outweighs the rare false-allow (a malformed-release host that node-pty would itself error on).
- `npm run make` + `npm run test:smoke` are the only heavy local steps; no retry storm surface (single packaged binary, single smoke run).
</threat_model>

<verification>
- `npm run test:unit` GREEN (os-gate.test.ts added, all pre-existing unit tests still GREEN, security.guard at 20 keys).
- `npm run make` produces a runnable `out/Just-Wrapper-darwin-arm64/Just-Wrapper.app` with the placeholder icon (SC1 mac).
- `npm run test:smoke` passes the packaged PTY round-trip (SC3) on macOS.
- `npx tsc --noEmit` clean; no hardcoded Apple secret anywhere.
</verification>

<success_criteria>
- The pure OS gate + its wiring make a pre-1809 Windows host detectable and blockable before any node-pty spawn (SC4 logic, verified by os-gate.test.ts; the live dialog is a Plan-03 human/CI item).
- A real macOS `.app` builds via `npm run make` with no manual post-processing and carries the placeholder icon + appId metadata (SC1 mac, D-07).
- The packaged app round-trips a PTY echo from inside the ASAR archive (SC3) — proven by the extended smoke on macOS.
- Signing is OFF by default and a documented env-gated flip away from ON; no secret is committed (D-04).
- Zero new bridge keys; proven node-pty ASAR mechanics (D-06) untouched.
</success_criteria>

<output>
Create `.planning/phases/08-cross-platform-packaging/08-01-SUMMARY.md` when done. Include the `npm run make` and `npm run test:smoke` stdout (the SC1-mac + SC3 proofs), confirm EXPECTED_API_KEYS stayed 20, and note the 17763-vs-18309 discrepancy was preserved (not silently changed) for the human to confirm.
</output>
