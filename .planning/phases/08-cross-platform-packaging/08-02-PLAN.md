---
phase: 08-cross-platform-packaging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/shell-discovery.ts
  - src/main/__tests__/shell-discovery.test.ts
  - src/main/readiness-probe.ts
  - src/main/__tests__/readiness-probe.test.ts
autonomous: true
requirements: [PKG-01]
user_setup: []

must_haves:
  truths:
    - "WindowsShellProvider enumerates PowerShell / CMD / Git Bash / WSL from env-expanded well-known paths, filtered to on-disk, with a Windows-aware default placed first so the dropdown is NEVER empty (D-02/D-05)"
    - "Git Bash and WSL readiness probes reuse the POSIX buildPosixProbe verbatim (they are bash/POSIX) (D-03)"
    - "CMD and PowerShell readiness either produce a send-vs-match marker keyed off a PRODUCED output line, or degrade-loudly via a documented 'auto-run unsupported on <shell>' signal — never a silent mis-fire (D-03)"
    - "The Windows fills are electron-free pure helpers with injected deps so they unit-test in the Node env on macOS"
    - "Zero new bridge keys — any degrade notice reuses the existing onPtyStatus notice channel; EXPECTED_API_KEYS stays 20"
  artifacts:
    - path: "src/main/shell-discovery.ts"
      provides: "buildWindowsShellList pure builder + filled WindowsShellProvider enumeration (D-02)"
      contains: "buildWindowsShellList"
      exports: ["buildWindowsShellList", "WindowsShellProvider", "selectShellProvider"]
    - path: "src/main/readiness-probe.ts"
      provides: "WindowsReadinessProbe.forShell() per-shell marker (POSIX reuse for Git Bash/WSL; CMD/PowerShell marker-or-degrade) (D-03)"
      contains: "WindowsReadinessProbe"
      exports: ["buildPosixProbe", "WindowsReadinessProbe", "selectReadinessProbe"]
    - path: "src/main/__tests__/shell-discovery.test.ts"
      provides: "Windows enumeration unit tests (injected existsFn fixtures, never-empty, default-first, de-dupe, labels)"
      contains: "buildWindowsShellList"
    - path: "src/main/__tests__/readiness-probe.test.ts"
      provides: "Windows per-shell readiness unit tests (POSIX reuse + CMD/PowerShell marker-or-degrade)"
      contains: "win32"
  key_links:
    - from: "src/main/shell-discovery.ts WindowsShellProvider"
      to: "buildWindowsShellList"
      via: "real fs.existsSync injected into the pure builder; Windows-aware default first"
      pattern: "buildWindowsShellList"
    - from: "src/main/readiness-probe.ts WindowsReadinessProbe.forShell"
      to: "buildPosixProbe (Git Bash/WSL) or degrade marker (CMD/PowerShell)"
      via: "shell-path basename branch"
      pattern: "buildPosixProbe"
---

<objective>
Fill the two Windows seams that prior phases explicitly deferred to Phase 8: the real
`WindowsShellProvider` enumeration (PowerShell / CMD / Git Bash / WSL) behind the existing
`ShellDiscovery` seam, and `WindowsReadinessProbe.forShell()` (currently throws) behind the
existing `ReadinessProbeProvider` seam. Both are filled with the same interface-first /
electron-free / injected-dependency pure-helper pattern as the macOS providers so they
unit-test on the macOS dev box in the Node env — the real-Windows BYTE correctness is a
CI-smoke + human-verify item (Plan 03), but the enumeration logic, the never-empty
invariant, and the per-shell readiness strategy are all unit-provable here.

This is a vertical slice (MVP_MODE): a Windows user can pick a real shell from the dropdown
AND a Windows session carrying a startup command either auto-runs (Git Bash/WSL) or degrades
loudly (CMD/PowerShell) instead of crashing — one end-to-end Windows capability, not a
disconnected layer. The plan is file-disjoint from Plan 01, so both run in Wave 1.

Purpose: Close the PROJECT.md "Active" Windows-shell-discovery deferral and make Windows
auto-run safe (D-02/D-03), with zero new bridge keys.
Output: filled `WindowsShellProvider` + a `buildWindowsShellList` pure builder, filled
`WindowsReadinessProbe.forShell()`, and extended unit tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cross-platform-packaging/08-CONTEXT.md
@.planning/phases/08-cross-platform-packaging/08-RESEARCH.md
@.planning/phases/08-cross-platform-packaging/08-PATTERNS.md
@CLAUDE.md

# Files this plan fills/extends (analogs + targets are the SAME files)
@src/main/shell-discovery.ts
@src/main/readiness-probe.ts
@src/main/shell-resolver.ts
@src/main/__tests__/shell-discovery.test.ts
@src/shared/api-types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill WindowsShellProvider enumeration (D-02 / D-05 never-empty)</name>
  <files>src/main/shell-discovery.ts, src/main/__tests__/shell-discovery.test.ts</files>
  <read_first>
    - .planning/phases/08-cross-platform-packaging/08-PATTERNS.md "shell-discovery.ts" section (lines 30-70) — the pure-builder shape to mirror (default FIRST, injected existsFn, de-dupe, basename label), the exact stub to replace (lines 57-65), the candidate paths + the Pitfall-3 latent bug (resolveShell() yields /bin/zsh on Windows), and the test analog (lines 70).
    - .planning/phases/08-cross-platform-packaging/08-RESEARCH.md Pattern 1 (lines 154-173) + Pitfall 3 (lines 310-314) + Assumptions A1-A3 (lines 412-414) + Open Q2 (lines 428-431) — the env-expansion candidate set, the Windows-aware-default requirement, the [ASSUMED] path tags.
    - src/main/shell-discovery.ts (lines 1-99, ALL) — the SAME-FILE analog: buildShellList (48-62), MacShellProvider (69-80), the WindowsShellProvider STUB (87-94) to replace, selectShellProvider (97-99).
    - src/main/shell-resolver.ts (lines 31-37) — resolveShell() returns process.env.SHELL || '/bin/zsh' (POSIX) — DO NOT rely on it for the Windows default.
    - src/main/__tests__/shell-discovery.test.ts (lines 41-98) — the injected-existsFn fixture style + the existing selectShellProvider('win32') case (lines 86-92) that asserts non-empty + non-throwing; the Windows fill must keep that GREEN.
  </read_first>
  <behavior>
    - buildWindowsShellList(candidates, windowsDefault, existsFn): places windowsDefault FIRST, filters to on-disk via existsFn, de-dupes by path, labels via the {basename → friendly} label map (or basename). Mirror buildShellList exactly but split on `[\\/]` for the Windows basename (paths use backslashes).
    - Given a default `C:\Windows\System32\cmd.exe` and candidates [powershell.exe path, cmd.exe path, git bash path, wsl.exe path] with existsFn returning true for all → output starts with cmd.exe (default first) and contains all unique entries, each labeled (PowerShell / CMD / Git Bash / WSL or basename).
    - With existsFn returning true for ONLY the default → output is exactly [{default, label}] (D-05 never-empty even when nothing else is on disk).
    - With the default ALSO present in candidates → de-duped (appears once).
    - selectShellProvider('win32').discover() never throws and returns length ≥ 1 (the existing test stays GREEN against the filled provider).
  </behavior>
  <action>
    Add an exported pure `buildWindowsShellList(candidates: string[], windowsDefault: string, existsFn: (p: string) => boolean): DiscoveredShell[]` mirroring `buildShellList` (default FIRST for the D-05 never-empty invariant, injected existsFn for testability, de-dupe by path, label by basename split on `[\\/]`). Apply a small friendly label map { 'powershell.exe': 'PowerShell', 'pwsh.exe': 'PowerShell 7', 'cmd.exe': 'CMD', 'bash.exe': 'Git Bash', 'wsl.exe': 'WSL' } falling back to the raw basename for anything unmapped (labeling is Claude's discretion per D-02).
    Fill `WindowsShellProvider.discover()`: compute the candidate set from env-EXPANDED well-known paths (NOT hardcoded `C:\`): PowerShell `path.join(process.env.SystemRoot ?? 'C:\\Windows', 'System32\\WindowsPowerShell\\v1.0\\powershell.exe')` [CITED CLAUDE.md], PowerShell 7 `path.join(process.env.ProgramFiles ?? 'C:\\Program Files', 'PowerShell\\7\\pwsh.exe')` [A1], CMD `path.join(process.env.SystemRoot ?? 'C:\\Windows', 'System32\\cmd.exe')` [A2], Git Bash `path.join(process.env.ProgramFiles ?? 'C:\\Program Files', 'Git\\bin\\bash.exe')` (also try `Git\\usr\\bin\\bash.exe`) [A3], WSL `path.join(process.env.SystemRoot ?? 'C:\\Windows', 'System32\\wsl.exe')` [CITED CLAUDE.md]. Compute the Windows-aware DEFAULT independently of `resolveShell()` (Pitfall 3 — resolveShell() returns /bin/zsh on Windows): prefer `process.env.ComSpec` (cmd.exe) else the CMD path else the first candidate, so the dropdown is never empty even if every well-known path is missing. Wire real `fs.existsSync` into `buildWindowsShellList`. Import `path` from 'node:path' (fs is already imported). Tag the [ASSUMED] candidate paths with code comments citing A1-A3 so the human-verify on real Windows knows what to confirm.
    Extend src/main/__tests__/shell-discovery.test.ts with a `buildWindowsShellList` describe block covering every <behavior> case using the existing `const exists = (paths) => (p) => paths.includes(p)` injected-stub idiom with Windows-style backslash fixture paths; keep the existing selectShellProvider('win32') case passing.
  </action>
  <verify>
    <automated>npm run test:unit -- src/main/__tests__/shell-discovery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/shell-discovery.ts` exports `buildWindowsShellList` and the filled `WindowsShellProvider` no longer just echoes `resolveShell().shell`; it has NO `import ... from 'electron'`.
    - The candidate paths use `process.env.SystemRoot` / `process.env.ProgramFiles` / `process.env.ComSpec` expansion (grep finds `process.env.SystemRoot` and `process.env.ComSpec` in shell-discovery.ts) — no bare hardcoded `C:\\Windows` outside the env-fallback default.
    - `npm run test:unit -- src/main/__tests__/shell-discovery.test.ts` exits 0; the new buildWindowsShellList cases AND the pre-existing buildShellList/selectShellProvider cases are all GREEN.
    - The default-first + never-empty assertions hold: a fixture where existsFn returns true only for the default yields exactly one entry (the default).
    - [ASSUMED] code comments cite A1-A3 next to the pwsh/git-bash/cmd candidate paths.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>WindowsShellProvider enumerates PowerShell/CMD/Git Bash/WSL from env-expanded paths with a Windows-aware default first; never-empty holds; new + existing shell-discovery unit tests GREEN; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill WindowsReadinessProbe.forShell() — POSIX reuse + CMD/PowerShell marker-or-degrade (D-03)</name>
  <files>src/main/readiness-probe.ts, src/main/__tests__/readiness-probe.test.ts</files>
  <read_first>
    - .planning/phases/08-cross-platform-packaging/08-PATTERNS.md "readiness-probe.ts" section (lines 74-111) — the send-vs-match matcher shape Git Bash/WSL reuse verbatim, the per-shell strategy, the stub to replace (lines 98-107), the degrade-loudly channel (reuse onPtyStatus notice — ZERO new bridge key, lines 109), and the test analog (lines 111).
    - .planning/phases/08-cross-platform-packaging/08-RESEARCH.md Pattern 2 (lines 175-188) + Pitfall 6 (lines 328-332) + Assumptions A4-A5 (lines 415-416) + Open Q3 (lines 433-436) — the per-shell candidate markers, the CMD/PowerShell echo-self-match hazard, the degrade-loudly fallback (locked D-03), the [ASSUMED] tags.
    - src/main/readiness-probe.ts (lines 1-137, ALL) — the SAME-FILE analog: the ShellReadinessProbe interface (29-42), PROBE_SCAN_LIMIT 8KB (56), buildPosixProbe (78-100), MacReadinessProbe (109-118), the WindowsReadinessProbe THROW stub (126-131) to replace, selectReadinessProbe (134-136).
    - src/shared/api-types.ts (lines 78-87) — the `notice?: string` field on PtyStatusPayload riding onPtyStatus, the existing 05.1 degrade-notice channel (ZERO new bridge key) to reuse if CMD/PowerShell degrade.
    - src/main/__tests__/readiness-probe.test.ts (lines 23-93, Group 1) — the pure-helper assertion style; the existing `selectReadinessProbe('win32').forShell()` THROWS case (lines 72-75) that this fill CHANGES — update it to assert the new per-shell behavior, do not leave the throw assertion.
  </read_first>
  <behavior>
    - Git Bash (a shellPath whose basename is bash.exe / contains 'bash') → WindowsReadinessProbe.forShell returns a probe whose `marker` starts with `: __JW_READY_` and ends with `\r` (POSIX reuse — Git Bash IS bash), and matches() fires ONLY on a nonce after a newline boundary (identical to buildPosixProbe).
    - WSL (basename wsl.exe / contains 'wsl') → same POSIX-reuse probe (the launched shell inside WSL is bash/zsh).
    - CMD (basename cmd.exe) and PowerShell (powershell.exe / pwsh.exe): EITHER return a marker whose SEND token differs from the MATCH token so the matcher keys off a PRODUCED output line and never trips on the command-echo (Pitfall 6), OR — the locked-safe default given no real-Windows byte capture is available — mark the shell as readiness-UNSUPPORTED so the caller degrades loudly. Pick ONE of these per shell and make the unit test assert exactly that contract. The degrade path must NOT throw at forShell() time in a way that crashes the spawn; it signals "no auto-run" via a probe-shaped result or an explicit unsupported flag the caller already handles through the onPtyStatus notice channel.
    - selectReadinessProbe('win32').forShell('/x/bash.exe') no longer throws; it returns a valid POSIX-reuse probe. (The Phase-8 fill removes the unconditional throw.)
  </behavior>
  <action>
    Fill `WindowsReadinessProbe.forShell(shellPath)`: branch on the shell basename. For Git Bash (basename matches /bash/i) and WSL (basename matches /wsl/i) REUSE `buildPosixProbe` verbatim with a fresh `__JW_READY_<hex>__` crypto nonce (they are bash/POSIX — high confidence). For CMD (/cmd/i) and PowerShell (/powershell|pwsh/i): given NO real-Windows byte capture is available on the macOS dev box (Open Q3, A4-A5), prefer the LOCKED-SAFE degrade (D-03) — return a readiness result that signals "auto-run unsupported on <shell>" rather than a guessed echo marker that could mis-fire and inject garbage into a `claude --rc` session (a mis-fire is worse than a clear notice — RESEARCH lines 188/331). Encode the degrade so the create() probe caller already routes it through the EXISTING onPtyStatus `notice?: string` channel (api-types.ts:78-87) — exactly like the 05.1 ready-timeout notice — adding ZERO new bridge key. If the implementer instead chooses to ATTEMPT a CMD/PowerShell marker, it MUST use distinct send-vs-match tokens (the matched token appears on a PRODUCED line, not the command-echo line — Pitfall 6) and the unit test must encode that exact contract; do NOT ship a marker whose nonce appears in BOTH the echoed input and the output without a producing-line guard. Tag the CMD/PowerShell decision with an [ASSUMED]/A4-A5 code comment and a one-line note that real-Windows byte validation is the human-verify item. Reuse the existing crypto import + buildPosixProbe; do NOT change buildPosixProbe, PROBE_SCAN_LIMIT, MacReadinessProbe, or the interface shape.
    Update src/main/__tests__/readiness-probe.test.ts Group 1: REPLACE the `selectReadinessProbe('win32').forShell() THROWS` case with cases asserting the new per-shell contract — Git Bash/WSL return a `: __JW_READY_...\r` POSIX-reuse probe (marker prefix/suffix + matches() send-vs-match behavior), and CMD/PowerShell return the chosen degrade-or-marker contract (assert exactly what was implemented). Keep all pre-existing buildPosixProbe / MacReadinessProbe cases unchanged and GREEN.
  </action>
  <verify>
    <automated>npm run test:unit -- src/main/__tests__/readiness-probe.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/readiness-probe.ts` `WindowsReadinessProbe.forShell()` no longer unconditionally throws; `grep -n "throw new Error('Windows readiness probe is implemented in Phase 8" src/main/readiness-probe.ts` returns nothing (the Phase-8 stub throw is removed).
    - Git Bash and WSL paths route to a `: __JW_READY_<hex>__\r` POSIX-reuse probe (the test asserts marker prefix `: __JW_READY_` + `\r` suffix + send-vs-match matches() behavior).
    - CMD and PowerShell route to the implemented degrade-or-marker contract, and the test asserts exactly that; no marker is shipped whose nonce trips on the command-echo line (Pitfall 6).
    - The degrade path (if chosen) reuses the existing onPtyStatus `notice?: string` channel — NO change to EXPECTED_API_KEYS (it stays 20); `src/main/window-config.ts` is NOT edited by this plan.
    - `npm run test:unit -- src/main/__tests__/readiness-probe.test.ts` exits 0 (Group 1 GREEN including the rewritten win32 cases; Group 2 unaffected).
    - [ASSUMED]/A4-A5 code comment present on the CMD/PowerShell decision flagging real-Windows byte validation as the human-verify item.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>WindowsReadinessProbe.forShell() reuses buildPosixProbe for Git Bash/WSL and degrades-loudly (or ships a producing-line-guarded marker) for CMD/PowerShell; the unconditional throw is gone; the degrade reuses onPtyStatus (zero new keys); readiness-probe unit tests GREEN; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS env / filesystem → shell enumeration | env vars (SystemRoot/ProgramFiles/ComSpec) and on-disk existence drive the dropdown; the default is always present so the list is never empty |
| readiness marker bytes → PTY | the readiness marker is a literal/crypto-nonce built in main; on an unverified shell the safe choice is degrade-loudly, not inject |
| main → renderer (contextBridge) | unchanged — degrade notice reuses the existing onPtyStatus channel; zero new keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | Windows readiness marker bytes injected into the PTY | mitigate | The marker is a `: ` no-op (Git Bash/WSL) or a literal/crypto-nonce echo; on CMD/PowerShell where a safe send-vs-match cannot be byte-verified on the dev box, DEGRADE-LOUDLY (no inject) rather than risk garbage entering a `claude --rc` session (D-03 locked safety) |
| T-08-06 | Denial of Service | empty shell dropdown on Windows | mitigate | buildWindowsShellList places a Windows-aware default (ComSpec/cmd.exe) FIRST and never returns empty (D-05); resolveShell() is NOT used for the Windows default (Pitfall 3) |
| T-08-07 | Information Disclosure | readiness nonce leaking into rendered scrollback | mitigate | The send-vs-match split + the caller's existing nonce-scrub (05.1 invisibility) keep the nonce out of scrollback; this plan does not change the caller's withhold-probe-bytes behavior; the nonce is uniqueness-only (V6 n/a) |
| T-08-08 | Elevation of Privilege | new bridge key | accept (no change) | This plan adds ZERO bridge keys; the degrade notice rides the existing onPtyStatus `notice?:string`; window-config.ts EXPECTED_API_KEYS stays 20 (security.guard GREEN) |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs ZERO new packages (RESEARCH Package Legitimacy Audit: no new installs) |

## Reliability & Cost Lens
- Both fills are pure + injected-dep, so they have no runtime cost surface (no spawn, no network) and no retry-storm path; the only "cost" is the unit-test run (< 30s).
- The degrade-loudly default for CMD/PowerShell trades a missing auto-run feature for zero mis-fire risk — the correct reliability call when byte-correctness is unverifiable on the dev box.
</threat_model>

<verification>
- `npm run test:unit` GREEN (shell-discovery + readiness-probe extensions GREEN; all pre-existing unit tests still GREEN).
- `grep -c` EXPECTED_API_KEYS region still 20 (security.guard untouched, GREEN).
- `npx tsc --noEmit` clean.
- The real-Windows BYTE correctness of the enumeration paths + CMD/PowerShell readiness is flagged [ASSUMED] in code and is the Plan-03 CI-smoke + human-verify item.
</verification>

<success_criteria>
- Windows users get a populated shell dropdown (PowerShell/CMD/Git Bash/WSL, on-disk filtered) that is never empty (D-02/D-05).
- A Windows session with a startup command auto-runs on Git Bash/WSL (POSIX probe reuse) and degrades loudly (clear notice, no mis-fire) on CMD/PowerShell (D-03).
- Both fills are electron-free + injected-dep and unit-proven on the macOS dev box; real-Windows byte correctness is the CI/human-verify item.
- Zero new bridge keys; security.guard stays GREEN at 20.
</success_criteria>

<output>
Create `.planning/phases/08-cross-platform-packaging/08-02-SUMMARY.md` when done. State the chosen CMD/PowerShell readiness strategy (marker vs degrade) and WHY, confirm EXPECTED_API_KEYS stayed 20, and list the [ASSUMED] Windows paths + CMD/PowerShell byte-semantics that Plan 03's CI smoke + human-verify must confirm on real Windows.
</output>
