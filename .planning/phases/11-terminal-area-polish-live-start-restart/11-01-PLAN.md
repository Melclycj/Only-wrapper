---
phase: 11-terminal-area-polish-live-start-restart
plan: 01
type: execute
wave: 2
depends_on: ["11-00"]
files_modified:
  - src/renderer/Sidebar.tsx
  - src/renderer/SessionManager.tsx
  - tests/smoke/header-controls.smoke.test.ts
  - tests/smoke/startup-command.smoke.test.ts
autonomous: true
requirements: [SESS-07]
user_setup: []

must_haves:
  truths:
    - "No restart affordance is reachable anywhere — the sidebar ↻ button is gone, the context-menu Restart item is gone, and a grep for the restart-session testid in src/renderer returns 0"
    - "The Remove/Delete confirm copy escalates when the target row's agentState is working or waiting (D-04), wired from the pure buildConfirmBody — and the ConfirmModal stays a dumb controlled component"
    - "Recycle = Remove → Inactive List → Start ▶ is the only recycle path; a configured session keeps its logical id + recipe across the cycle (app-restart-restore.smoke stays GREEN as the positive proof)"
    - "The ptyRestart machinery + handleRestart helper are KEPT but un-surfaced (flagged retained); EXPECTED_API_KEYS stays exactly 20 (security.guard.test.ts GREEN unchanged)"
  artifacts:
    - path: "src/renderer/SessionManager.tsx"
      provides: "ConfirmModal body wired to buildConfirmBody; context-menu Restart arm removed; onRestart prop dropped from Sidebar; handleRestart flagged retained"
      contains: "buildConfirmBody"
    - path: "src/renderer/Sidebar.tsx"
      provides: "The ↻ restart-session button block + onRestart prop removed from SortableRowProps/SidebarProps"
      contains: "Sidebar"
    - path: "tests/smoke/header-controls.smoke.test.ts"
      provides: "Asserts the context-menu Restart item is ABSENT (replaces the Restart-driving block); keeps the header-restart absent assertion"
      contains: "not.toContain"
    - path: "tests/smoke/startup-command.smoke.test.ts"
      provides: "SC3 rewritten to the recycle model (Remove → dormant → Start ▶ re-runs the stored command); the menuAction Restart drive removed"
      contains: "inactive-list"
  key_links:
    - from: "src/renderer/SessionManager.tsx"
      to: "src/renderer/confirm-copy.ts"
      via: "buildConfirmBody({ removeMode, configured, isRunning, agentState }) → ConfirmModal body prop"
      pattern: "buildConfirmBody"
    - from: "src/renderer/Sidebar.tsx"
      to: "(removed)"
      via: "the restart-session ↻ button + onRestart prop deleted"
      pattern: "restart-session"
---

<objective>
Delete every restart UI entry point (the sidebar ↻ button and the context-menu Restart item)
and wire the D-04 agent-aware confirm copy from the pure `buildConfirmBody` (Plan 00) — the
SESS-07 lifecycle simplification. After this plan the live vocabulary is exactly Start /
Remove / Clear; recycling is Remove → Inactive List → Start ▶ (a fresh process), with no
restart verb and no `exit`-to-recycle. The `ptyRestart` IPC machinery + the `handleRestart`
helper + the `— restarted —` separator code are KEPT but un-surfaced (D-01 "remove the UI,
keep the mechanism"), so `EXPECTED_API_KEYS` stays 20 and `src/main/*` is untouched.

The three restart-asserting smoke specs are updated IN THIS PLAN (RESEARCH Pitfall 1 — never
leave a restart smoke RED after deleting the UI that drove it). The 06.1 "automated green is
not proof for lifecycle changes" lesson applies: the unit/grep/smoke guards here are
necessary but the real proof is the Plan 03 BLOCKING human gate.

Purpose: SESS-07 / D-01 (remove restart UI) + D-04 (escalation copy) + D-02 (recycle model).
Output: two deletion sites, the wired confirm copy, two rewritten smoke specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-CONTEXT.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-RESEARCH.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-PATTERNS.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-UI-SPEC.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-00-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the restart UI entry points (sidebar ↻ + context-menu Restart) and wire the D-04 confirm copy</name>
  <files>src/renderer/Sidebar.tsx, src/renderer/SessionManager.tsx</files>
  <read_first>
    - src/renderer/Sidebar.tsx lines 80-90, 145-170, 205-215, 314-350, 430-490 (the onRestart prop on both SortableRowProps + SidebarProps, the ↻ block ~334-349, the comment blocks at ~88/~209/~317 referencing Restart, and the onRestart pass-through ~484)
    - src/renderer/SessionManager.tsx lines 229-246 (handleRestart — the retained helper), 636 (the onRestart={handleRestart} prop on <Sidebar>), 698-717 (the ConfirmModal body ternary to replace), 718-755 (the context-menu items array incl. the Restart ternary ~728-730), 575-598 (closingSession / closingIsRunning / menuIsDormant derivations)
    - src/renderer/confirm-copy.ts (the buildConfirmBody signature from Plan 00) and src/renderer/__tests__/confirm-copy.test.ts
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-PATTERNS.md lines 153-249 (the exact deletion sites + the wiring pattern) and 226-272 (the Sidebar ↻ block + button shape to KEEP for surviving controls)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-UI-SPEC.md lines 181-196 (the selector contract: restart-session REMOVED, everything else SURVIVES)
  </read_first>
  <action>
    In `Sidebar.tsx`: DELETE the entire `{!running && !dormant && ( ... data-testid="restart-session"
    ... )}` ↻ button block (~334-349). Remove the `onRestart` field from BOTH `SortableRowProps`
    (~84/~149) and `SidebarProps`, remove the `onRestart` destructure (~169) and the
    `onRestart={onRestart}` pass-through to the sortable row (~484). Remove or correct the Restart
    references in the comment blocks (~88, ~209, ~317) so no stale "↻ Restart" prose remains. The
    surviving per-row controls (Start ▶ on dormant, Remove ✕) and their `.row-control` shape are
    untouched.
    In `SessionManager.tsx`: (a) Replace the inline ConfirmModal `body={...}` ternary (~705-713) with
    a call to `buildConfirmBody({ removeMode, configured: closingSession?.configured === true,
    isRunning: closingIsRunning, agentState: closingSession?.agentState })` imported from
    `./confirm-copy`. ConfirmModal stays dumb (receives the computed `body` string). The title +
    confirmLabel ternaries are UNCHANGED (only the body escalates). (b) In the context-menu items
    array, change the Restart ternary (~728-730) so a live (non-dormant) row offers NO Restart:
    `menuIsDormant ? { label: 'Start', onSelect: () => handleStart(menuState.id) } : null` and ensure
    the items array filters out nulls (or restructure so the live branch simply omits the entry).
    Keep the Edit / Start-without-command / Delete-or-Remove entries intact. (c) Remove the
    `onRestart={handleRestart}` prop from `<Sidebar>` (~636). (d) Keep `handleRestart` (~235-246) but
    add the comment `// D-01: retained machinery, no UI entry point` directly above it — it must be
    unreachable from any UI surface but the function body + `window.api.ptyRestart` call stay. Do NOT
    touch `src/main/*` and do NOT touch `window-config.ts` (EXPECTED_API_KEYS stays 20). Do NOT remove
    the `— restarted —` separator machinery in SessionView (D-01 keeps it dormant).
  </action>
  <verify>
    <automated>grep -rc 'data-testid="restart-session"' src/renderer && npm run test:unit -- confirm-copy && npx tsc --noEmit && eslint src/renderer/Sidebar.tsx src/renderer/SessionManager.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'restart-session' src/renderer` → 0 (the ↻ button testid is gone from all renderer source)
    - `grep -c "label: 'Restart'" src/renderer/SessionManager.tsx` → 0 (the context-menu Restart arm is removed)
    - `grep -c "onRestart" src/renderer/Sidebar.tsx` → 0 (prop fully removed from interfaces + usage)
    - `grep -c "buildConfirmBody" src/renderer/SessionManager.tsx` → 1+ (confirm body wired from the pure builder)
    - `grep -c "D-01: retained machinery" src/renderer/SessionManager.tsx` → 1 (handleRestart kept + flagged)
    - `grep -c "ptyRestart" src/main/window-config.ts` → 1 (bridge key UNCHANGED — EXPECTED_API_KEYS stays 20)
    - `grep -c "dangerouslySetInnerHTML" src/renderer/SessionManager.tsx` → 0 (D-04 copy is a text-node string)
    - `npx tsc --noEmit` exits 0; `eslint` clean on both files
  </acceptance_criteria>
  <done>The two restart UI entry points are deleted, handleRestart/ptyRestart kept un-surfaced, EXPECTED_API_KEYS unchanged at 20, and the D-04 escalation copy is wired from buildConfirmBody.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the restart-asserting smoke specs to the recycle model (Wave-0 lockstep)</name>
  <files>tests/smoke/header-controls.smoke.test.ts, tests/smoke/startup-command.smoke.test.ts</files>
  <read_first>
    - tests/smoke/header-controls.smoke.test.ts lines 1-50 (the describe + helpers incl. clickMenuItem), 106-115 (the header-restart absent assertion to KEEP), 115-170 (the Restart-driving block to REPLACE)
    - tests/smoke/startup-command.smoke.test.ts lines 72-78 (the menuAction helper), 114-135 (the SC3 Restart-re-runs block — the D-05-voided test to rewrite), 137-225 (the R1 dormant-Start block + idle-start-session driving idiom to reuse for the rewrite), 226-end
    - tests/smoke/app-restart-restore.smoke.test.ts lines 117-215 (the Remove → confirm-close → assert-in-inactive-list → first-Start idiom — the canonical recycle-without-restart pattern to mirror; this spec STAYS GREEN unchanged)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-PATTERNS.md lines 352-407 (the header-controls absent-assertion pattern + the startup-command SC3 rewrite pattern + the contextMenuLabels helper)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-RESEARCH.md lines 123-130 (Pitfall 1 — the three affected smoke files + the exact line refs)
  </read_first>
  <action>
    In `header-controls.smoke.test.ts`: KEEP the `expect(await hasTestId('header-restart')).toBe(false)`
    assertion (~109). DELETE the Restart-driving it-block (~115-170) that does `clickMenuItem('Restart')`
    and asserts the `— restarted —` separator. REPLACE it with an it-block that opens the row context
    menu and asserts the menu does NOT contain a 'Restart' item: read the menu item labels (a
    `contextMenuLabels()` helper querying `.context-menu-item` textContent — add it if not present,
    per PATTERNS ~369-378) and `expect(labels).not.toContain('Restart')`, then press Escape to close.
    Update the describe/comment text so it no longer claims restart-in-place is reachable via the menu.
    In `startup-command.smoke.test.ts`: REWRITE the SC3 it-block (~114-135) from the D-05-voided
    "Restart re-runs the command after the — restarted — separator" to the recycle model: Remove the
    configured live session (open the row context menu → click 'Remove' → confirm-close → wait for the
    row to land in the `inactive-list` container) then drive the dormant Start ▶ via
    clickByTestId('idle-start-session') and assert the SAME stored startupCommand re-runs (output
    visible) WITHOUT a `— restarted —` separator (fresh process). Reuse the app-restart-restore Remove→
    Inactive idiom + the existing R1 idle-start-session driving idiom already in this file (~148-225).
    Remove the now-unused `menuAction(id, 'Restart')` drive and the `— restarted ` SEPARATOR assertion
    from SC3. The R1 dormant-Start block and the no-command bare-shell block stay. Do NOT touch
    app-restart-restore.smoke.test.ts — it is already the positive recycle proof and must stay GREEN.
  </action>
  <verify>
    <automated>grep -rc "clickMenuItem('Restart')\|menuAction(id, 'Restart')" tests/smoke && npm run test:smoke -- --spec tests/smoke/header-controls.smoke.test.ts --spec tests/smoke/startup-command.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "Restart')" tests/smoke/header-controls.smoke.test.ts tests/smoke/startup-command.smoke.test.ts` shows the Restart-DRIVING calls are gone (no `clickMenuItem('Restart')` / `menuAction(id, 'Restart')`)
    - `grep -c "not.toContain('Restart')" tests/smoke/header-controls.smoke.test.ts` → 1 (the absence assertion)
    - `grep -c "inactive-list" tests/smoke/startup-command.smoke.test.ts` → 1+ (SC3 rewritten to the Remove→Inactive→Start recycle)
    - `grep -c "— restarted" tests/smoke/startup-command.smoke.test.ts` → 0 in the SC3 block (the voided separator assertion removed)
    - `npm run test:smoke -- --spec tests/smoke/header-controls.smoke.test.ts --spec tests/smoke/startup-command.smoke.test.ts` exits 0 (both specs GREEN against the packaged app)
    - `tests/smoke/app-restart-restore.smoke.test.ts` is byte-unchanged (`git diff --stat` shows it not modified)
  </acceptance_criteria>
  <done>The two restart-asserting smoke specs are rewritten in lockstep with the UI deletion — header-controls asserts the menu Restart is absent, startup-command SC3 proves the recycle model, and no smoke is left RED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC `window.api`) | FROZEN — this plan removes UI call-sites of `ptyRestart` but keeps the bridge key; no key added/removed |
| user strings → React render | the D-04 confirm body + `{name}` rendered as React text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering (XSS) | ConfirmModal body (D-04 escalation + `{name}`) | mitigate | body comes from the pure `buildConfirmBody` string and renders as a React text node; `{name}` stays inside the existing template literal; `grep dangerouslySetInnerHTML` → 0 (acceptance gate) |
| T-11-02 | Elevation | IPC bridge surface (`EXPECTED_API_KEYS`) | mitigate | D-01 keeps the existing `ptyRestart` key (removes only UI call-sites); `window-config.ts` untouched → `EXPECTED_API_KEYS` stays 20; `security.guard.test.ts` GREEN (verified in Plan 03 full-suite) |
| T-11-03 | Repudiation | retained `handleRestart` dead machinery | accept | Intentionally retained un-surfaced (D-01); flagged `// D-01: retained machinery, no UI entry point`; unreachable from any UI surface (grep gates confirm no caller) — low risk, no attack surface |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero packages added this phase; no install task |
</threat_model>

<verification>
- `grep -rc 'restart-session' src/renderer` → 0 and `grep -c "label: 'Restart'" src/renderer/SessionManager.tsx` → 0 (no restart affordance reachable)
- `grep -c "ptyRestart" src/main/window-config.ts` → 1 (bridge unchanged, EXPECTED_API_KEYS stays 20)
- `npm run test:unit` GREEN; `npx tsc --noEmit` exits 0; `eslint` clean on changed files
- `npm run test:smoke` GREEN incl. the rewritten header-controls + startup-command specs and the unchanged app-restart-restore positive proof
</verification>

<success_criteria>
- No restart affordance reachable anywhere (sidebar ↻ + context-menu Restart both deleted; grep guards GREEN)
- Recycle = Remove → Inactive List → Start ▶ is the only path; configured session keeps logical id + recipe (app-restart-restore.smoke GREEN)
- D-04 escalation copy wired from buildConfirmBody; ConfirmModal stays dumb
- `ptyRestart` machinery + handleRestart kept un-surfaced; EXPECTED_API_KEYS stays 20; main untouched
- The two restart-asserting smoke specs rewritten in lockstep — no RED smoke
</success_criteria>

<output>
Create `.planning/phases/11-terminal-area-polish-live-start-restart/11-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- **Modified** `src/renderer/Sidebar.tsx` — ↻ `restart-session` button block REMOVED; `onRestart` prop removed from `SortableRowProps` + `SidebarProps`.
- **Modified** `src/renderer/SessionManager.tsx` — ConfirmModal body wired to `buildConfirmBody`; context-menu Restart arm removed; `onRestart` pass-through dropped; `handleRestart` flagged `// D-01: retained machinery, no UI entry point`.
- **Modified** `tests/smoke/header-controls.smoke.test.ts` — new `not.toContain('Restart')` menu-absence assertion (+ a `contextMenuLabels()` helper if newly added).
- **Modified** `tests/smoke/startup-command.smoke.test.ts` — SC3 rewritten to the recycle model.
- **Removed affordances** (selector contract): `data-testid="restart-session"` (sidebar), the context-menu `Restart` item.
