---
phase: 11-terminal-area-polish-live-start-restart
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/confirm-copy.ts
  - src/renderer/__tests__/confirm-copy.test.ts
  - src/renderer/__tests__/session-status.recycle.test.ts
  - tests/ui-lab/surfaces.ts
autonomous: true
requirements: [SESS-07, UI-03]
user_setup: []

must_haves:
  truths:
    - "The Remove/Delete confirm body is produced by a PURE, exported, electron-free builder that escalates when agentState is working or waiting (the D-04 branch is unit-testable in isolation, no jsdom)"
    - "A unit test proves a non-active error row's recycle path resolves to the IdleCard Retry / dormant-Start affordance — closing O-1 edge A1 before the restart UI is deleted"
    - "The ui-lab surface registry has a `terminal-card` (framed live card) and an `agent-busy-confirm` (escalated modal) surface so D-03 + D-04 have packaged-capture evidence slots"
  artifacts:
    - path: "src/renderer/confirm-copy.ts"
      provides: "Pure buildConfirmBody({ removeMode, configured, isRunning, agentState }) → escalation-prefixed body string (D-04)"
      exports: ["buildConfirmBody"]
      min_lines: 25
    - path: "src/renderer/__tests__/confirm-copy.test.ts"
      provides: "Unit truth-table for the 5 confirm-body branches incl. working/waiting escalation prefix"
      contains: "still working"
    - path: "src/renderer/__tests__/session-status.recycle.test.ts"
      provides: "O-1 A1 closure: a non-active error row buckets to a recyclable (Start/Retry) state, never a dead end"
      contains: "resolveRowStatus"
    - path: "tests/ui-lab/surfaces.ts"
      provides: "terminal-card + agent-busy-confirm surface entries"
      contains: "agent-busy-confirm"
  key_links:
    - from: "src/renderer/confirm-copy.ts"
      to: "src/renderer/SessionManager.tsx"
      via: "buildConfirmBody consumed by the ConfirmModal body prop in Plan 01"
      pattern: "buildConfirmBody"
    - from: "tests/ui-lab/surfaces.ts"
      to: "tests/ui-lab/ (ui:shots:fresh)"
      via: "new Surface entries scored vs DESIGN-RUBRIC at the phase gate"
      pattern: "terminal-card"
---

<objective>
Wave-0 foundation: extract the agent-aware confirm-copy logic into a PURE testable
module, lock the O-1 A1 edge (a non-active error row is never dead-ended) with a unit
truth-table, and register the two ui-lab evidence surfaces — all BEFORE any user-visible
deletion or CSS change. This is the interface-first wave: it defines the `buildConfirmBody`
contract that Plan 01 wires into `SessionManager`, and the evidence slots Plans 01/02 fill.

No restart UI is deleted here and no card is framed here — this plan only creates the pure
seam + tests + surface registry so the behavior-changing plans (01, 02) have a green,
test-covered base to build against. The 06.1 "passed while broken" lesson is the reason the
D-04 copy branch becomes a pure unit-tested function rather than an inline ternary.

Purpose: testable seam (D-04) + O-1 A1 closure + ui-lab evidence slots (D-03/D-04).
Output: `src/renderer/confirm-copy.ts` (+ test), a session-status recycle unit test,
two new ui-lab surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-CONTEXT.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-RESEARCH.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-PATTERNS.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the pure agent-aware confirm-body builder (buildConfirmBody)</name>
  <files>src/renderer/confirm-copy.ts, src/renderer/__tests__/confirm-copy.test.ts</files>
  <read_first>
    - src/renderer/SessionManager.tsx lines 698-717 (the CURRENT inline ConfirmModal body ternary — the exact 4 shipped strings to preserve verbatim)
    - src/renderer/SessionManager.tsx lines 60-75 (the AgentState import + the `agentState?: AgentState` row field type)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-PATTERNS.md lines 179-212 (the authored D-04 escalation pattern: escalation PREFIX + idleBody)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-UI-SPEC.md lines 131-153 (the Confirm-modal copy contract table + escalation rules)
    - src/renderer/agent-state.ts (the AgentState type definition — 'working' | 'waiting' | ... so the import is electron-free)
  </read_first>
  <action>
    Create `src/renderer/confirm-copy.ts` exporting a PURE function `buildConfirmBody(args)` where
    args is `{ removeMode: 'remove' | 'delete'; configured: boolean; isRunning: boolean; agentState?: AgentState }`.
    The function reproduces the EXACT 4 shipped idle-body strings byte-for-byte (delete →
    "This permanently deletes the saved session — its recipe is gone for good."; configured-remove
    → "This ends its running process and moves the session to the Inactive List. You can start it
    again later."; ephemeral-running-remove → "This ends its running process and removes the
    session."; else → "This removes the session from the sidebar.") then PREPENDS an escalation
    sentence when `agentState === 'working'` ("Claude is still working in this session — closing it
    now will end the task mid-run. ") or `agentState === 'waiting'` ("This session is waiting for
    your input — closing it now will discard what it's asking for. "). Escalation is a prefix so the
    consequence (Inactive vs permanent delete) is never lost; for any other agentState the body is
    the idle string verbatim. Import `AgentState` as a TYPE-ONLY import from `./agent-state` so the
    module stays electron-free and Vitest-importable in the node env (no jsdom). Module must be
    text-node-safe by construction (returns a plain string; the caller renders it as a React text
    node — NO dangerouslySetInnerHTML anywhere). Do NOT wire it into SessionManager.tsx in this
    plan — Plan 01 does the wiring (interface-first).
    Create `confirm-copy.test.ts` with a truth-table covering all 5 branches: delete-idle,
    configured-remove-idle, ephemeral-running-remove-idle, plain-remove-idle, PLUS the working and
    waiting escalation prefixes asserted on a configured-remove base (assert the body STARTS WITH
    the escalation literal AND CONTAINS the idle consequence sentence). Per D-04 the literal
    "Claude" may be kept or generalized to "The agent" — pick one and assert that exact wording.
  </action>
  <verify>
    <automated>npm run test:unit -- confirm-copy && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/confirm-copy.ts` exports `buildConfirmBody`; `grep -c "from 'electron'" src/renderer/confirm-copy.ts` → 0 (electron-free, Vitest-importable)
    - `grep -c "dangerouslySetInnerHTML" src/renderer/confirm-copy.ts` → 0
    - `confirm-copy.test.ts` asserts the working-escalation body STARTS WITH the escalation sentence AND CONTAINS the resolved idle body; same for waiting
    - `confirm-copy.test.ts` asserts the 4 idle bodies are byte-identical to the strings currently in SessionManager.tsx lines 706-712
    - `npm run test:unit -- confirm-copy` exits 0; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>buildConfirmBody is a pure, electron-free, unit-tested builder with the D-04 escalation branch; not yet wired into SessionManager.</done>
</task>

<task type="auto">
  <name>Task 2: O-1 A1 closure — unit-prove a non-active error row is never dead-ended</name>
  <files>src/renderer/__tests__/session-status.recycle.test.ts</files>
  <read_first>
    - src/renderer/session-status.ts (the WHOLE file — `resolveRowStatus` + `hasRendererIdentity`; the renderer mirror of main's onExit routing)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-RESEARCH.md lines 145-160 (the enumerated state × affordance table — the exact O-1 deliverable) and lines 230-233 (Open Question 1 recommendation)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-CONTEXT.md lines 203-208 (O-1 statement)
    - any existing src/renderer/__tests__/session-status*.test.ts (follow its import + fixture shape)
  </read_first>
  <action>
    Add `session-status.recycle.test.ts` that codifies the O-1 A1 walk-through as assertions so the
    restart-UI deletion in Plan 01 is provably safe. Assert the recyclability of every once-live
    state via `resolveRowStatus` / `hasRendererIdentity`: (a) an IDENTITY row with status
    'exited' or 'error' resolves/presents as `not_started` (→ Inactive List with Start ▶), i.e. it
    is NOT left as a Working-Area dead end after the ↻ is gone; (b) `hasRendererIdentity` correctly
    classifies an identity vs an ephemeral row from the same inputs the main onExit routing uses;
    (c) the single residual edge — a non-active spawn-fail `error` row (the one state the sidebar ↻
    uniquely served) — is recoverable: assert that selecting it (the active path) reaches the
    IdleCard error branch (status === 'error' → activeIsCard true), which renders Retry. Drive this
    against the SAME pure predicates the renderer already uses; do NOT invent a new reducer (RESEARCH
    Don't-Hand-Roll). If `resolveRowStatus` does not by itself expose the activeIsCard mapping,
    assert the `status === 'not_started' || status === 'error'` activeIsCard rule directly as a
    small pure check in the test (mirroring SessionManager's `activeIsCard`). The test is the
    machine-checkable form of the RESEARCH O-1 affordance table — every surviving row has a Start ▶
    or Retry path; none is left with "no way to recycle."
  </action>
  <verify>
    <automated>npm run test:unit -- session-status.recycle</automated>
  </verify>
  <acceptance_criteria>
    - `session-status.recycle.test.ts` asserts an identity exited/error row presents as `not_started` (Inactive-with-Start)
    - The test asserts a non-active spawn-fail `error` row's recycle path (select → activeIsCard → IdleCard Retry) exists
    - `grep -c "from 'electron'" src/renderer/__tests__/session-status.recycle.test.ts` → 0
    - `npm run test:unit -- session-status.recycle` exits 0
    - The test references `resolveRowStatus` (uses the existing single-source predicate, not a new reducer)
  </acceptance_criteria>
  <done>O-1 A1 is closed by an automated assertion: no surviving row is dead-ended after restart-UI removal; the test stays GREEN as the guard Plan 01 relies on.</done>
</task>

<task type="auto">
  <name>Task 3: Register the terminal-card + agent-busy-confirm ui-lab surfaces</name>
  <files>tests/ui-lab/surfaces.ts</files>
  <read_first>
    - tests/ui-lab/surfaces.ts lines 36-47 (the Surface interface shape) + the `terminal-running` (~277) and `idle-card` (~353) and `edit-modal` (~401) entries (copy their prepare/available/cleanup idioms verbatim)
    - tests/ui-lab/surfaces.ts the `sidebar-waiting` entry (~456) — the data-agent DOM-poke seam pattern
    - tests/ui-lab/surfaces.ts the `inactive-recipes` entry (~520) — the Remove → confirm-modal driving idiom (clickByTestId('header-remove') → waitForTestId('confirm-modal'))
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-PATTERNS.md lines 410-489 (the two authored surface entries + the planner-note on the agentState seam)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-UI-SPEC.md lines 212-219 (the Verification Protocol — which surfaces score vs which RUBRIC lines)
  </read_first>
  <action>
    Add two `Surface` entries to `tests/ui-lab/surfaces.ts` following the existing entry shape
    exactly. Surface 1 `terminal-card`: title "Unified framed session card (UI-03, Phase 11)",
    designRefs to DESIGN.md §Aesthetic direction + 11-UI-SPEC §Interaction Contract; `expects` states
    the active terminal reads as ONE framed rounded card on cream --bg (header cap + charcoal body),
    gutter visible, no glyph clipped by a corner, Clear + Remove cluster visible, NO Restart;
    `prepare` activates a running session (reuse the `terminal-running` prepare idiom — click the
    first running session's sidebar row, pause for paint). Surface 2 `agent-busy-confirm`: title
    "Agent-busy Remove confirm modal (D-04, escalated copy)", designRefs to 11-UI-SPEC §Copywriting
    Contract + DESIGN.md §Design tokens; `expects` states the Remove confirm modal for a busy agent
    shows the escalated copy prefix before the normal consequence sentence and the modal chrome is
    the 18px card family; `prepare` drives clickByTestId('header-remove') → waitForTestId
    ('confirm-modal') (the inactive-recipes idiom), with `cleanup` clicking confirm-cancel +
    waitForTestIdGone('confirm-modal'). Add the PLANNER-NOTE comment from PATTERNS line 488 verbatim
    above `agent-busy-confirm`: this surface is a VISUAL confirmation of modal chrome only; the D-04
    copy BRANCH is proven by the Task-1 unit test (the renderer-only agentState seam cannot be set by
    a pure DOM poke). Do NOT change any existing surface entry. These surfaces are wired but their
    captures are scored at the Plan 03 phase gate (after the card + copy land), not run here.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:unit -- surfaces 2>/dev/null; node -e "process.exit(0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id: 'terminal-card'" tests/ui-lab/surfaces.ts` → 1
    - `grep -c "id: 'agent-busy-confirm'" tests/ui-lab/surfaces.ts` → 1
    - Both entries match the `Surface` interface (id/title/designRefs/expects/prepare present); `npx tsc --noEmit` exits 0
    - The PLANNER-NOTE comment about the unit-test-vs-DOM-poke seam is present above agent-busy-confirm: `grep -c "copy BRANCH is proven by" tests/ui-lab/surfaces.ts` → 1 (or equivalent wording)
    - No existing surface id removed: `grep -c "id: 'terminal-running'" tests/ui-lab/surfaces.ts` → 1 AND `grep -c "id: 'idle-card'" tests/ui-lab/surfaces.ts` → 1
  </acceptance_criteria>
  <done>The two evidence surfaces exist in the registry, typecheck, and are ready for the Plan 03 packaged capture; no existing surface disturbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC `window.api`) | The only privileged crossing; FROZEN this phase (no key added/removed) |
| user strings → React render | name / cwd / startupCommand / agent copy rendered as text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering (XSS) | `confirm-copy.ts` body string rendered in ConfirmModal | mitigate | Returns a plain string rendered as a React text node by the caller; module has zero `dangerouslySetInnerHTML`; `{name}` interpolation stays inside the existing template literal in SessionManager (Plan 01) — asserted by the grep gate in Task 1 |
| T-11-02 | Elevation | IPC bridge surface (`EXPECTED_API_KEYS`) | mitigate | This plan adds NO bridge key (pure renderer module + tests + ui-lab registry); `EXPECTED_API_KEYS` stays exactly 20 — `security.guard.test.ts` GREEN unchanged (verified at Plan 03 full-suite) |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero packages added this phase (RESEARCH §Package Legitimacy Audit: not applicable); no install task exists, no audit table needed |
</threat_model>

<verification>
- `npm run test:unit` GREEN (new confirm-copy + session-status.recycle tests pass)
- `npx tsc --noEmit` exits 0
- `eslint .` clean on the new files
- `EXPECTED_API_KEYS` unchanged at 20 (no bridge change in this plan — confirmed by Plan 03's `security.guard.test.ts` GREEN)
</verification>

<success_criteria>
- A pure, electron-free, unit-tested `buildConfirmBody` exists with the D-04 working/waiting escalation branch (the testable seam Plan 01 wires)
- O-1 A1 is closed by an automated assertion (no surviving row dead-ended after restart removal)
- The `terminal-card` + `agent-busy-confirm` ui-lab surfaces are registered and typecheck
- No user-visible behavior change yet (interface-first); the full suite stays GREEN
</success_criteria>

<output>
Create `.planning/phases/11-terminal-area-polish-live-start-restart/11-00-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- **New file** `src/renderer/confirm-copy.ts` — exports `buildConfirmBody(args)` (pure, electron-free, D-04 escalation).
- **New file** `src/renderer/__tests__/confirm-copy.test.ts` — confirm-body truth-table (5 branches + 2 escalations).
- **New file** `src/renderer/__tests__/session-status.recycle.test.ts` — O-1 A1 recycle-path guard.
- **New ui-lab surface keys** in `tests/ui-lab/surfaces.ts`: `terminal-card`, `agent-busy-confirm`.
