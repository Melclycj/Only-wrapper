---
phase: 11-terminal-area-polish-live-start-restart
plan: 02
type: execute
wave: 2
depends_on: ["11-00"]
files_modified:
  - src/renderer/terminal-area.css
  - src/renderer/terminal.css
  - src/renderer/index.tsx
  - src/renderer/IdentityHeader.tsx
autonomous: true
requirements: [UI-03]
user_setup: []

must_haves:
  truths:
    - "The active IdentityHeader cap + the --term-bg terminal viewport render as ONE rounded card (--surface/--line frame, --radius corner family, --shadow-pop lift) floating on the cream --bg with a --space-4 gutter — not an edge-to-edge rectangle (SC1)"
    - "The dormant IdleCard reads as a sibling of the live card (same framing language) so the terminal area always looks intentional whether live, dormant, or empty (SC1/SC2)"
    - "FIDELITY GUARDRAIL (D-03a, BLOCKING): radius/padding/shadow apply to the card WRAPPER only — never to .viewport-stack / .term-mount / .xterm*; the existing ResizeObserver on .term-mount re-fits automatically; no glyph is clipped by a corner; scroll/alt-screen/resize/app-restart-restore smoke stay GREEN"
    - "The live control cluster (Clear + Remove) reads as a designed cluster with Remove in the --color-danger ramp on hover/focus; all new CSS uses var() only (tokens-completeness.test.ts GREEN)"
  artifacts:
    - path: "src/renderer/terminal-area.css"
      provides: "The unified session-card frame: .terminal-card wrapper (surface/line/radius/shadow-pop) + the --space-4 gutter on .terminal-area + IdleCard sibling reframing + the Remove danger ramp"
      contains: ".terminal-card"
      min_lines: 30
    - path: "src/renderer/IdentityHeader.tsx"
      provides: "The header cap structure compatible with being the card's top (no structural testid change; header-remove gains the danger-ramp class hook)"
      contains: "identity-header"
  key_links:
    - from: "src/renderer/terminal-area.css"
      to: "src/renderer/index.tsx"
      via: "import order: fonts → tokens.css → terminal.css → terminal-area.css"
      pattern: "terminal-area.css"
    - from: "src/renderer/terminal-area.css (.terminal-card)"
      to: ".viewport-stack / .term-mount (inset:0)"
      via: "wrapper framing only; ResizeObserver on .term-mount re-fits on the inner-box change (O-2)"
      pattern: "terminal-card"
---

<objective>
Turn the active IdentityHeader + terminal viewport into ONE framed, breathing session card
(UI-03 / D-03) floating on the cream `--bg`, with the dormant IdleCard reading as its sibling
— the headline "not edge-to-edge" move that closes 09-HUMAN-UAT Gap 2 (S1). This is a
container-layer CSS change composed from the LOCKED Phase-9 tokens (no new values): the card
frame goes on a WRAPPER, never on the xterm element, so the BLOCKING fidelity guardrail
(D-03a) holds — the existing `ResizeObserver` on `.term-mount` re-fits automatically against
the new inner box (O-2, RESEARCH-confirmed; do NOT add a manual fit). The live control cluster
(Clear + Remove) is polished with Remove in the danger ramp (D-15 precedent).

Per the CONTEXT Discretion, the terminal-area styles are EXTRACTED into a new
`terminal-area.css` (mirroring the Phase-10 `sidebar.css` extraction) to keep `terminal.css`
under the 800-line file discipline. This plan is renderer-CSS-only + a tiny IdentityHeader
class hook; it touches NO files that Plan 01 touches (parallel-safe in Wave 2). The card
framing claim is STRUCTURAL — it is proven by the Plan 03 packaged `ui:shots:fresh` capture,
not by an injection preview.

Purpose: UI-03 / D-03 (unified card) + D-03a (fidelity guardrail) + the Remove danger ramp.
Output: `terminal-area.css`, the import wiring, the IdentityHeader card-cap hook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-CONTEXT.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-RESEARCH.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-PATTERNS.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-UI-SPEC.md
@src/renderer/tokens.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract terminal-area styles into terminal-area.css and frame the unified card (D-03 / D-03a)</name>
  <files>src/renderer/terminal-area.css, src/renderer/terminal.css, src/renderer/index.tsx</files>
  <read_first>
    - src/renderer/terminal.css lines 32-110 (the .terminal-area / .viewport-stack / .session-view / .term-mount / .xterm* block — the framing target + the NEVER-radius-these elements) AND lines 200-228 (the .modal-dialog card DNA: --radius / --shadow-dialog reference shape) AND lines 650-674 (.idle-card-stage + .idle-card current rules)
    - src/renderer/tokens.css (the FULL token set: --bg, --surface, --line, --line-soft, --radius, --radius-lg, --shadow-pop, --shadow-dialog, --space-1..--space-8, --term-bg)
    - src/renderer/index.tsx (the current CSS import order: @fontsource fonts → tokens.css → terminal.css — add terminal-area.css after terminal.css)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-PATTERNS.md lines 27-150 (the sidebar.css active-card analog + the EXACT card-frame pattern to author + the .idle-card-stage transparent flip + the modal card DNA)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-UI-SPEC.md lines 35-62 (the spacing scale + card-gutter contract --space-4→--space-6), 156-179 (the Interaction Contract: live unified card + radius range + header cap join + IdleCard sibling + the BLOCKING fidelity guardrail)
    - src/renderer/SessionManager.tsx lines 646-697 (the .terminal-area JSX — confirm where the .terminal-card wrapper class can be applied: either add a wrapper div around IdentityHeader + .viewport-stack, OR style .terminal-area directly — planner's call already made: prefer a .terminal-card class on the existing container to avoid a structural re-parent that would force the ResizeObserver re-bind walk)
  </read_first>
  <action>
    Create `src/renderer/terminal-area.css` and MOVE the terminal-area presentation rules
    (.terminal-area, .viewport-stack, .session-view, .session-view[hidden-pane], .session-view.active,
    .term-mount, .xterm* sizing, .idle-card-stage, .idle-card and family, the .identity-header /
    .header-control* / .row-control rules) out of terminal.css into terminal-area.css VERBATIM first
    (pure extraction, zero rendered change), keeping terminal.css under 800 lines. Wire the import in
    index.tsx AFTER terminal.css so cascade order is preserved. THEN author the D-03 card frame on the
    WRAPPER: apply `padding: var(--space-4)` + `background: var(--bg)` to `.terminal-area` (the cream
    gutter the card floats on), and frame the inner container (the existing `.terminal-card`-or-
    `.terminal-area`-child that wraps IdentityHeader + .viewport-stack) with `background: var(--surface)`,
    `border: 1px solid var(--line)`, `border-radius: var(--radius)`, `box-shadow: var(--shadow-pop)`,
    `overflow: hidden` (masks the container corners, NOT terminal cells), and `display:flex;
    flex-direction:column; flex:1 1 auto; min-height:0`. Keep the header cap join as the shipped
    `border-bottom: 1px solid var(--line)` hairline (UI-SPEC default). Reframe `.idle-card-stage`
    background from `--term-bg` to `transparent` so the dormant IdleCard reads as the card sibling on
    the same ground (UI-SPEC D-03 sibling). Add the Remove danger ramp to the header control:
    `.identity-header .row-control-close:hover, .identity-header .row-control-close:focus-visible {
    color: var(--color-danger); background: color-mix(in oklch, var(--color-danger) 8%, transparent);
    border-color: var(--color-danger); }`. ABSOLUTE RULE (D-03a): NO border-radius / padding / margin
    on `.viewport-stack`, `.term-mount`, `.session-view .xterm`, `.xterm-viewport`, or `.xterm-screen`
    — those keep `inset:0` / `width:100%;height:100%` exactly as shipped (no re-fit path; would clip
    glyphs). Add NO manual `fit.fit()` call anywhere — the existing ResizeObserver on `.term-mount`
    re-fits when the new gutter shrinks the inner box (O-2). Use `var()` for every authored value (no
    literal hex/oklch/px-off-scale — tokens-completeness bans literals). The gutter default is
    `--space-4` (16px); leave a code comment that --space-5/--space-6 is the only permitted RUBRIC
    tuning axis.
  </action>
  <verify>
    <automated>npm run test:unit -- tokens-completeness && npx tsc --noEmit && node -e "const fs=require('fs');const t=fs.readFileSync('src/renderer/terminal.css','utf8').split('\n').length;if(t>800){console.error('terminal.css over 800 lines: '+t);process.exit(1)}console.log('terminal.css '+t+' lines')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/renderer/terminal-area.css` exists and contains `.terminal-card` (or the framed `.terminal-area`-child) with `border-radius: var(--radius)` AND `box-shadow: var(--shadow-pop)` AND `background: var(--surface)` AND `border: 1px solid var(--line)`
    - `grep -c "var(--space-4)" src/renderer/terminal-area.css` → 1+ (the gutter)
    - The fidelity guardrail holds: `grep -nE '\.(viewport-stack|term-mount|xterm)[^{]*\{' src/renderer/terminal-area.css` shows NONE of those selectors carry `border-radius` or `padding` — verify by grep that no `border-radius`/`padding` line appears within the `.viewport-stack`/`.term-mount`/`.xterm` rule bodies
    - `grep -c "fit.fit()\|\.fit(" src/renderer/terminal-area.css` → 0 (no manual fit added; CSS file anyway)
    - `index.tsx` imports `terminal-area.css` AFTER `terminal.css`: `grep -n "terminal-area.css\|terminal.css" src/renderer/index.tsx` shows terminal.css line precedes terminal-area.css line
    - `npm run test:unit -- tokens-completeness` exits 0 (every new var() resolves; no banned literal)
    - terminal.css is under 800 lines (the node guard exits 0)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The unified card frame is authored on the wrapper only (fidelity-safe), terminal-area styles extracted into terminal-area.css under the 800-line discipline, IdleCard reframed as the card sibling, Remove danger ramp added, all via var() tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Tune the IdentityHeader cap as the card top + verify the no-regression smoke set</name>
  <files>src/renderer/IdentityHeader.tsx, src/renderer/terminal-area.css</files>
  <read_first>
    - src/renderer/IdentityHeader.tsx (the FULL file — the .identity-header structure, the .row-name / .status-badge / .header-controls cluster, the header-control-clear + row-control-close buttons with their data-testids clear-terminal / header-remove)
    - src/renderer/terminal-area.css (the rules authored in Task 1 — the card cap join + header padding)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-PATTERNS.md lines 275-326 (the header control shapes to carry verbatim + the always-visible cluster rules + the Remove danger ramp)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-UI-SPEC.md lines 65-83 (Typography: header name 14px/700, the single identity weight) + 166-170 (the live control cluster contract: Clear + Remove only, always visible, gap var(--space-1))
    - tests/smoke/header-controls.smoke.test.ts AND tests/smoke/app-restart-restore.smoke.test.ts (the fidelity-critical specs that must stay GREEN — confirm the testids clear-terminal / header-remove / identity-header are untouched)
  </read_first>
  <action>
    Tune `IdentityHeader.tsx` only as the card cap: ensure the header name weight is 700 (move from
    600 if still 600, per UI-SPEC single-identity-weight) — this is a class/token change in
    terminal-area.css `.identity-header .row-name { font-weight: 700; }`, NOT a structural JSX change.
    Keep the header cluster as Clear + Remove ONLY (it already is — IdentityHeader takes onClear +
    onRemove props; NO Restart/Start). Keep ALL data-testids byte-identical: `identity-header`,
    `clear-terminal`, `header-remove` SURVIVE (selector contract). The `header-remove` button already
    carries `row-control row-control-close` classes — confirm the danger-ramp rule authored in Task 1
    targets `.identity-header .row-control-close`. Apply the header cap padding from the --space scale
    (`--space-2` vertical / `--space-3` horizontal, snapping the shipped 8px/14px) in terminal-area.css.
    Do NOT add/remove any control, do NOT change the cluster's right-alignment (`margin-left:auto`),
    do NOT alter focus rings (the 2px var(--color-accent) :focus-visible stays). Then RUN the
    fidelity-critical smoke set to prove D-03a holds: the card framing must NOT regress scroll,
    alt-screen, resize, or app-restart-restore. The card claim itself (visual framing, no clipped row)
    is deferred to the Plan 03 packaged capture — this task proves only that the smoke gates stay
    GREEN after the CSS change.
  </action>
  <verify>
    <automated>npx tsc --noEmit && eslint src/renderer/IdentityHeader.tsx && npm run test:smoke -- --spec tests/smoke/app-restart-restore.smoke.test.ts --spec tests/smoke/header-controls.smoke.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-testid="identity-header"' src/renderer/IdentityHeader.tsx` → 1 AND `grep -c 'data-testid="clear-terminal"' src/renderer/IdentityHeader.tsx` → 1 AND `grep -c 'data-testid="header-remove"' src/renderer/IdentityHeader.tsx` → 1 (selector contract preserved)
    - `grep -c "row-control-close" src/renderer/terminal-area.css` → 1+ (the danger ramp targets the Remove control)
    - `grep -c "font-weight: 700\|font-weight:700" src/renderer/terminal-area.css` → 1+ on the .identity-header .row-name rule
    - `grep -rc "restart\|Restart\|Start" src/renderer/IdentityHeader.tsx` → 0 (no restart/start verb on the live header — Clear + Remove only)
    - `npm run test:smoke -- --spec tests/smoke/app-restart-restore.smoke.test.ts --spec tests/smoke/header-controls.smoke.test.ts` exits 0 (fidelity-critical specs GREEN after the card frame)
    - `npx tsc --noEmit` exits 0; `eslint` clean
  </acceptance_criteria>
  <done>The IdentityHeader reads as the card's top cap with the Clear + Remove cluster (danger-ramp Remove), all testids preserved, and the fidelity-critical smoke specs stay GREEN — D-03a no-regression confirmed at the automated layer (visual proof deferred to Plan 03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC `window.api`) | FROZEN — this plan is CSS + a class hook; no IPC touched |
| user strings → React render | session name in the header cap renders as a React text node (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Elevation | IPC bridge surface (`EXPECTED_API_KEYS`) | mitigate | Pure CSS + class change; no bridge key added/removed; `EXPECTED_API_KEYS` stays 20 (verified Plan 03 full-suite) |
| T-11-04 | Tampering (fidelity) | xterm content path (the Core Value) | mitigate | D-03a: radius/padding ONLY on the card wrapper, never on `.viewport-stack`/`.term-mount`/`.xterm*` (grep gate); existing ResizeObserver re-fits; scroll/alt-screen/resize/app-restart-restore smoke GREEN gate it |
| T-11-01 | Tampering (XSS) | header cap session name | mitigate | Name renders as a React text node (no JSX change to interpolation); no `dangerouslySetInnerHTML` introduced |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero packages added (renderer-only CSS); no install task |
</threat_model>

<verification>
- `npm run test:unit -- tokens-completeness` GREEN (every new var() resolves; no banned literal) + `status-colors.test.ts` GREEN
- terminal.css under 800 lines after the terminal-area.css extraction
- `npm run test:smoke` GREEN incl. app-restart-restore + scroll + alt-screen (D-03a no-regression)
- The card frame is authored on the wrapper only — no radius/padding on `.viewport-stack`/`.term-mount`/`.xterm*` (grep gate)
- `npx tsc --noEmit` exits 0; `eslint` clean on changed files
</verification>

<success_criteria>
- The active IdentityHeader + terminal viewport render as ONE framed rounded card on the cream --bg with a --space-4 gutter (SC1) — structural, proven by the Plan 03 packaged capture
- The dormant IdleCard reads as a sibling of the live card (SC1/SC2)
- D-03a BLOCKING fidelity guardrail holds: wrapper-only framing, automatic ResizeObserver re-fit, no clipped glyph, fidelity smoke GREEN
- The Clear + Remove cluster reads as a designed cluster with Remove in the danger ramp; all CSS via var() tokens
- terminal-area.css extracted; terminal.css under 800 lines
</success_criteria>

<output>
Create `.planning/phases/11-terminal-area-polish-live-start-restart/11-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (this plan)

- **New file** `src/renderer/terminal-area.css` — the extracted terminal-area styles + the D-03 `.terminal-card` frame + the --space-4 gutter on `.terminal-area` + the IdleCard `.idle-card-stage` transparent reframe + the `.identity-header .row-control-close` danger ramp.
- **New CSS classes / rules**: `.terminal-card` (the unified card wrapper frame), the `.identity-header .row-control-close:hover/:focus-visible` danger-ramp rule, the `.identity-header .row-name { font-weight: 700 }` cap-weight rule.
- **Modified** `src/renderer/terminal.css` — terminal-area presentation rules MOVED out (under 800 lines).
- **Modified** `src/renderer/index.tsx` — `terminal-area.css` import added after `terminal.css`.
- **Modified** `src/renderer/IdentityHeader.tsx` — card-cap tuning (no structural/testid change; Clear + Remove cluster intact).
