---
phase: 11-terminal-area-polish-live-start-restart
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - .planning/phases/11-terminal-area-polish-live-start-restart/11-HUMAN-UAT.md
autonomous: false
requirements: [UI-03, SESS-07]
user_setup: []

must_haves:
  truths:
    - "The full suite is GREEN against the PACKAGED app (npm run test: unit + smoke), EXPECTED_API_KEYS is exactly 20 (security.guard GREEN), tokens-completeness + status-colors GREEN, and a grep proves restart-session is absent in the packaged build"
    - "A fresh packaged no-injection ui:shots:fresh capture of terminal-card + idle-card + terminal-running + agent-busy-confirm scores against DESIGN-RUBRIC and shows a framed breathing card with NO clipped last row / corner glyph"
    - "The operator confirms LIVE in the running app: framed session card + native terminal fidelity + Remove→Start recycle + no-restart-anywhere + agent-busy escalation copy — this BLOCKING human-verify is the ONLY thing that flips nyquist_compliant true (human_verify_mode: end-of-phase, never auto-approved)"
  artifacts:
    - path: ".planning/phases/11-terminal-area-polish-live-start-restart/11-HUMAN-UAT.md"
      provides: "The end-of-phase manual checklist (SC1-SC4 + the D-04 escalation + the no-restart-anywhere scan), mirroring 09-HUMAN-UAT.md"
      contains: "SC1"
  key_links:
    - from: ".planning/phases/11-terminal-area-polish-live-start-restart/11-VALIDATION.md"
      to: "the BLOCKING human-verify sign-off"
      via: "nyquist_compliant flips true only on explicit operator approval"
      pattern: "nyquist_compliant"
---

<objective>
The phase gate. Run the full suite against the PACKAGED app, take a fresh no-injection
ui:shots:fresh capture of the framed card surfaces and score them against DESIGN-RUBRIC, then
present the BLOCKING end-of-phase human-verify that confirms the framed session card + native
terminal fidelity + the Remove→Start recycle + no-restart-anywhere + the agent-busy escalation
copy LIVE. Per `human_verify_mode: end-of-phase`, `nyquist_compliant` in 11-VALIDATION.md stays
false until the operator's explicit approval — automated green alone is NOT proof for these
lifecycle-adjacent + visual changes (the 06.1 "passed while broken" and 10-10 "never present a
known-failing app" lessons are the reason this gate exists and is blocking).

This plan adds no production code — Plans 01 + 02 delivered the behavior. It produces the
manual checklist, runs the packaged proof, and gates the phase on the live sign-off (mirroring
the Phase-9 09-03 + the Phase-10 gate-plan pattern).

Purpose: UI-03 + SESS-07 phase gate — packaged proof + the BLOCKING human-verify.
Output: `11-HUMAN-UAT.md`, the packaged capture evidence, the nyquist flip on approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-CONTEXT.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-UI-SPEC.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-VALIDATION.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-01-SUMMARY.md
@.planning/phases/11-terminal-area-polish-live-start-restart/11-02-SUMMARY.md
@.planning/phases/09-design-token-foundation/09-HUMAN-UAT.md
@tests/ui-lab/DESIGN-RUBRIC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite evidence chain + fresh packaged capture scored vs DESIGN-RUBRIC</name>
  <files>.planning/phases/11-terminal-area-polish-live-start-restart/11-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/09-design-token-foundation/09-HUMAN-UAT.md (the end-of-phase checklist format to mirror — SC rows + PASS/FAIL + evidence-path columns)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-VALIDATION.md (the observable-behaviors→evidence map + the manual-only verifications table — the checklist must cover every row)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-UI-SPEC.md lines 212-219 (the Verification Protocol: injection-is-preview / packaged-is-proof; which surfaces score vs which RUBRIC lines)
    - tests/ui-lab/DESIGN-RUBRIC.md (the screenshot-checkable expectations the captures are scored on)
    - artifacts/ui-lab/phase9-baseline/terminal-running.png reference (the BEFORE evidence the after-captures pair against)
    - package.json scripts (test, test:unit, test:smoke, ui:shots:fresh, package)
  </read_first>
  <action>
    Run the packaged evidence chain in order and record the terminal evidence (NEVER present a
    known-failing app to the human — the 10-10 rule): (1) `npm run test:unit` GREEN; (2) `npx tsc
    --noEmit` exits 0; (3) `eslint .` clean (scoped — pre-existing spike .cjs lint debt is out of
    scope, note it if present); (4) `npm run test` (full unit + smoke) GREEN — explicitly confirm
    `security.guard.test.ts` GREEN at EXPECTED_API_KEYS===20, `tokens-completeness.test.ts` +
    `status-colors.test.ts` GREEN, the rewritten `header-controls` + `startup-command` specs GREEN,
    and `app-restart-restore` GREEN; (5) grep the packaged build for the restart-session testid →
    absent (`grep -rc 'restart-session' .vite out 2>/dev/null` or grep the renderer source as the
    proxy — 0); (6) `npm run ui:shots:fresh` (packaged, no-injection — the structural card change is
    INVALID under injection preview) and confirm the `terminal-card`, `idle-card`, `terminal-running`,
    and `agent-busy-confirm` surfaces capture cleanly. Score the captures against DESIGN-RUBRIC:
    the framed card reads as ONE rounded breathing card on cream --bg (not edge-to-edge), the IdleCard
    reads as its sibling, NO last-row / corner-glyph clip (the D-03a fidelity proof), the Clear+Remove
    cluster reads designed, and the agent-busy-confirm modal shows the escalated copy chrome. If ANY
    automated step is RED or any capture shows a clipped row / unframed rectangle, STOP — do NOT
    advance to Task 2; route the failure back (the human gate is never run against a known-failing
    build). Author `11-HUMAN-UAT.md` mirroring 09-HUMAN-UAT.md: one row per success criterion
    (SC1 framed card + cluster, SC2 Working/Inactive clarity, SC3 lifecycle Start/Remove/Clear +
    Remove→Start recycle + no-restart-anywhere, SC4 fidelity-unchanged + EXPECTED_API_KEYS=20 +
    recipe-preserved) PLUS a row for the D-04 agent-busy escalation copy, each with the exact
    operator steps, the expected outcome, the backing automated evidence + capture path, and a
    PASS/FAIL slot.
  </action>
  <verify>
    <automated>npm run test && grep -rc 'restart-session' src/renderer; npm run ui:shots:fresh</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test` exits 0 (full unit + smoke GREEN against the packaged app)
    - `security.guard.test.ts` GREEN with EXPECTED_API_KEYS===20 (in the test:unit output)
    - `tokens-completeness.test.ts` + `status-colors.test.ts` GREEN
    - `grep -rc 'restart-session' src/renderer` → 0 (no restart affordance in the build)
    - `npm run ui:shots:fresh` exits 0 and emits captures for `terminal-card`, `idle-card`, `terminal-running`, `agent-busy-confirm`
    - `11-HUMAN-UAT.md` exists with one row per SC1-SC4 + a D-04 escalation row, each with operator steps + expected outcome + evidence path + PASS/FAIL slot (`grep -c "SC1\|SC2\|SC3\|SC4" 11-HUMAN-UAT.md` → covers all four)
    - The captures show NO clipped last row / corner glyph (D-03a) and a framed (not edge-to-edge) card — recorded in the UAT evidence column
  </acceptance_criteria>
  <done>The full suite is GREEN against the packaged app, the fresh capture scores against DESIGN-RUBRIC with no clipped glyph, and 11-HUMAN-UAT.md is the ready end-of-phase checklist — the app is in a known-GOOD state to present to the human.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: BLOCKING end-of-phase human-verify — framed card + fidelity + recycle + no-restart + agent-busy copy LIVE</name>
  <read_first>
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-HUMAN-UAT.md (the checklist authored in Task 1)
    - .planning/phases/11-terminal-area-polish-live-start-restart/11-VALIDATION.md (the Manual-Only Verifications table)
  </read_first>
  <what-built>
    The terminal area is now a unified, framed session card (UI-03 / D-03): the active session's
    identity header + charcoal terminal render as ONE rounded card breathing on the cream background,
    with the dormant IdleCard reading as its sibling. The session lifecycle is simplified to
    Start / Remove / Clear (SESS-07 / D-01) — every restart control is gone; recycling a session is
    Remove → Inactive List → Start ▶ (a fresh process), no `exit` required. The Remove/Delete confirm
    copy escalates when an agent is busy (D-04). The hidden `ptyRestart` machinery is kept un-surfaced;
    EXPECTED_API_KEYS stays 20; the xterm/PTY fidelity path is untouched. Full suite GREEN against the
    packaged app + a fresh packaged capture scored vs DESIGN-RUBRIC (Task 1).
  </what-built>
  <how-to-verify>
    Run the PACKAGED app (`npm run make`, open the built app — NOT `npm start`, the gate uses the
    packaged build) and walk 11-HUMAN-UAT.md:
    1. SC1 — Glance at the active session: it reads as ONE framed, breathing terminal card (rounded
       corners, soft shadow, a visible gutter to the cream background), NOT an edge-to-edge rectangle.
       The Clear + Remove controls read as one designed cluster (Remove warms to red on hover).
    2. SC1/Core-Value fidelity — Type in the terminal; run `claude --rc` (the canonical 🛋️ scenario),
       `vim`, and a scrolling command. Confirm scroll, ANSI/truecolor, alt-screen enter/exit, and
       resize are all unaffected — NO glyph is clipped by a rounded corner, the last row is fully
       visible. This is the BLOCKING Core-Value check.
    3. SC2 — Confirm the Working Area (the live framed card) vs the Inactive List (dormant entries in
       the sidebar) is obvious at a glance; the dormant IdleCard reads as the live card's sibling.
    4. SC3 — Confirm there is NO restart control anywhere (no ↻ on any sidebar row, no 'Restart' in any
       right-click context menu, none on the header). Recycle a running session: Remove it → it lands
       in the Inactive List → click Start ▶ → it starts fresh. You are never forced to type `exit`.
    5. SC4 — A configured session you Remove → Start keeps its name/icon/recipe (same logical identity).
    6. D-04 — With an agent mid-task (`claude` working or waiting for input), click Remove → confirm
       the modal copy escalates ("…still working… end the task mid-run" / "…waiting for your input…
       discard what it's asking for") before the normal consequence sentence.
    Mark each row PASS/FAIL in 11-HUMAN-UAT.md. If anything regresses terminal fidelity or any restart
    control is still reachable, report it — do not approve.
  </how-to-verify>
  <acceptance_criteria>
    - The operator explicitly approves (or reports specific gaps for gap-closure routing)
    - On approval: 11-HUMAN-UAT.md rows marked PASS; 11-VALIDATION.md `nyquist_compliant` flipped to true
    - This checkpoint is BLOCKING and NEVER auto-approvable (workflow.auto_advance is ignored for the Core-Value fidelity + the end-of-phase human-verify gate)
  </acceptance_criteria>
  <resume-signal>Type "approved" (flips nyquist_compliant true + closes UI-03/SESS-07) or describe the specific gaps for gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC `window.api`) | FROZEN — this plan runs tests + a manual gate; no code change |
| operator sign-off → nyquist gate | the human approval is the only path that flips nyquist_compliant true |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Elevation | IPC bridge surface (`EXPECTED_API_KEYS`) | mitigate | The gate's full suite runs `security.guard.test.ts` and confirms EXPECTED_API_KEYS===20 — a regression to the bridge surface is caught here before sign-off |
| T-11-04 | Tampering (fidelity) | xterm content path (the Core Value) | mitigate | The BLOCKING human-verify includes a live fidelity walk (scroll/ANSI/alt-screen/resize, no clipped glyph) + the packaged smoke set GREEN; nyquist does not flip without it |
| T-11-05 | Repudiation | nyquist sign-off provenance | mitigate | `nyquist_compliant` flips only on the explicit operator "approved"; never auto-approved (human_verify_mode: end-of-phase; auto_advance ignored) |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | Zero packages added this phase; no install task |
</threat_model>

<verification>
- `npm run test` GREEN against the packaged app (unit + smoke), incl. security.guard (EXPECTED_API_KEYS=20), tokens-completeness, status-colors, the rewritten restart smokes, app-restart-restore
- `grep -rc 'restart-session' src/renderer` → 0
- `npm run ui:shots:fresh` packaged captures of terminal-card / idle-card / terminal-running / agent-busy-confirm scored vs DESIGN-RUBRIC — framed card, no clipped glyph
- The BLOCKING operator sign-off flips `nyquist_compliant` true (and only then)
</verification>

<success_criteria>
- Full suite GREEN against the packaged app; EXPECTED_API_KEYS=20; tokens/status guards GREEN; no restart-session testid in the build
- Fresh packaged capture scores against DESIGN-RUBRIC: framed breathing card, IdleCard sibling, no clipped last row / corner glyph (D-03a proof)
- The operator confirms LIVE: framed card + native terminal + Remove→Start recycle + no-restart-anywhere + agent-busy escalation copy
- `nyquist_compliant` flips true ONLY on the explicit approval; UI-03 + SESS-07 close
</success_criteria>

<output>
Create `.planning/phases/11-terminal-area-polish-live-start-restart/11-03-SUMMARY.md` when done.
On operator approval, flip `nyquist_compliant: true` in 11-VALIDATION.md.
</output>

## Artifacts this phase produces (this plan)

- **New file** `.planning/phases/11-terminal-area-polish-live-start-restart/11-HUMAN-UAT.md` — the end-of-phase manual checklist (SC1-SC4 + D-04 escalation + no-restart scan), mirroring 09-HUMAN-UAT.md.
- **Packaged capture evidence** for the `terminal-card`, `idle-card`, `terminal-running`, `agent-busy-confirm` ui-lab surfaces (the D-03/D-04 visual proof).
- **State change** on approval: `nyquist_compliant: true` in `11-VALIDATION.md` (closes UI-03 + SESS-07).
