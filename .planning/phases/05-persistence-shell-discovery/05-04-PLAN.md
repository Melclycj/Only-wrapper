---
phase: 05-persistence-shell-discovery
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - package.json
  - package-lock.json
  - src/renderer/Sidebar.tsx
  - src/renderer/SessionManager.tsx
  - src/renderer/terminal.css
  - tests/smoke/reorder.smoke.test.ts
  - .planning/phases/05-persistence-shell-discovery/05-VALIDATION.md
autonomous: false
nyquist_compliant: true
requirements: [NAV-04]
must_haves:
  truths:
    - "The user can drag a sidebar row to reorder sessions; the new order persists across restarts (NAV-04/SC3/D-08)"
    - "Reordering reindexes order densely 0..n-1 and persists via persistOrder without a save button/spinner (D-13)"
    - "@dnd-kit/core + @dnd-kit/sortable are verified on npm before install ([ASSUMED] gate)"
    - "The collapsed rail reflects the saved order (D-08 discretion)"
    - "All Phase-5 unit + smoke tests are GREEN and 05-VALIDATION.md nyquist_compliant is true"
  artifacts:
    - path: "src/renderer/Sidebar.tsx"
      provides: "dnd-kit SortableContext + useSortable rows, drag handle, persistOrder wiring"
      contains: "useSortable"
    - path: "tests/smoke/reorder.smoke.test.ts"
      provides: "drag-to-reorder persistence smoke (NAV-04)"
      contains: "reorder"
  key_links:
    - from: "src/renderer/Sidebar.tsx"
      to: "src/renderer/session-reorder.ts"
      via: "onDragEnd → reorder(sessions, fromId, toId) (optimistic local)"
      pattern: "reorder"
    - from: "src/renderer/SessionManager.tsx"
      to: "window.api.persistOrder"
      via: "onReorder → persistOrder([{ id, order }])"
      pattern: "persistOrder"
---

<objective>
The drag-to-reorder vertical slice + the phase Nyquist sign-off. After this plan the user can
drag a sidebar row to a new position and that order survives an app restart — the literal
NAV-04/SC3 capability — built on the pure `reorder` reducer (Plan 05-01) and the `persistOrder`
IPC (Plan 05-01) wired through `@dnd-kit/sortable`.

This plan installs the `[ASSUMED]` dnd-kit packages, so it OPENS with a blocking human-verify
checkpoint (verify each on npmjs.com before install) — never auto-approvable. Then:
1. Verify + install `@dnd-kit/core@6.3.1` + `@dnd-kit/sortable@10.0.0` (gated).
2. Wrap the sidebar rows in `SortableContext` + `useSortable`; add a hover drag-handle (⠿),
   a pointer-sensor activation distance (so a plain click still switches and control clicks
   still fire), and the drag-active styling (UI-SPEC §5).
3. On drop, compute the new order via the pure `reorder()` reducer (optimistic local), and call
   `window.api.persistOrder([{ id, order }])` — main validates + persists (debounced, silent).
4. The collapsed rail renders in saved `order` (D-08 discretion).
5. Final phase validation: run the full suite, write the reorder smoke, and flip
   `05-VALIDATION.md` `nyquist_compliant: true`.

Purpose: deliver persistent custom ordering (the sidebar-as-launchpad value-prop) and close the
phase with a green Nyquist sign-off.

Output: dnd-kit installed (gated), sortable sidebar, persistOrder wiring, reorder smoke, sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-persistence-shell-discovery/05-CONTEXT.md
@.planning/phases/05-persistence-shell-discovery/05-RESEARCH.md
@.planning/phases/05-persistence-shell-discovery/05-PATTERNS.md
@.planning/phases/05-persistence-shell-discovery/05-UI-SPEC.md
@.planning/phases/05-persistence-shell-discovery/05-VALIDATION.md
@.planning/phases/05-persistence-shell-discovery/05-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `tests/smoke/reorder.smoke.test.ts` — drag-to-reorder persistence smoke (NAV-04)
- New deps: `@dnd-kit/core@6.3.1`, `@dnd-kit/sortable@10.0.0`
- Consumes (Plan 05-01): `reorder` (session-reorder.ts), `window.api.persistOrder`, PtyManager `setOrder`
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify @dnd-kit packages on npm before install ([ASSUMED] legitimacy gate)</name>
  <files>(none — verification gate, no files changed)</files>
  <what-built>
    Nothing yet — this is the package-legitimacy gate BEFORE the install task. RESEARCH tagged
    `@dnd-kit/core@6.3.1` and `@dnd-kit/sortable@10.0.0` as `[ASSUMED]` (discovered via
    ecosystem/training knowledge, not registry-verified at name level), so per the package
    legitimacy protocol they require a blocking human-verify checkpoint before install. This
    checkpoint is NEVER auto-approvable (workflow.auto_advance is ignored for legitimacy gates).
  </what-built>
  <action>
    STOP — do not install anything yet. Present the npmjs.com verification steps below to the user
    and WAIT for the resume signal. Per the package-legitimacy protocol for `[ASSUMED]` packages,
    no `npm install` of @dnd-kit/* may run until the user approves. This gate is not
    auto-approvable even under yolo/auto_advance.
  </action>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/@dnd-kit/core — confirm it exists, is published by `clauderic`, repo github.com/clauderic/dnd-kit, latest stable at/above 6.3.1, healthy weekly downloads, no obvious typosquat.
    2. Visit https://www.npmjs.com/package/@dnd-kit/sortable — confirm same maintainer/repo, latest stable at/above 10.0.0.
    3. Confirm neither package ships a suspicious `postinstall` script.
    4. (Optional) If the user prefers ZERO new dependencies, the documented fallback (RESEARCH Alternatives / A1) is native HTML5 drag-and-drop — approve "use-native-dnd" instead and Task 2 implements reorder with `draggable`/`onDragOver`/`onDrop` + no dnd-kit install.
  </how-to-verify>
  <verify>
    <automated>MISSING — human-verify legitimacy gate; the dnd-kit install it precedes is automated-verified in Task 2 (npx vitest run src/renderer/__tests__/session-reorder.test.ts)</automated>
  </verify>
  <resume-signal>Type "approved" to install @dnd-kit/core + @dnd-kit/sortable, or "use-native-dnd" to take the zero-dependency HTML5 DnD fallback, or describe concerns.</resume-signal>
  <done>The user has verified @dnd-kit/core + @dnd-kit/sortable on npmjs.com and signaled "approved" (or chosen "use-native-dnd"); Task 2 proceeds accordingly.</done>
</task>

<task type="auto">
  <name>Task 2: Install dnd-kit (gated) + sortable sidebar + persistOrder wiring</name>
  <files>package.json, package-lock.json, src/renderer/Sidebar.tsx, src/renderer/SessionManager.tsx, src/renderer/terminal.css</files>
  <read_first>
    - src/renderer/session-reorder.ts (the pure reorder reducer from Plan 05-01 — call it on drop)
    - src/renderer/Sidebar.tsx (whole file — the rows to wrap in SortableContext; the existing onClick switch L138 + control stopPropagation that must keep working through a pointer-sensor activation distance)
    - src/renderer/SessionManager.tsx (the sessions list + activeId; add the onReorder handler that calls persistOrder)
    - src/shared/api-types.ts (persistOrder signature — Plan 05-01)
    - .planning/phases/05-persistence-shell-discovery/05-UI-SPEC.md §5 "drag-to-reorder" (drag handle ⠿, pointer-sensor activation distance, drag-active styling, collapsed-rail reflects order)
    - .planning/phases/05-persistence-shell-discovery/05-RESEARCH.md Pattern 5 (dnd-kit sortable: useSortable + SortableContext + arrayMove) + Pitfall 6 (dense reindex)
    - .planning/phases/05-persistence-shell-discovery/05-PATTERNS.md §"src/renderer/Sidebar.tsx" assignment
  </read_first>
  <action>
    If Task 1 resolved "approved": install `npm install @dnd-kit/core@6.3.1 @dnd-kit/sortable@10.0.0`. If it resolved "use-native-dnd": skip the install and implement reorder with native HTML5 DnD (`draggable`, `onDragStart/onDragOver/onDrop`) instead, still calling the pure `reorder()` reducer + `persistOrder`.

    dnd-kit path: wrap the `.sidebar-row` list in `<DndContext>` + `<SortableContext>` (vertical list strategy). Convert each row to use `useSortable({ id: s.logicalId })`, applying its `transform`/`transition`/`setNodeRef`. Add a pointer sensor with an activation constraint (distance ~5px) so a plain click still fires the existing `onSelect` switch and the control buttons' `stopPropagation` still works (UI-SPEC §5 — do NOT make the whole row a drag handle without an activation distance, or clicks break). Add a left-edge drag-handle glyph (⠿) shown on hover in --ink-faint, `cursor: grab`→`grabbing`. On `onDragEnd`, if there is a real move, call the pure `reorder(sessions, activeId-of-drag, over-id)` to get the densely-reindexed list (optimistic local update via the SessionManager's setSessions), then call `window.api.persistOrder(next.map(s => ({ id: s.logicalId, order: s.order })))`. Persistence is silent — NO save button, spinner, or toast (D-13).

    Add the drag-active styling to terminal.css per UI-SPEC §5: lifted row `opacity: 0.9`, blue focus-ring outline `oklch(0.62 0.14 248)`, soft shadow. The collapsed rail must render rows in saved `order` (it already maps `sessions` which Plan 05-03 sorts by order — confirm the order is preserved when collapsed; full collapsed-rail DnD is optional per D-08).

    Wire `onReorder` from Sidebar up to SessionManager (new prop) so the persistOrder call lives in the container alongside the other window.api calls.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/__tests__/session-reorder.test.ts && grep -c "persistOrder" src/renderer/SessionManager.tsx</automated>
  </verify>
  <acceptance_criteria>
    - If approved: `package.json` contains `@dnd-kit/core` and `@dnd-kit/sortable`; if use-native-dnd: neither is added and Sidebar uses `draggable`/`onDrop`
    - `src/renderer/Sidebar.tsx` reorder path calls the pure `reorder(` reducer from session-reorder.ts (not an ad-hoc inline sort)
    - `src/renderer/SessionManager.tsx` calls `window.api.persistOrder(` with `{ id, order }` entries on reorder
    - A plain row click still switches sessions and control-button clicks still fire (pointer-sensor activation distance present — grep for the activation constraint, or for native-dnd the onClick path is untouched)
    - No save button / spinner / toast added for reorder (D-13 silent persistence)
    - `npx vitest run src/renderer/__tests__/session-reorder.test.ts` exits 0
  </acceptance_criteria>
  <done>Dragging a sidebar row reorders sessions via the pure reducer and persists the dense order through persistOrder; clicks/controls still work; persistence is silent.</done>
</task>

<task type="auto">
  <name>Task 3: Reorder persistence smoke + full-suite run + 05-VALIDATION.md nyquist sign-off</name>
  <files>tests/smoke/reorder.smoke.test.ts, .planning/phases/05-persistence-shell-discovery/05-VALIDATION.md</files>
  <read_first>
    - tests/smoke/persistence.smoke.test.ts (the persistence smoke analog from Plans 05-02/03 — same launch/assert style)
    - tests/smoke/multi-session-keepalive.smoke.test.ts (analog for multi-row assertions in the built app)
    - wdio.conf.ts (spec glob + appBinaryPath)
    - src/renderer/session-reorder.ts (the contract the smoke validates end-to-end)
    - .planning/phases/05-persistence-shell-discovery/05-VALIDATION.md (the map to finalize — Per-Task Verification Map, Wave 0 Requirements, Sign-Off; flip nyquist_compliant: true)
    - .planning/phases/05-persistence-shell-discovery/05-RESEARCH.md Validation Architecture (the full Phase Requirements → Test Map + Sampling Rate already transcribed into 05-VALIDATION.md)
  </read_first>
  <action>
    Write `tests/smoke/reorder.smoke.test.ts` (WDIO/Mocha): launch the built app, create 3 sessions, drag the third above the first (drive the dnd via the DOM/sendInputEvent as the existing smokes drive interactions; if WDIO cannot reliably drive HTML5/dnd-kit pointer DnD, assert the persisted `order` after calling the reorder path through `browser.electron.execute` or via the window.api.persistOrder bridge, and mark the pure drag-gesture as a `// MANUAL:` phase-gate step). Assert the store file's `sessions` array reflects the new dense order after the reorder.

    Finalize `05-VALIDATION.md` (the Per-Task Verification Map was filled at planning time):
    - Confirm the Per-Task Verification Map row for every task in Plans 05-01..05-04 matches the actual `<verify>` commands shipped.
    - Mark each map row's Status as the run result (✅ green).
    - Validation Sign-Off: check every box; the frontmatter already has `nyquist_compliant: true` + `wave_0_complete: true` set here — confirm they are true; set Approval to today's date.

    Run the FULL suite (`npm test`) and confirm green BEFORE flipping the sign-off date.
  </action>
  <verify>
    <automated>npm test && grep -c "nyquist_compliant: true" .planning/phases/05-persistence-shell-discovery/05-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `tests/smoke/reorder.smoke.test.ts` exists, is matched by the wdio spec glob, and asserts the persisted `order` reflects a reorder
    - `05-VALIDATION.md` Per-Task Verification Map has a row for every task in Plans 05-01..05-04 with its exact automated command + threat ref
    - `05-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`
    - No 3 consecutive tasks in the map lack an automated verify (continuity holds)
    - `npm test` exits 0 (full unit + smoke suite green)
  </acceptance_criteria>
  <done>Reorder persistence is smoke-proven; 05-VALIDATION.md is fully filled with the per-task map and signed off (nyquist_compliant: true); the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main (persistOrder) | Untrusted reorder payload (`{ id, order }[]`) written to disk |
| npm registry→repo (dnd-kit install) | `[ASSUMED]` packages added to the dependency tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | persistOrder IPC → store | mitigate | `setOrder` validates each entry main-side BEFORE write: `id` is a known LogicalId AND `order` is `Number.isFinite` (Shared Pattern B, wired Plan 05-01, backed by the store in Plan 05-02). A forged reorder payload is a silent no-op. |
| T-05-SC | Tampering | npm install (@dnd-kit/core, @dnd-kit/sortable) | mitigate | `[ASSUMED]` packages — BLOCKING human-verify checkpoint (Task 1) on npmjs.com before install, never auto-approvable. Zero-dep native-DnD fallback offered. slopcheck already [OK] (RESEARCH audit). |
| T-05-08 | Tampering | dense reindex collision (Pitfall 6) | accept | The pure `reorder` reducer reindexes densely 0..n-1 so no two records share an `order`; unit-tested (session-reorder.test.ts). Low severity — a transient collision only mis-sorts the sidebar, never corrupts identity. |
</threat_model>

<verification>
- `npx vitest run src/renderer/__tests__/session-reorder.test.ts` — dense reindex GREEN.
- `npm test` — full unit + smoke suite GREEN.
- Phase-gate manual: drag a row, restart the app, confirm the new order persists (canonical launchpad scenario).
</verification>

<success_criteria>
- Dragging a sidebar row reorders sessions and the order persists across restarts (NAV-04/SC3).
- The dnd-kit install was gated behind a blocking human-verify checkpoint (or the native fallback taken).
- Reorder persistence is silent (no save UI) and dense-reindexed (D-13/Pitfall 6).
- 05-VALIDATION.md is fully filled and nyquist_compliant: true; the full suite is green.
</success_criteria>

<output>
Create `.planning/phases/05-persistence-shell-discovery/05-04-SUMMARY.md` when done.
</output>
