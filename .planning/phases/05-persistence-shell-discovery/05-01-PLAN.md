---
phase: 05-persistence-shell-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.main.config.ts
  - forge.config.ts
  - src/main/store-schema.ts
  - src/main/shell-discovery.ts
  - src/main/window-bounds.ts
  - src/renderer/session-reorder.ts
  - src/shared/api-types.ts
  - src/main/window-config.ts
  - src/preload/index.ts
  - src/main/pty-manager.ts
  - src/main/__tests__/store-schema.test.ts
  - src/main/__tests__/session-store.test.ts
  - src/main/__tests__/shell-discovery.test.ts
  - src/main/__tests__/window-bounds.test.ts
  - src/renderer/__tests__/session-reorder.test.ts
autonomous: true
nyquist_compliant: true
requirements: [PERS-01, PERS-02, NAV-04]
must_haves:
  truths:
    - "lowdb@7.0.1 is installed and marked external so the main bundle keeps a real runtime import()"
    - "coerceOnLoad forces every restored record to not_started and clears ptyPid (D-01/SC2)"
    - "buildShellList always includes the resolved $SHELL, filters to on-disk, de-dupes (D-05/D-06)"
    - "reorder reindexes order densely 0..n-1 with no gaps or collisions (NAV-04/Pitfall 6)"
    - "the contextBridge exposes exactly 18 keys (15 + discoverShells/persistOrder/persistUiState); no raw ipcRenderer"
  artifacts:
    - path: "src/main/store-schema.ts"
      provides: "StoreSchema type, SCHEMA_VERSION, coerceOnLoad pure helper"
      contains: "export function coerceOnLoad"
    - path: "src/main/shell-discovery.ts"
      provides: "ShellDiscovery seam, parseEtcShells, buildShellList, MacShellProvider, WindowsShellProvider stub, selectShellProvider"
      contains: "export function buildShellList"
    - path: "src/main/window-bounds.ts"
      provides: "validateBounds pure helper (off-screen rejection)"
      contains: "export function validateBounds"
    - path: "src/renderer/session-reorder.ts"
      provides: "pure reorder reducer (arrayMove + dense reindex)"
      contains: "export function reorder"
    - path: "src/main/window-config.ts"
      provides: "EXPECTED_API_KEYS grown to 18"
      contains: "discoverShells"
  key_links:
    - from: "src/preload/index.ts"
      to: "src/main/window-config.ts EXPECTED_API_KEYS"
      via: "security.guard.test.ts exact-set assertion"
      pattern: "discoverShells"
    - from: "src/shared/api-types.ts"
      to: "src/preload/index.ts"
      via: "ElectronAPI type implemented by the api object"
      pattern: "persistOrder"
---

<objective>
Foundation slice for Phase 5 persistence + shell discovery. This plan lays the contracts
the later three slices build against, with NO behavior visible to the user yet — it is the
interface-first wave.

Three things ship here:
1. **Wave 0 RED test stubs** — five new Vitest files (store-schema, session-store,
   shell-discovery, window-bounds, session-reorder) so every later task has an automated
   `<automated>` verify target. The `security.guard.test.ts` goes RED automatically the
   moment EXPECTED_API_KEYS grows (no edit to that file).
2. **Four pure, electron-free modules** — `store-schema.ts` (coerceOnLoad, D-01),
   `shell-discovery.ts` (parse/build/provider seam, D-05/06/07), `window-bounds.ts`
   (validateBounds, D-12/Pitfall 5), and `session-reorder.ts` (dense reindex, NAV-04/Pitfall 6).
   These are the unit-testable cores the Wave-2/3/4 wrappers delegate to.
3. **The atomic 3-key contextBridge lockstep + lowdb install + external-bundling config** —
   `discoverShells`/`persistOrder`/`persistUiState` added to all five lockstep edit points in
   ONE task, plus `lowdb@7.0.1` installed and marked `external` in vite.main.config.ts and
   kept in forge.config.ts's ignore allow-list (Pitfall 1 / Pitfall 2).

Purpose: Define every type, channel, and pure helper so Wave 2-4 implement against fixed
contracts with no scavenger hunt. lowdb is `[VERIFIED: npm registry]` (CLAUDE.md-locked) so
its install needs NO human-verify checkpoint; the `[ASSUMED]` dnd-kit packages are NOT
installed here — they are gated behind checkpoints in Plan 05-04 (reorder slice).

Output: 4 pure modules, 5 RED test files, an 18-key bridge, lowdb installed + externalized.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-persistence-shell-discovery/05-CONTEXT.md
@.planning/phases/05-persistence-shell-discovery/05-RESEARCH.md
@.planning/phases/05-persistence-shell-discovery/05-PATTERNS.md
@.planning/phases/05-persistence-shell-discovery/05-VALIDATION.md
</context>

<artifacts_this_phase_produces>
These symbols are NEWLY CREATED by Phase 5 — do not flag them as drift/unknown references:

**New files (this plan):**
- `src/main/store-schema.ts` — exports: `SCHEMA_VERSION`, `StoreSchema` (interface), `coerceOnLoad`
- `src/main/shell-discovery.ts` — exports: `DiscoveredShell` (interface), `ShellDiscovery` (interface), `parseEtcShells`, `buildShellList`, `MacShellProvider`, `WindowsShellProvider`, `selectShellProvider`
- `src/main/window-bounds.ts` — exports: `WindowBounds` (interface), `validateBounds`, `DEFAULT_BOUNDS`
- `src/renderer/session-reorder.ts` — exports: `reorder`
- `src/main/__tests__/store-schema.test.ts`, `src/main/__tests__/session-store.test.ts`, `src/main/__tests__/shell-discovery.test.ts`, `src/main/__tests__/window-bounds.test.ts`, `src/renderer/__tests__/session-reorder.test.ts`

**New API keys / IPC channels (this plan):**
- `discoverShells` → channel `shell:discover` (invoke)
- `persistOrder` → channel `store:persist-order` (send)
- `persistUiState` → channel `store:persist-ui` (send)

**New symbols produced by later Phase 5 plans (do not flag):**
- `SessionStore` (class) — Plan 05-02 (`src/main/session-store.ts`)
- `PtyManager.hydrate`, `PtyManager.setOrder` — Plan 05-02
- `IdleCard` — Plan 05-03 (`src/renderer/IdleCard.tsx`)
- `WelcomeEmptyState` — Plan 05-03 (`src/renderer/WelcomeEmptyState.tsx`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED test stubs + four pure electron-free modules</name>
  <files>src/main/store-schema.ts, src/main/shell-discovery.ts, src/main/window-bounds.ts, src/renderer/session-reorder.ts, src/main/__tests__/store-schema.test.ts, src/main/__tests__/session-store.test.ts, src/main/__tests__/shell-discovery.test.ts, src/main/__tests__/window-bounds.test.ts, src/renderer/__tests__/session-reorder.test.ts</files>
  <read_first>
    - src/main/shell-resolver.ts (the EXACT analog for the electron-free pure-module header + resolveShell() reuse — Shared Pattern A)
    - src/main/__tests__/shell-resolver.test.ts (the EXACT Vitest analog: mutate-and-restore, describe/it/expect)
    - src/main/window-config.ts (electron-free pure-const module header convention)
    - src/shared/types.ts L31-95 (SessionRecord + SessionStatus + SessionIconSpec — DO NOT reshape)
    - .planning/phases/05-persistence-shell-discovery/05-RESEARCH.md Pattern 1 (store-schema verbatim target) + Pattern 2 (shell-discovery verbatim target)
    - .planning/phases/05-persistence-shell-discovery/05-PATTERNS.md §"Pattern Assignments (new files)" + §"New Tests (Wave 0)"
    - vitest.config.ts (include globs — renderer test MUST live under src/renderer/__tests__/ to be picked up)
  </read_first>
  <behavior>
    - store-schema: coerceOnLoad(rec) returns a record with status === 'not_started' and ptyPid === undefined, every OTHER field (logicalId, name, icon, cwd, shell, startupCommand, order, lastActive) unchanged. A persisted status of 'running' and a persisted ptyPid=1234 both vanish.
    - shell-discovery: parseEtcShells strips '#'-comments and blank lines, trims. buildShellList puts resolvedShell FIRST, filters to existsFn===true, de-dupes by path, labels each by basename. If /etc/shells is empty, the result still contains the resolved $SHELL (D-05 safety). selectShellProvider('win32') → WindowsShellProvider (non-empty, never throws); selectShellProvider('darwin') → MacShellProvider.
    - window-bounds: validateBounds(saved, displays) returns saved when it intersects a display work-area; returns DEFAULT_BOUNDS (centered/sane) when saved is off-screen, partially-checked: a bounds rect on a now-disconnected monitor is rejected (Pitfall 5).
    - session-reorder: reorder(sessions, fromId, toId) returns a new array with the row moved (arrayMove semantics) and every order reindexed densely 0..n-1 — no gaps, no duplicate order values (Pitfall 6).
  </behavior>
  <action>
    Create the four pure modules and their five colocated Vitest files. Write the tests FIRST so they go RED, then implement until GREEN.

    `store-schema.ts`: copy the electron-free header from shell-resolver.ts L1-6. Export `SCHEMA_VERSION = 1 as const`, the `StoreSchema` interface (`version: number; sessions: SessionRecord[]; ui: { collapsed?: boolean; bounds?: { x: number; y: number; width: number; height: number } }`), and the pure `coerceOnLoad(rec: SessionRecord): SessionRecord` per RESEARCH Pattern 1 (spread the record, override `status: 'not_started'`, `ptyPid: undefined`). Import `SessionRecord` type-only from `../shared/types`.

    `shell-discovery.ts`: copy the seam header + Windows-deferred comment from shell-resolver.ts L16-30. Export `DiscoveredShell` (`{ path: string; label: string }`), `ShellDiscovery` (`{ discover(): DiscoveredShell[] }`), the pure `parseEtcShells(contents: string): string[]`, the pure `buildShellList(etcShellPaths, resolvedShell, existsFn): DiscoveredShell[]` (resolvedShell first, on-disk filter, de-dupe, basename label — RESEARCH Pattern 2 verbatim), `MacShellProvider` (reads `/etc/shells` via fs.readFileSync in try/catch, falls back to `$SHELL`-only, calls resolveShell().shell — import resolveShell from './shell-resolver', do NOT recompute), `WindowsShellProvider` (stub returning resolveShell().shell as a single entry so it is never empty — D-05 holds cross-platform; the real PowerShell/CMD/Git Bash/WSL enumeration is Phase 8 per D-07), and `selectShellProvider(platform: NodeJS.Platform)`.

    `window-bounds.ts`: model on pty-manager.ts clampDimension L69-73 (pure validate with documented edges). Export `WindowBounds` (`{ x: number; y: number; width: number; height: number }`), `DEFAULT_BOUNDS` (width 1200, height 800, x/y centered or 0), and pure `validateBounds(saved: WindowBounds | undefined, displays: { workArea: WindowBounds }[]): WindowBounds`. Accept `displays` as an argument (electron-free) so Vitest mocks `screen.getAllDisplays()`. A saved rect whose top-left does not fall within any display work-area → DEFAULT_BOUNDS.

    `session-reorder.ts`: copy the pure-reducer shape from src/renderer/session-close.ts (React/xterm-free). Export `reorder(sessions: SessionRecord[], fromId: LogicalId, toId: LogicalId): SessionRecord[]` — find indices, arrayMove (implement inline, no dep yet — dnd-kit's arrayMove is added in 05-04), then `.map((s, i) => ({ ...s, order: i }))` for dense 0..n-1 reindex. Import types type-only from `../shared/types`.

    Tests: mirror shell-resolver.test.ts structure. store-schema.test.ts asserts the not_started/ptyPid coercion + field preservation. shell-discovery.test.ts injects a fixture `/etc/shells` string + an `existsFn`, asserts $SHELL-always-present + on-disk filter + de-dupe + the win32/darwin provider selection. window-bounds.test.ts passes mock displays arrays. session-reorder.test.ts asserts move + dense order (no gaps after a middle move). Do NOT create session-store.test.ts content beyond a RED stub here IF the SessionStore module does not exist yet — instead write session-store.test.ts as a stub that imports `../session-store` and `it.todo`/expects the module, OR (preferred) write the full round-trip/corrupt/debounce expectations now and let them stay RED until Plan 05-02 implements SessionStore. Mark the file header `// RED until Plan 05-02 implements src/main/session-store.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/main/__tests__/store-schema.test.ts src/main/__tests__/shell-discovery.test.ts src/main/__tests__/window-bounds.test.ts src/renderer/__tests__/session-reorder.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/store-schema.ts` contains `export function coerceOnLoad` and `export const SCHEMA_VERSION`
    - `src/main/shell-discovery.ts` contains `export function buildShellList`, `export function parseEtcShells`, `export class MacShellProvider`, `export class WindowsShellProvider`, `export function selectShellProvider`
    - `src/main/window-bounds.ts` contains `export function validateBounds`
    - `src/renderer/session-reorder.ts` contains `export function reorder`
    - `npx vitest run src/main/__tests__/store-schema.test.ts src/main/__tests__/shell-discovery.test.ts src/main/__tests__/window-bounds.test.ts src/renderer/__tests__/session-reorder.test.ts` exits 0
    - `src/main/__tests__/session-store.test.ts` exists (RED stub referencing `../session-store`, header notes it stays RED until Plan 05-02)
    - No module under src/main/store-schema.ts, shell-discovery.ts, window-bounds.ts, src/renderer/session-reorder.ts imports `'electron'` (grep `grep -L "from 'electron'"` confirms electron-free)
  </acceptance_criteria>
  <done>Four pure modules implemented + GREEN under their unit tests; session-store.test.ts present as a RED stub for Plan 05-02; all four pure modules are electron-free.</done>
</task>

<task type="auto">
  <name>Task 2: Install lowdb + mark external + atomic 3-key contextBridge lockstep</name>
  <files>package.json, package-lock.json, vite.main.config.ts, forge.config.ts, src/shared/api-types.ts, src/main/window-config.ts, src/preload/index.ts, src/main/pty-manager.ts</files>
  <read_first>
    - src/main/window-config.ts L62-78 (EXPECTED_API_KEYS — the exact 15-key array to grow to 18)
    - src/shared/api-types.ts L69-136 (ElectronAPI — listSessions invoke shape + ptyUpdateProfile send shape to mirror)
    - src/preload/index.ts L35-151 (the api object — listSessions L105 invoke + ptyUpdateProfile L128 send to mirror)
    - src/main/pty-manager.ts L30-48 (PTY_CHANNELS), L487-550 (registerIpc), L557-571 (unregisterIpc), L439-458 (updateProfile validate-in-main discipline to mirror for persistOrder)
    - src/shared/__tests__/security.guard.test.ts (asserts Object.keys(exposed).sort() === EXPECTED_API_KEYS.sort() — goes RED at 15→18, GREEN when preload matches)
    - vite.main.config.ts (external array — add 'lowdb' next to 'node-pty')
    - forge.config.ts L23-35 (the ignore allow-list — must keep node_modules/lowdb + node_modules/steno; Pitfall 2)
    - .planning/phases/05-persistence-shell-discovery/05-PATTERNS.md §C "contextBridge 5-point lockstep" + §B "Validate-in-main"
    - .planning/phases/05-persistence-shell-discovery/05-RESEARCH.md Pattern 6 (the 3 new keys + their channels + shapes) + Pitfall 1/Pitfall 2 (external + ignore allow-list)
  </read_first>
  <action>
    First install lowdb (it is `[VERIFIED: npm registry]`, CLAUDE.md-locked, slopcheck OK — NO human-verify checkpoint needed): `npm install lowdb@7.0.1`. Confirm steno@^4.0.2 came along transitively. Do NOT install @dnd-kit/* or electron-window-state here — those are `[ASSUMED]` and are gated behind checkpoints in Plan 05-04.

    Mark lowdb external in `vite.main.config.ts`: add `'lowdb'` to `rollupOptions.external` so the dynamic `import('lowdb')` in Plan 05-02 resolves the real ESM package at runtime instead of being down-leveled (Pitfall 1). Mirror exactly how `'node-pty'` is treated.

    Update `forge.config.ts` `packagerConfig.ignore` to KEEP `/node_modules/lowdb` and `/node_modules/steno` (and the path segments leading to them) alongside the existing node-pty keep-clause (Pitfall 2 — otherwise the packaged app throws MODULE_NOT_FOUND lowdb at Phase 8). Mirror the existing node-pty `if (file === '/node_modules/node-pty' || file.startsWith('/node_modules/node-pty/'))` block for both lowdb and steno.

    Add the THREE new bridge keys in ONE atomic change across all five lockstep edit points (mirroring the documented 04-01 expansion):
    1. `src/shared/api-types.ts` — add to `ElectronAPI`: `discoverShells: () => Promise<DiscoveredShell[]>` (mirror listSessions invoke; import `DiscoveredShell` type-only from `../main/shell-discovery`), `persistOrder: (orders: { id: LogicalId; order: number }[]) => void` (mirror ptyUpdateProfile send), `persistUiState: (ui: { collapsed?: boolean; bounds?: { x: number; y: number; width: number; height: number } }) => void` (mirror ptyUpdateProfile send).
    2. `src/main/window-config.ts` — append `'discoverShells'`, `'persistOrder'`, `'persistUiState'` to `EXPECTED_API_KEYS` (now 18). Add a reviewed-expansion comment block in the same style as the 04-01 block, noting the security V5 validate-in-main requirement for persistOrder/persistUiState (threat T-05-01).
    3. `src/preload/index.ts` — add the three impls: `discoverShells: () => ipcRenderer.invoke('shell:discover')`, `persistOrder: (orders) => { ipcRenderer.send('store:persist-order', orders); }`, `persistUiState: (ui) => { ipcRenderer.send('store:persist-ui', ui); }`.
    4. `src/main/pty-manager.ts` — add channel constants `discover: 'shell:discover'`, `persistOrder: 'store:persist-order'`, `persistUi: 'store:persist-ui'` to PTY_CHANNELS. Register handlers in registerIpc inside the idempotency guard, with SYMMETRIC teardown in unregisterIpc. For now `discover` returns `selectShellProvider(process.platform).discover()` (import from './shell-discovery'); `persistOrder`/`persistUi` call NEW methods `this.setOrder(orders)` / `this.setUiState(ui)` — add minimal validate-in-main stubs (per Shared Pattern B: `setOrder` type-guards each entry's `id` is a known LogicalId AND `order` is `Number.isFinite`; `setUiState` type-guards each of x/y/width/height is finite, collapsed is boolean) that mutate in-memory state. Wiring these to the actual store is Plan 05-02 — keep them as guarded no-op-safe setters so the lockstep + the channel surface is complete now.
    5. `src/shared/__tests__/security.guard.test.ts` — NO code change (it reads EXPECTED_API_KEYS); it goes GREEN once preload exposes exactly the 18-key set.
  </action>
  <verify>
    <automated>npx vitest run src/shared/__tests__/security.guard.test.ts && grep -c "lowdb" vite.main.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains `"lowdb": "7.0.1"`; `node_modules/steno` exists
    - `vite.main.config.ts` `rollupOptions.external` includes `'lowdb'` (grep `grep "lowdb" vite.main.config.ts` non-empty)
    - `forge.config.ts` ignore allow-list keeps `node_modules/lowdb` and `node_modules/steno` (grep both names present)
    - `src/main/window-config.ts` EXPECTED_API_KEYS contains `discoverShells`, `persistOrder`, `persistUiState` (18 entries total)
    - `src/preload/index.ts` exposes `discoverShells`, `persistOrder`, `persistUiState` and still no raw `ipcRenderer`
    - `src/main/pty-manager.ts` PTY_CHANNELS contains `shell:discover`, `store:persist-order`, `store:persist-ui`; registerIpc and unregisterIpc both reference all three symmetrically
    - `npx vitest run src/shared/__tests__/security.guard.test.ts` exits 0 (the 18-key set matches)
    - `@dnd-kit/core`, `@dnd-kit/sortable`, `electron-window-state` are NOT in package.json (deferred to 05-04)
  </acceptance_criteria>
  <done>lowdb installed + externalized + kept in the packaging allow-list; the bridge is an exact 18-key surface asserted GREEN by the security guard; persistOrder/persistUiState/discoverShells channels are wired with validate-in-main setters ready for Plan 05-02 to back with the store.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer→main (contextBridge) | Untrusted renderer payloads (`persistOrder` order arrays, `persistUiState` bounds/collapse) cross here and will be written to disk |
| disk→main (store file) | A forged/corrupt `just-wrapper-store.json` on disk is untrusted input parsed on load (consumed in Plan 05-02; the schema/coerce contract is defined here) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `persistOrder` / `persistUiState` IPC handlers (pty-manager.ts) | mitigate | Validate-in-main BEFORE any write: `setOrder` guards each `id` is a known LogicalId AND `order` is `Number.isFinite`; `setUiState` guards x/y/width/height finite + collapsed boolean (Shared Pattern B). A forged payload is a silent no-op, never writes arbitrary data. |
| T-05-02 | Tampering | restored record `status`/`ptyPid` (store-schema.coerceOnLoad) | mitigate | `coerceOnLoad` forces `status: 'not_started'` + `ptyPid: undefined` — persisted status/PID are NEVER trusted (D-01/SC2). Unit-tested in store-schema.test.ts. |
| T-05-03 | Tampering / Elevation | persisted/discovered `shell` path | mitigate | `buildShellList` filters to on-disk `/etc/shells` + `$SHELL` entries only (D-06); the dropdown-only field (D-05, no free-text, landed in Plan 05-03) removes the renderer's ability to inject an arbitrary path. Shell only ever spawns through the existing validated `create()` path. |
| T-05-SC | Tampering | npm install (lowdb) | mitigate | lowdb is `[VERIFIED: npm registry]` (CLAUDE.md-locked + slopcheck OK + repo confirmed); no postinstall script → no human-verify checkpoint required. `[ASSUMED]` dnd-kit packages are NOT installed here (gated in Plan 05-04). |
</threat_model>

<verification>
- `npx vitest run` — the four new pure-module tests GREEN; session-store.test.ts RED (expected, Plan 05-02); security.guard.test.ts GREEN at 18 keys.
- `grep` confirms lowdb external in vite.main.config.ts + kept in forge.config.ts ignore.
- No electron import in the four pure modules.
</verification>

<success_criteria>
- Four pure electron-free modules exist and pass their colocated Vitest suites.
- The contextBridge is an exact 18-key surface (security guard GREEN); no raw ipcRenderer.
- lowdb@7.0.1 installed, marked external, and kept in the packaging allow-list.
- session-store.test.ts present as a RED stub establishing the Plan 05-02 contract.
- T-05-01 validate-in-main setters present; T-05-02 coerceOnLoad unit-proven.
</success_criteria>

<output>
Create `.planning/phases/05-persistence-shell-discovery/05-01-SUMMARY.md` when done.
</output>
