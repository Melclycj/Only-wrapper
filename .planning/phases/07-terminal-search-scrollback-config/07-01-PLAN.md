---
phase: 07-terminal-search-scrollback-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/main/switch-keys.ts
  - src/main/store-schema.ts
  - src/main/pty-manager.ts
  - src/main/window-config.ts
  - src/main/index.ts
  - src/preload/index.ts
  - src/shared/api-types.ts
  - src/main/__tests__/switch-keys.test.ts
  - src/main/__tests__/scrollback-clamp.test.ts
  - src/main/__tests__/pty-validation.test.ts
  - src/main/__tests__/session-store.test.ts
  - src/shared/__tests__/security.guard.test.ts
autonomous: true
requirements: [TERM-10, TERM-11]
must_haves:
  truths:
    - "matchSearchKey returns {kind:'search'} for macOS Cmd+F and Windows Ctrl+F, and null for macOS Ctrl+F (no readline theft)"
    - "clampScrollback clamps any input to 1000-50000 with default 5000 and non-finite to 5000"
    - "setUiState validates and clamps a scrollback field; a forged/out-of-range payload clamps or no-ops"
    - "The preload surface exposes exactly 20 keys (getUiState added); the security guard passes against EXPECTED_API_KEYS"
    - "@xterm/addon-search is installed at a 5.x-compatible pinned version requiring no native rebuild"
  artifacts:
    - path: "src/main/switch-keys.ts"
      provides: "{ kind: 'search' } SwitchIntent variant + matchSearchKey(i, platform) pure matcher"
      contains: "kind: 'search'"
    - path: "src/main/store-schema.ts"
      provides: "ui.scrollback?: number on StoreSchema + clampScrollback pure helper"
      contains: "clampScrollback"
    - path: "src/main/window-config.ts"
      provides: "EXPECTED_API_KEYS expanded to 20 with getUiState"
      contains: "getUiState"
  key_links:
    - from: "src/main/index.ts"
      to: "matchSearchKey"
      via: "before-input-event dispatch over 'session:switch'"
      pattern: "matchSearchKey"
    - from: "src/preload/index.ts"
      to: "pty:get-ui-state"
      via: "getUiState invoke"
      pattern: "getUiState"
---

<objective>
Lay the contract foundation for both Phase 7 features as an interface-first Wave 0: install the one new dependency, add the pure search-chord matcher and scrollback clamp, extend the validated persistence path with a scrollback field, expand the contextBridge surface by exactly one new read key (getUiState) through the atomic lockstep, and land every Wave 0 RED test so the two feature slices (Plan 02 search, Plan 03 scrollback) build against frozen contracts.

Purpose: Per the project's interface-first convention (mirrors 04-01/05-01/06-01 foundation waves), all cross-cutting contracts — the SwitchIntent variant, the clamp helper, the setUiState validator extension, and the 19→20 bridge expansion — must exist and be test-pinned before the renderer slices wire them, so neither feature plan invents a contract mid-flight.
Output: An installed @xterm/addon-search, an extended pure matcher + clamp + validator, a 20-key bridge surface with the new getUiState read key, and a green Wave 0 unit suite (matchers, clamp, validators, schema round-trip, security guard at 20 keys).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-terminal-search-scrollback-config/07-CONTEXT.md
@.planning/phases/07-terminal-search-scrollback-config/07-RESEARCH.md
@.planning/phases/07-terminal-search-scrollback-config/07-PATTERNS.md
@.planning/phases/07-terminal-search-scrollback-config/07-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This is the source-of-truth list of NEW symbols Phase 7 creates (read by plan-review-convergence to exclude them from drift verification). Plan 01 creates the contract symbols; Plans 02/03 create the components.

- **`{ kind: 'search' }`** — new `SwitchIntent` union variant (`src/main/switch-keys.ts`).
- **`matchSearchKey(i: KeyInput, platform: NodeJS.Platform): SwitchIntent | null`** — new pure matcher (`src/main/switch-keys.ts`). Note: NOT a change to `matchSwitchKey`; a sibling matcher mirroring `matchClearKey`.
- **`isKeyF(i: KeyInput): boolean`** — new defensive key helper (`src/main/switch-keys.ts`).
- **`clampScrollback(n: unknown): number`** — new pure helper (`src/main/store-schema.ts`).
- **`StoreSchema.ui.scrollback?: number`** — additive schema field (`src/main/store-schema.ts`).
- **`getUiState`** — new validated read bridge key (the 20th key): `() => Promise<{ collapsed?: boolean; bounds?: {...}; scrollback?: number }>` over the `pty:get-ui-state` invoke channel. **EXPECTED_API_KEYS count becomes 20.**
- **`persistUiState` payload widening** — gains `scrollback?: number` (SAME key, wider payload — NOT a new key).
- New test files: `src/main/__tests__/scrollback-clamp.test.ts`; extensions to `switch-keys.test.ts`, `pty-validation.test.ts`, `session-store.test.ts`, `security.guard.test.ts`.

Plans 02/03 add: `SearchBar` / `PreferencesModal` components, a `searchOpenId` state field, a `scrollback` SessionManager state + SessionView prop, the `search-*` / `pref-*` / `open-preferences` data-testids.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install @xterm/addon-search + land the Wave 0 RED unit tests (search-chord, clamp, setUiState scrollback, persist round-trip)</name>
  <read_first>
    - .planning/phases/07-terminal-search-scrollback-config/07-RESEARCH.md (§Standard Stack, §Package Legitimacy Audit, §Validation Architecture — version-verification commands, decorations-gated event, -1 sentinel)
    - .planning/phases/07-terminal-search-scrollback-config/07-VALIDATION.md (§Per-Task Verification Map, §Wave 0 Requirements)
    - .planning/phases/07-terminal-search-scrollback-config/07-PATTERNS.md (§Test File Analogs)
    - src/main/__tests__/switch-keys.test.ts (the existing matcher test to extend)
    - src/main/__tests__/store-schema.test.ts (pure-helper test idiom for the new scrollback-clamp test)
    - src/main/__tests__/pty-validation.test.ts (the setUiState validate-in-main test to extend)
    - src/main/__tests__/session-store.test.ts (the persist/load round-trip test to extend)
    - package.json (deps block — the pinned @xterm/* 5.x line and the postinstall fix-node-pty.cjs hook)
  </read_first>
  <action>
    First verify the resolved version at execute time: run `npm view @xterm/addon-search version` and `npm view @xterm/addon-search@0.16.0 peerDependencies` — confirm `latest` has NOT moved to a 5.6-only line and the peer is `@xterm/xterm@^5.0.0`. Install with an EXACT pin (no caret, matching the project's pinned @xterm/* convention): `npm install @xterm/addon-search@0.16.0` (or 0.15.0 if 0.16.0's peer no longer satisfies 5.5.0 — pick the one whose peer is `@xterm/xterm@^5.0.0`; NEVER the `0.17.0-beta.*` line). It is pure JS — confirm the postinstall does not attempt @electron/rebuild on it.

    Then land the four RED unit-test additions (tests reference symbols Plan 01 Tasks 2-3 create, so they are RED until those tasks land — interface-first, mirrors prior foundation waves):
    - Extend `switch-keys.test.ts`: add a `matchSearchKey(input, platform)` describe block covering: macOS (`'darwin'`) Cmd+F (meta, isKeyF) → `{kind:'search'}`; macOS Ctrl+F (control, isKeyF) → `null` (D-03 readline-forward-char must survive); Windows (`'win32'`) Ctrl+F (control, !meta, isKeyF) → `{kind:'search'}`; non-keyDown → `null`; a non-F key with the primary modifier → `null`; assert it accepts the physical `code: 'KeyF'` as well as `key: 'f'`.
    - Add `scrollback-clamp.test.ts`: cover `clampScrollback(999) === 1000`, `clampScrollback(99999) === 50000`, `clampScrollback(5000) === 5000`, default for `undefined`/`NaN`/`Infinity`/non-number → `5000`, and rounding of a fractional value.
    - Extend `pty-validation.test.ts`: `setUiState({ scrollback: 3000 })` holds 3000; `setUiState({ scrollback: 999 })` clamps to 1000; `setUiState({ scrollback: 'x' })` and a forged non-object leave scrollback unset/unchanged (no throw, T-05-01).
    - Extend `session-store.test.ts`: a persist→load round-trip preserves a valid `ui.scrollback`; a loaded `ui` slot with an absent `scrollback` is tolerated (read-time default applies, no throw — coerceOnLoad untouched for ui).
    Do NOT use watch-mode flags. These tests must run under `npm run test:unit`.
  </action>
  <verify>
    <automated>npm view @xterm/addon-search@0.16.0 peerDependencies | grep -q '\^5.0.0' && node -e "require('@xterm/addon-search')" && echo OK</automated>
    <automated>npm run test:unit 2>&1 | grep -E "matchSearchKey|clampScrollback|scrollback" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains `@xterm/addon-search` pinned to an exact `0.15.0` or `0.16.0` (no caret), and `node -e "require('@xterm/addon-search')"` exits 0 with no native-rebuild error.
    - `npm run test:unit` shows the new `matchSearchKey`, `clampScrollback`, scrollback-setUiState, and scrollback-round-trip tests present and currently RED (referencing not-yet-implemented symbols) — they become GREEN after Tasks 2-3.
    - No `0.17.0-beta.*` version is installed (grep `package.json` confirms a stable `0.1[56].0`).
    - No watch-mode flag appears in any new test or script invocation.
  </acceptance_criteria>
  <done>The addon is installed at a 5.x-compatible exact pin requiring no rebuild, and all four Wave 0 RED unit-test additions exist and run under `npm run test:unit`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add { kind: 'search' } variant + matchSearchKey + clampScrollback + extend the setUiState/store-schema scrollback path (GREEN the pure tests)</name>
  <read_first>
    - src/main/switch-keys.ts (the SwitchIntent union at lines 14-22; matchClearKey at lines 94-102; isKeyK at lines 76-78 — the verbatim templates)
    - src/main/store-schema.ts (StoreSchema.ui at lines 30-37; coerceOnLoad at lines 53-60 — the pure-helper template)
    - src/main/pty-manager.ts (setUiState at lines 1075-1093; getUiState at lines 1095-1102 — the validate-in-main pattern to extend)
    - .planning/phases/07-terminal-search-scrollback-config/07-PATTERNS.md (§switch-keys.ts, §store-schema.ts, §pty-manager.ts — the annotated diffs)
    - src/main/index.ts (lines 97-113 — the before-input-event dispatch block where matchSearchKey is wired as a sibling)
  </read_first>
  <behavior>
    - matchSearchKey on darwin + Cmd+F (meta true, isKeyF) → { kind: 'search' }
    - matchSearchKey on darwin + Ctrl+F (control true, meta false) → null (readline forward-char preserved — D-03)
    - matchSearchKey on win32 + Ctrl+F (control true, meta false) → { kind: 'search' }
    - matchSearchKey on non-keyDown or non-F key → null
    - clampScrollback: 999→1000, 99999→50000, 5000→5000, non-finite/non-number→5000, fractional→rounded
    - setUiState({ scrollback }) holds clampScrollback(scrollback); forged/non-finite scrollback leaves it unchanged
  </behavior>
  <action>
    In `switch-keys.ts`: add `| { kind: 'search' }` to the `SwitchIntent` union (a new variant — D-02, zero new bridge key, rides 'session:switch'). Add `isKeyF(i)` mirroring `isKeyK` (`i.key === 'f' || i.code === 'KeyF'`). Add a NEW exported `matchSearchKey(i: KeyInput, platform: NodeJS.Platform): SwitchIntent | null` — NOT a change to matchSwitchKey. Per Pitfall 2 / D-03 the platform asymmetry is the crux: `if (i.type !== 'keyDown') return null; if (!isKeyF(i)) return null;` then on `platform === 'darwin'` match `i.meta` (Cmd+F) only — macOS `i.control` alone returns null so readline forward-char survives; on `platform === 'win32'` (and other non-darwin) match `i.control && !i.meta` (Ctrl+F). Pass `process.platform` from the caller for testability.

    In `index.ts` `before-input-event` (after the existing matchClearKey block, lines 108-112): add a sibling block `const search = matchSearchKey(key, process.platform); if (search) { event.preventDefault(); win.webContents.send('session:switch', search); }` so the find chord rides the SAME 'session:switch' channel and never reaches xterm/PTY (D-02/D-03). Import matchSearchKey alongside matchSwitchKey/matchClearKey.

    In `store-schema.ts`: add `scrollback?: number` to `StoreSchema.ui` (additive, migration-safe — absent → read-time default; do NOT bump SCHEMA_VERSION). Add an exported pure `clampScrollback(n: unknown): number` mirroring the coerceOnLoad style: non-number or non-finite → 5000 (the DEFAULT, midpoint of 3000/10000 per D-04); else `Math.max(1000, Math.min(50000, Math.round(n)))` (bounds 1000-50000, D-04). No I/O, no electron — Vitest-importable.

    In `pty-manager.ts` `setUiState` (lines 1075-1093): import clampScrollback from store-schema; extend the validator with a scrollback branch — `const { scrollback } = ui as { scrollback?: unknown }; if (typeof scrollback === 'number' && Number.isFinite(scrollback)) { this.uiState.scrollback = clampScrollback(scrollback); }` (T-05-01: a forged/out-of-range payload clamps or no-ops, never writes arbitrary data). Widen the `uiState` field type and `getUiState()`'s return type to include `scrollback?: number`.
  </action>
  <verify>
    <automated>npm run test:unit 2>&1 | grep -E "matchSearchKey|clampScrollback|setUiState|store-schema|pty-validation" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Source: `switch-keys.ts` contains `kind: 'search'` and an exported `matchSearchKey` taking a platform arg; `matchSwitchKey` signature is UNCHANGED.
    - Source: `store-schema.ts` contains `clampScrollback` and `scrollback?: number` on `StoreSchema.ui`; `SCHEMA_VERSION` is still `2`.
    - Source: `index.ts` contains `matchSearchKey(key, process.platform)` inside the `before-input-event` handler.
    - Behavior (test): the `matchSearchKey` darwin-Ctrl+F→null case passes; `clampScrollback(999)===1000`, `clampScrollback(99999)===50000`, non-finite→5000 all pass.
    - `npm run test:unit` exits 0 for the switch-keys, scrollback-clamp, and pty-validation suites (the previously-RED Task 1 pure tests are now GREEN).
  </acceptance_criteria>
  <done>The search-chord variant + matcher, the clampScrollback helper, and the setUiState scrollback validate/clamp path all exist, are wired into before-input-event, and the Task 1 pure unit tests are GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Atomic bridge lockstep — add the getUiState read key (19→20) + widen persistUiState payload; keep security.guard GREEN at 20</name>
  <read_first>
    - src/main/window-config.ts (EXPECTED_API_KEYS at lines 87-107 — the 19-key array; the pickDirectory precedent comment at lines 78-86 for the 18→19 lockstep template)
    - src/preload/index.ts (persistUiState at lines 168-175; pickDirectory at lines 177-185 — the invoke-key precedent for getUiState)
    - src/shared/api-types.ts (persistUiState at lines 179-183; pickDirectory at lines 184-194 — the ElectronAPI surface to extend)
    - src/shared/__tests__/security.guard.test.ts (asserts Object.keys(exposed).sort() === EXPECTED_API_KEYS — must stay GREEN at 20)
    - src/main/pty-manager.ts (getUiState at lines 1095-1102; registerIpc at lines 1131-1203 — where the pty:get-ui-state invoke handler is wired alongside pty:list)
    - src/main/index.ts (syncStore at lines 39-47 — getUiState() is already main-internal; the new key exposes the read to the renderer)
  </read_first>
  <action>
    Resolve RESEARCH Open Question 1 (boot-read channel) by adding ONE new validated READ bridge key — the single allowable 19→20 expansion stated in the security context — following the exact `pickDirectory` 18→19 lockstep. Do NOT widen `listSessions`'s return shape (it returns `SessionRecord[]` and is consumed by many callers/tests). Touch all four lockstep sites atomically:

    1. `api-types.ts`: (a) widen the `persistUiState` payload type to `{ collapsed?: boolean; bounds?: {...}; scrollback?: number }` — SAME key, wider payload, NOT a new key. (b) add the new method `getUiState: () => Promise<{ collapsed?: boolean; bounds?: { x: number; y: number; width: number; height: number }; scrollback?: number }>` — a request-response invoke mirroring `discoverShells`/`pickDirectory`; document it as the 20th key, main-internal UI prefs exposed read-only for the renderer's boot-read of scrollback (and collapse, if useful later).
    2. `window-config.ts`: append `'getUiState'` to `EXPECTED_API_KEYS` (now 20 entries) and update the header comment to note getUiState is the new Phase-7 key (scrollback rides the widened persistUiState — no second key).
    3. `preload/index.ts`: widen the `persistUiState` payload type to include `scrollback?: number`; add `getUiState: () => ipcRenderer.invoke('pty:get-ui-state')` mirroring the pickDirectory invoke shape.
    4. `pty-manager.ts` `registerIpc`: wire `ipcMain.handle('pty:get-ui-state', () => this.getUiState())` alongside the existing `pty:list` handler; ensure `unregisterIpc` removes it symmetrically (mirror how the other handlers are torn down). Add a `getUiState` channel name to the PTY_CHANNELS map if that pattern is used.

    Add a corresponding assertion in `security.guard.test.ts` only if the existing dynamic assertion needs the count documented — the test already asserts `Object.keys(exposed).sort() === EXPECTED_API_KEYS`, so it goes RED→GREEN with NO test code change once the preload + window-config arrays match. Verify the count: the resulting EXPECTED_API_KEYS length is exactly 20.
  </action>
  <verify>
    <automated>node -e "const {EXPECTED_API_KEYS}=require('./src/main/window-config.ts'); " 2>/dev/null; grep -c "'" src/main/window-config.ts >/dev/null; npm run test:unit 2>&1 | grep -E "security.guard|EXPECTED_API_KEYS|preload contextBridge" | tail -5</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -E "api-types|window-config|preload|pty-manager" | head -5; echo "tsc-checked"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `window-config.ts` EXPECTED_API_KEYS array contains `'getUiState'` and has exactly 20 entries (verify by counting the quoted entries between `[` and `]`).
    - Source: `preload/index.ts` exposes `getUiState` via `ipcRenderer.invoke('pty:get-ui-state')` and its `persistUiState` payload type includes `scrollback?: number`; the file contains NO raw `ipcRenderer` leak (the no-raw-ipcRenderer contract holds).
    - Source: `api-types.ts` `ElectronAPI` declares `getUiState` returning a Promise of `{ collapsed?; bounds?; scrollback? }`, and `persistUiState`'s payload includes `scrollback?: number`.
    - Source: `pty-manager.ts` `registerIpc` wires `pty:get-ui-state` → `getUiState()`.
    - Test: `security.guard.test.ts` passes — `Object.keys(exposed).sort()` equals the 20-key `EXPECTED_API_KEYS` (the search chord adds zero; getUiState is the only new key); `expect(exposed).not.toHaveProperty('ipcRenderer')` still passes.
    - `npx tsc --noEmit` reports no errors in the touched files.
  </acceptance_criteria>
  <done>The contextBridge surface is exactly 20 keys with the new `getUiState` read channel wired end-to-end (api-types + window-config + preload + pty-manager registerIpc), persistUiState carries the widened scrollback payload, and the security guard is GREEN at 20.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (IPC) | `persistUiState({ scrollback })` and the new `getUiState` invoke cross the contextBridge; the renderer is treated as untrusted input. |
| main → disk (lowdb) | the validated scrollback is written to the store; only main writes disk (renderer-never-touches-disk). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `setUiState` scrollback field | mitigate | Validate finite-number + `clampScrollback` (1000-50000) IN MAIN before any disk write — a forged/out-of-range `persistUiState` payload clamps or no-ops, never writes arbitrary data (T-05-01 precedent extended). Unit-pinned in pty-validation.test.ts. |
| T-07-02 | Elevation | contextBridge surface | mitigate | The find chord adds ZERO new keys (rides 'session:switch'); the scrollback persist rides the existing `persistUiState` (widened payload, same key). Exactly ONE new validated read key `getUiState` is added (19→20) through the full atomic lockstep (api-types + window-config + preload + registerIpc); `security.guard.test.ts` asserts the exact 20-key surface and that no raw `ipcRenderer` leaks. |
| T-07-03 | Tampering | new dependency supply chain | mitigate | `@xterm/addon-search` is the official scoped `@xterm/*` core-team package (slopcheck `[OK]`, ~379K weekly downloads, no postinstall, official monorepo per RESEARCH §Package Legitimacy Audit). Pinned EXACT to a 5.x-compatible `0.15.0`/`0.16.0`; the `0.17.0-beta.*` 5.6-only line is forbidden. Pure JS — no native build, no `@electron/rebuild`. Version re-verified via `npm view` at install time. |
| T-07-SC | Tampering | npm install | mitigate | The one install (`@xterm/addon-search`) is the audited `[OK]` package; no `[ASSUMED]`/`[SUS]` packages, so no blocking human checkpoint is required (the Package Legitimacy Gate is satisfied upstream in RESEARCH). |
</threat_model>

<verification>
- `npm run test:unit` GREEN: switch-keys (incl. matchSearchKey darwin-Ctrl+F→null), scrollback-clamp, pty-validation (scrollback clamp/no-op), session-store round-trip, and security.guard (20-key surface).
- `npx tsc --noEmit` clean across the touched main/shared/preload files.
- `node -e "require('@xterm/addon-search')"` loads without a native-rebuild error.
</verification>

<success_criteria>
- The SwitchIntent `'search'` variant + `matchSearchKey(i, platform)` exist and are wired into `before-input-event` (chord never reaches the PTY).
- `clampScrollback` and the `ui.scrollback` schema field exist; `setUiState` validates and clamps scrollback in main.
- The contextBridge surface is exactly 20 keys (`getUiState` added; search adds zero; scrollback rides `persistUiState`); `security.guard.test.ts` is GREEN.
- `@xterm/addon-search` is installed at a 5.x-compatible exact pin requiring no rebuild.
- All Wave 0 pure unit tests are GREEN; interactive/renderer behavior is deferred to Plans 02/03.
</success_criteria>

<output>
Create `.planning/phases/07-terminal-search-scrollback-config/07-01-SUMMARY.md` when done.
</output>
