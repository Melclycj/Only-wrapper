---
phase: 07-terminal-search-scrollback-config
plan: 04
type: execute
wave: 4
depends_on: ["07-02", "07-03"]
files_modified:
  - src/renderer/__tests__/search-bar.smoke.test.ts
  - .planning/phases/07-terminal-search-scrollback-config/07-VALIDATION.md
autonomous: false
requirements: [TERM-10, TERM-11]
must_haves:
  truths:
    - "A best-effort smoke spec drives find-chord-opens-bar (or documents why it is manual-only)"
    - "The full automated suite (unit + smoke) is GREEN"
    - "The macOS-first manual checklist is signed off by the user (search bar, N-of-M, Aa, Esc, live scrollback, decrease-trim)"
    - "nyquist_compliant flips to true in 07-VALIDATION.md only after the manual checklist passes"
  artifacts:
    - path: ".planning/phases/07-terminal-search-scrollback-config/07-VALIDATION.md"
      provides: "Signed-off validation strategy with nyquist_compliant: true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "07-VALIDATION.md"
      to: "human verification"
      via: "the manual-only checklist sign-off"
      pattern: "Manual-Only"
---

<objective>
Close Phase 7 with the Nyquist sign-off: add the best-effort smoke coverage for the search bar (or document it manual-only with justification), run the full automated suite, then walk the user through the macOS-first manual checklist for the interactive surfaces that cannot be reliably asserted headless (search bar over a real WebGL buffer, "N of M" count, live scrollback fan-out, decrease-trim). Only after the user approves does `nyquist_compliant` flip to true.

Purpose: 07-VALIDATION.md gates the phase on a macOS-first manual checklist for the WebGL/interactive surface (justified — headless match-count + live-buffer assertions are brittle). This plan is the blocking human-verify that flips the validation flag, mirroring the Phase 6.1 end-of-phase human-verify precedent.
Output: A smoke spec (best-effort), a green full suite, and a signed-off 07-VALIDATION.md with `nyquist_compliant: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-terminal-search-scrollback-config/07-VALIDATION.md
@.planning/phases/07-terminal-search-scrollback-config/07-UI-SPEC.md
@.planning/phases/07-terminal-search-scrollback-config/07-02-SUMMARY.md
@.planning/phases/07-terminal-search-scrollback-config/07-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW symbols this plan creates (for plan-review-convergence drift exclusion):
- `src/renderer/__tests__/search-bar.smoke.test.ts` — a WDIO smoke spec (best-effort) OR a documented manual-only justification stub.
- The `nyquist_compliant: true` + `wave_0_complete: true` frontmatter flip in 07-VALIDATION.md.

No production-source symbols are created — this is a verification/sign-off plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Best-effort search-bar smoke spec + full automated suite green</name>
  <read_first>
    - .planning/phases/07-terminal-search-scrollback-config/07-VALIDATION.md (§Wave 0 Requirements — the smoke spec gate; §Manual-Only Verifications — the justification template)
    - .planning/phases/07-terminal-search-scrollback-config/07-RESEARCH.md (§Validation Architecture — the WDIO xterm driver capabilities + the manual-only justification for headless match-count brittleness)
    - wdio.conf.ts (the smoke harness config — how existing specs boot Electron + reach the renderer)
    - the existing smoke specs in src/renderer/__tests__/ (e.g. keyboard-switch.smoke.test.ts — the xterm driver + sendInputEvent pattern for driving the native before-input-event chord path, since WDIO CDP browser.keys does NOT reach before-input-event per the 04-03 finding)
    - .planning/phases/07-terminal-search-scrollback-config/07-UI-SPEC.md (the search-bar data-testids: search-bar, search-input, search-count, etc.)
  </read_first>
  <action>
    Create `src/renderer/__tests__/search-bar.smoke.test.ts` as a best-effort WDIO spec: boot the app, start a session, drive the find chord via the native path (`webContents.sendInputEvent` — NOT `browser.keys`, which does not reach before-input-event, per the 04-03 keyboard-switch finding), and assert the `search-bar` overlay appears and the `search-input` is focusable. If the existing xterm WDIO driver CANNOT reliably reach the "N of M" count against a WebGL buffer (the documented brittleness), scope the smoke to the drivable assertions (bar opens, input present, Esc closes) and document the count/Aa/navigation as MANUAL-ONLY in the spec header + in 07-VALIDATION.md, with the justification verbatim from the RESEARCH/VALIDATION manual-only rationale. Do NOT use `waitForTimeout`/`sleep` — wait on the overlay selector becoming visible. Do NOT chase a flaky headless match-count assertion — the manual checklist owns it.

    Then run the FULL automated suite (`npm run test`) and confirm GREEN: unit (matchers, both clamps, validators, schema, 20-key security guard) + smoke (the existing 14 specs + the new search-bar smoke). If any pre-existing smoke regressed because of the Plan 02/03 renderer changes, fix the test wiring (not by relaxing assertions). Paste the actual command output into the SUMMARY (never claim green without stdout).
  </action>
  <verify>
    <automated>npm run test:unit 2>&1 | tail -5</automated>
    <automated>npm run test:smoke 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Source: `search-bar.smoke.test.ts` exists and drives the find chord via `sendInputEvent` (NOT `browser.keys`); it asserts the `search-bar` overlay appears; it uses no `waitForTimeout`/`sleep`.
    - Any assertion deferred to manual (count/Aa against WebGL) is documented in the spec header AND in 07-VALIDATION.md with the manual-only justification.
    - `npm run test:unit` exits 0 (20-key security guard GREEN).
    - `npm run test:smoke` exits 0 (all specs incl. the new search-bar smoke; no pre-existing smoke regressed).
    - The SUMMARY records the actual test stdout (not a bare "tests pass" claim).
  </acceptance_criteria>
  <done>The best-effort search-bar smoke spec exists (with any manual-only deferrals justified), and the full unit + smoke suite is GREEN with pasted evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Blocking macOS-first human-verify of the interactive search + scrollback surface</name>
  <action>
    This is a BLOCKING human-verify checkpoint — pause and present the macOS-first manual checklist below to the user. Do NOT auto-approve (workflow.auto_advance is ignored for this gate, mirroring the Phase 6.1 end-of-phase precedent). First confirm Task 1's full automated suite is GREEN, then launch the app and present the 13-item checklist. On the user's explicit "approved", flip `nyquist_compliant: true` and `wave_0_complete: true` in 07-VALIDATION.md and tick its Validation Sign-Off checkboxes. On any failure, capture it as a gap for `/gsd-plan-phase --gaps` and leave the flags false.
  </action>
  <what-built>
    Phase 7 is fully implemented and all automated suites are GREEN (20-key security guard, both clamps, the search-chord matcher incl. macOS-Ctrl+F→null, the persist round-trip, plus the renderer search bar + Preferences modal + live scrollback fan-out). What remains is the interactive surface that 07-VALIDATION.md justifies as macOS-first manual-only (a rendered xterm + WebGL canvas makes "N of M" and live-buffer assertions brittle headless).
  </what-built>
  <how-to-verify>
    Launch the app: `npm start` (or `npm run package` then open out/Just-Wrapper-darwin-arm64/Just-Wrapper.app). Then run this checklist (macOS):

    SEARCH (TERM-10 / SC1 / SC3 / D-01):
    1. In an active session, run a command that fills scrollback (e.g. `ls -la /usr/bin`), then press Cmd+F. CONFIRM: the search bar overlay opens top-right and the input is focused.
    2. Type a query that appears multiple times (e.g. `bin`). CONFIRM: matches highlight (amber wash), the active match is stronger amber, and the count shows "N of M" (e.g. "1 of 12").
    3. Press Enter repeatedly, then Shift+Enter. CONFIRM: the active match cycles forward then backward and the "N of M" index updates; it wraps at the ends.
    4. Toggle "Aa". CONFIRM: case-sensitivity flips and the count/highlights recompute.
    5. Search for something with no matches. CONFIRM: the count reads "No matches" (not red) and there are no highlights.
    6. Press Esc (or click ✕). CONFIRM: the bar closes and highlights clear.
    7. With the bar CLOSED, type into the terminal. CONFIRM: keystrokes reach the shell normally — no swallowed input, no stray chars (SC3 no-interference-when-closed).
    8. macOS fidelity check: inside `claude --rc` or a readline prompt, press Ctrl+F (NOT Cmd+F). CONFIRM: it moves the cursor forward one char (readline forward-char survives — D-03); it does NOT open the search bar.

    SCROLLBACK (TERM-11 / SC2 / D-04 / D-05 / D-06):
    9. Open ≥2 sessions. Click the sidebar gear → Preferences opens showing the current scrollback value.
    10. Change scrollback to a new value (e.g. 2000). CONFIRM: open terminals AND a newly-created session all honor the new value (no restart needed — D-05 live-apply).
    11. Type a value outside 1,000–50,000 (e.g. 99). CONFIRM: it snaps to the nearest bound (1,000) on commit and the guidance hint shows (not a red error).
    12. Fill a terminal past the new cap, then lower the scrollback value. CONFIRM: older rows are trimmed (D-06 — expected, not a bug).
    13. Fully quit and reopen the app, open Preferences. CONFIRM: the scrollback value you set persisted (restored via getUiState).
  </how-to-verify>
  <verify>
    <human-check>All 13 macOS-first checklist items confirmed by the user (search bar open/navigate/count/Aa/dismiss/no-interference/Ctrl+F-readline-survival; scrollback live-apply/clamp/decrease-trim/restart-persist).</human-check>
  </verify>
  <acceptance_criteria>
    - All 13 checklist items confirmed by the user, OR any failure is captured as a gap (to be closed via `/gsd-plan-phase --gaps`).
    - On approval: `nyquist_compliant: true` and `wave_0_complete: true` are set in 07-VALIDATION.md frontmatter, and the Validation Sign-Off checkboxes are ticked.
  </acceptance_criteria>
  <done>The user approved the macOS-first manual checklist and `nyquist_compliant: true` is set in 07-VALIDATION.md, OR failures are captured as gaps and the flag stays false.</done>
  <resume-signal>Type "approved" to flip nyquist_compliant, or describe the failing item(s) for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds no production code, no new IPC surface, and no new dependency — it is verification + sign-off only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | premature nyquist sign-off | mitigate | `nyquist_compliant` flips to true ONLY after the user explicitly approves the macOS-first manual checklist (blocking human-verify, never auto-approvable) — mirrors the Phase 6.1 end-of-phase gate. The automated 20-key security guard + both clamp suites remain the standing regression net. |
| T-07-02 | Elevation | bridge surface | mitigate | No bridge change in this plan; the surface stays at the 20 keys frozen by Plan 01. Re-confirmed by the standing `security.guard.test.ts` in the suite this plan runs. |
</threat_model>

<verification>
- `npm run test` (unit + smoke) GREEN with pasted stdout.
- The 13-item macOS-first manual checklist confirmed by the user.
- 07-VALIDATION.md `nyquist_compliant: true` only after approval.
</verification>

<success_criteria>
- A best-effort search-bar smoke spec exists (manual-only deferrals justified).
- The full automated suite is GREEN (20-key security guard, both clamps, matcher, round-trip, smoke).
- The user signs off the macOS-first manual checklist (search bar, N-of-M, Aa, Esc no-interference, macOS-Ctrl+F readline survival, live scrollback, decrease-trim, restart-persist).
- `nyquist_compliant` flips to true in 07-VALIDATION.md.
</success_criteria>

<output>
Create `.planning/phases/07-terminal-search-scrollback-config/07-04-SUMMARY.md` when done.
</output>
