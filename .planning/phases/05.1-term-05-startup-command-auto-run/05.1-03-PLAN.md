---
phase: 05.1-term-05-startup-command-auto-run
plan: 03
type: execute
wave: 3
depends_on: [05.1-02]
files_modified:
  - src/main/pty-manager.ts
  - src/shared/api-types.ts
  - src/renderer/SessionView.tsx
  - src/main/__tests__/readiness-probe.test.ts
  - tests/smoke/startup-command.smoke.test.ts
  - .planning/phases/05.1-term-05-startup-command-auto-run/05.1-VALIDATION.md
autonomous: false
requirements: [TERM-05]
must_haves:
  truths:
    - "On ready-timeout the buffered shell output is flushed (a usable bare prompt appears) and the command is NOT injected (D-04 / SC4)"
    - "A non-intrusive ready-fail notice surfaces to the renderer over the existing onPtyStatus channel — no new bridge key (D-04)"
    - "Restarting a session re-runs its startup command after the `— restarted HH:MM —` separator (SC3)"
    - "The probe nonce never appears in the rendered scrollback (D-02) — verified by the real cold-spawn E2E"
    - "The saved command stays visible on the IdleCard as the manual-run fallback when the notice fires"
  artifacts:
    - path: "src/main/pty-manager.ts"
      provides: "READINESS_TIMEOUT_MS constant + the timeout-flush-and-notice branch completing the probe state machine"
      contains: "READINESS_TIMEOUT_MS"
    - path: "src/shared/api-types.ts"
      provides: "Optional notice field on PtyStatusPayload (reuses onPtyStatus — zero new bridge keys)"
      contains: "notice"
    - path: "src/renderer/SessionView.tsx"
      provides: "Renders the transient ready-fail notice from the onPtyStatus payload"
      contains: "onPtyStatus"
  key_links:
    - from: "src/main/pty-manager.ts timeout branch"
      to: "renderer (onPtyStatus subscription)"
      via: "this.send(PTY_CHANNELS.status, { id, status, notice })"
      pattern: "PTY_CHANNELS\\.status"
    - from: "src/renderer/SessionView.tsx"
      to: "PtyStatusPayload.notice"
      via: "onPtyStatus payload notice field → transient term/notice render"
      pattern: "notice"
---

<objective>
Complete TERM-05 by adding the failure-handling slice and proving the whole feature on a real cold spawn. Two refinements on the Plan 02 happy-path slice:

1. **Timeout/notice fallback (D-04 / SC4):** add `READINESS_TIMEOUT_MS` (~4000 ms, a named constant mirroring `STOP_GRACE_MS`) and the timeout branch in the `create()` probe hook — on timeout with the probe unsettled, DO NOT inject; FLUSH the buffered real shell output to the renderer (so the bare prompt appears normally — a bare shell beats garbled keystrokes, protecting SC4), restore normal forwarding, and emit a non-intrusive ready-fail notice. The saved command stays on the IdleCard for manual run.
2. **The notice transport (D-04, RESEARCH Open Q1):** REUSE the existing `onPtyStatus` / `pty:status` channel — add an optional `notice` field to `PtyStatusPayload` (a 1-field type extension to `api-types.ts`), and render it transiently in `SessionView`. This adds ZERO bridge keys, so `EXPECTED_API_KEYS` stays at 18 and `security.guard.test.ts` stays GREEN with no lockstep churn — the lighter option the research recommended evaluating first.

Then verify end-to-end: turn the Plan 01 timeout unit assertion GREEN, finish the `startup-command.smoke.test.ts` E2E (real cold-spawn auto-run + lands-in-history + nonce-absent invisibility + restart re-run + timeout fallback), flip `nyquist_compliant: true` in VALIDATION.md, and run a blocking human-verify of the canonical `🛋️ Parlour Claude RC` scenario.

Purpose: close SC3, SC4, and D-04 and prove SC1/D-02 against a real shell. Output: the completed probe state machine, the notice reuse, the GREEN E2E + sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/DESIGN.md
@.planning/phases/05.1-term-05-startup-command-auto-run/05.1-CONTEXT.md
@.planning/phases/05.1-term-05-startup-command-auto-run/05.1-RESEARCH.md
@.planning/phases/05.1-term-05-startup-command-auto-run/05.1-PATTERNS.md
@.planning/phases/05.1-term-05-startup-command-auto-run/05.1-VALIDATION.md
@.planning/phases/05.1-term-05-startup-command-auto-run/05.1-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS PLAN:

- `READINESS_TIMEOUT_MS` (exported named constant in `pty-manager.ts`, ~4000 ms, `STOP_GRACE_MS`-style; tunable from measured cold-spawn latency)
- `PtyStatusPayload.notice?` (optional string field on the EXISTING payload type — reuses the EXISTING `onPtyStatus`/`pty:status` channel; NOT a new bridge key)

NO new bridge key. `EXPECTED_API_KEYS` stays at 18. `security.guard.test.ts` stays GREEN unchanged (the surface is unchanged — the notice rides the already-exposed `onPtyStatus`).
Consumed (introduced earlier): `wireNormalOnData` (Plan 02), `selectReadinessProbe`/`ShellReadinessProbe` (Plan 01).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Timeout-flush-and-notice branch + READINESS_TIMEOUT_MS + onPtyStatus notice reuse</name>
  <read_first>
    - `src/main/pty-manager.ts` lines 61-66 (`STOP_GRACE_MS` — mirror its named-constant style for `READINESS_TIMEOUT_MS`), the Plan-02 probe hook in `create()` (the `// TODO(Plan 03 / D-04)` seam), lines 317-333 (`setStatus` — how status payloads are built + sent over `PTY_CHANNELS.status`), and lines 393-398 (`send()`)
    - `src/shared/api-types.ts` lines 60-71 (`PtyStatusPayload` — extend with an optional `notice` field) and lines 110-111 (`onPtyStatus` signature — unchanged, the notice rides this)
    - `src/shared/__tests__/security.guard.test.ts` lines 40-52 (the `Object.keys(exposed).sort() === EXPECTED_API_KEYS` assertion — confirm it stays GREEN because NO key is added) and `src/main/window-config.ts` lines 76-95 (`EXPECTED_API_KEYS` — confirm it is NOT touched)
    - `src/main/__tests__/readiness-probe.test.ts` (Plan 01 — the RED timeout assertion: `vi.useFakeTimers()` + `vi.advanceTimersByTime(READINESS_TIMEOUT_MS)` → buffer flushed via `send`, command NOT written, notice emitted)
    - `.planning/phases/05.1-term-05-startup-command-auto-run/05.1-RESEARCH.md` lines 142-155 (the timeout-branch illustrative flow), lines 232-235 (Pitfall 5 — bridge drift; reuse `onPtyStatus` to avoid it), lines 309-319 (Open Q1 notice transport + Open Q2 timeout duration)
    - `.planning/phases/05.1-term-05-startup-command-auto-run/05.1-PATTERNS.md` lines 151-160 (bridge lockstep — only if a key is added; we are REUSING instead)
  </read_first>
  <behavior>
    - With `vi.useFakeTimers()`, a non-empty `startupCommand`, and NO matching `_fireData`: advancing `READINESS_TIMEOUT_MS` flushes the buffered bytes via `this.send(PTY_CHANNELS.data, { id, data: buffer })` AND `child.write` is NOT called with the command (D-04 / SC4 — bare shell, never inject).
    - On timeout, a ready-fail notice is emitted over `PTY_CHANNELS.status` with a `notice` field (e.g. a short message like "Startup command didn't auto-run — shell wasn't ready in time") and a `status` that does NOT regress the live lifecycle status (carry the current `running` status; the notice is additive).
    - If the probe matches BEFORE the timeout, the timer is cleared and the notice is never emitted (happy path from Plan 02 unaffected).
    - `READINESS_TIMEOUT_MS` is an exported named constant.
  </behavior>
  <action>
    Complete the probe state machine and wire the notice over the existing channel — no new bridge key.

    1. In `src/main/pty-manager.ts`, add `export const READINESS_TIMEOUT_MS = 4000;` near `STOP_GRACE_MS`, with a comment that it is the bounded readiness wait (D-04 / RESEARCH Open Q2) and is tunable from measured cold-spawn latency.

    2. Fill in the Plan-02 `// TODO(Plan 03 / D-04)` seam in the probe hook: after `child.write(probe.marker)`, start `const timer = setTimeout(() => { if (settled) return; settled = true; offProbe.dispose(); this.send(PTY_CHANNELS.data, { id, data: buffer }); this.wireNormalOnData(id, child); /* emit notice */ }, READINESS_TIMEOUT_MS)`. In the match branch (Plan 02), add `clearTimeout(timer)` before `offProbe.dispose()` so a successful probe cancels the timeout (and never emits the notice). Order the `const timer` declaration so the match-branch closure can reference it (hoist via `let timer` if needed).

    3. Emit the ready-fail notice over the EXISTING `pty:status` channel (RESEARCH Open Q1 — the lighter reuse path; AVOIDS Pitfall 5 bridge drift). Send `this.send(PTY_CHANNELS.status, { id, status: <current status, 'running'>, notice: '<short D-04 message>' })`. Do NOT add a new channel, do NOT touch `EXPECTED_API_KEYS`, do NOT touch the preload. The notice is informational only — it must NOT change the session's lifecycle status (carry `running`).

    4. In `src/shared/api-types.ts`, add an optional `notice?: string` field to `PtyStatusPayload` with a doc comment: a transient, non-lifecycle informational message (D-04 ready-fail) that rides the existing `onPtyStatus` subscription — NOT a new bridge key. Keep `onPtyStatus`'s signature unchanged.

    5. Security V7: the notice message is a FIXED string — it must NOT include the `startupCommand`, the nonce, or any buffered bytes (no secret leakage). Lifecycle logging only; do not log the buffer on flush.
  </action>
  <verify>
    <automated>npm run test:unit 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `READINESS_TIMEOUT_MS` is exported from `pty-manager.ts` (grep present).
    - The Plan 01 timeout unit assertion is now GREEN: advancing `READINESS_TIMEOUT_MS` flushes `buffer` via `send(PTY_CHANNELS.data, …)` and `child.write` is NOT called with the command; a matching probe before timeout clears the timer and emits NO notice.
    - All Plan 02 happy-path assertions remain GREEN; the full `readiness-probe.test.ts` file is GREEN (no RED probe-hook tests remain).
    - `PtyStatusPayload` has an optional `notice?: string`; `EXPECTED_API_KEYS` (window-config.ts) is UNCHANGED at 18 keys; `security.guard.test.ts` is GREEN (no new bridge key — grep confirms no `EXPECTED_API_KEYS` diff and no new preload method).
    - The notice message string contains NO `startupCommand`/nonce/buffer interpolation (grep the timeout branch: the notice is a fixed literal).
    - `npx tsc --noEmit -p tsconfig.json` + `npx eslint src/main/pty-manager.ts src/shared/api-types.ts` pass.
  </acceptance_criteria>
  <done>The probe state machine is complete: ready→inject (Plan 02) and timeout→flush+notice+no-inject (this task). The D-04 notice rides the existing `onPtyStatus` channel with zero bridge-key churn; the full unit suite is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Render the ready-fail notice + finish the cold-spawn E2E smoke + flip nyquist_compliant</name>
  <read_first>
    - `src/renderer/SessionView.tsx` lines 200-225 (the `onPtyExit` passive notice via `term.write('\r\n\x1b[2m[process exited]\x1b[0m\r\n')` and the `onPtyStatus` subscription that writes the `— restarted HH:MM —` separator on a non-first `running`) — render the new `notice` field consistently with these dim/transient writes
    - `src/shared/api-types.ts` (Task 1 — the `PtyStatusPayload.notice` field the renderer reads)
    - `.planning/DESIGN.md` (token system + status language — the non-intrusive notice treatment; prefer a dim inline term write or a small idle-card/header hint per D-04, Claude's discretion)
    - `tests/smoke/startup-command.smoke.test.ts` (Plan 01 scaffold — finish the assertions) and `tests/smoke/helpers/xterm-driver.ts` (`ensureSession`, `sendKeys`, `readBuffer`, `waitForText`, per-id `sendKeysTo`/`readBufferOf`/`waitForTextIn`, `openContextMenu`/`clickMenuItem` for restart) and `tests/smoke/session-edit.smoke.test.ts` (the edit-modal flow to set `startupCommand`)
    - `tests/smoke/pty-roundtrip.smoke.test.ts` (the `\r`/Enter convention + the build-and-run cadence)
    - `.planning/phases/05.1-term-05-startup-command-auto-run/05.1-VALIDATION.md` (the sign-off checklist + `nyquist_compliant` flag to flip)
  </read_first>
  <action>
    1. In `src/renderer/SessionView.tsx`, extend the existing `onPtyStatus(id, (p) => { … })` subscription to render `p.notice` when present: a non-intrusive transient surface (DESIGN tokens) — e.g. a dim inline `term.write` line consistent with the existing `[process exited]` / restart-separator style, OR a small idle-card/header hint (Claude's discretion per D-04). It must NOT replace or suppress the lifecycle status badge — the notice is additive and informational. The saved command remains on the IdleCard as the manual-run fallback (no change to IdleCard's read-only display — D-04).

    2. Finish `tests/smoke/startup-command.smoke.test.ts` so every scaffolded assertion is real and GREEN against the BUILT app:
       - (SC1) Create a session with `startupCommand: echo JW_STARTUP_OK` (via the edit modal), Start it (cold spawn via `ensureSession`/the Start path), `waitForText('JW_STARTUP_OK')`, assert `readBuffer()` contains both the typed command and its output.
       - (D-02) Assert `readBuffer()` does NOT contain `__JW_READY_` (the probe nonce sentinel is invisible).
       - (SC1 history) `browser.keys(['ArrowUp'])` recalls the command on the prompt line.
       - (SC3) Restart the session (per-id helpers / restart control) and assert `JW_STARTUP_OK` appears AGAIN after the `— restarted` separator.
       - (SC2) A session created with NO startup command shows a bare prompt and no injected line.
       - (SC4/D-04) Document + implement (or skip-with-reason if not deterministically forceable in CI) the timeout-fallback assertion: when the probe cannot settle, a usable bare prompt remains and the notice surfaces. Tune `READINESS_TIMEOUT_MS` against the real cold-spawn latency observed during this E2E bring-up (RESEARCH Open Q2) and the discard-vs-flush boundary / `matches` regex against the captured real zsh/bash byte stream (RESEARCH Open Q3) — adjust `readiness-probe.ts`'s matcher if the real bytes require it, keeping the unit tests GREEN.
       - Enter is `browser.keys(['Enter'])` (CR — matches the `\r` injection).

    3. Update `.planning/phases/05.1-term-05-startup-command-auto-run/05.1-VALIDATION.md`: fill the per-task verification map rows with the real task IDs, check the sign-off boxes, and set `nyquist_compliant: true` + `wave_0_complete: true` in the frontmatter once the full suite is GREEN.
  </action>
  <verify>
    <automated>npm run build && npm run test 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - `SessionView.tsx` renders `p.notice` when present (grep `notice` in the `onPtyStatus` handler) without suppressing the status badge.
    - `startup-command.smoke.test.ts` asserts ALL of: `JW_STARTUP_OK` present (SC1), `__JW_READY_` ABSENT (D-02), ArrowUp history recall (SC1), restart re-run after the separator (SC3), bare-prompt no-injection for empty command (SC2); the timeout-fallback case is implemented or skip-documented with a concrete reason.
    - `npm run test` (unit + smoke against the built app) is GREEN.
    - `05.1-VALIDATION.md` has `nyquist_compliant: true` and the sign-off checklist boxes checked.
    - `RESEARCH` Open Q2 (timeout duration) and Open Q3 (matcher vs. real bytes) are resolved with the measured values noted in the SUMMARY; if the matcher regex changed, the `readiness-probe.test.ts` pure assertions are still GREEN.
  </acceptance_criteria>
  <done>The ready-fail notice renders non-intrusively; the real cold-spawn E2E proves auto-run + history + invisibility + restart re-run + (timeout fallback); the full suite is GREEN and VALIDATION.md is signed off.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the canonical Parlour Claude RC scenario (SC1/SC4 native feel)</name>
  <what-built>
    The full TERM-05 auto-run feature: a session with a saved `startupCommand` auto-runs it after the shell is genuinely ready (invisible round-trip probe, no garbled keystrokes), the command lands in shell history, restart re-runs it, and a ready-fail timeout leaves a clean bare shell with a non-intrusive notice. All automated unit + smoke tests are GREEN; this checkpoint confirms the subjective "native feel" that CI cannot assert (per VALIDATION.md Manual-Only Verifications).
  </what-built>
  <how-to-verify>
    1. Run the built app (`npm start` or the packaged build you just produced).
    2. Create a session — Name: `Parlour Claude RC`, Icon: `🛋️`, Path: a REAL project directory, Command: `claude --rc` (or, if `claude` is unavailable, use `echo HELLO && pwd` as a stand-in).
    3. Click ▶ Start.
    4. CONFIRM: after a brief moment the command appears typed at a CLEAN prompt and runs — no stray/garbled characters, no duplicate or blank prompt, no visible probe artifact (no `__JW_READY_…` text anywhere).
    5. CONFIRM: press ↑ (ArrowUp) at the prompt — the startup command is recalled from history (SC1).
    6. CONFIRM: Restart the session — the command runs again after the `— restarted HH:MM —` separator (SC3).
    7. CONFIRM: a session with NO startup command starts as a normal bare shell (SC2).
  </how-to-verify>
  <action>This is a blocking human-verify checkpoint — there is no code to write. Present the <what-built> summary and the numbered <how-to-verify> steps to the user, then PAUSE for the <resume-signal>. Do not proceed to mark the phase complete until the user types "approved" or reports issues to fix.</action>
  <resume-signal>Type "approved" if the auto-run feels native (clean, no garble, command in history, restart re-runs), or describe any flicker / garble / duplicate-prompt / visible-nonce issue to fix.</resume-signal>
  <verify><human-check>The canonical scenario verification steps 4-7 all CONFIRM (clean auto-run, history recall, restart re-run, empty→bare-shell) per the user's "approved" signal.</human-check></verify>
  <done>The user has confirmed the auto-run feels native — clean prompt, no garble/duplicate, command in shell history, restart re-runs, empty command starts a bare shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → renderer (onPtyStatus event) | The ready-fail notice rides the EXISTING `pty:status` channel as an additive optional `notice` field — no new bridge surface, no new untrusted inbound path. |
| main → spawned shell (timeout path) | On timeout NOTHING is written to the PTY (the safe outcome — never inject-anyway); the buffered shell output is flushed read-only to the renderer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-04 | Information Disclosure | the ready-fail notice payload | mitigate | The notice is a FIXED literal string — it interpolates NO `startupCommand`, nonce, or buffered bytes; no secret leaks to the renderer/logs (V7). |
| T-05.1-05 | Spoofing/Tampering | bridge surface drift | mitigate | The notice REUSES the already-reviewed `onPtyStatus`/`pty:status` channel (RESEARCH Open Q1) — `EXPECTED_API_KEYS` stays at 18, `security.guard.test.ts` stays GREEN, so no unreviewed key can leak (Pitfall 5 avoided by design). |
| T-05.1-01 | Tampering/EoP | timeout path | accept (safe-by-default) | On timeout the command is NOT injected — the bare shell is the safe outcome; no command ever runs without confirmed readiness (D-04 / SC4). |
</threat_model>

<verification>
- `npm run test:unit` — the full `readiness-probe.test.ts` (incl. the timeout state machine) is GREEN; hydrate SC5 + lifecycle stay GREEN.
- `npm run test` — unit + `startup-command.smoke.test.ts` (real cold spawn) GREEN: auto-run visible, nonce absent (D-02), history recall (SC1), restart re-run (SC3), empty→bare-shell (SC2).
- `security.guard.test.ts` GREEN with `EXPECTED_API_KEYS` unchanged at 18 (notice reuse — no new key).
- `npx tsc --noEmit` + `npx eslint` on all modified files pass.
- Human-verify: canonical `🛋️ Parlour Claude RC` scenario feels native — no garble, command in history, restart re-runs (SC1/SC4).
- `05.1-VALIDATION.md` `nyquist_compliant: true`.
</verification>

<success_criteria>
- On ready-timeout the buffer flushes to a usable bare prompt and the command is NOT injected (D-04 / SC4).
- A non-intrusive ready-fail notice surfaces over the existing `onPtyStatus` channel — zero new bridge keys (`EXPECTED_API_KEYS` stays 18).
- Restart re-runs the startup command after the `— restarted HH:MM —` separator (SC3).
- The probe nonce never appears in the rendered scrollback (D-02) — proven by the cold-spawn E2E.
- The saved command stays on the IdleCard as the manual-run fallback when the notice fires.
- The canonical scenario passes human-verify; the full suite is GREEN; VALIDATION.md signed off.
</success_criteria>

<output>
Create `.planning/phases/05.1-term-05-startup-command-auto-run/05.1-03-SUMMARY.md` when done.
</output>
