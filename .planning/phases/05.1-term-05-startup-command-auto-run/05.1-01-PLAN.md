---
phase: 05.1-term-05-startup-command-auto-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/readiness-probe.ts
  - src/main/__tests__/readiness-probe.test.ts
  - tests/smoke/startup-command.smoke.test.ts
  - src/main/__tests__/pty-hydrate.test.ts
autonomous: true
requirements: [TERM-05]
must_haves:
  truths:
    - "The ReadinessProbe seam exists as an electron-free, Vitest-importable module mirroring shell-discovery.ts"
    - "buildPosixProbe(nonce) produces a `: <nonce>\\r` no-op marker and a matcher that returns false on the bare echoed input line and true once the nonce appears on a produced output line"
    - "selectReadinessProbe('darwin') returns a MacReadinessProbe; selectReadinessProbe('win32') returns a WindowsReadinessProbe whose forShell() throws (Phase-8 stub)"
    - "The Wave 0 unit + smoke scaffolds exist and fail RED against the not-yet-implemented create() probe hook"
    - "pty-hydrate.test.ts asserts hydrate() spawns no PTY → no auto-run (SC5) and stays GREEN"
  artifacts:
    - path: "src/main/readiness-probe.ts"
      provides: "ShellReadinessProbe + ReadinessProbeProvider interfaces, buildPosixProbe, MacReadinessProbe, WindowsReadinessProbe stub, selectReadinessProbe(platform)"
      contains: "export function selectReadinessProbe"
    - path: "src/main/__tests__/readiness-probe.test.ts"
      provides: "Pure marker-build/match unit tests + FakeChild-driven probe-hook state-machine assertions (probe bytes withheld, inject-on-match with CR, flush-on-timeout no-inject)"
      contains: "buildPosixProbe"
    - path: "tests/smoke/startup-command.smoke.test.ts"
      provides: "Real cold-spawn E2E scaffold: auto-run visible, lands-in-history, nonce-absent invisibility, restart re-run, timeout fallback"
      contains: "ensureSession"
    - path: "src/main/__tests__/pty-hydrate.test.ts"
      provides: "Extended assertion: hydrate spawns no PTY → no startup-command injection (SC5)"
      contains: "not.toHaveBeenCalled"
  key_links:
    - from: "src/main/readiness-probe.ts"
      to: "src/main/pty-manager.ts (create() probe hook, Plan 02)"
      via: "selectReadinessProbe(process.platform).forShell(record.shell)"
      pattern: "selectReadinessProbe"
    - from: "src/main/__tests__/readiness-probe.test.ts"
      to: "src/main/pty-manager.ts FakeChild harness"
      via: "vi.mock('node-pty') + _fireData synthetic marker bytes"
      pattern: "_fireData"
---

<objective>
Establish the Phase 05.1 contracts and the full Wave 0 RED test scaffold for TERM-05 startup-command auto-run, BEFORE any behavior lands. This plan delivers the interface-first foundation the implementation slices (Plans 02, 03) build against:

1. The `ReadinessProbe` seam (`src/main/readiness-probe.ts`) — a pure, electron-free, Vitest-importable module mirroring `src/main/shell-discovery.ts` EXACTLY: a capability interface, a pure `buildPosixProbe(nonce)` helper, an implemented `MacReadinessProbe` (zsh/bash), a `WindowsReadinessProbe` stub that THROWS (Phase-8 — D-03), and a `selectReadinessProbe(platform)` picker.
2. The unit + E2E test scaffolds that encode SC1–SC5 / D-02 / D-04 as failing assertions (RED until Plan 02/03 implement the create() probe hook).
3. The SC5 extension to `pty-hydrate.test.ts` (already GREEN — hydrate spawns no PTY → nothing to inject).

Purpose: Lock the probe contract + readiness/match semantics + the deterministic FakeChild test strategy so the implementation has zero interpretation latitude. This mirrors the established Phase 02–05 Wave 0 RED-stub convention (interface-first, tests RED, then implement).

Output: One new pure seam module, one new unit test file, one new E2E smoke file, and an extended hydrate test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-term-05-startup-command-auto-run/05.1-CONTEXT.md
@.planning/phases/05.1-term-05-startup-command-auto-run/05.1-RESEARCH.md
@.planning/phases/05.1-term-05-startup-command-auto-run/05.1-PATTERNS.md
@.planning/phases/05.1-term-05-startup-command-auto-run/05.1-VALIDATION.md
</context>

<artifacts_this_phase_produces>
New symbols introduced by THIS PLAN (source-grounding drift verification must exclude these as newly-created, not pre-existing):

- `ShellReadinessProbe` (interface — `marker: string`, `matches(buffer: string): boolean`)
- `ReadinessProbeProvider` (interface — `forShell(shellPath: string): ShellReadinessProbe`)
- `buildPosixProbe(nonce: string): ShellReadinessProbe` (pure helper)
- `MacReadinessProbe` (class implementing ReadinessProbeProvider — zsh/bash)
- `WindowsReadinessProbe` (class implementing ReadinessProbeProvider — Phase-8 stub that THROWS)
- `selectReadinessProbe(platform: NodeJS.Platform): ReadinessProbeProvider`
- New test file `src/main/__tests__/readiness-probe.test.ts`
- New E2E smoke file `tests/smoke/startup-command.smoke.test.ts`

NOT introduced here (consumed by later plans): `wireNormalOnData()`, `READINESS_TIMEOUT_MS`, the `PtyStatusPayload.notice` field — those are Plan 02/03 artifacts.
NO new bridge key is introduced this phase (the D-04 notice reuses the existing `onPtyStatus`/`pty:status` channel — see Plan 03).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the ReadinessProbe seam (src/main/readiness-probe.ts)</name>
  <read_first>
    - `src/main/shell-discovery.ts` (THE analog — copy its structure exactly: file-top "MAIN-PROCESS … platform-aware seam (D-XX)" doc comment, interface, pure injected-dependency helper, Mac provider implemented, Windows provider stub, `select…(platform)` picker at the bottom)
    - `.planning/phases/05.1-term-05-startup-command-auto-run/05.1-RESEARCH.md` lines 239-287 (the verbatim probe contract — `ShellReadinessProbe`/`ReadinessProbeProvider`/`buildPosixProbe`/`MacReadinessProbe`/`WindowsReadinessProbe`/`selectReadinessProbe`) and lines 108-120 (the invisibility + send-vs-match rationale)
    - `.planning/phases/05.1-term-05-startup-command-auto-run/05.1-PATTERNS.md` lines 24-54 (seam-shape conventions to carry over) and lines 209-213 (Pitfall 1 — match produced output, not the echoed input)
    - `.planning/phases/05.1-term-05-startup-command-auto-run/05.1-CONTEXT.md` D-01 (one-shot transparent probe, NO persistent rc/env changes) and D-02 (probe invisible) and D-03 (macOS now, Windows Phase-8 stub)
  </read_first>
  <action>
    Create `src/main/readiness-probe.ts` as a pure, electron-free, Vitest-importable module mirroring `shell-discovery.ts`. Per D-01/D-02/D-03 and the RESEARCH contract:

    - File-top doc comment in the `shell-discovery.ts` style stating MAIN-PROCESS only, the platform-aware readiness seam (D-03), pure helpers electron-free/Vitest-importable, and that the probe changes NO shell state (D-01: uses the POSIX `:` no-op builtin + a self-generated nonce; no env vars, no rc edits, no persistent hooks).
    - Export interface `ShellReadinessProbe` with a readonly `marker: string` (bytes to write to the PTY to elicit a detectable round-trip) and `matches(buffer: string): boolean` (true once `buffer` shows the shell PROCESSED the marker — distinct from the bare echoed input).
    - Export interface `ReadinessProbeProvider` with `forShell(shellPath: string): ShellReadinessProbe` (mirrors `ShellDiscovery.discover()`).
    - Export PURE `buildPosixProbe(nonce: string): ShellReadinessProbe` taking the nonce as a parameter (injected for testability, mirroring `buildShellList`'s injected `existsFn`). Marker = `: ${nonce}\r` (the `:` POSIX no-op builtin — the nonce is its argument, nothing executes, nothing persists; D-01 compliant). The CR terminator is `\r` (NOT `\n`) per Pitfall 4 / termios ICRNL. The `matches` matcher must be the SEND-vs-MATCH split (Pitfall 1): return true only when the nonce appears on a line AFTER a newline (the shell re-prompted having processed the line), NOT merely on the echoed-input line. Use a regex of the RESEARCH shape `${nonce}[^\n]*\n[\s\S]*` as the starting matcher; document inline that the exact regex is E2E-tunable (RESEARCH Open Q3) and tuned against real cold zsh/bash byte captures during Plan 03's E2E bring-up.
    - Export class `MacReadinessProbe implements ReadinessProbeProvider`: `forShell(_shellPath)` returns `buildPosixProbe(<unique nonce>)`. Generate the nonce with `crypto.randomBytes(...).toString('hex')` (or `crypto.randomUUID()`) wrapped in a `__JW_READY_<hex>__` sentinel token so the smoke test's "nonce absent in buffer" assertion has a stable prefix to grep for. Add the analog's one-shell-covers-both note (zsh + bash share the POSIX `:` builtin + CR semantics — one probe covers both on macOS).
    - Export class `WindowsReadinessProbe implements ReadinessProbeProvider`: `forShell(_shellPath)` THROWS `new Error('Windows readiness probe is implemented in Phase 8 (D-03 seam stub).')` — per PATTERNS line 40-41 / RESEARCH 277-281 the Windows stub THROWS (there is NO safe no-op readiness probe), unlike `WindowsShellProvider` which returns a default.
    - Export `selectReadinessProbe(platform: NodeJS.Platform): ReadinessProbeProvider` returning `platform === 'win32' ? new WindowsReadinessProbe() : new MacReadinessProbe()` (mirrors `selectShellProvider`).

    Do NOT import from `electron` or `node-pty` (keep it Vitest-importable in the Node env). Do NOT wire this into `pty-manager.ts` yet — Plan 02 consumes it.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx eslint src/main/readiness-probe.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/readiness-probe.ts` exports `ShellReadinessProbe`, `ReadinessProbeProvider`, `buildPosixProbe`, `MacReadinessProbe`, `WindowsReadinessProbe`, `selectReadinessProbe` (grep each symbol present).
    - The module imports NEITHER `electron` NOR `node-pty` (assert via grep: no `from 'electron'` / `from 'node-pty'`).
    - `buildPosixProbe('X').marker` equals `: X\r` exactly (CR terminator, `:` no-op, nonce-as-argument — verified by the Task 2 unit test).
    - `selectReadinessProbe('win32')` returns an instance whose `forShell('/x')` THROWS; `selectReadinessProbe('darwin')` returns a `MacReadinessProbe` (verified by the Task 2 unit test).
    - `npx tsc --noEmit` passes; `npx eslint src/main/readiness-probe.ts` passes (electron-free invariant holds, no `no-require-imports`/no-electron violations).
  </acceptance_criteria>
  <done>The ReadinessProbe seam compiles, lints, is electron/node-pty-free, and matches the RESEARCH contract (interface + pure helper + Mac provider + Windows-throwing stub + platform picker).</done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 RED scaffolds — readiness-probe.test.ts + startup-command.smoke.test.ts + SC5 hydrate extension</name>
  <read_first>
    - `src/main/__tests__/pty-lifecycle.test.ts` lines 40-104 (THE FakeChild node-pty mock harness — `_fireData`/`_fireExit`, `spawnMock`, `vi.mock('node-pty')`, `vi.mock('../shell-resolver')`, `fakeWindow()` with `webContents.send: vi.fn()`) and lines 111-163 (`beforeEach`/`afterEach`, `setPlatform`, the `vi.useFakeTimers()` + `vi.advanceTimersByTime()` grace-timer pattern to copy for the timeout state machine)
    - `tests/smoke/pty-roundtrip.smoke.test.ts` (whole file — the E2E template) and `tests/smoke/helpers/xterm-driver.ts` exports (`ensureSession`, `sendKeys`, `readBuffer`, `waitForText`, plus per-id `sendKeysTo`/`readBufferOf`/`waitForTextIn`, `clickAddSession`, `openContextMenu`, `clickMenuItem`)
    - `tests/smoke/session-edit.smoke.test.ts` (the established way to open the edit modal and SET a profile field including `startupCommand` — the smoke test must set a startup command on a session before asserting auto-run)
    - `src/main/__tests__/pty-hydrate.test.ts` (the existing GREEN hydrate test — extend it, do not rewrite; the `dormantRecord()` factory + `spawnMock` are already there)
    - `.planning/phases/05.1-term-05-startup-command-auto-run/05.1-RESEARCH.md` lines 350-373 (the Req→Test map + deterministic testing notes) and `.planning/phases/05.1-term-05-startup-command-auto-run/05.1-VALIDATION.md` lines 42-65 (per-SC test map + Wave 0 requirements)
    - `src/main/readiness-probe.ts` (from Task 1 — import `buildPosixProbe`/`selectReadinessProbe` for the pure unit assertions)
  </read_first>
  <action>
    Create the three Wave 0 test artifacts. The unit-level probe-hook tests reference symbols Plan 02/03 will add (`wireNormalOnData`, the create() probe gate, `READINESS_TIMEOUT_MS`) so they fail RED now — that is the intended Wave 0 state.

    (a) `src/main/__tests__/readiness-probe.test.ts` — copy the `pty-lifecycle.test.ts` harness (mock `node-pty` with `FakeChild` exposing `_fireData`/`_fireExit`, mock `electron`, mock `../shell-resolver`, `fakeWindow()` with `webContents.send: vi.fn()`, `setPlatform('darwin')` in `beforeEach`, restore + `vi.useRealTimers()` in `afterEach`). Two test groups:
      - PURE helper tests (no FakeChild): `buildPosixProbe('NONCE').marker === ': NONCE\r'`; `matches('NONCE\r')` (bare echoed-input line only) is `false`; `matches('NONCE\nuser@host% ')` (nonce followed by a newline + re-prompt) is `true` (Pitfall 1 send-vs-match split). `selectReadinessProbe('win32').forShell('/x')` THROWS; `selectReadinessProbe('darwin')` is a `MacReadinessProbe` and `forShell('/bin/zsh').marker` starts with `: __JW_READY_` and ends with `\r`.
      - State-machine tests via the FakeChild harness (these drive the create() probe hook Plan 02/03 implements — RED now). Create a session whose record carries a non-empty `startupCommand` (set it via `updateProfile` after a `create()`, or via a hydrated `dormantRecord` promoted by `create({id})`). Assert: (1) bytes fired via `child._fireData(...)` BEFORE the marker round-trips are NOT forwarded — `fakeWindow().webContents.send` is never called with `{ channel: 'pty:data' }` carrying those bytes (D-02 invisibility); (2) on `child._fireData('<nonce>\nprompt% ')` matching the probe, `child.write` is called with an arg `=== startupCommand + '\r'` (SC1 injection, CR terminator); (3) with `vi.useFakeTimers()` + `vi.advanceTimersByTime(READINESS_TIMEOUT_MS)` and NO matching `_fireData`, the buffered bytes ARE flushed via `send(PTY_CHANNELS.data, …)` AND `child.write` was NOT called with the command (D-04 timeout: bare shell, never inject — SC4); (4) an empty/whitespace `startupCommand` → `child.write` is never called with a command and `wireNormalOnData` forwards normally (SC2/TERM-03). Import `READINESS_TIMEOUT_MS` and `PTY_CHANNELS` from `../pty-manager` — these references make the file RED until Plan 02/03 land.
      Add a leading comment block in the `pty-lifecycle.test.ts` style stating these are Wave 0 RED until Plans 02/03 implement the create() probe hook, listing the exact symbols (`wireNormalOnData`, `READINESS_TIMEOUT_MS`, the probe gate) that turn them GREEN.

    (b) `tests/smoke/startup-command.smoke.test.ts` — copy `pty-roundtrip.smoke.test.ts` + use the `xterm-driver` helpers. Scaffold (RED until Plan 02/03 + a build): create a session with a real startup command (open the edit modal the `session-edit.smoke.test.ts` way, set `startupCommand` to a benign visible command e.g. `echo JW_STARTUP_OK`), Start it (cold spawn), then assert via `waitForText`/`readBuffer`: (SC1) the command's OUTPUT (`JW_STARTUP_OK`) appears AND the command text appears typed at the prompt; (D-02) the probe nonce sentinel `__JW_READY_` does NOT appear anywhere in the buffer (`expect(await readBuffer()).not.toContain('__JW_READY_')`); (SC1 history) after the command runs, `browser.keys(['ArrowUp'])` recalls it on the prompt line; (SC3) Restart the session (per-id `sendKeysTo`/`readBufferOf` or the restart control) and assert the command runs AGAIN after the `— restarted` separator; (SC4/D-04) a documented timeout-fallback assertion sketch (a session whose probe cannot settle leaves a usable bare prompt and surfaces the notice). Enter is `browser.keys(['Enter'])` (produces CR, matches the `\r` injection). Mark the spec's leading comment as Wave 0 RED until the create() probe hook + build land.

    (c) Extend `src/main/__tests__/pty-hydrate.test.ts` — add ONE test asserting SC5: `hydrate([dormantRecord({ startupCommand: 'echo NOPE' })])` followed by NO `create({id})` results in `spawnMock` NOT being called AND `child.write` never invoked (a dormant restore spawns no PTY → there is nothing to inject into; SC5 is satisfied structurally — D-06). Reuse the existing `dormantRecord()` factory and `spawnMock`. Keep the rest of the file GREEN.
  </action>
  <verify>
    <automated>npm run test:unit 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/__tests__/readiness-probe.test.ts` exists; its PURE helper assertions PASS (marker shape, send-vs-match `matches`, Windows-throws, Mac picker) and its state-machine assertions FAIL RED (they reference `READINESS_TIMEOUT_MS` / the create() probe gate that Plan 02/03 add) — the run shows the file's RED tests are the probe-hook tests, not the pure-helper tests.
    - `tests/smoke/startup-command.smoke.test.ts` exists and references `ensureSession`/`readBuffer`/`waitForText` and asserts both `JW_STARTUP_OK` presence and `__JW_READY_` absence (grep both literals present in the file).
    - `pty-hydrate.test.ts` has a new SC5 test asserting `spawnMock` not called after hydrate with a `startupCommand`-bearing dormant record, and the file's pre-existing tests still PASS GREEN.
    - `npm run test:unit` runs without a harness/import crash (the only failures are the intended RED probe-hook assertions).
  </acceptance_criteria>
  <done>All three Wave 0 scaffolds exist; pure-helper + SC5-hydrate assertions are GREEN; the probe-hook state-machine + E2E assertions are RED, pinning the exact contract Plans 02/03 must satisfy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main (contextBridge IPC) | The renderer sets `startupCommand` only via the already-validated `pty:update-profile` path; it has NO auto-run write path. No boundary change this plan (pure module + tests only). |
| main → spawned shell (PTY write) | This phase writes a saved string into a live PTY as executable input. Modeled in Plans 02/03; this plan only defines the probe marker (a self-generated nonce, no user data interpolated). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-01 | Tampering/EoP | startupCommand reaching the PTY | accept (same trust boundary) | It is the USER'S OWN saved command run in their OWN shell — mirrors native-terminal trust; no new privilege. It reaches the PTY only via the main-owned record set through the already-validated `updateProfile` path (renderer has no auto-run write path). Enforced in Plan 02. |
| T-05.1-02 | Injection | the probe marker string | mitigate | `buildPosixProbe` uses a FIXED `:` POSIX no-op builtin + a SELF-GENERATED crypto nonce; NO user-controlled data is interpolated into the marker. The nonce is for uniqueness only (V6 n/a — no crypto-strength requirement). Implemented in this plan's Task 1. |
| T-05.1-03 | Information Disclosure | probe nonce / command in logs | mitigate | The `readiness-probe.ts` module performs NO logging; the probe bytes (nonce-bearing) are discarded, never logged (reuses the existing "never log PTY data" rule — V7). No `console.log` of nonce/buffer/startupCommand in this module. |
</threat_model>

<verification>
- `npm run test:unit` — the pure probe helper tests + the SC5 hydrate extension are GREEN; the probe-hook state-machine tests are RED (intended Wave 0 state, turn GREEN in Plan 02/03).
- `npx tsc --noEmit -p tsconfig.json` — the seam module type-checks.
- `npx eslint src/main/readiness-probe.ts src/main/__tests__/readiness-probe.test.ts` — no lint errors; the seam stays electron/node-pty-free.
- Bridge surface unchanged: `src/shared/__tests__/security.guard.test.ts` stays GREEN (no new key — D-04 notice reuses `onPtyStatus`, decided in Plan 03).
</verification>

<success_criteria>
- The `ReadinessProbe` seam exists mirroring `shell-discovery.ts`, electron-free, with the RESEARCH contract (interface + pure `buildPosixProbe` + Mac provider + Windows-throwing stub + platform picker).
- `buildPosixProbe` produces a `: <nonce>\r` no-op marker and a send-vs-match `matches` (false on bare echo, true on produced output).
- Wave 0 scaffolds (`readiness-probe.test.ts`, `startup-command.smoke.test.ts`, the SC5 hydrate extension) exist and pin SC1–SC5/D-02/D-04 as assertions, RED where they depend on the unimplemented create() probe hook.
- No source behavior changed in `pty-manager.ts`; bridge surface untouched.
</success_criteria>

<output>
Create `.planning/phases/05.1-term-05-startup-command-auto-run/05.1-01-SUMMARY.md` when done.
</output>
