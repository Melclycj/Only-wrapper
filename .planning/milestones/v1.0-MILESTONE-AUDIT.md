---
milestone: v1.0
audited: 2026-06-10
status: tech_debt
scores:
  requirements: 27/27
  phases: 9/9
  integration: 20/20
  flows: 5/6
gaps: {}  # No critical blockers — no unsatisfied requirements, no broken seams
tech_debt:
  - phase: 02-pty-core-terminal-fidelity
    items:
      - "VERIFICATION.md frontmatter still status: human_needed — human fidelity (claude --rc / vim / truecolor / CJK / bracketed-paste) was actually confirmed at the Phase-3 03-03 checkpoint and is recorded Validated in PROJECT.md; frontmatter never flipped to passed."
      - "Nyquist: 02-VALIDATION.md nyquist_compliant: false / wave_0_complete: false (status: draft) — formal Nyquist validation never completed for this early phase."
  - phase: 01-project-scaffold-dev-infrastructure
    items:
      - "Nyquist: 01-VALIDATION.md nyquist_compliant: false / wave_0_complete: false (status: draft)."
  - phase: 03-multi-session-session-lifecycle
    items:
      - "Nyquist: 03-VALIDATION.md nyquist_compliant: false / wave_0_complete: false (status: draft) — despite VERIFICATION.md passed (6/6) with all 7 human-fidelity items user-confirmed."
  - phase: 05-persistence-shell-discovery
    items:
      - "VERIFICATION.md frontmatter still status: human_needed — quit→reopen restore + drag-reorder + shell-dropdown were confirmed (PROJECT.md Validated list; 05-VALIDATION.md nyquist_compliant: true / status: complete); frontmatter not flipped."
  - phase: 06-robustness-flow-control-polish
    items:
      - "Abandoned phase: no 06-VERIFICATION.md exists. TERM-09 + TERM-12 are mapped to 'Phase 6' in the REQUIREMENTS.md traceability table but were actually delivered + verified in Phase 6.1 (06.1-VERIFICATION.md passed 5/5, human_verified 2026-06-09). Traceability points at the wrong phase — documentation mismatch, not a coverage gap."
  - phase: 06.1-terminal-lifecycle-state-machine-and-agent-state-detection-r
    items:
      - "CR-01..CR-04/WR-02 code-review remediation (commit 4ae8e1a) landed AFTER the user's final hands-on approval and was NOT re-hand-verified — covered GREEN by 267 unit + 14/14 smoke only (internal hardening: restart in-flight lock, atomic flush, quitting-flag reset, order clamp, handleRestart pid guard)."
      - "Open todo: 2026-06-09-redo-phase-06.1-code-review-criticals.md"
  - phase: 08-cross-platform-packaging
    items:
      - "REAL-HARDWARE WINDOWS UAT OUTSTANDING (08-HUMAN-UAT.md: 3 pending + 1 partial). CI on windows-latest builds the .exe/installer GREEN and the packaged PTY round-trip smoke passes 10/15; the 5 still-red smokes are POSIX/zsh-coded assertions, not product defects. But: (1) real-Windows install+run never human-verified; (2) Windows shell dropdown + per-shell auto-run byte semantics unverified on real hardware; (3) pre-1809 native error dialog never observed live (logic-proven only, os-gate.test.ts 9/9)."
      - "Follow-up: port the 5 POSIX-coded smoke specs to be Windows-shell-aware so the Windows smoke leg goes fully green rather than best-effort (08-HUMAN-UAT Test 4)."
  - phase: cross-cutting (integration checker WARNINGs)
    items:
      - "WARNING 1 (TERM-05, Windows CMD/PowerShell): probe.unsupported notice is built by buildDegradeProbe() but never read by create() in pty-manager.ts — on CMD/PowerShell the user sees the generic READINESS_FAIL_NOTICE timeout message after 4s instead of the specific 'auto-run unsupported on <shell> — start the command manually' notice. Behaviour is safe (command correctly not injected); UX message is less specific than designed. Windows-only."
      - "WARNING 2 (TERM-07): restart() does not pass startupCommand explicitly — it relies on create() recovering it via prior = sessions.get(id)?.record. Currently correct, but a fragile dependency on the invariant that a stop()-initiated exit does not delete the session from the map."
      - "WARNING 3 (UX): start-affordances suppresses the sidebar 'Start without command' ⏵ on the active dormant card row (R3 fix) — users must use the right-click menu. Intentional, documented, but a discoverability tradeoff."
  - phase: backlog (pending todos)
    items:
      - "2026-06-06-edit-modal-does-not-prefill-saved-cwd-and-startup-command.md"
      - "2026-06-06-add-folder-picker-for-working-directory-selection.md"
      - "2026-06-06-improve-start-control-discoverability-for-live-sessions.md"
      - "2026-06-06-address-deferred-code-review-findings-phase-05-1.md"
nyquist:
  compliant_phases: ["04", "05", "05.1", "06", "06.1", "07", "08"]
  partial_phases: ["01", "02", "03"]
  missing_phases: []
  overall: partial
---

# Milestone v1.0 — Audit Report

**Project:** Just-Wrapper — Local Terminal Session Manager
**Audited:** 2026-06-10
**Status:** `tech_debt` — all 27 requirements functionally satisfied and integrated; no critical blockers and no broken seams, but a real verification tail (Windows real-hardware UAT), early-phase Nyquist gaps, and a small backlog need a decision before archiving.

## Verdict in one line

v1.0 is **substantively complete and end-to-end wired on macOS**. The only thing standing between "done on macOS + Windows-CI" and "done, both platforms human-verified" is **Windows real-hardware verification** — everything else is documentation lag, internal hardening already covered by tests, and a short polish backlog.

## Requirements Coverage (27/27 satisfied)

Three-source cross-reference (REQUIREMENTS.md traceability `[x]` + phase SUMMARY `requirements-completed` frontmatter + phase VERIFICATION.md) agrees on all 27 v1 requirements. The integration checker independently confirmed every requirement is WIRED across its phase boundaries (no stubs, no orphans).

| Group | REQ-IDs | Status |
|-------|---------|--------|
| Identity | IDENT-01/02/03 | satisfied |
| Session mgmt | SESS-01/02/03/04 | satisfied |
| Sidebar/Nav | NAV-01/02/03/04/05 | satisfied |
| Terminal | TERM-01..12 | satisfied (TERM-09/12 verified in 06.1, see note) |
| Persistence | PERS-01/02 | satisfied |
| Packaging | PKG-01 | satisfied on macOS (verified); Windows build CI-proven, real-hardware run deferred |

**Notes:**
- **TERM-09 / TERM-12 traceability mismatch:** mapped to "Phase 6" in REQUIREMENTS.md but delivered and verified in **Phase 6.1** (Phase 6 was abandoned and has no VERIFICATION.md). Not a coverage gap — the requirements are verified — but the traceability table should be corrected to point at 6.1.
- **PKG-01** is the only requirement with a substantive open tail: the macOS package is built + human-verified; the Windows package is CI-built + PTY-smoke-proven but never installed/run on real Windows hardware by a human.

## Phase Verification Summary (9 real phases; Phase 6 abandoned)

| Phase | VERIFICATION status | Score | Human-verify | Note |
|-------|--------------------|-------|--------------|------|
| 01 | passed | 4/4 | n/a (auto) | Nyquist draft (false) |
| 02 | human_needed* | 5/5 | confirmed at Phase-3 checkpoint + PROJECT.md Validated | *frontmatter not flipped; Nyquist draft (false) |
| 03 | passed | 6/6 | complete (all 7 fidelity items) | 2 accepted overrides (D-03a Close, TERM-05 deferred); Nyquist draft (false) |
| 04 | passed | 5/5 | approved 2026-06-05 | — |
| 05 | human_needed* | 4/4 | confirmed (PROJECT.md Validated; VALIDATION complete) | *frontmatter not flipped |
| 05.1 | passed | 5/5 | approved (canonical claude --rc) | — |
| 06 | — (abandoned) | — | — | superseded by 06.1; no VERIFICATION.md |
| 06.1 | passed | 5/5 | approved 2026-06-09 (4 rounds) | post-approval CR remediation test-covered only |
| 07 | passed | 9/9 | signed-off 2026-06-10 | — |
| 08 | human_needed | 8/8 (logic) | macOS canonical verified; Windows real-hardware PENDING | the real open item |

\* Phases 02 and 05 read `human_needed` in their VERIFICATION.md frontmatter, but the human checks were in fact completed and recorded in the corresponding VALIDATION.md / PROJECT.md Validated list — this is stale frontmatter, not missing verification.

## Cross-Phase Integration (20/20 wired, 0 contradictions)

- **IPC bridge contract:** all 20 `EXPECTED_API_KEYS` mirrored exactly across preload ↔ main `registerIpc()` ↔ renderer. No drift. (window-config.ts / preload/index.ts / pty-manager.ts)
- **logicalId/ptyPid invariant:** `restart()` reuses the same `logicalId`, returns a new `ptyPid`; carried through `create()` + SessionStore hydrate. Holds across all layers.
- **Persistence round-trip:** save → debounced lowdb write → `before-quit` flush + `closed`-path flush → `coerceOnLoad` (forces `not_started`, clears pid) → `hydrate` → dormant restore → Start. Fully connected.
- **Startup-command auto-run (05.1)** funnels through the single `create()` readiness-probe hook on all three spawn paths (new / restart / dormant Start) — survives the 06.1 two-bucket redesign.
- **No cross-phase contradictions:** the 06.1 "Stop verb abolished / Remove-to-Inactive" redesign supersedes Phase 03's ring-buffer replay model without breaking TERM-06 keep-alive.

## Canonical Definition-of-Done (5/6 fully E2E)

| # | DoD step | Status |
|---|----------|--------|
| 1 | Create "Parlour Claude RC" 🛋️ session running `claude --rc`, interact natively | ✅ macOS verified |
| 2 | Normal shell + manual `cd` + run `codex`/`claude --rc` | ✅ macOS verified |
| 3 | Switching never kills/restarts a process | ✅ verified |
| 4 | Rename/re-icon + restart preserve logical ID | ✅ verified |
| 5 | Profiles persist + restore after full close/reopen | ✅ verified |
| 6 | Builds + runs packaged on **both** Windows and macOS | ⚠️ macOS verified; Windows CI-built + smoke-proven, real-hardware run deferred |

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 01 | exists | false (draft) | `/gsd-validate-phase 01` |
| 02 | exists | false (draft) | `/gsd-validate-phase 02` |
| 03 | exists | false (draft) | `/gsd-validate-phase 03` |
| 04–08 (+05.1, 06, 06.1) | exists | true | — |

Overall: **partial** — the three earliest phases were completed before Nyquist validation was being flipped; their functional verification passed and (for 02/03) was human-confirmed, but the formal `nyquist_compliant` flag was never set.

## Recommendation

No critical blockers → milestone is **not** blocked from completion. But because the debt above includes a genuine product-verification tail (Windows real hardware), this is `tech_debt`, not `passed`. Decide one of:

1. **Verify Windows on real hardware first**, then complete — closes DoD step 6 honestly for both platforms.
2. **Complete now, carry the debt into v1.1 backlog** — accept "macOS-verified + Windows-CI-proven" as the v1.0 bar and schedule Windows hardware UAT + Nyquist 01-03 + the 4 todos + 3 WARNINGs as v1.1's opening work.

Either way, before archiving, fix the **TERM-09/TERM-12 traceability pointer** (Phase 6 → Phase 6.1) and optionally flip the stale `human_needed` frontmatter on 02/05 to `passed` so the archived record matches reality.
