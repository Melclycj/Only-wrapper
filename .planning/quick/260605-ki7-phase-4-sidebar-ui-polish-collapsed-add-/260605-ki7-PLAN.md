---
phase: quick-260605-ki7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/Sidebar.tsx
  - src/renderer/terminal.css
autonomous: true
requirements: [NAV-02, SESS-03]
must_haves:
  truths:
    - "When the sidebar is collapsed, the add-session button shows only '+' (no 'Add session' text)"
    - "When the sidebar is expanded, the add-session button shows '+ Add session'"
    - "Each row's controls are compact icon buttons: Edit (pencil), Delete (trash/✕), and conditional Restart (↻) — not text labels"
    - "Clicking the pencil icon opens the edit form for that session (onEdit)"
    - "Clicking the trash icon opens the close confirm for that session (onClose)"
    - "Clicking the restart icon (non-running rows only) restarts that session (onRestart)"
    - "The context menu, SessionManager wiring, and all session behavior are unchanged"
  artifacts:
    - path: "src/renderer/Sidebar.tsx"
      provides: "Collapsed-aware add-session label + icon-glyph row controls (Restart · Edit · Delete)"
      contains: "edit-session"
    - path: "src/renderer/terminal.css"
      provides: "Compact square icon-button styling for .row-control"
  key_links:
    - from: "src/renderer/Sidebar.tsx edit button"
      to: "onEdit(s.logicalId)"
      via: "onClick with e.stopPropagation()"
      pattern: "onEdit\\(s\\.logicalId\\)"
    - from: "src/renderer/Sidebar.tsx add-session button"
      to: "collapsed prop"
      via: "conditional label render"
      pattern: "collapsed \\?"
---

<objective>
Phase-4 sidebar UI polish from UAT feedback. Two presentational changes to the
session sidebar, plus the supporting CSS:

1. Collapsed add-session button shows only "+" (the full "+ Add session" text
   does not fit the ~52px icon-only rail).
2. The per-row text controls ("Restart" / "Close") become compact, always-readable
   ICON buttons (Restart ↻ · Edit ✎ · Delete ✕), and a NEW Edit pencil icon is
   surfaced inline on the row (previously edit was reachable only via the context
   menu / double-click).

Purpose: Make the collapsed rail visually correct and give every row a calm,
icon-only control cluster consistent with the cozy DESIGN.md aesthetic — without
touching any session behavior, wiring, or the E2E-depended testids.

Output: Edited `src/renderer/Sidebar.tsx` (render-only changes) and edited
`src/renderer/terminal.css` (compact icon-button styling). No new files, no logic,
no IPC, no SessionManager changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/renderer/Sidebar.tsx
@src/renderer/terminal.css
@.planning/phases/04-session-identity-sidebar-ui/04-CONTEXT.md
@.planning/DESIGN.md
</context>

<constraints>
HARD RULES (CLAUDE.md + UAT constraints):
- This is PURE presentational UI polish — NO behavior/logic change, NO IPC, NO
  SessionManager change, NO context-menu change. The renderer stays free of any
  electron / node-pty import.
- Do NOT rename or remove ANY testid the E2E driver depends on:
  `add-session`, `close-session`, `restart-session`. Only ADD `edit-session`.
- Keep `aria-label` and `onClick` (with `e.stopPropagation()`) on every existing
  row-control button. The Close button KEEPS `onClose`, the Restart button KEEPS
  `onRestart` and its `!running` conditional, and both KEEP their `data-action`.
- The right-click context menu remains the collapsed-mode control surface
  (D-03 / D-11). The new inline pencil is an expanded-mode affordance only — the
  existing `.sidebar.collapsed .row-controls { display: none }` rule already hides
  the whole control cluster (including the new pencil) when collapsed; do not
  weaken it.
- Edit was already wired: `onEdit` is an existing prop, called today by
  double-click. This plan adds an inline button that calls the SAME `onEdit(id)`
  — no new prop, no new handler.

VERIFICATION FACTS (confirmed during planning — rely on these):
- No unit test or smoke test asserts on the literal button TEXT "Close" or
  "Restart". The strings appear ONLY in code comments. All assertions are
  testid-based.
- The 3 Phase-4 smoke tests reach Edit via the CONTEXT MENU
  (`clickMenuItem('Edit')` on `.context-menu-item`), NOT via row buttons — so the
  inline pencil swap cannot affect them.
- `close-session` / `restart-session` testids are not currently exercised by any
  smoke assertion; preserving them keeps the contract intact regardless.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Collapsed add-session label + icon-glyph row controls (Sidebar.tsx) and compact icon-button CSS (terminal.css)</name>
  <files>src/renderer/Sidebar.tsx, src/renderer/terminal.css</files>
  <action>
Make three render-only edits. Do NOT add or change any prop, handler, state, or
import. Do NOT touch `renderIcon`, `isRunning`, the row click/keydown/contextMenu
handlers, the collapsed status dot, or the rail tooltip.

CHANGE 1 — `.add-session` collapsed label (Sidebar.tsx, the trailing add button).
The add-session `<button>` currently renders the static child text `+ Add session`.
Replace that child with a conditional driven by the existing `collapsed` prop:
render the single character `+` when `collapsed` is true, and `+ Add session` when
`collapsed` is false. KEEP `type="button"`, `className="add-session"`,
`data-testid="add-session"`, and `onClick={onAdd}` exactly as-is. (Recommended:
wrap the visible text in a `<span>` and add `aria-label="Add session"` on the
button so the collapsed "+"-only state stays accessible — but the testid and
onClick must not change.)

CHANGE 2 — Row controls become icon glyphs (Sidebar.tsx, inside the
`<span className="row-controls">`). Render order in the row MUST be:
Restart (if shown) · Edit · Delete. Specifically:
  (a) Restart button — KEEP the existing `!running &&` conditional, KEEP
      `data-testid="restart-session"`, `data-action="restart"`,
      `aria-label={`Restart ${s.name}`}`, the `onClick` that calls
      `e.stopPropagation()` then `onRestart(s.logicalId)`, and `className="row-control"`.
      ONLY change the button's visible child text from `Restart` to a restart glyph
      `↻` (U+21BB). Add `title="Restart session"` is already present — keep it. Wrap
      the glyph in `<span aria-hidden="true">` so screen readers read the aria-label,
      not the glyph.
  (b) Edit button — NEW button inserted BETWEEN Restart and Close, inside
      `.row-controls`. It is NOT conditional (always rendered, like Close).
      `type="button"`, `className="row-control"`, `data-testid="edit-session"`,
      `data-action="edit"`, `title="Edit session"`, `aria-label={`Edit ${s.name}`}`,
      and `onClick={(e) => { e.stopPropagation(); onEdit(s.logicalId); }}`. Visible
      child is a pencil glyph `✎` (U+270E) wrapped in `<span aria-hidden="true">`.
      This reuses the EXISTING `onEdit` prop — do not add a prop.
  (c) Delete (Close) button — KEEP `data-testid="close-session"`,
      `data-action="close"`, `aria-label={`Close ${s.name}`}`, `title="Close session"`,
      the `onClick` calling `e.stopPropagation()` then `onClose(s.logicalId)`, and
      both classes `row-control row-control-close`. ONLY change the visible child
      text from `Close` to a trash/✕ glyph — use `✕` (U+2715) wrapped in
      `<span aria-hidden="true">`. (Trash emoji is avoided to keep a crisp monochrome
      glyph that inherits `currentColor`; the existing `.row-control-close:hover`
      red ramp makes intent unmistakable.)

CHANGE 3 — Compact icon-button styling (terminal.css, the `.sidebar-row .row-control`
rule block near the existing `.row-controls` rules). Restyle `.row-control` from a
text pill into a compact square monochrome icon button:
  - Set a fixed square footprint in the ~22–26px range (e.g. width:24px; height:24px)
    with `display:inline-flex; align-items:center; justify-content:center;`.
  - Replace the text padding (`padding: 2px 8px`) with `padding:0` and a small
    `border-radius` (e.g. 7px) consistent with the existing rows.
  - Make it monochrome: `border-color: transparent` (drop the boxed look so the
    glyph reads as an icon, not a button chip), `background: transparent`,
    `color: var(--ink-soft)`, `line-height:1`, and a glyph `font-size` around
    14–15px so the symbol is legible at 24px.
  - Hover: keep the existing subtle warm treatment — `background: var(--bg-sunk);
    color: var(--ink);` (the current `.row-control:hover` rule already does this;
    adjust only if the transparent border needs a matching hover border, otherwise
    leave it).
  - Do NOT change `.row-control-close:hover` (the destructive red ramp stays).
  - Do NOT change `.sidebar-row .row-controls` (opacity reveal-on-hover stays) and
    do NOT change `.sidebar.collapsed .row-controls { display: none }` — the whole
    cluster (now including the pencil) must stay hidden in the collapsed rail
    (Pitfall 5 / D-11). Verify that rule still lists `.row-controls`.
Keep all DESIGN.md tokens (`--ink-soft`, `--ink`, `--bg-sunk`, `--line`) — no new
colors, no hardcoded hex. Cozy/warm aesthetic preserved.
  </action>
  <verify>
    <automated>cd /Users/jerry/Project/Just-wrapper && npm run test:unit && npx tsc --noEmit && grep -q 'data-testid="edit-session"' src/renderer/Sidebar.tsx && grep -q 'onEdit(s.logicalId)' src/renderer/Sidebar.tsx && grep -q 'data-testid="add-session"' src/renderer/Sidebar.tsx && grep -q 'data-testid="close-session"' src/renderer/Sidebar.tsx && grep -q 'data-testid="restart-session"' src/renderer/Sidebar.tsx && grep -Eq "collapsed \?" src/renderer/Sidebar.tsx && ! grep -Eq '>\s*Restart\s*<|>\s*Close\s*<' src/renderer/Sidebar.tsx && grep -q 'row-controls' src/renderer/terminal.css && grep -A12 'sidebar.collapsed .row-name' src/renderer/terminal.css | grep -q 'row-controls'</automated>
  </verify>
  <done>
- `npm run test:unit` is GREEN (82/82 — no test regressed; assertions are
  testid-based, so the icon swap does not affect them).
- `npx tsc --noEmit` is clean (no type errors).
- Sidebar.tsx contains `data-testid="edit-session"` whose `onClick` calls
  `onEdit(s.logicalId)` after `e.stopPropagation()`.
- The testids `add-session`, `close-session`, `restart-session` are all still
  present and unchanged.
- The literal child text `Restart` and `Close` no longer appears as button text
  in Sidebar.tsx (replaced by `↻` / `✕` glyphs); the comments may still mention them.
- The add-session button renders `+` only when `collapsed` (a `collapsed ?`
  expression gates its label), and `+ Add session` when expanded.
- terminal.css `.row-control` is a compact square icon button (~22–26px,
  monochrome `var(--ink-soft)`, subtle hover) and the
  `.sidebar.collapsed { ... .row-controls }` hide rule still includes `.row-controls`.
- No new prop, handler, import, IPC call, or SessionManager change; renderer remains
  electron/node-pty-free.
  </done>
</task>

</tasks>

<verification>
- `npm run test:unit` → 82/82 GREEN (no regression; all assertions testid-based).
- `npx tsc --noEmit` → clean.
- Source greps confirm: `edit-session` testid added + wired to `onEdit`; the three
  pre-existing testids preserved; `collapsed ?` gates the add-session label; no
  `Restart`/`Close` literal button text remains; collapsed hide rule still covers
  `.row-controls`.
- Smoke tests are unaffected (they reach Edit via the context menu, not row buttons)
  — `npm run test:smoke` may be run for full confidence but is not required by these
  source-grepable acceptance criteria.
</verification>

<success_criteria>
- Collapsed sidebar add button shows only "+"; expanded shows "+ Add session".
- Row controls are compact monochrome icon buttons in order Restart(↻, if shown) ·
  Edit(✎) · Delete(✕), each preserving its testid, aria-label, onClick, and
  stopPropagation.
- New inline pencil opens the edit form via the existing `onEdit` (no new wiring).
- Context menu, SessionManager, and all session behavior unchanged; renderer stays
  electron/node-pty-free.
- `npm run test:unit` (82/82) and `npx tsc --noEmit` both pass.
</success_criteria>

<output>
Create `.planning/quick/260605-ki7-phase-4-sidebar-ui-polish-collapsed-add-/260605-ki7-SUMMARY.md` when done.
</output>
