---
status: complete
slug: rhythm-type-scale
quick_id: 260621-dw3
date: 2026-06-21
branch: gsd/phase-03-multi-session-session-lifecycle
source: .planning/design/DESIGN-AUDIT-2026-06-21.md (Wave 3 of 4 — P1 #1 / #12 + radius)
---

# Quick Task — Design Audit Wave 3: rhythm + type scale + radius rename

Pays down the audit's "defined-not-applied" token debt. CSS-only, VALUE-PRESERVING-first.

## Scope
1. Migrate the 25 hardcoded spacing literals onto the `--space-*` scale (value-preserving
   exact matches; the few one-offs snapped to nearest step, flagged).
2. Establish a `--text-*` type scale (5 steps) and migrate font-size literals onto it.
3. Rename `--radius` → `--radius-card` and update all 5 consumers (pure rename).

## Discipline
Value-preserving first — replacing a literal with an equal-valued token is zero visual
change. Every value-CHANGING edit is listed explicitly + verified by ui-lab before/after.

## Files
`tokens.css` + `terminal.css` / `terminal-area.css` / `sidebar.css` / `form.css` +
`__tests__/tokens-completeness.test.ts` (token-floor assertions).

## Verify
`npm run test:unit` + `npx tsc --noEmit` + grep-for-bare-`var(--radius)` (must be 0) +
ui-lab before/after byte-compare (only the 3 snap sites + live-timestamp surfaces differ).
