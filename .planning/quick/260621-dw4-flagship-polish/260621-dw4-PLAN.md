---
status: complete
slug: flagship-polish
quick_id: 260621-dw4
date: 2026-06-21
branch: gsd/phase-03-multi-session-session-lifecycle
source: .planning/design/DESIGN-AUDIT-2026-06-21.md (Wave 4 of 4 — flagship polish, optional)
---

# Quick Task — Design Audit Wave 4: flagship polish

The optional final wave. Judgment applied (not every audit P2 mechanically): empty-state +
status-signal shape-coding deferred (both already decent / low ROL). Did the high-value set.

## Scope
- **A — Motion + prefers-reduced-motion**: modal enter (scrim fade + card fade/scale),
  status-badge/dot color easing, AND a global `prefers-reduced-motion` guard (was absent —
  real a11y gap). Compositor-friendly only (opacity/transform).
- **B — Destructive clarity** (P1 #6, #10): context-menu separator before the first
  destructive item; permanent Delete one notch heavier (`danger-strong`) than reversible
  Remove, via a clean optional ConfirmModal `confirmVariant` prop (default-preserving).
- **C — Consistency** (P1 #4, #3): IdentityHeader icon gets the sidebar/idle-card icon tile;
  removed the 2 confirmed-dead `--term-text`/`--term-faint` tokens.
- **D — xterm ANSI palette** (P1 #2): full coordinated 16-color palette in TERMINAL_THEME
  (was bg/fg/cursor only → ANSI programs fell back to xterm defaults). Hue-aligned to brand,
  legibility-verified, default fg unchanged. Operator approved the swatch before apply.

## Files
tokens.css, terminal.css, form.css, terminal-area.css (CSS) · ContextMenu.tsx,
ConfirmModal.tsx, SessionManager.tsx, SessionView.tsx (TSX) · ansi-palette.mjs (generator).

## Verify
`npm run test:unit` (481) + `npx tsc --noEmit` + `npm run package` (build sound) +
ui:shots wave4-after (separator + header icon render). Motion + ANSI colors = manual eyeball.
